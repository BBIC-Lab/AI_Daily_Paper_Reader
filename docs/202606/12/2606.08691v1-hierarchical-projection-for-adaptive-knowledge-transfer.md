---
title: Hierarchical Projection for Adaptive Knowledge Transfer
title_zh: 层次投影用于自适应知识迁移
authors: "Samhita Pal, Tian Gu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08691v1"
tags: ["query:ndai"]
score: 9.0
evidence: 通过分层投影进行有限标注数据的自适应知识迁移
tldr: 现代数据应用中，目标数据有限但多源信息可用，直接组合可能因相关性差异或虚假信号而失败。本文提出投影迁移学习框架，融合分层贝叶斯建模与自适应投影实现选择性知识迁移，通过源选择与特征选择两级解耦，有效缓解负迁移并保持可解释性，实验表明优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08691v1/fig-005.webp\", \"caption\": \"Figure 1: Schematic diagram of the proposed two-stage ProjectionTL.\", \"page\": 5, \"index\": 5, \"width\": 826, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2606.08691v1/fig-006.webp\", \"caption\": \"Figure 2: Illustration of multi-modal transfer learning problem with block-missingness.\", \"page\": 5, \"index\": 6, \"width\": 664, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2606.08691v1/fig-001.webp\", \"caption\": \"Figure 3: Boxplots of average estimation error (MSE, left panel) and support recovery by Matthews Correlation Coefficient (MCC, right panel) of ProjectionTL, TRADER, TransGLM, and CONCERT are shown as functions of the overlap ratio ω ∈ [0.6, 1]. Results are averaged over 100 Monte Carlo replicates with n0 = nk = 100, p = 200, s0 = 5.\", \"page\": 7, \"index\": 1, \"width\": 827, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2606.08691v1/fig-004.webp\", \"caption\": \"Figure 4: Multimodal data structure of the ADNI transfer-learning design.\", \"page\": 9, \"index\": 4, \"width\": 664, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2606.08691v1/fig-002.webp\", \"caption\": \"Figure 5: Out-of-sample prediction MSE minus in-sample prediction MSE comparison of full-target vs the validation-set.\", \"page\": 13, \"index\": 2, \"width\": 582, \"height\": 575}, {\"url\": \"assets/figures/arxiv/2606.08691v1/fig-003.webp\", \"caption\": \"Figure 6: Sensitivity of ProjectionTL to the projection tuning parameter λ across overlap ratios ω ∈ {0.6, 0.7, 0.8, 0.9, 1.0}; Target LASSO is shown as a fixed reference.\", \"page\": 13, \"index\": 3, \"width\": 793, \"height\": 342}]"
motivation: 目标数据集有限时，多源异构数据的朴素整合易受相关度变化和虚假信号影响，导致性能下降，亟需可信的跨域学习方法。
method: 提出ProjectionTL框架，先构建源引导分层先验实现全局源选择，再通过后验投影在特征级别选择性保留与目标一致的坐标，进行精细迁移。
result: 模拟和真实生物医学应用显示，该方法在准确性、稳定性和可解释性上均优于现有方法。
conclusion: ProjectionTL为高维异构数据整合提供了可扩展、可泛化的可信跨域学习策略，无缝桥接统计建模与现代机器学习。
---

## 摘要
现代数据驱动的应用越来越多地涉及从多个异构源学习，其中目标数据集有限，但相关信息跨域可用。当相关性变化或存在虚假信号时，简单组合这些源可能会降低性能，这为可信跨域学习提出了根本挑战。我们提出投影迁移学习（ProjectionTL），一个将层次贝叶斯建模与自适应投影相结合的统一框架，用于选择性知识迁移。关键思想是在两个层面解耦迁移：首先，我们构建一个源引导的层次先验，使用数据驱动的权重聚合跨源信息，捕获每个源与目标之间的全局对齐；其次，我们通过在特征层面运行的后验投影步骤来细化这种借用，有选择地保留与目标信号显示局部一致的坐标。这种两阶段设计使方法能够同时执行源选择和特征选择，从而在保持可解释性的同时减轻负迁移。ProjectionTL提供了一种原则性的方法来整合跨域异构数据，桥接统计建模和现代机器学习范式，实现稳健且可解释的迁移。通过模拟和真实世界生物医学应用，我们展示了与现有方法相比提高的准确性、稳定性和可解释性。我们的框架为高维环境中的可信跨域学习提供了一种可扩展和可泛化的策略。

## Abstract
Modern data-driven applications increasingly involve learning from multiple heterogeneous sources, where a target dataset is limited but related information is available across domains. Naively combining these sources can degrade performance when relevance varies or spurious signals are present, posing a fundamental challenge for trustworthy cross-domain learning. We propose Projection Transfer Learning (ProjectionTL), a unified framework that integrates hierarchical Bayesian modeling with adaptive projection for selective knowledge transfer. The key idea is to decouple transfer at two levels: first, we construct a source-guided hierarchical prior that aggregates information across sources using data-driven weights, capturing global alignment between each source and the target; second, we refine this borrowing through a posterior-projection step that operates at the feature level, selectively retaining coordinates that exhibit local agreement with the target signal. This two-stage design enables the method to simultaneously perform source selection and feature selection, thereby mitigating negative transfer while preserving interpretability. ProjectionTL provides a principled approach to integrating heterogeneous data across domains, bridging statistical modeling and modern machine learning paradigms for robust and interpretable transfer. Through simulations and real-world biomedical applications, we demonstrate improved accuracy, stability, and interpretability compared to existing methods. Our framework offers a scalable and generalizable strategy for trustworthy cross-domain learning in high-dimensional settings.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：现代数据应用中，目标数据集样本有限，但存在多个相关但异构的辅助源（如不同队列的医学数据）。简单合并这些源容易引入负迁移（negative transfer），尤其当源与目标仅部分重叠或存在虚假信号时，会导致模型性能下降。
- **核心挑战**：需要一种能够**自适应地决定“从哪些源（source-level）借信息”以及“借哪些特征（feature-level）**”的迁移学习方法，同时兼容特征空间不完全重叠和块缺失（block-wise missingness）。
- **整体含义**：论文提出 **ProjectionTL** 框架，将层次贝叶斯先验与后验投影相结合，实现全局源选择与局部特征选择的两阶段解耦，旨在高维稀疏回归设定下提供一种**可信、可解释且稳健**的跨域知识迁移策略。

## 2. 论文提出的方法论

### 2.1 问题设定
- 目标数据集：$(X, Y)$，$X\in \mathbb{R}^{n_0\times p}$，$Y\in \mathbb{R}^{n_0}$。
- $K$ 个源数据集已各自拟合得到系数向量 $\hat{\omega}_k \in \mathbb{R}^p, k=1,\dots,K$。
- 目的：估计稀疏的目标系数 $\beta$，利用源信息提升小样本目标下的估计和预测。

### 2.2 第一阶段：源级别借用（向量级先验）
- 对每个特征 $j$，构造先验均值：
  $$\mu_j = \sum_{k=1}^{K} \eta_k \cdot \frac{\|\hat{\beta}\|}{\|\hat{\omega}_k\|} \cdot \hat{\omega}_{kj} + \eta_{K+1}\cdot 0,$$
  其中 $\hat{\beta}$ 为目标数据全样本估计（用于构建先验的超参数），权重 $\eta=(\eta_1,\dots,\eta_K,\eta_{K+1})$ 位于概率单纯形上。
- 权重 $\eta$ 服从 Dirichlet 先验，其浓度参数 $\theta_k$ 为源系数 $\hat{\omega}_k$ 与目标验证估计 $\hat{\beta}_{\text{val}}$ 的**余弦相似度**：
  $$\theta_k = \frac{\hat{\omega}_k^\top \hat{\beta}_{\text{val}}}{\|\hat{\omega}_k\|\,\|\hat{\beta}_{\text{val}}\|},$$
  $\eta_{K+1}$ 对应“不借用”选项，当所有源都错配时，先验退化为均值为0的非信息正态分布。
- 目标系数先验：$\beta_j \mid \mu_j, \sigma^2 \sim \mathcal{N}(\mu_j, a_n^{-1}\sigma^2)$，$\sigma^2$ 取逆Gamma分布。
- 由此得到源调整后的后验分布 $ \beta \mid Y, X, \eta, \sigma^2 \sim \mathcal{N}(\mu_\eta, \sigma^2 V_n) $，其中 $\mu_\eta = V_n (X^\top Y + a_n W \eta)$，$W$ 为缩放后的源系数矩阵。

### 2.3 第二阶段：分量级别精炼（后验投影）
- 对后验抽取的每个样本 $\beta$，通过加权 $\ell_1$ 投影得到稀疏版本 $\beta^*$：
  $$\beta^* = \arg\min_{u\in\mathbb{R}^p} \left\{ \|X\beta - Xu\|_2^2 + \lambda \sum_{j=1}^p \frac{|u_j|}{w_j} \right\},$$
  其中自适应权重 $w_j$ 反映各源在坐标 $j$ 上与目标验证估计的一致性：
  $$w_j = \sum_{k=1}^K \exp\left\{ -(\hat{\beta}_j - \hat{\omega}_{kj})^2 \right\}.$$
  协调一致的坐标获得更大的 $w_j$，惩罚更小，更易被保留。
- 最终估计取投影样本的聚合（如中位数概率模型，MPM），得到稀疏点估计并附有投影入选概率。

### 2.4 多模态与块缺失扩展
- 多源系数估计直接整合进先验均值，缺失模态不贡献；投影权重自动惩罚缺乏一致支持的坐标，无需插补或显式缺失建模。

## 3. 实验设计

### 3.1 模拟实验
- **场景**：高维线性回归，控制重叠比例 $\omega \in \{0.6, 0.7, 0.8, 0.9, 1.0\}$，$\omega$ 表示目标支持集中被源共享的比例。部分源还包含虚假非零信号。
- **数据集生成**：$n_0=n_k=100$，$p=200$，$s_0=5$，$K=10$，$n_{\text{spur}}=20$，$\sigma=1$，100次重复。
- **对比方法**：
  - 迁移学习方法：**ProjectionTL**, **TRADER**（向量级借用，无分量投影），**TransGLM**，**CONCERT**（分量级借用，但无自适应源权重）。
  - 仅目标方法：LASSO, Bayesian sparse projection posterior (SPP), Bayesian Horseshoe (HS)。
- **评估指标**：系数 MSE、支持集恢复的 Matthews Correlation Coefficient (MCC)。

### 3.2 真实数据分析（ADNI）
- **数据来源**：ADNI 各阶段（ADNI1, GO, 2, 3, 4），预测基线 MMSE 得分。
- **研究 1（异质单模态源）**：目标为新入组参与者，两个源分别为早期队列（$n_1=128$）和较近期队列（$n_2=769$），特征为 $p=166$ 个 PET SUVR。比较 BR2、ProjectionTL、TRADER、TransGLM、CONCERT。
- **研究 2（多模态块缺失）**：使用固定时间点，模态包括结构 MRI、淀粉样 PET、基因表达（经 SIS 降维）。构造多个基于观测模态模式的源，目标数据集从中采样构建。方法同上，但对其他方法缺失模态采用零填充。

## 4. 资源与算力
- 论文**未明确提及**所使用的 GPU 型号、数量或具体训练时长。实验主要在模拟和中等规模真实数据上完成，计算复杂度可能主要由后验 MCMC 采样和投影优化构成，但无算力相关报告。

## 5. 实验数量与充分性
- **模拟实验**：覆盖 5 种重叠率，每种 100 次重复，总计至少 500 次模拟运行；另附有 $\lambda$ 敏感性分析（5 种 $\lambda$ 比例 × 5 种 $\omega$）。
- **真实数据实验**：2 种不同设置（异质源、多模态块缺失）。
- **对比基准**：包含 4 种迁移学习方法和 3 种目标专用方法，覆盖了主流高维迁移和稀疏估计方法。
- **评价公平性**：模拟数据生成过程公开，评估指标通用，超参数选择（如 $\lambda$ 通过交叉验证）有报告；对比方法采用其推荐设置。整体实验设计较为充分，能在不同对齐程度与缺失模式下客观评估方法性能。

## 6. 论文的主要结论与发现
- ProjectionTL 在所有重叠比例下均取得最低的系数 MSE 和最高的 MCC，且对 $\omega$ 变化稳健，有效实现正迁移。
- 当不存在有效源时，方法能自动退回“不借用”模式，避免负迁移。
- 真实数据分析中，ProjectionTL 在异质源和多模态块缺失两场景下均获得最低测试 MSE。
- 理论部分证明了在高维设定下，投影后验能以最优速率聚集，且在有/无信息源下均保持良好的收缩性质。

## 7. 优点
- **双层自适应借用**：同时实现源级选择（通过 Dirichlet 权重）和分量级选择（通过投影稀疏化），更精细地控制迁移范围。
- **无需个体级数据**：仅需源的系数估计，适合数据共享受限的场景。
- **块缺失兼容性**：直接操作系数估计，无需插补，自然支持多模态部分缺失。
- **可解释性强**：源权重和分量入选概率提供了明确的借用决策依据。
- **理论保障**：给出后验收缩速率和模型选择一致性条件。
- **鲁棒性验证**：模拟与真实数据均表现出优于现有方法且避免负迁移的特性。

## 8. 不足与局限
- **线性模型假设**：当前框架仅限于高维线性回归，未扩展到非线性或更复杂的预测结构。
- **计算开销**：涉及贝叶斯 MCMC 抽样和逐样本投影优化，在高维或大源数量时可能计算量较大。
- **超参数选择**：投影惩罚参数 $\lambda$ 通过交叉验证选取，先验中的 $a_n$ 等未详尽讨论；对权重 $\zeta$（设为1）的敏感性未分析。
- **理论依赖**：收缩速率证明依赖于特征有界、设计矩阵良好等假设，实际复杂数据可能不满足。
- **对比广度有限**：未比较当前主流的神经网络迁移方法，多模态实验中其他方法被迫需零填充，对比不完全公平。
- **不确定性量化欠缺**：投影后虽可得到入选概率，但未提供后投影置信区间或 debiased 推断。

（完）
