---
title: Efficient Adaptive Data Acquisition via Pretrained Belief Representations
title_zh: 通过预训练信念表示实现高效自适应数据获取
authors: "Daolang Huang, Zhuoyue Huang, Conor Hassan, Luigi Acerbi, Samuel Kaski, Tom Rainforth"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25197v1"
tags: ["query:ndai"]
score: 9.0
evidence: 利用预训练基础模型作为信念状态编码器进行策略学习
tldr: 自适应数据采集的策略学习面临后验近似偏差或直接策略学习低效的问题。本文提出POLAR框架，通过预训练预测模型将观测历史编码为充分信念状态，分离表示学习与策略学习，统一了贝叶斯实验设计、优化和主动学习。POLAR在多种任务中超越现有摊销方法，大幅减少所需训练样本，提升了可扩展性与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25197v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of POLAR. Left: POLAR uses a pretrained tabular foundation model as a belief encoder and trains a policy head on top of it. Top right: Policy learning is driven by task-specific utilities, while backbone adaptation is supervised by an optional prediction loss. Bottom right: At deployment, the policy maps the current history to the next design in a single forward pass.\", \"page\": 2, \"index\": 2, \"width\": 829, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2606.25197v1/fig-001.webp\", \"caption\": \"Figure 2: Location finding. (a) EIG against total training samples in the 2D setting. Error bars denote standard error across 1000 runs. (b) The same comparison in the 5D setting. (c) Example design trajectories for all methods on a shared latent source configuration.\", \"page\": 7, \"index\": 1, \"width\": 811, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2606.25197v1/fig-003.webp\", \"caption\": \"Figure 4: Hyperparameter optimisation on HPO-B. Average regret (left) and average rank (right) aggregated across the six search spaces. Shaded regions denote one standard error across the test tasks. POLAR achieves the lowest regret and the best average rank throughout the acquisition trajectory.\", \"page\": 9, \"index\": 3, \"width\": 814, \"height\": 283}]"
motivation: 现有方法或依赖易出错的后验模型，或未能利用模型表示，导致自适应数据采集策略学习困难。
method: POLAR使用预训练基础模型作为信念状态编码器，解耦表示学习与策略学习，针对不同任务仅需调整任务效用函数训练策略头。
result: POLAR在多个数据采集任务中胜过当前最先进的摊销方法，且训练样本需求显著降低。
conclusion: POLAR为摊销数据采集提供了简单、高效且可扩展的统一框架，是该领域的重要进展。
---

## 摘要
学习自适应数据获取的有效策略仍具挑战性：基于后验的方法依赖可能被错误指定或存在偏差的替代模型与后验近似，而直接的策略学习方法从历史观测映射，未能利用可用的模型表示，使学习更加困难。我们提出基于信念表示的策略学习（POLAR），其核心见解是最优数据获取仅通过充分信念状态依赖于观测历史。具体而言，POLAR 利用预训练的预测基础模型作为信念状态编码器，在其表示之上训练策略头，从而将表示学习与策略学习解耦。这产生了一个简单、统一的摊销策略学习框架，适用于贝叶斯实验设计、贝叶斯优化和主动学习，仅训练策略所用的任务特定效用函数有所不同。实验表明，POLAR 在多种任务上优于最先进的摊销方法，同时所需训练样本大幅减少，展示了摊销数据获取在可扩展性和效率上的重大进步。

## Abstract
Learning effective policies for adaptive data acquisition remains challenging: posterior-based methods rely on surrogate models and posterior approximations that can be misspecified or biased, while direct policy-learning methods map from historical observations and fail to exploit available model representations, making learning harder. We introduce policy learning with belief representations (POLAR), based on the insight that optimal data acquisition depends on the observation history only through a sufficient belief state. Specifically, POLAR decouples representation learning from policy learning by leveraging pretrained predictive foundation models as belief-state encoders, training a policy head on top of their representations. This yields a simple, unified amortised policy learning framework for Bayesian experimental design, Bayesian optimisation, and active learning, differing only in the task-specific utility used to train the policy. Empirically, we find that POLAR outperforms state-of-the-art amortised methods across diverse tasks while requiring far fewer training samples, demonstrating a significant step in the scalability and efficiency of amortised data acquisition.

---

## 论文详细总结（自动生成）

```markdown
# 论文深度分析与总结：通过预训练信念表示实现高效自适应数据获取

## 1. 论文核心问题与研究动机
- **核心挑战**：在自适应数据获取（如贝叶斯实验设计、贝叶斯优化、主动学习）中，学习一个能有效选择下一查询（设计）的策略十分困难。
- **现有方法的两难困境**：
  - **基于后验的方法**：依赖对隐变量的后验近似（如高斯过程、神经网络预测器）来计算采集函数。后验近似一旦存在偏差或错误指定，误差会逐步累积，导致次优决策。
  - **直接策略学习方法**：直接从原始观测历史映射到采集动作，避免了显式后验推断。但这种方法需同时学习“如何表示历史数据”和“如何基于该表示做出决策”，任务复杂度极高，训练样本效率低下。
- **根本洞察**：贝叶斯最优策略仅通过“充分信念状态”依赖于历史数据，即任何能得到相同任务相关后验的数据集应产生相同的下一动作。因此，理想的策略输入不是原始数据，也不是显式后验，而是一个能保留后验信息的**中间信念表示**。

## 2. 方法论：POLAR 框架
### 2.1 核心思想
- 提出 **POLAR (POlicy LeArning with Belief Representations)**，将自适应数据采集的策略学习解耦为两步：
  1. 使用预训练的**表格基础模型 (Tabular Foundation Model, TFM)** 作为信念编码器，将观测历史映射为候选点相关的隐表示。
  2. 在此表示之上训练一个轻量级**策略头**，直接输出下一个查询的决策。
- 这一设计使得策略头无需学习复杂的后验推断过程，只需学习“如何基于信念表示做决策”。

### 2.2 关键技术细节与公式
- **信念表示编码**：给定历史数据 $D_{t-1}$ 和候选点 $x$，TFM 前向传播生成候选条件化的隐表示 $r_{\psi}(x, D_{t-1}) \in \mathbb{R}^{d_r}$。作者认为该表示在预训练过程中被塑造为蕴含预测不确定性、局部结构等后验关键信息的“信念状态”。
- **策略头**：是一个简单MLP $g_{\eta}: \mathbb{R}^{d_r} \to \mathbb{R}$。对于候选池 $C_t$ 中的每个 $x$，将 $r_{\psi}$ 映射为一个标量logit，经Softmax归一化后形成随机策略 $\pi_{\eta}(\cdot | D_{t-1}, C_t)$。部署时直接选择得分最高的候选。
- **策略训练目标**：使用基于任务评分规则的策略梯度。
  - 定义任务特定的轨迹终局得分 $b_{\Phi}(D_T, w)$（例如贝叶斯实验设计中的信息增益下界，贝叶斯优化中的最佳观测值）。
  - 计算局部一步效用增量 $u_t = b_{\Phi}(D_t, w) - b_{\Phi}(D_{t-1}, w)$。
  - 策略损失为：
    $$ \mathcal{L}_{\text{pol}}(\eta) = -\mathbb{E}_{\tau \sim \pi_{\eta}} \left[ \sum_{t=1}^{T} u_t \log \pi_{\eta}(x_t; D_{t-1}) \right] $$
    这是一种低方差的局部信用分配技巧，而非无偏的全局回报估计。
- **骨干网络自适应性**：
  - 可选用辅助监督预测损失 $\mathcal{L}_{\text{pred}}(\psi)$ 来微调TFM骨干（如对数似然或分位数回归损失），以对齐下游任务分布。
  - **关键设计**：梯度解耦。策略梯度不传播到骨干网络（$\nabla_{\eta} \mathcal{L}_{\text{pol}}$ 只更新策略头），只有 $\mathcal{L}_{\text{pred}}$ 才更新骨干参数 $\psi$。这防止了高方差策略梯度破坏预训练表示。

### 2.3 统一决策论视角
- 在贝叶斯决策理论下，各类自适应采集问题均可归约为最大化终局评分（或最小化后验不确定性）。目标变量 $w$ 可以是参数、预测值或最优输入。POLAR统一处理这些任务的唯一不同在于任务评分规则 $b_{\Phi}$ 的定义方式。

## 3. 实验设计与对比方法
### 3.1 基准任务与数据集
- **贝叶斯实验设计**：
  - **位置寻找**：2D和5D变体，从噪声距离测量推断隐藏源位置（$T=30, 50$）。
  - **CES（常替代弹性）**：从配对偏好查询推断经济效用参数（$T=10$）。
- **贝叶斯优化**：
  - **HPO-B基准**：大规模超参数优化，在6个搜索空间上测试（维度2到16），包含数千个真实模型评估。
  - **DOCKSTRING分子对接**：真实世界高维（512维二进制指纹）分子优化，跨蛋白靶点的元学习，测试零样本迁移。
- **主动学习**（附录）：
  - 损失驱动的主动学习任务，在合成GP函数和标准回归函数上评估，优化加权预测性能。

### 3.2 对比方法
- **贝叶斯实验设计**：Random, DAD（深度自适应设计）, RL-BOED（强化学习贝叶斯最优实验设计）, ALINE（联合摊销推断与采集）。
- **贝叶斯优化**：Random, GP, Meta-GP, PFNs4BO, NAP（端到端元贝叶斯优化）, TabICL（同骨干但用EI采集函数）。
- **主动学习**：GP-RS, GP-US, GP-VR, GP-VR$\omega$（加权方差缩减）。

### 3.3 评估协议
- **训练效率对比**：以“任务特定训练样本数”为统一横坐标，确保公平比较不同架构的样本消耗。
- **指标**：贝叶斯实验设计用EIG的sPCE下界和sNMC上界评估；贝叶斯优化用归一化遗憾值和平均排名；主动学习用加权RMSE和NLPD。

## 4. 资源与算力
- **硬件环境**：在配备 **NVIDIA H200 GPU** 的集群上进行所有实验。
- **总算力**：整个研究（含开发、调参、基线评估）总计消耗约 **2000 GPU小时**。
- **单任务训练时长**：
  - HPO-B：约 **30分钟** 每搜索空间。
  - 位置寻找：约 **5小时**。
  - CES：约 **16小时**。
  - DOCKSTRING：约 **1天**。

## 5. 实验数量与充分性
- **任务多样性**：覆盖贝叶斯实验设计、贝叶斯优化、主动学习三大领域，共5个以上基准（含真实世界数据）。
- **对比全面性**：与每个领域的经典方法和当前最先进的摊销方法（包括 DAD、RL-BOED、ALINE、NAP）进行了系统对比。
- **消融实验丰富**：
  - 骨干网络冻结 vs. 微调。
  - 策略梯度解耦 vs. 耦合（证明解耦至关重要）。
  - 预训练初始化 vs. 从头训练。
  - 不同预训练骨干（TabICL vs. TabPFN）。
  - 不同表示层（倒数几层的效果）。
  - 主动学习中对比损失感知与非感知基线。
- **统计严谨性**：所有实验均报告多次运行的均值和标准误差，采用交叉验证和固定种子，确保客观性和可复现性。

## 6. 主要结论与发现
- **样本效率极大提升**：POLAR 在各种任务上显著超越当前最先进的摊销方法，同时所需训练样本减少了 **10到100倍**。
- **性能领先**：在贝叶斯实验设计、贝叶斯优化、分子优化等多类任务中，POLAR 的最终性能均处于最优水平，或与最佳方法持平但效率更高。
- **解耦与预训练是核心**：消融实验明确证实，使用预训练骨干、解耦策略梯度比从头训练或耦合训练的效果有根本性提升，预训练信念表示是样本效率的关键。
- **通用框架**：通过仅改变任务评分规则，同一个 POLAR 架构可无缝应用于不同的自适应采集场景。

## 7. 优点与亮点
- **创新的中间层设计**：提出“信念表示”这一概念，在原始历史数据和显式后验之间取得了优雅的平衡，解决了现有方法的根本痛点。
- **巧妙利用预训练模型**：将大型表格基础模型视为现成的信念编码器，避免了昂贵且不稳定的在线后验推断。
- **梯度解耦机制**：将表示学习（监督预测损失）与决策学习（策略梯度）的梯度完全分离，稳定了训练过程，保护了预训练知识。
- **实现简洁且统一**：基于同一套架构和训练流程，仅需自定义任务效用即可处理贝叶斯设计、优化和主动学习，降低了应用门槛。

## 8. 不足与局限
- **受限于候选池设定**：当前策略在有限的候选集合中选择，不直接输出连续空间的精确坐标。虽提及可通过候选生成器扩展，但未在文中实现和验证。
- **局部信用分配的非最优性**：采用一步增量作为奖励信号，虽方差低，但在有严重延迟收益的任务中可能不是最优策略。需价值函数网络补充。
- **领域偏移敏感性**：当真实任务分布与TFM的合成预训练数据差异过大时，需要借助辅助预测损失进行微调，这可能增加一定的训练成本。
- **部署延迟**：相比于简单的后验采集（如DAD），通过大型TFM前向传播引入的耗时略高（CES任务约0.03秒每次），但仍在可接受范围内。

（完）
```
