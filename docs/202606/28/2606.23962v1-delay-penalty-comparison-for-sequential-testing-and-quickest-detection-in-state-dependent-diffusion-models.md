---
title: Delay-Penalty Comparison for Sequential Testing and Quickest Detection in State-Dependent Diffusion Models
title_zh: 状态依赖扩散模型中序贯检验与最快检测的延迟惩罚比较
authors: Ye Liang
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23962v1"
tags: ["query:ndai"]
score: 9.0
evidence: 扩散模型中的序列检验与最快检测
tldr: 本文研究状态依赖扩散模型中的顺序检验与贝叶斯最速检测，针对非马尔可夫后验建立增广状态滤波框架，推导出自由边界问题及延迟惩罚比较原理。该原理表明，逐点更大的运行延迟惩罚会缩小继续区域、导致更早停止，对线性及非线性延迟代价均适用。通过Shiryaev示例数值验证了警报阈值对延迟代价的单调性，阐明了状态信息与非线性成本对决策边界的联合影响。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23962v1/fig-001.webp\", \"caption\": \"Fig. 1 Illustration of the optimal stopping boundaries in the (Xt, pt) state space. The optimal policy partitions the space into a continuation region Cc1 and a stopping region Dc1 , separated by the boundary bc1 (x). A sample trajectory of the augmented process is shown, which starts at (X0, p0) and triggers an alarm at the optimal stopping time τ∗ upon hitting the boundary. Additionally, the figure demonstrates the comparative statics: a higher marginal delay penalty (c1 > c2) strictly lowers the optimal stopping threshold, such that bc1 (x) ≤ bc2 (x).\", \"page\": 10, \"index\": 1, \"width\": 645, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2606.23962v1/fig-002.webp\", \"caption\": \"Fig. 2 Constant-SNR Shiryaev model (λ = 0.05, ρ = 1.0). (a) Value functions Vc below the obstacle G(p) = 1 − p; the smooth-fit points p∗(c) (markers) move left and the continuation region shrinks as c increases. (b) The optimal threshold p∗(c) is monotone decreasing in the delay cost rate c, as predicted by Corollary 8.\", \"page\": 15, \"index\": 2, \"width\": 757, \"height\": 286}]"
motivation: 研究状态依赖扩散观测下顺序检验与最速检测中延迟惩罚对最优停止规则的影响。
method: 建立增广状态滤波框架，将其公式化为退化自由边界问题，并推导延迟惩罚比较原理。
result: 得到延迟惩罚增加导致更早停止的比较结论，给出最优边界单调性，并通过数值示例验证。
conclusion: 该框架揭示了状态依赖信息与非线性延迟代价如何共同决定检验与检测规则的几何结构。
---

## 摘要
我们研究漂移在两个备择假设间变化且信噪比可能取决于当前观测值的扩散观测的序贯检验与贝叶斯最快检测问题。在此设定下，后验概率通常并非封闭的一维马尔可夫统计量：自然的充分状态是由后验概率（或似然比）和观测扩散组成的增广过程。我们在这一共同的滤波框架内表述检验与最快检测问题，并识别出相应的退化自由边界问题。主要贡献是一个延迟惩罚比较原理。对于共同的终端虚警或终端决策成本，逐点更大的运行延迟惩罚会增加继续观测的价值，缩小继续区域，并导致更早停止。当停止集具有单侧后验表示时，这给出了最优报警边界的序关系。该结果适用于线性延迟成本以及经过适当马尔可夫增广后的非线性边际延迟惩罚，并通过一个恒定信噪比的 Shiryaev 例子加以说明，其中报警阈值通过数值计算得出并显示为延迟成本的单调函数。该框架阐明了状态依赖信息与非线性延迟成本如何共同影响序贯检验与最快检测规则的几何结构。

## Abstract
We study sequential testing and Bayesian quickest detection for diffusion observations whose drift changes between two alternatives while the signal-to-noise ratio may depend on the current observation. In this setting the posterior probability is generally not a closed one-dimensional Markov statistic: the natural sufficient state is the augmented process consisting of the posterior (or likelihood ratio) and the observed diffusion. We formulate both testing and quickest detection within this common filtering framework and identify the corresponding degenerate free-boundary problems. The main contribution is a delay-penalty comparison principle. For a common terminal false-alarm or terminal decision cost, a pointwise larger running delay penalty increases the value of continuation, shrinks the continuation region, and yields earlier stopping. When the stopping set has a one-sided posterior representation, this gives an order relation for the optimal alarm boundaries. The result applies to linear delay costs and to nonlinear marginal delay penalties after the appropriate Markovian augmentation, and is illustrated by a constant signal-to-noise Shiryaev example in which the alarm threshold is computed numerically and shown to be monotone in the delay cost. The framework clarifies how state-dependent information and nonlinear delay costs jointly affect the geometry of sequential testing and quickest-detection rules.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：在状态依赖扩散模型中（如漂移系数 $\mu_i(x)$ 和扩散系数 $\sigma(x)$ 随状态 $x$ 变化），后验概率 $\Pi_t$ 通常不再是一维马尔科夫过程，因此经典的序贯检验（sequential testing）与贝叶斯最快检测（quickest detection）无法直接简化为标量阈值问题。
- **核心问题**：当运行延迟惩罚（running delay penalty）改变时，最优停止规则（如报警边界）如何变化？具体而言，若增加延迟代价，决策者是否会更早报警，边界如何移动？
- **整体含义**：文章旨在建立一个统一的框架，将状态依赖扩散观测下的检验与检测问题表述为增广状态空间的最优停止问题，并证明一个“延迟惩罚比较原理”：逐点更大的运行延迟惩罚必导致继续区域缩小、停止时间更早，且若停止集具有单侧后验结构，则报警边界单调下降。

### 2. 方法论
#### 2.1 统一滤波框架
- 将序贯检验和贝叶斯最快检测都形式化为基于后验概率 $\Pi_t$（或似然比 $\Phi_t$）的最优停止问题（式（4）和（13））。
- 在状态依赖信噪比 $\vartheta(x) = \frac{\mu_1(x) - \mu_0(x)}{\sigma(x)}$ 下，证明 $(\Pi_t, X_t)$ 是时序齐次的 $\mathbb{F}^X$-马尔科夫充分统计量，其生成元为退化椭圆算子（式（16））：
  $$(\mathcal{L}_T w)(p,x) = \frac12 \vartheta^2 p^2(1-p)^2 w_{pp} + \vartheta\sigma p(1-p) w_{px} + \frac12\sigma^2 w_{xx} + [\mu_0 + (\mu_1-\mu_0)p] w_x$$
  最快检测情形则加上漂移项 $\lambda(1-p)\partial_p$。

#### 2.2 延迟惩罚比较定理（核心贡献）
- **定理6**：设 $Y$ 为公共马尔科夫状态，终端成本 $G$ 相同，运行成本 $f_1 \ge f_2$ 逐点成立，则：
  - 值函数 $V_1 \ge V_2$；
  - 继续区域 $C_1 \subseteq C_2$，停止区域 $D_2 \subseteq D_1$，且最优停止时间 $\tau_1^* \le \tau_2^*$ a.s.；
  - 若停止区域可表为 $D_i = \{ (p,x) : p \ge b_i(x) \}$，则边界函数满足 $b_1(x) \le b_2(x)$。
- 该定理仅需成本函数的逐点序关系，不依赖停止集的单调性或光滑性，对线性延迟成本（$f=c\,p$）和非线性边际惩罚（通过适当的增广状态）均适用。
- 对非线性延迟 $g$，通过引入统计量 $\Psi_t = \mathbb{E}_\pi[g'(t-\theta)\mathbf{1}_{\{\theta\le t\}} \mid \mathcal{F}_t^X]$，保持马尔科夫性（命题10），从而比较原理依然有效。

#### 2.3 验证与数值方法
- 值函数满足变分不等式 $\max\{ -(\mathcal{L}V + f),\, V - G\} = 0$，在继续区域内退化为椭圆偏微分方程。
- 数值示例中采用有限差分法（中心差分离散扩散系数，迎风格式处理漂移项）和投影迭代求解障碍问题，确保解满足变分不等式，并用鞅验证定理证明最优性。

### 3. 实验设计
- **场景**：恒定信噪比的 Shiryaev 模型（$\rho = \vartheta$ 常数），后验过程为封闭的一维扩散：
  $$d\Pi_t = \lambda(1-\Pi_t)dt + \rho \Pi_t(1-\Pi_t)d\bar{B}_t$$
- **基准**：无外部数据集或对比方法。数值实验仅用于**验证定理预测的单调性**，而非性能比较。
- **评估指标**：不同延迟成本率 $c$ 下的最优后验阈值 $p^*(c)$（及对应的似然比阈值 $\Phi^* = p^*/(1-p^*)$），考察 $c$ 增大时 $p^*$ 是否单调下降。

### 4. 资源与算力
- **未提及任何 GPU 型号、数量或训练时间**。数值解法为简单一维有限差分，在 $[0,1]$ 区间上使用 500~4000 个网格点，通过投影迭代求解，计算量极低，普通 CPU 即可完成。

### 5. 实验数量与充分性
- 仅包含**一个数值示例**（表1与图2），展示了 $\lambda=0.05, \rho=1.0$ 时 $c = 0.5, 1.0, 2.0, 5.0$ 四个成本率下的阈值结果。
- 同时展示了值函数曲线与阈值随 $c$ 的变化图。网格分辨率经双重验证（结果对半网格细化稳定）并与 shooting 法交叉核对。
- 实验**非常精简但目标明确**：仅用于例证比较原理的单调性结论，而非全面的经验评估。从理论证明的角度看实验是充分的，但缺乏与其他检测方法的性能对比或真实数据应用。

### 6. 主要结论与发现
- **比较原理成立**：更大的运行延迟惩罚缩小继续区域、导致更早停止，并在单侧停止集情形下压低报警边界（$p$ 阈值或边界函数 $b(x)$ 下降）。
- **线性与非线性推广**：线性延迟 $c$ 增大直接导致阈值 $p^*(c)$ 单调下降（图2）；非线性边际惩罚（如指数型）可通过增广状态类似地导出边界单调性。
- **状态依赖性澄清**：当 $\vartheta$ 依赖于 $X_t$ 时，停止边界由一维阈值扩展为观测依赖的曲线 $b_c(x)$，定理6(iii) 保证该曲线族在惩罚增大时整体下移。

### 7. 优点
- **理论统一性强**：将序贯检验和最快检测纳入同一马尔科夫最优停止框架，生成元显式给出，明确了状态依赖情况下的充分统计量必须包含观测状态 $X_t$。
- **比较原理普适**：只需成本函数的逐点大小关系，不依赖具体模型参数，对线性、非线性（经增广）延迟代价均适用，且不要求停止区域预先具备单调性（仅在边界推论时才需要）。
- **数学严谨**：变分不等式、鞅验证定理、自由边界退化性分析（如 Hessian 矩阵行列式为零）均给出，比较定理的证明简洁而一般。

### 8. 不足与局限
- **无实际数据验证**：完全为理论分析，仅有一个玩具数值示例，缺乏真实应用场景的数据测试。
- **实验覆盖窄**：未对比其他序贯方法（如 SPRT、CUSUM、Shiryaev-Roberts 等在非状态依赖情况下的变体），也未展示二维边界曲线 $b_c(x)$ 的实际形态或计算。
- **强假设依赖**：
  - 假设 Novikov 条件确保似然比为真鞅；
  - 数值示例中仅处理了恒定信噪比，未展示状态依赖下的二维自由边界解法；
  - 非线性惩罚的增广可能导致无限维状态，文中未给出降维方案。
- **比较原理限于贝叶斯设定**：未扩展到 minimax 准则（如 CUSUM），文中明确说明留作未来工作。
- **停止区域单侧性依赖前导结果**：边界单调性结论依赖于 Gapeev–Shiryaev 或 Ernst–Peskir 证明的“单调信噪比导致单侧停止集”这一外部结果，文章本身未证明该性质。

（完）
