---
title: Towards Robust EEG Decoding Based on Riemannian Self-Attention
title_zh: 基于黎曼自注意力的稳健脑电解码方法研究
authors: "Shaocheng Jin, Tao Zhou, Rui Wang, Ziheng Chen, Xiaoqing Luo, Xiaojun Wu, Josef Kittler"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25456v1"
tags: ["query:ndai"]
score: 10.0
evidence: 用于BCI中鲁棒EEG解码的黎曼自注意力
tldr: 针对脑电图解码中现有对称正定矩阵学习方法未显式捕捉局部关系、对噪声敏感以及常用黎曼度量（如仿射不变度量）处理病态矩阵受限的问题，本文提出基于Bures-Wasserstein度量的黎曼自注意力网络，并利用功率变形广义Bures-Wasserstein度量构建可学习版本GBWAtt，在三个基准数据集上验证了方法的鲁棒性与有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25456v1/fig-005.webp\", \"caption\": \"Figure 1: An overview of the proposed BWAtt. (a) illustrates the overall architecture of the BWAtt network; (b) shows the SPD modeling module; and (c) presents the SPD self-attention mechanism based on Bures-Wasserstein geometry.\", \"page\": 4, \"index\": 5, \"width\": 831, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2606.25456v1/fig-002.webp\", \"caption\": \"Table 3: Average performance (± standard deviation) over 10 runs, comparing GBWAtt with SOTA methods on three EEG datasets.\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2606.25456v1/fig-001.webp\", \"caption\": \"Table 5: Component-wise ablations on MAMEM and ERN.\", \"page\": 7, \"index\": 1, \"width\": 502, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2606.25456v1/fig-006.webp\", \"caption\": \"Figure 3: Heatmaps of GBWAtt for the S3 subject across four motor-imagery classes on the BCIC-IV-2a dataset. The x-axis and y-axis represent time and EEG channels, respectively.\", \"page\": 9, \"index\": 6, \"width\": 834, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2606.25456v1/fig-003.webp\", \"caption\": \"Figure 5: Heatmaps of GBWAtt for the S11 subject across five different frequencies on the MAMEM-SSVEP-II dataset. The x-axis and y-axis represent time and EEG channels, respectively.\", \"page\": 13, \"index\": 3, \"width\": 936, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2606.25456v1/fig-004.webp\", \"caption\": \"Figure 7: The heatmaps and the visualization result of GBWAtt for two classes on the BCI-ERN datasets S7 model. The x-axis represents time, and the y-axis represents EEG channels.\", \"page\": 13, \"index\": 4, \"width\": 948, \"height\": 227}]"
motivation: 现有EEG解码方法忽略低信噪比信号的局部依赖性，且传统黎曼度量难以处理病态对称正定矩阵。
method: 基于Bures-Wasserstein度量的黎曼自注意力机制，并引入功率变形广义Bures-Wasserstein度量实现可学习网络GBWAtt。
result: 在三个EEG基准数据集上，GBWAtt展现出优越的鲁棒性和解码性能。
conclusion: 提出的黎曼自注意力网络通过可学习度量增强了EEG解码的鲁棒性，为脑机接口提供了有效新方案。
---

## 摘要
基于脑电图（EEG）的脑机接口（BCI）实现了大脑与外部环境之间的直接交互，在辅助技术、医疗康复和娱乐等领域具有重要应用。近年来，基于对称正定（SPD）学习的脑电解码方法展现出卓越的性能。然而，这些方法通常采用基本的网络架构，并未明确捕捉脑电信号间的局部关系。由于脑电信号固有的低信噪比（SNR），这一局限性对脑电解码尤为不利。此外，现有的大多数基于黎曼流形的方法仅限于特定的度量。应用最广泛的是仿射不变度量（AIM），但该度量对SPD矩阵具有二次依赖性，且无法处理病态SPD矩阵，这阻碍了网络的有效性。相比之下，Bures-Wasserstein度量（BWM）对SPD矩阵呈线性依赖，并在病态情况下表现出更优的性能。为克服这些挑战，我们提出了一种基于BWM的黎曼自注意力网络。此外，最近引入的幂变形广义Bures-Wasserstein度量揭示了SPD矩阵与矩阵幂变形之间的非线性关系。该度量对SPD流形的几何结构提供了更精细的表示。因此，我们将模型扩展为一个可学习版本。为简洁起见，我们称其为GBWAtt。在三个脑电基准数据集上的实验结果验证了所提方法的稳健性和有效性。代码可在 https://github.com/jissc/GBWAtt 获取。

## Abstract
Brain-Computer Interface (BCI) based on electroencephalography (EEG) enables direct interaction between the brain and external environments and has significant applications in assistive technologies, medical rehabilitation, and entertainment. Recently, EEG decoding methods based on Symmetric Positive Definite (SPD) learning have demonstrated superior performance. However, these methods typically employ basic network architectures and do not explicitly capture local relationships between EEG signals. This limitation is problematic for EEG signals due to their inherently low Signal-to-Noise Ratio (SNR). Moreover, most existing Riemannian manifold-based methods are restricted to specific metrics. The most widely used is the Affine-Invariant Metric (AIM). However, it has a quadratic dependency on the SPD matrices and cannot handle ill-conditioned SPD matrices, which hinders the effectiveness of networks. In contrast, the Bures-Wasserstein Metric (BWM) exhibits linear dependence on SPD matrices and demonstrates superior performance for ill conditioning. To overcome these challenges, we propose a Riemannian self-attention network based on the BWM. Additionally, the recently introduced power-deformed generalized Bures-Wasserstein metric reveals a nonlinear relationship between SPD matrices and matrix power deformation. This metric provides a more nuanced representation of the geometric structure of the SPD manifold. Consequently, we extend our model to a learnable version. For simplicity, we refer to it as GBWAtt. Experimental results on three EEG benchmarking datasets validate the robustness and effectiveness of our proposed method. The code is available at https://github.com/jissc/GBWAtt.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将使用中文，以 Markdown 形式为您提供对这篇论文的结构化、深入且客观的总结。

### 1. 论文核心问题与整体含义

*   **研究背景**：基于脑电图（EEG）的脑机接口（BCI）在多个领域有重要应用。近年来，利用对称正定（SPD）矩阵对 EEG 信号的二阶统计量（如协方差矩阵）进行建模，并在黎曼流形上进行学习的方法，因能捕捉脑电信号的非欧几何特性而表现出色。
*   **核心问题与动机**：
    *   **结构局限**：现有基于 SPD 的深度学习方法大多采用朴素架构，未能显式捕捉 EEG 信号间的**局部依赖关系**，这对于处理低信噪比（SNR）的 EEG 信号尤为关键。
    *   **度量鲁棒性不足**：大多数方法使用的仿射不变度量（AIM）对 SPD 矩阵具有**二次依赖性**，在处理**病态 SPD 矩阵**时数值不稳定，影响模型性能。
*   **整体含义**：本文旨在解决上述两个关键挑战。提出一个更鲁棒的黎曼自注意力网络，通过引入数值稳定性更好的 Bures-Wasserstein 度量（BWM），来显式建模 SPD 特征间的局部关系，并进一步通过可学的权力变形广义 BWM 来适应数据几何结构，从而实现更稳健、高效的 EEG 解码。

### 2. 方法论

论文提出了一种名为 GBWAtt 的黎曼自注意力网络，其核心思想和技术细节如下：

*   **核心思想**：
    1.  在 SPD 流形上，基于 Bures-Wasserstein（BW）几何结构，构建能够捕捉序列内局部关系的自注意力机制。
    2.  利用可学习的幂变形广义 Bures-Wasserstein 度量（$\theta$-GBWM），动态适应 SPD 流形的几何结构，并提升对病态矩阵的鲁棒性。
*   **关键技术细节与算法流程**：
    *   **时空特征提取模块**：
        *   使用两个卷积层从原始 EEG 信号中提取特征。第一个卷积层进行空间滤波，第二个卷积层提取时空特征。
        *   输出记为 $G \in \mathbb{R}^{n \times l}$。
    *   **SPD 建模模块**：
        *   将特征 $G$ 沿通道维度分割成 $m$ 个不重叠的片段 $\{ \tilde{G}_1, \dots, \tilde{G}_m \}$。
        *   计算每个片段 $\tilde{G}_i$ 的协方差矩阵，并进行迹归一化和正则化（添加小扰动），得到最终的 SPD 序列 $\{ X_1, \dots, X_m \}$。
    *   **SPD 自注意力模块 （核心贡献）**：
        *   **SPD 变换层**：使用双线性映射 $f(X_i, W) = W X_i W^\top$，其中 $W$ 是行满秩矩阵，将输入 SPD 矩阵从 $\mathbb{S}^+\\_n$ 映射到 $\mathbb{S}^+\\_k$ （低维），生成查询（$Q_i$）、键（$K_i$）和值（$V_i$。
        *   **SPD 注意力层**：在 BW 度量下，定义注意力分数。
            *   相似度计算：$a_{ij} = \frac{1}{1 + \log(1 + d_{BW}(Q_i, K_j))}$，其中 $d_{BW}$ 是 BW 距离。
            *   归一化：使用 Softmax 函数对 $a_{ij}$ 矩阵进行归一化，得到注意力权重矩阵 $\tilde{A}$。
        *   **SPD 聚合层**：使用加权 Fréchet 均值（WFM）代替欧氏空间的加权平均来聚合信息。对于第 $i$ 个输出，其聚合结果为 $R_i = \text{WFM}(\{ \tilde{A}_{ij} \}_{j=1}^m, \{ V_j \}_{j=1}^m)$。
    *   **流形到欧氏空间的嵌入映射**：
        *   非线性激活：使用 ReEig 层，通过阈值化输入矩阵的特征值来引入非线性，同时保持 SPD 属性。
        *   空间映射：使用 LogEig 层，$\hat{R} = V \text{diag}(\log(\sigma_1), \dots, \log(\sigma_k)) V^\top$，将 SPD 矩阵映射到其切空间（一个对称矩阵空间）。
        *   特征展平与分类：将映射后的对称矩阵的上三角部分展平为向量，输入到全连接层和 Softmax 函数中进行分类。
    *   **扩展到 GBWAtt**：通过一个微分同胚映射 $\varphi(X) = (M^{\frac{1}{2}} X M^{\frac{1}{2}})^\theta$，将上述基于 BWM 的自注意力推广到基于 $\theta$-GBWM 的版本，其中 $M$ 和 $\theta$ 是可学习的参数，使模型能自适应地调整流形几何结构。

### 3. 实验设计

*   **数据集与场景**：实验在三个公开的 EEG 基准任务上进行，覆盖了多种 BCI 范式。
    *   **运动想象 （MI）**：使用 **BCIC-IV-2a** 数据集，包含 9 名受试者的四分类任务（想象左手、右手、脚、舌头运动）。
    *   **稳态视觉诱发电位 （SSVEP）**：使用 **MAMEM-SSVEP-II** 数据集，包含 11 名受试者的五分类任务。
    *   **错误相关负波 （ERN）**：使用 **BCI-ERN** 数据集，包含 26 名受试者的二分类任务（检测 P300 拼写任务中的错误反馈）。
*   **对比方法 （Benchmark）**：对比分为两大类，以全面评估模型性能。
    *   **欧氏深度学习模型**：ShallowConvNet， EEGNet, SCCNet, MBEEGSE, TCNet-Fusion， FBCNet。
    *   **黎曼几何深度学习模型**：SPDNet, SPDNetBN, MAtt， GDLNet。
*   **评估指标**：MI 和 SSVEP 任务使用**准确率**，ERN 任务使用**AUC 值**。所有结果报告了 10 次运行的平均值和标准差。

### 4. 资源与算力

论文明确指出所有实验均在配备以下硬件的平台上进行：
*   **CPU**：Intel i9-13900HX
*   **内存**：16GB
*   **GPU**：单块 NVIDIA RTX4060

此外，论文未明确报告每个实验或完整训练过程的具体 GPU 小时数。

### 5. 实验数量与充分性

论文进行了较为全面和充分的实验，以验证所提方法的有效性和鲁棒性。

*   **主实验**：在 3 个不同性质的数据集上对比了 8 个 SOTA 方法，证明了 GBWAtt 的优越性。
*   **消融实验**：
    *   **模块级消融**：逐一移除时空特征提取模块、GBW-SSAM 等核心部件，并用欧氏自注意力模块替换 GBW-SSAM，验证了各组件的重要性。
    *   **组件级消融**：深入分析了四个关键设计选择：度量（欧氏、AIM、BWM、GBWM）、注意力评分函数、聚合方式（欧氏平均、切空间平均、Fréchet 均值）以及流形到欧氏映射层。量化了每个设计对性能的贡献。
*   **参数与超参数分析**：研究了变形参数 $\theta$ 不同取值对性能的影响。
*   **泛化性与鲁棒性分析**：
    *   **大数据集**：在两个大规模临床 EEG 数据集（TUAB， TUEV）上，将 GBWAtt 集成到 EEG 基础模型（BIOT， LaBraM）中进行测试。
    *   **有限数据**：通过在 MAMEM 数据集上按比例减少训练数据，评估了模型的小样本泛化能力。
    *   **噪声鲁棒性**：对输入数据添加不同强度的高斯噪声，并与 MAtt 对比了性能下降程度，同时统计了模型处理后病态 SPD 特征的比例。
*   **可解释性**：通过绘制梯度热力图和脑地形图，分析了模型对 MI、SSVEP 和 ERN 任务中关键通道和时间段的响应，符合神经科学先验知识。
*   **公平性**：论文严格遵循了各数据集标准的训练/验证/测试划分协议，并与已发表的 SOTA 方法结果进行了对比，对比过程公平。

### 6. 主要结论与发现

*   **GBWAtt 性能最优**：在包含运动想象、SSVEP 和 ERN 的三个主流 EEG 解码基准测试中，所提出的 GBWAtt 均实现了最优性能，显著优于传统的欧氏深度学习模型和 SOTA 的 SPD 学习方法（如 MAtt）。
*   **$\theta$-GBWM 度量更优**：基于可学习的幂变形广义 Bures-Wasserstein 度量（$\theta$-GBWM）在所有数据集上均优于标准的 BWM 以及仿射不变度量（AIM），证明了动态几何建模和数值稳定性的优势。
*   **几何保真设计至关重要**：消融研究证实，在 SPD 流形上使用基于测地线距离的相似度函数和 Fréchet 均值聚合，比直接套用欧氏空间的操作（切线空间内积、欧氏平均）效果更好，突出了在流形内部保持几何结构的重要性。
*   **鲁棒性强**：GBWAtt 在数据量稀缺及高噪声环境下仍表现出优于 MAtt 的鲁棒性，并能产生更少的病态 SPD 特征，这与 BWM 更好的数值稳定性理论相符。

### 7. 优点

*   **方法论创新**：首次将 Bures-Wasserstein 度量引入到 SPD 自注意力机制中，有效解决了 AIM 在处理病态矩阵时的数值不稳定性。
*   **可学习几何**：通过 $\theta$-GBWM 将固定的度量参数化为可学习的版本，使网络能自适应地调整几何结构以适应数据，思路新颖。
*   **全面的实验验证**：实验设计非常扎实，不仅覆盖了三个多样化的 EEG 基准任务，还包含了 SOTA 对比、多层次消融、参数分析、鲁棒性测试、大型临床数据集验证以及模型可解释性分析，证据链完整。
*   **开源可复现**：论文提供了代码仓库链接，有助于提升研究的可复现性。

### 8. 不足与局限

*   **计算开销**：与基于欧氏空间或较简单黎曼度量（如 MAtt）的方法相比，GBWAtt 涉及 BW 几何下的迭代求解（如 Fréchet 均值）和矩阵操作，导致额外的计算成本和更长的训练时间，论文对此有明确提及。
*   **极端噪声下的性能差距缩小**：论文自身分析表明，尽管 GBWAtt 通常更鲁棒，但在输入协方差结构因极强噪声而严重破坏时，其相对于基线方法的优势会变小，因为此时所有模型的 SPD 输入已信息量不足。
*   **应用范围局限**：该方法作为方法论研究，目前仅在三个特定类型的 EEG 任务上进行了验证。其在其他 BCI 范式（如情感识别、睡眠分期）或其他非脑电时序数据上的适用性尚未可知。
*   **伦理声明**：论文声明其使用公开的、去标识化数据集，目的是方法论研究而非临床决策，本身不引入额外伦理风险。

（完）
