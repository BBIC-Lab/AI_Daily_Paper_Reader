---
title: "Zero-order Parameter-free Optimization for LMO-based Methods: Novel Approach for Efficient Fine-tuning"
title_zh: 面向基于LMO方法的零阶无参数优化：高效微调的新方法
authors: "Dmitriy Bystrov, Daniil Medyakov, Dmitry Bylinkin, Aleksandr Beznosikov"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14970v1"
tags: ["query:ndai"]
score: 9.0
evidence: 零阶无参数优化用于微调LLMs
tldr: 本研究针对大语言模型微调中反向传播内存开销大的问题，探索零阶优化与无参数优化的结合。提出AdaNAGED方法，通过线性最小化预言机（LMO）利用参数块几何结构，实现零阶优化中步长等参数的自动自适应调整和非欧几里得更新。在OPT-1.3B模型上验证了收敛性和高效微调性能。
source: arxiv
selection_source: fresh_fetch
motivation: 零阶优化虽节省内存但需昂贵的人工调参，且现有方法忽视参数块异质性，缺乏自动适应问题特性的能力。
method: 提出AdaNAGED，将无梯度训练、自适应参数调整与基于LMO的几何感知更新统一，实现零阶优化的无参数自适应。
result: 理论证明了收敛性，并在OPT-1.3B大规模微调任务上验证了方法的有效性。
conclusion: AdaNAGED通过无参数自适应和几何结构利用，实现了大模型微调中零阶优化的高效自动调参，避免了反向传播的内存瓶颈。
---

## 摘要
微调大型语言模型（LLMs）已成为现代优化的核心应用，使预训练模型能够适应各种下游任务和领域特定数据。大规模微调的一个主要障碍是反向传播的内存开销，这需要存储激活值、梯度和优化器状态。零阶（ZO）优化提供了一种内存高效的替代方案，但其性能对步长和平滑参数高度敏感，通常需要昂贵的特定任务调优。无参数（PF）优化通过在没有问题相关常数先验知识的情况下自适应算法参数来解决这个问题。此外，大规模微调可以受益于考虑参数块异质结构的几何感知更新，这可以通过利用线性最小化预言（LMO）的方法来建模。在这项工作中，我们研究了基于LMO的ZO优化的PF自适应，并引入了\texttt{AdaNAGED}，一种统一了无梯度训练、自适应调优和非欧几里得更新几何的方法。我们建立了收敛保证，并在使用\texttt{OPT}-1.3\mathrm{B}模型的大规模LLM微调任务上验证了该方法。

## Abstract
Fine-tuning large language models (LLMs) has become a central application of modern optimization, enabling pretrained models to adapt to diverse downstream tasks and domain-specific data. A major obstacle in large-scale fine-tuning is the memory overhead of backpropagation, which requires storing activations, gradients, and optimizer states. Zeroth-order (ZO) optimization offers a memory-efficient alternative, but its performance is highly sensitive to the stepsize and smoothing parameter, often requiring costly task-specific tuning. Parameter-free (PF) optimization addresses this issue by adapting algorithmic parameters without prior knowledge of problem-dependent constants. Moreover, large-scale fine-tuning can benefit from geometry-aware updates that account for the heterogeneous structure of parameter blocks, which can be modeled through methods that exploit linear minimization oracle (LMO). In this work, we study PF adaptation for LMO-based ZO optimization and introduce $\texttt{AdaNAGED}$, a method that unifies gradient-free training, adaptive tuning, and non-Euclidean update geometry. We establish convergence guarantees and validate the method on large-scale LLM fine-tuning task with $\texttt{OPT}-1.3\mathrm{B}$ model.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心问题**：大语言模型（LLM）微调过程中，传统依赖反向传播的一阶优化方法会产生巨大的内存开销，而零阶（ZO）优化虽能缓解内存压力，但其性能高度依赖步长和平滑参数等超参数的手动调节，成本高昂且不切实际。
*   **研究动机**：
    *   **内存瓶颈**：LLM微调时，反向传播需要存储激活值、梯度和优化器状态，对硬件要求极高。
    *   **调参难题**：ZO优化的性能对超参数极为敏感，理论上最优的超参数依赖于实际中不可知的问题常数（如Lipschitz常数），导致实践中依赖重复的、昂贵的网格搜索。
    *   **几何结构忽视**：现有ZO优化方法多采用欧几里得更新，未能充分利用模型中不同参数块（如向量、矩阵）的异质几何结构来提升优化效率。
*   **整体含义**：本文旨在提出一种统一的无参数（PF）、零阶（ZO）且几何感知的优化框架，以实现LLM微调中**自动自适应超参数**、**避免反向传播内存成本**以及**利用参数结构实现高效更新**的三大目标。

### 2. 论文提出的方法论

*   **整体框架与核心思想**：
    *   提出 **`AdaNAGED`** (ADAptive Norm-Agnostic Gradient-frEe Descent) 方法，结合了零阶梯度估计、无参数步长与平滑参数自适应、以及基于线性最小化预言机（LMO）的非欧几里得更新。
    *   核心思想是沿优化轨迹在线估计局部光滑性常数 $L$，并以此动态调整步长 $\gamma_t$ 和平滑参数 $\tau_t$，从而摆脱对预先知晓问题常数的依赖。

*   **关键技术细节与公式**：
    1.  **局部光滑性估计器**：
        为替代不可知的全局Lipschitz常数 $L$，使用ZO梯度差分来近似局部光滑性：
        $$L_t = \frac{\| g_t(x_{t+1}, e_t) - g_t(x_t, e_t) \|_*}{\| x_{t+1} - x_t \|}$$
        其中 $g_t(x, e_t)$ 是在点 $x$ 处、沿随机方向 $e_t$ 的ZO梯度估计。为降低内存开销，方向 $e_t$ 仅存储其随机种子。
    2.  **无参数步长与平滑参数**：
        *   聚合局部光滑性估计：$S_t = S_{t-1} + L_t$。
        *   自适应步长：$\gamma_t = \sqrt{\frac{f(x_0) - \underline{f}}{\rho \sqrt{S_{t-1}}}}$，其中 $\rho$ 为LMO半径，$f(x_0)-\underline{f}$ 是估计的初始最优性间隙。
        *   自适应平滑参数：$\tau_t = \rho C_2 \gamma_t$，确保与步长线性缩放以控制误差界。
    3.  **LMO驱动的几何感知更新**：
        *   模型更新方向由LMO给出：$v_t = \text{lmo}(g_t(x_t, e_t)) = \arg\min_{\|v\| \le \rho} \langle g_t(x_t, e_t), v \rangle$。
        *   通过选择不同的范数 $\|\cdot\|$，该框架可涵盖多种优化器。例如：
            *   选择 $\ell_\infty$ 范数：退化为ZO-SignSGD类型算法。
            *   选择谱范数：退化为ZO-Muon类型算法，通过LMO计算矩阵参数的极分解。
        *   对于谱范数下的LMO，为降低成本，采用Newton-Schulz迭代近似极分解算子，形成更实用的 `AdaMuGED` 算法。

### 3. 实验设计

*   **任务与数据集**：在 **SST-2** 情感分类任务上微调预训练模型，这是一个标准的LLM下游任务基准。
*   **模型**：使用 **OPT-1.3B** 模型，该规模足以体现反向传播的内存压力。
*   **对比方法**：将提出的两种方法 `AdaNAGED`（基于$\ell_\infty$）和 `AdaMuGED`（矩阵几何）与三个经过**手动调参**的零阶基准方法进行对比：
    *   **ZO-AdaMM**（自适应动量方法）。
    *   **ZO-SignSGD**（基于符号梯度的方法）。
    *   **ZO-Muon**（基于矩阵几何的方法）。
*   **训练与评估**：所有方法均训练 **20,000** 步，报告训练过程中达到的最佳准确率。

### 4. 资源与算力

*   **硬件配置**：实验基于 **PyTorch** 实现，使用 **1 块 NVIDIA A100 GPU** 和 1 个 Intel Xeon 2.20 GHz CPU。
*   **算力开销**：论文提及，所有实验的总运行时间约为 **100 小时**。

### 5. 实验数量与充分性

*   **实验数量**：实验部分包含了在**单一基准任务**（SST-2）上对一种规模的模型（OPT-1.3B）的评估，对比了提出的2种方法变体与3种基准方法。
*   **充分性与公平性评估**：
    *   **优点**：对比基准覆盖了主流的ZO优化器（基于动量、符号和矩阵几何），且所有基准方法都经过了手动调优，比较公平。
    *   **不足**：实验规模非常有限，仅在**一个数据集**和**一个模型**上进行了验证。缺乏在更多样的NLP任务、不同规模/架构的模型（如LLaMA系列）上的测试，且没有进行消融实验（例如，评估参数 $\xi$ 或不同Newton-Schulz步数 $q$ 的影响）。因此，实验结果的普适性和稳健性论证不够充分。

### 6. 论文的主要结论与发现

*   **理论发现**：论文为所提出的 `AdaNAGED` 及其 `AdaMuGED` 变体提供了严格的收敛性保证，在凸且光滑的假设下，得到了 $O(1/\varepsilon^2)$ 的渐近最优收敛率。理论分析额外揭示了归因于参数自适应和随机梯度估计的额外因子。
*   **实证发现**：在OPT-1.3B/SST-2任务上，提出的无参数方法性能与经过精细手动调参的零阶基准方法**具有竞争力**。具体地，`AdaNAGED` 达到了 0.903 的准确率，`AdaMuGED` 达到了 0.908，仅以微小差距（0.2-0.9个百分点）落后于最佳调优基准（ZO-Muon为0.921）。

### 7. 优点

*   **方法创新与完整**：首次成功将零阶优化、无参数自适性和LMO几何感知更新统一在一个框架下，理论扎实。
*   **内存与调参效率高**：方法在推理过程中通过种子重生技术，不存储ZO扰动方向，保持内存高效；且完全免除代价高昂的手动超参数搜索，实用性强。
*   **理论保证**：即使在零阶且无参数的情况下，仍能提供收敛性证明，增强了方法的可靠性。
*   **框架灵活**：通过更换范数，`AdaNAGED` 框架可以轻松衍生出多种风格的优化器，适用性广。

### 8. 不足与局限

*   **实验验证极不充分**：这是论文最大的局限。仅在**一项任务**（SST-2）和**一种模型**（OPT-1.3B）上进行了验证，缺乏多样性和普适性证据，难以支撑其作为通用方法的宣称。缺乏消融研究和稳健性测试。
*   **理论与实际的差距**：理论分析基于目标函数**L-光滑、凸且M-Lipschitz连续**的假设。虽然文中提及了LLMs可表现出近凸性，但这与高度非凸的深度神经网络实际本质仍存在差距，理论的直接指导意义可能受限。
*   **特定组件的依赖**：方法需要预先提供一个目标函数值的**下确界** $\underline{f}$（例如 $\underline{f}=0$），这在某些任务中可能无法轻易获得或也不准确。
*   **算法实现复杂性**：`AdaMuGED` 版本需要在每一步训练中进行Newton-Schulz迭代，虽然避免了昂贵的SVD分解，但依然引入了额外的计算开销，其实际效率可能低于标准ZO方法。

（完）
