---
title: Label Shift Aware Adaptation for Online Zero-shot Learning with Contrastive Language-Image Pre-Training (CLIP)
title_zh: 面向在线零样本学习的标签偏移感知自适应方法——基于对比语言-图像预训练（CLIP）
authors: "Pengxiao Han, Changkun Ye, Yanshuo Wang, Jinguang Tong, Miaohua Zhang, Xuesong Li, Jie Hong, Lars Petersson"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15169v1"
tags: ["query:ndai"]
score: 9.0
evidence: 针对零样本CLIP的标签偏移自适应方法
tldr: 针对在线零样本学习中使用CLIP模型时，测试数据标签分布与训练数据不一致导致性能下降的问题，本文提出标签偏移感知（LSA）方法。LSA将在线分类建模为域适应任务，仅利用无标签测试数据将CLIP的源预测适应到目标分布，并引入标签偏移校正，有效缓解分布不匹配。实验表明，该方法在多个数据集上一致超越现有基于CLIP的在线零样本学习技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15169v1/fig-001.webp\", \"caption\": \"Figure 2: The framework of the proposed LSA. Test samples arrive sequentially in a streaming manner. A frozen CLIP zero-\", \"page\": 4, \"index\": 1, \"width\": 1029, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2606.15169v1/fig-002.webp\", \"caption\": \"Table 1\", \"page\": 6, \"index\": 2, \"width\": 1025, \"height\": 318}]"
motivation: 现有方法忽略CLIP训练数据的分布与在线测试数据分布的差异，导致标签偏移时性能显著衰退。
method: LSA将在线零样本分类视为无监督域适应问题，仅使用无标签测试数据调整CLIP预测，并进行标签偏移校正。
result: 在多个数据集上，LSA性能一致优于现有最先进的基于CLIP的在线零样本学习方法。
conclusion: 通过显式建模和校正标签偏移，LSA增强了CLIP在在线零样本学习场景下的鲁棒性和准确性。
---

## 摘要
视觉-语言模型，如对比语言-图像预训练（CLIP），在数据稀缺场景中已得到广泛研究。该领域中一个尤为挑战且贴近现实的任务是基于CLIP的在线零样本学习：在顺序推理阶段，固定特征提取和模型参数，由CLIP按随机顺序依次预测未知测试样本。现有的大多数方法通过利用到来的测试样本来在线调整表征，但却忽略了CLIP初始训练数据的分布。当测试数据中的标签分布与训练域不同时，这种不匹配可能导致性能下降。为弥补这一不足，我们提出了标签偏移感知（LSA）方法，将在线零样本分类任务形式化为域自适应问题。具体而言，LSA将CLIP（在未知源分布上训练）计算出的预测结果，仅利用无标签测试数据将其自适应到目标分布，并应用标签偏移校正来缓解源域与目标域之间的不匹配。在多个数据集上的大量实验表明，所提出的LSA方法一致地优于基于CLIP的最新在线零样本学习方法。

## Abstract
Vision-language models like Contrastive Language-Image Pre-Training (CLIP) have been extensively studied in data-scarce scenarios. A particularly challenging and realistic task in this area is online zero-shot learning with CLIP, where unknown test samples are predicted sequentially in random order by CLIP while keeping the feature extraction and model parameters fixed during the sequential inference phase. Most existing approaches in this setting address the problem by adapting representations online using incoming test samples, while neglecting the distribution of the data on which CLIP was initially trained. This mismatch can lead to degraded performance when the label distribution in the test data differs from that of the training domain. To address this gap, we propose Label Shift Aware (LSA), which formulates the online zero-shot classification task as a domain adaptation problem. Specifically, LSA adapts the predictions computed by CLIP, which was trained on an unknown source distribution, to a target distribution using only unlabeled test data, and applies label shift correction to mitigate the mismatch between the source and target domains. The extensive experiments across multiple datasets demonstrate that the proposed LSA consistently outperforms state-of-the-art online zero-shot learning methods based on CLIP.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Label Shift Aware Adaptation for Online Zero-shot Learning with Contrastive Language-Image Pre-Training (CLIP)》的详细结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景**：视觉-语言模型（如 CLIP）在零样本学习（ZSL）与测试时自适应（TTA）中表现出色。然而，一个更具挑战性且贴近实际的场景是**在线零样本学习**（Online ZSL），即测试数据以流形式依次到达，模型需即时预测，且不能存储历史数据。
*   **核心问题**：现有在线 ZSL 方法（如 OnZeta）主要关注多模态对齐或提示工程，但它们普遍忽略了 CLIP **预训练数据（源域）与在线测试数据（目标域）之间的标签分布不匹配问题**。当测试流中的类别分布发生“标签偏移”时，CLIP 的预测会产生偏差，导致性能显著下降。
*   **整体含义**：本文旨在解决在线零样本场景下的标签偏移问题，提出一种无需修改 CLIP 参数、轻量级的在线自适应框架，以在流式数据中保持高效和鲁棒的分类性能。

### 2. 论文提出的方法论

论文将在线零样本分类视作一种特殊的**无监督域适应任务**，并基于**标签偏移假设**设计算法。

*   **核心思想与假设**：
    *   将问题定义为：将未知源分布 $p_{\text{source}}(x,y)$ 上训练的分类器 $f(x)$，适配到目标分布 $p_{\text{target}}(x,y)$，且仅依赖无标签的在线目标样本 $\{x_t\}$。
    *   假设 CLIP 强大的预训练已缓解协变量偏移（$p(x)$ 变化），**标签偏移**（$p(y)$ 变化而 $p(x|y)$ 不变）是性能下降的主因。

*   **关键技术细节**：
    1.  **标签偏移估计（LSA 权重生成）**：
        *   假设源域的标签分布 $p_{\text{source}}(y)$ 未知但大致均匀，将其设为 $\mathbf{c} = \mathbf{1}/K$。
        *   利用 EM 算法动态估计目标域的标签分布 $\boldsymbol{\pi} \in \Delta^{K-1}$。
        *   E 步：计算每个样本属于各类别的后验概率 $g_{t,j}$，其依据当前估计的 $\boldsymbol{\pi}^{(m)}$ 和 CLIP 预测 $f(x_t)_j$。
        *   M 步：结合数据项（$g_{t,j}$ 的累积）与 Dirichlet 先验 $\boldsymbol{\alpha}$ 更新 $\boldsymbol{\pi}^{(m+1)}$。更新公式为一个线性组合，其混合权重由超参数 $\lambda_t$ 控制。
    2.  **主动先验自适应**：
        *   $\lambda_t$ 并非固定值，而是随时间步 $t$ 线性增长：$\lambda_t = \frac{t}{N} \lambda_0$。
        *   这使得算法在早期样本少时倾向于信任先验，后期样本充足时更依赖实时数据分布。
    3.  **LSA 分类器校正**：
        *   利用估计出的目标标签分布 $\boldsymbol{\pi}$，对 CLIP 原始预测进行后验调整（重加权）：
        *   $\tilde{f}(x_t)_j = \frac{f(x_t)_j / \pi_j}{\sum_{l=1}^K f(x_t)_l / \pi_l}$。
        *   这一操作旨在抵消源域与目标域标签分布不匹配带来的偏差，且不涉及任何梯度计算或参数更新。
*   **整体框架流程**：如算法2所示，测试样本到来 -> CLIP 给出预测 -> 存入缓冲 -> 更新 $\lambda_t$ -> EM 算法估计当前 $\boldsymbol{\pi}$ -> 用 $\boldsymbol{\pi}$ 校正预测 -> 输出最终分类精度。

### 3. 实验设计

*   **数据集**：
    *   **大规模通用数据集**：ImageNet。
    *   **13个多样化标准数据**：Aircraft, Caltech101, Cars, Cifar10, Cifar100, CUB, DTD, EuroSAT, Flowers, Food101, Pets, SUN, UCF101。覆盖通用分类、细粒度识别、纹理、遥感、场景等。
*   **Benchmark 与对比方法**：
    *   **Baseline**：标准 CLIP 零样本分类（CLIP baseline）。
    *   **对比方法**：TPT（测试时提示调优）和 OnZeta（在线零样本分类 SOTA）。
    *   **自身变体**：LSA 与 OnZeta 结合（LSA+OnZeta），以证明其可作为插件。
*   **评价协议**：遵循 Qian 和 Hu (2024) 设定的在线协议。数据以单次流式到达，不存储、不重训。为消除流随机顺序的影响，每次都执行5次不同随机排列的测试并取平均 Top-1 准确率。

### 4. 资源与算力

*   **提及情况**：论文**未明确说明**使用的 GPU 型号、数量或任何实验的训练 / 推理时长。
*   **推断**：由于方法**无训练**（training-free），且在线推理只涉及轻量级的 EM 迭代和代数运算，不更新大型模型参数，因此推断其对算力要求极低，适合资源受限的边缘设备部署。但原文缺少对算力和推理延迟的定量分析。

### 5. 实验数量与充分性

*   **实验组数**：
    1.  **ImageNet 实验**：在5种不同架构的 CLIP 骨干网络上（ResNet-50, ViT-B/32, ViT-B/16, ViT-L/14, ViT-L/14@336px）验证。
    2.  **13个数据集实验**：在 ResNet-50 和 ViT-B/16 两个骨干上进行全面 SOTA 对比。
    3.  **消融研究**：论文提及在补充材料中进行了消融实验，分析各组件的贡献以及超参数 $\lambda_0$ 的影响，但正文未呈现数据。
*   **充分性与公平性**：
    *   实验覆盖了从轻量 CNN 到大型 ViT 的多种模型，以及14个不同性质的数据集，**范围广泛**。
    *   与 TPT、OnZeta 等代表性方法对比，比较**基线合理**。所有方法均遵循同一在线协议和多次取平均的方法，**评估客观公平**。
    *   但正文中消融实验结果的缺失，降低了对方法内部机制理解的可达性。

### 6. 论文的主要结论与发现

*   LSA 在所有测试场景下，均一致地优于 CLIP baseline 和 OnZeta，在 ImageNet 上最大提升 $2.56%$（ViT-B/16），在 13 个数据集上平均相较 OnZeta 提升 $2.91%$（ResNet-50）和 $2.02%$（ViT-B/16）。
*   标签偏移是影响 CLIP 在线零样本性能的关键因素，通过显式建模和动态纠正目标域标签分布，可以有效提升分类鲁棒性。
*   LSA 作为独立于模型和骨干的轻量级模块，可以即插即用，且无需训练或存储数据，适合在线流式环境。

### 7. 优点

*   **视角新颖**：首次将在线零样本 CLIP 问题明确阐述为标签偏移下的域适应任务，为该领域提供了新的切入点。
*   **方法轻量高效**：完全无训练，不更新 CLIP 参数，仅通过后验重加权校正，内存占用极低，计算开销小，适合实时部署。
*   **兼容性强**：方法无模型结构依赖，可作为即插即用模块与任何 CLIP 分类器（甚至如 OnZeta 这类新方法）结合，进一步提升性能。
*   **实验扎实**：在14个数据集、5种骨干模型上验证，证明了方法的泛化性和一致性改进。

### 8. 不足与局限

*   **分布假设强度**：方法严重依赖“标签偏移”假设成立（即 $p(x|y)$ 不变）。当协变量偏移和标签偏移同时发生，或存在概念漂移（条件分布 $p(x|y)$ 也变化）时，方法可能失效，论文未对此进行讨论或验证。
*   **源分布均匀性假设**：算法中为处理未知的 $p_{\text{source}}(y)$，将其假定为均匀分布 $\mathbf{c}=\mathbf{1}/K$。若 CLIP 的真实预训练类别分布极不均匀，此近似会引入估计偏差。
*   **超参数敏感性**：性能依赖于 Dirichlet 先验强度（通过 $\lambda_0$ 控制），虽然在补充材料中研究了其影响，但实际应用中可能需要针对新环境进行调整。
*   **消融实验未在主文呈现**：关于各组件（如主动先验自适应）贡献及关键超参数影响的实验被放到了补充材料中，削弱了论点在正文中的完整性。
*   **缺乏对协变量偏移的讨论**：主要动机之一是 CLIP 预训练已缓解协变量偏移，但并未提供实验或理论证据支撑这一关键前提。

（完）
