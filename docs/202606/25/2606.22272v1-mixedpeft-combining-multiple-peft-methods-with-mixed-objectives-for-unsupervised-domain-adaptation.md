---
title: "MixedPEFT: Combining Multiple PEFT Methods with Mixed Objectives for Unsupervised Domain Adaptation"
title_zh: MixedPEFT：结合多种PEFT方法与混合目标进行无监督领域自适应
authors: "Mohammed Rawhani, Dervis Karaboga, Ozkan Ufuk Nalbantoglu, Alper Basturk, Bahriye Akay"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22272v1"
tags: ["query:ndai"]
score: 9.0
evidence: 使用参数高效微调的无监督域适应
tldr: "预训练模型领域自适应时全微调昂贵且易灾难性遗忘，本文提出MixedPEFT，融合可逆适配器与LoRA，通过混合目标（源域分类+目标域MLM）进行无监督训练。在MNLI 20个域迁移上，仅用7%参数，精度较UDapter提升1.41点，超越全调DANN，为参数高效域自适应立新标杆。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22272v1/fig-002.webp\", \"caption\": \"TABLE I: Comprehensive Comparison with Current State-of-the-Art UDApter and our Previous Approach (PREV.) (Macro F1 Scores in %)\", \"page\": 4, \"index\": 2, \"width\": 474, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2606.22272v1/fig-001.webp\", \"caption\": \"TABLE III: Comparison with Traditional UDA Methods (DANN, DSN) and Upper Bound (Macro F1 Scores in %)\", \"page\": 5, \"index\": 1, \"width\": 493, \"height\": 419}]"
motivation: 全微调成本高且灾难性遗忘，需要参数高效的无监督领域自适应方法。
method: 联合可逆适配器与LoRA，同步优化源域分类和目标域掩码语言建模。
result: "在MNLI多域迁移上，以7%参数超越现有参数高效和全调方法，提升1.41个百分点。"
conclusion: 精心设计的PEFT组合与混合目标能显著提升参数高效无监督领域自适应性能。
---

## 摘要
预训练语言模型在应用于新领域时表现不佳，因为全量微调计算成本高且容易导致灾难性遗忘。本研究通过提出一种新颖的参数高效策略来应对这一挑战，该策略用于无监督领域自适应，将自定义PEFT架构与混合目标训练相结合。我们的方法同时优化带标签源领域数据上的分类性能和未标记目标领域数据上的掩码语言建模（MLM），在适应源领域任务的同时保留目标领域知识。我们的方法在统一的参数高效框架内使用了可逆适配器和低秩适应（LoRA）的自定义组合。通过在Multi-Genre自然语言推理（MNLI）数据集上跨20个领域偏移的全面评估，我们的方法相较于现有方法取得了显著改进：比当前参数高效的先进方法UDapter提高了1.41个百分点，比全量微调的DANN基线提高了1.26个百分点，比DSN提高了0.86个百分点，同时仅使用了模型可训练参数的7%。这些结果为参数高效的无监督领域自适应设立了新的基准，并证明了精心设计的PEFT组合与并发优化能够超越现有的参数高效方法和传统的全量微调方法。

## Abstract
Pre-trained language models struggle when applied to new domains, as full fine-tuning is computationally expensive and prone to catastrophic forgetting. This study addresses this challenge by presenting a novel parameter-efficient strategy for unsupervised domain adaptation that combines custom PEFT architectures with mixed-objective training. Our approach simultaneously optimizes classification performance on labeled source domain data and masked language modeling (MLM) on unlabeled target domain data, preserving target domain knowledge while adapting to source domain tasks. Our method employs a custom union of invertible adapters and Low-Rank Adaptation (LoRA) within a unified parameter-efficient framework. Through comprehensive evaluation on the Multi-Genre Natural Language Inference (MNLI) dataset across 20 domain shifts, our approach achieves significant improvements over existing methods: 1.41 percentage points over the current parameter-efficient state-of-the-art UDapter, 1.26 percentage points over the fully-tuned DANN baseline, and 0.86 percentage points over DSN, while utilizing only 7% of the model's trainable parameters. These results establish new benchmarks for parameter-efficient unsupervised domain adaptation and demonstrate that carefully designed PEFT combinations with concurrent optimization can outperform both existing parameter-efficient methods and traditional fully-tuned approaches.

---

## 论文详细总结（自动生成）

## 论文总结：MixedPEFT —— 结合多 PEFT 方法与混合目标的无监督领域自适应

### 1. 核心问题与研究背景
- **问题**：预训练语言模型（如 BERT）在面临领域偏移时性能骤降，全量微调成本高、存储需求大，且易发生灾难性遗忘。
- **研究动机**：无监督领域自适应（UDA）通常仍依赖全参数更新，参数效率低下；参数高效微调（PEFT）方法（如适配器、LoRA）虽已兴起，但在 UDA 的应用中多为单一方法，未系统探索多种 PEFT 的组合及协同优化潜力。
- **目标**：在不使用目标领域标签的前提下，利用混合 PEFT 与混合目标训练，实现既参数高效又高性能的领域自适应。

### 2. 方法论
- **核心思想**：在精心设计的 PEFT 组合（可逆适配器 + LoRA）上，同时优化两个目标 —— 源域分类损失与目标域掩码语言建模（MLM）损失，从而兼顾任务适应与领域知识保持。
- **架构设计**：
  - 自定义联合（Custom Union）：  
    $$h = \text{InvertibleAdapter}(x) + \text{LoRA}(x)$$  
    两个模块独立贡献并保持跳跃连接，总可训参数仅约 768 万（占 BERT-base 的 7.055%）。
  - 为验证通用性，也使用 UniPELT 作为另一种 PEFT 统一框架。
- **训练流程**（两阶段，仅更新 PEFT 参数）：
  - **阶段 1（自适应预训练）**：对目标域未标注数据执行 MLM，损失为  
    $$L_{\text{MLM}} = -\sum_{i\in M} \log P(x_i \mid x_{\text{masked}})$$
  - **阶段 2（混合目标微调）**：将源域分类损失 $L_{\text{classification}}$ 与目标域 MLM 损失联合训练，总损失  
    $$L_{\text{total}} = \alpha \times L_{\text{classification}} + (1-\alpha) \times L_{\text{MLM}}$$  
    其中权重  
    $$\alpha = \frac{n_{\text{source}}}{n_{\text{source}} + n_{\text{target}}}$$  
    根据数据量动态平衡两个目标。

### 3. 实验设计
- **数据集与场景**：MNLI（Multi-Genre Natural Language Inference），构建 20 个领域迁移方向（如 Fiction→Government、Slate→Telephone 等），全面评估多风格迁移下的自然语言推理能力。
- **评估指标**：Macro F1 分数。
- **对比方法**：
  - 参数高效 SOTA：**UDapter**，以及作者先前顺序组合方法（PREV.）。
  - 全量微调传统 UDA：**DANN**、**DSN**。
  - 强基线：全量微调且有目标域标签的 **Upper Bound**。
- **实现细节**：基于 BERT-base-uncased（1.095 亿参数），可逆适配器降维因子 2，LoRA 秩=8、alpha=16；优化器 AdamW，学习率 1e-4，批大小 32（梯度累积 4 步），混合目标阶段 10 个 epoch。所有基线结果直接引用自 UDapter 的配置，确保公平对比。

### 4. 资源与算力
- 文中 **未明确说明** 使用的 GPU 型号、数量或实际训练时长。仅报告了可训练参数占比（7%），缺乏具体的算力消耗和效率对比。

### 5. 实验数量与充分性
- **主实验**：在 20 个域迁移对上，全面对比 5 类方法（UDapter、PREV.、DANN、DSN、Upper Bound），结果列于表 I 与表 III。
- **消融实验**：选取 2 个迁移方向，对比“单一可逆适配器”与“PEFT 组合”的效果（表 II），证实组合的必要性。
- **额外分析**：
  - 标准差分析（表 IV）评估稳定性。
  - 参数效率与性能对比（表 V）。
  - 通用性验证：在 UniPELT 框架下同样使用混合目标训练，观察到一致提升。
- **公平性**：严格遵循 UDapter 的超参数与评测协议，基线结果从官方代码库直接获得，实验设计较为充分、客观。

### 6. 主要结论与发现
- **性能突破**：Custom Union 平均 F1 达 76.39%，比 UDapter 高 **1.41 个百分点**，比 PREV. 高 0.95，比全量 DANN 高 **1.26 个百分点**，比 DSN 高 0.86，仅用 7% 参数；达到上限的 96.7%。
- **混合目标与组合的乘数效应**：单一 PEFT 方法在混合目标训练下提升有限，必须结合多种 PEFT 才能充分利用混合目标策略。
- **通用性**：混合目标训练在 UniPELT 上同样有效（76.28%），证明该策略不受限于特定 PEFT 架构。
- **稳定性**：在性能显著提升的同时，标准差与现有方法相当（0.43–0.45），未牺牲稳定性。

### 7. 优点
- **创新性结合**：首次将 PEFT 组合与混合目标（分类+MLM）训练引入无监督领域自适应，避免了复杂的对抗训练或域分离网络。
- **参数效率卓越**：仅需 7% 参数便超越多款全量微调方法，显著降低部署成本。
- **实验体系完善**：跨 20 个领域迁移，多类基线对照，消融与通用性验证齐备，结论可信度高。
- **动态损失平衡**：根据源域与目标域样本量自动调节混合权重，训练过程更稳健。

### 8. 不足与局限
- **任务与数据单一**：仅在 MNLI（自然语言推理）上验证，未覆盖情感分析、阅读理解等其他 NLP 任务，领域多样性有限。
- **模型规模受限**：仅使用 BERT-base，未在更大模型（RoBERTa、DeBERTa 等）或现代大语言模型上测试，泛化性未知。
- **算力信息缺失**：未报告 GPU 型号、训练时长等资源开销，不利于评估实际可行性和能耗。
- **组合策略固定**：PEFT 组合（可逆适配器+LoRA）为手工设计，未探索自动搜索或自适应选择机制。
- **超参数敏感性分析不足**：未讨论 LoRA 秩、适配器维度、学习率等关键超参数的影响，也没有与其他动态权重策略对比。
- **对比基准可能未充分优化**：全调 DANN、DSN 在参数高效设置下若配合现代微调技巧，对比结果或需谨慎解读。
- **可复现性**：未随文发布代码或详细配置，复现难度较高。

（完）
