---
title: "EMA-FS: Accelerating GBDT Training via Gain-Informed Feature Screening"
title_zh: EMA-FS：基于增益信息的特征筛选加速GBDT训练
authors: Yan Song
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.26337v1"
tags: ["query:ndai"]
score: 9.0
evidence: 使用历史分裂增益作为特征选择的重要性权重
tldr: "GBDT训练中构建特征直方图占据65-70%时间，传统随机子采样忽略特征重要性。本文提出EMA-FS，利用指数移动平均跟踪各特征历史分裂收益，仅对高收益特征构建直方图，在保持LightGBM直方图减法兼容性的同时实现加速。在稠密中高维数据集上最高取得2.61倍加速，稀疏数据无效果。进一步提出S-EMA-FS，通过增益加权随机采样统一确定性与随机筛选，代码仅120行C++且完全向后兼容。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26337v1/fig-003.webp\", \"caption\": \"Table 2: EMA-FS adaptations across LightGBM tree learner types.\", \"page\": 7, \"index\": 3, \"width\": 782, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-009.webp\", \"caption\": \"Table 3: Dataset characteristics.\", \"page\": 8, \"index\": 9, \"width\": 980, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-011.webp\", \"caption\": \"Table 5: Criteo CTR results (39 features, advertising click-through prediction).\", \"page\": 9, \"index\": 11, \"width\": 636, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-006.webp\", \"caption\": \"Table 6: Synthetic benchmark results (500 features, dense binary classification).\", \"page\": 10, \"index\": 6, \"width\": 764, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-004.webp\", \"caption\": \"Table 7: IEEE-CIS Fraud Detection results (432 features, real transaction fraud).\", \"page\": 10, \"index\": 4, \"width\": 636, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-005.webp\", \"caption\": \"Table 8: Bosch results (968 features, industrial defect detection, extremely sparse).\", \"page\": 10, \"index\": 5, \"width\": 636, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-001.webp\", \"caption\": \"Table 9: Cross-dataset summary of EMA-FS effectiveness at r=0.5.\", \"page\": 11, \"index\": 1, \"width\": 772, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-002.webp\", \"caption\": \"Table 10: EMA-FS (r=0.3) vs. random feature subsampling (f=0.3) across datasets.\", \"page\": 11, \"index\": 2, \"width\": 953, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-008.webp\", \"caption\": \"Table 12: S-EMA-FS results across all seven datasets. Configurations that beat baseline AUC while being faster are marked with ⋆.\", \"page\": 13, \"index\": 8, \"width\": 826, \"height\": 731}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-007.webp\", \"caption\": \"Table 13: Estimated EMA-FS speedup at r=0.3 across learner types.\", \"page\": 14, \"index\": 7, \"width\": 980, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2606.26337v1/fig-010.webp\", \"caption\": \"Table 16: Selected iteration-level metrics for EMA-FS (r=0.3) vs. baseline on the Synthetic dataset.\", \"page\": 19, \"index\": 10, \"width\": 510, \"height\": 255}]"
motivation: GBDT训练瓶颈在于耗时的特征直方图构建，现有随机特征子采样忽略预测效用造成信息浪费。
method: 基于指数移动平均维护特征分裂收益，预热后仅对收益排名前K的特征进行直方图构建，并扩展出增益加权随机采样的随机化版本。
result: "在稠密中高维数据上实现1.45-2.61倍加速，70%保留率下AUC提升0.11点；极稀疏数据无加速，但随机化版本提供灵活选择。"
conclusion: EMA-FS是一种有效、兼容且简便的GBDT训练加速算法，通过收益感知筛选避免无用直方图构建，随机化版本进一步统一了确定性与随机特征选择。
---

## 摘要
梯度提升决策树（GBDT），以LightGBM为代表，将训练时间的主要部分——通常65-70%——花费在构建每个特征的直方图上。现有方法如随机特征子采样（feature_fraction）丢弃特征时未考虑其预测效用。我们提出基于EMA的特征筛选（EMA-FS），这是一种算法级优化，在提升迭代过程中维护每个特征分裂增益的指数移动平均（EMA），并在短暂的热身期后，将直方图构建限制在根据历史增益排名前K的特征上。与随机子采样不同，EMA-FS是有信息指导的：它保留高增益特征，同时筛除低增益特征。它运行在每棵树级别，完全兼容LightGBM的直方图相减技巧，无需更改核心例程。我们在多个数据集上评估了EMA-FS，涵盖金融欺诈检测、广告点击率预测、工业质量控制和合成基准，特征维度从29到968。在密集、中高维数据上，它实现了显著加速：在30%保留率下，500维合成基准上达到2.61倍，432维IEEE-CIS欺诈数据集上达到1.45倍；在70%保留率下，它将AUC提高0.11点，同时实现1.34倍加速。在极度稀疏的数据（Bosch，缺失率>90%）上，它没有产生加速，因为LightGBM的稀疏分箱优化已经跳过了空值。我们进一步引入随机EMA-FS（S-EMA-FS），用增益加权的随机采样取代确定性的前K选择，并由集中参数β控制，将确定性EMA-FS（β→无穷）和随机子采样（β=0）统一在一个框架中。两者均在所有六种LightGBM树学习器中以约120行C++代码实现，并完全向后兼容。

## Abstract
Gradient Boosted Decision Trees (GBDT), exemplified by LightGBM, spend a dominant fraction of training time -- typically 65-70% -- constructing per-feature histograms. Existing approaches such as random feature subsampling (feature_fraction) discard features without regard for their predictive utility. We propose EMA-based Feature Screening (EMA-FS), an algorithm-level optimization that maintains an exponential moving average (EMA) of per-feature split gains across boosting iterations and, after a short warmup, restricts histogram construction to the top-K features ranked by historical gain. Unlike random subsampling, EMA-FS is informed: it retains high-gain features while screening out low-gain ones. Operating at the per-tree level, it preserves full compatibility with LightGBM's histogram subtraction trick, requiring no changes to core routines.   We evaluate EMA-FS on datasets spanning financial fraud detection, advertising click-through prediction, industrial quality control, and synthetic benchmarks, with feature dimensionalities from 29 to 968. On dense, moderate-to-high-dimensional data it achieves significant speedups: 2.61x on a 500-feature synthetic benchmark and 1.45x on the 432-feature IEEE-CIS Fraud dataset at 30% retention. At 70% retention it improves AUC by 0.11 points while delivering a 1.34x speedup. On extremely sparse data (Bosch, >90% missing) it yields no speedup, as LightGBM's sparse bin optimization already bypasses empty values.   We further introduce Stochastic EMA-FS (S-EMA-FS), which replaces deterministic top-K selection with gain-weighted random sampling controlled by a concentration parameter beta, unifying deterministic EMA-FS (beta -> infinity) and random subsampling (beta = 0) in one framework. Both are implemented in ~120 lines of C++ across all six LightGBM tree learners and are fully backward-compatible.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心瓶颈**：GBDT（以 LightGBM 为代表）在训练时，构建每个特征的直方图（histogram）占到总耗时约 65–70%，成为内存带宽瓶颈，传统的底层优化（如 SIMD、缓存优化）难以进一步加速。
- **现有方案缺陷**：随机特征子采样（如 `feature_fraction`）能减少直方图构建量，但选择过程完全随机，无视特征的真实预测价值，容易丢弃高增益特征，在激进采样率下造成不必要的精度损失。
- **整体含义**：本文提出基于信息增益的特征筛选方法 EMA‑FS，利用历史分裂增益自适应地识别并保留重要的特征，只对排名靠前的特征构建直方图。再进一步推广出随机化版本 S‑EMA‑FS，在一个框架内统一了确定性的增益筛选与随机的子采样，从而兼顾训练加速、精度保持与集成多样性。

### 2. 论文提出的方法论

- **核心思想**：观测到在大量数据集中，少数特征贡献了绝大部分的分裂增益，且特征重要性在 boosting 迭代间具有持续性。因此，可以用一个指数移动平均（EMA）在线追踪每个特征的历史增益，在短暂的预热后，每棵树只选择 EMA 值最高的前 $K = \lceil r \cdot F\rceil$ 个特征参与直方图构建和分裂搜索。

- **关键技术细节 (EMA‑FS)**
  - **增益跟踪与更新**：对于特征 $f$，记录当前树内部的**最大**分裂增益 $\hat{g}_f^{(t)}$（取最大值以避免惩罚仅局部重要的特征）。用 EMA 更新历史增益：
    $$g_f^{(t)} = \alpha \cdot \hat{g}_f^{(t)} + (1-\alpha) \cdot g_f^{(t-1)}$$
    $\alpha$ 为平滑参数（默认 0.3），使 EMA 能适应后期特征重要性的漂移，同时提供自然的“重新进入”机制（低增益特征 EMA 逐渐衰减，后期若变重要可再次被选中）。
  - **暖启与筛选**：前 $W$ 棵树（默认 5）作为暖启，使用全部特征。之后每棵树开始前，按 $g_f$ 降序选前 $K$ 个特征，整个树的所有节点共享同一组特征，从而**完全保留 LightGBM 的直方图相减加速技巧**（兄弟节点直方图需基于相同特征集合）。
  - **计算开销**：每棵树仅需 $O(F)$ 更新 EMA、$O(F\log F)$ 排序和掩码写入，相比直方图构建的 $O(NF)$ 可忽略。

- **随机化推广 (S‑EMA‑FS)**
  - **动机**：确定性的 EMA‑FS 缺失了随机森林中至关重要的树间多样性，而随机子采样虽有多样性却无信息指导。S‑EMA‑FS 用增益加权随机采样取代 top‑K 硬筛选：
    1. 从 EMA 值计算采样权重 $w_f = (g_f + \epsilon)^{\beta}$，其中 $\beta$ 为集中参数，$\epsilon$ 确保非零概率。
    2. 归一化为概率 $p_f$，不放回抽取 $K$ 个特征。
  - **统一框架**：$\beta=0$ 退化为均匀随机子采样（`feature_fraction`）；$\beta=1$ 为线性增益加权；$\beta \to \infty$ 逼近确定性的 EMA‑FS。该框架同时享有增益引导的采样效率和随机性带来的集成多样性。

- **实现**：所有方法以约 120 行 C++ 集成在 LightGBM 的全部六种树学习器（串行、GPU、特征并行、数据并行、投票并行、线性树）中，完全向后兼容，通过 6 个直观参数控制。

### 3. 实验设计

- **数据集与场景**
  - 低维：CreditCard Fraud (29 维)、Criteo CTR (39 维)。
  - 中高维稠密/混合：Synthetic 二分类 (500维)、IEEE-CIS Fraud (432维)、FraudDense (2M 样本 × 500 稠密特征)、FraudMixed (2M 样本 × 500 混合稀疏度特征)。
  - 极端稀疏：Bosch 工业缺陷检测 (968 维，缺失率 > 90%)。
  - 领域覆盖：金融欺诈、广告点击率、工业质检等表格数据常见场景。

- **基准与对比方法**
  - **基线**：未经修改的 LightGBM（100 棵树、31 叶、`deterministic=true`）。
  - **对比方法**：
    - 随机特征子采样（`feature_fraction=0.3`）；
    - EMA‑FS：不同特征保留比例 $r \in \{0.3, 0.5, 0.7\}$（部分数据集包含 0.1、0.9）；
    - S‑EMA‑FS：$r$ 范围 0.1–0.9，集中参数 $\beta \in \{0.5, 1.0\}$。
  - 评价指标：训练时间加速比、AUC 变化（部分给出对数损失）。

### 4. 资源与算力

- 论文中**未明确说明使用的具体 CPU 型号、GPU 型号或数量**。
- 记载的训练配置为 **4 线程（通过 `taskset` 固定到核心 0–3）**，时间测量基于 LightGBM 内部的 `GBDT::Train` 开销（不含数据加载）。
- 对于 GPU 树学习器，文中给出了预估加速比（约 2–2.5×），但**未提供实际 GPU 实验的测量结果**，主要实验仍在 CPU 上完成。

### 5. 实验数量与充分性

- **实验组数充足**：
  - 7 个数据集上的完整实验，涵盖从极低维到高维稀疏的各种数据特性。
  - 对 EMA‑FS 进行了多级保留比例（0.3、0.5、0.7 等）的消融，并与随机子采样在相同比例下直接对比。
  - 对 S‑EMA‑FS 扫描了 $r$ 与 $\beta$ 的组合，定位可同时提速且提升 AUC 的配置。
  - 验证了线性树学习器上的正确性、展示了迭代级 AUC 变化、分析了 Bosch 稀疏数据无效原因。
- **充分性与公平性**：
  - 实验对比直接、基线明确，参数和种子固定，配置重复 3 次取均值，具备可复现性。
  - 特别设计了 FraudDense 和 FraudMixed 两个大规模（200 万样本）真实风控场景基准，增强了结论的工程说服力。
  - **不足**：未与其他已发表的增益感知采样方法（如 LGBM‑CBFS）进行实验对比，仅作了概念性区分；GPU、分布式环境下的速度仅停留在理论估计，缺乏实测。

### 6. 论文的主要结论与发现

- **有效加速条件**：在特征较稠密且维度中等以上（100–500+）的数据集上，EMA‑FS 能获得显著加速（如 500 维合成数据上 2.61×，IEEE-CIS 上 1.45×），同时 AUC 损失可控；在 70% 保留率下甚至可通过筛除噪声特征实现隐式正则化，AUC 提升 0.11 点。
- **失效条件**：极度稀疏数据（Bosch，缺失率 >90%）因 LightGBM 的稀疏分箱已自动跳过空值，EMA‑FS 几乎无加速；特征数少于 50 的低维数据筛选余地太小；所有特征均匀重要时（如 Criteo）筛选会损害精度。
- **S‑EMA‑FS 的优势**：该框架能同时实现训练加速与精度提升（例如 FraudDense 上 1.34× 加速且 AUC +0.004，IEEE-CIS 上 1.21× 加速且 AUC +0.002），突破了确定性 EMA‑FS 或随机采样各自无法兼顾的局限。
- **实用价值**：在金融风控等特征维度常达数百的场景中，EMA‑FS 在极小代码改动下即可嵌入现有 LightGBM 流水线，并兼容所有学习器与数据并行下的通信压缩。

### 7. 优点

- **极简集成与兼容性**：仅约 120 行 C++，不影响直方图减法、所有学习器与现有参数，实际部署风险低。
- **清晰的创新与统一框架**：从观察“特征重要性持续性与计算瓶颈”出发，设计了轻量级 EMA 跟踪，并通过 S‑EMA‑FS 的 $\beta$ 参数优雅地统一了确定性格雷迪 Boosting 中的信息筛选与随机森林中的多样性机制。
- **扎实的工程验证**：覆盖了低维、高维、稀疏、稠密、超大规模等 7 种数据集，并给出了明确的“何时有效、何时无效”的边界条件，对实践具有直接指导意义。
- **引入隐式正则化**：实验证明在高比例保留时，通过去除噪声特征反而能提升 AUC，提供了额外的收益。

### 8. 不足与局限

- **实验对比局限性**：未与同样利用增益采样的同期方法（如 LGBM-CBFS）进行定量比较，导致无法判断 EMA/F S-EMA-FS 相较于同类增益感知采样方法的相对优势。
- **分布式与 GPU 实测缺失**：数据并行下的通信加速、GPU 学习的加速均停留于理论估计，缺少实际多机/多卡环境的测量支持。
- **参数敏感性**：预热期 $W$、EMA 平滑参数 $\alpha$、集中参数 $\beta$ 需根据数据特性手动设定，缺少自适应调节的机制；文中对不同数据集给出的最优 $\beta$ 不同（稠密噪声数据用 0.5，混合稀疏数据用 1.0），增加了调参负担。
- **极端场景的适用限制**：在极度稀疏或特征维度过低的数据上几乎无增益，甚至在稀疏数据上引入随机子采样时反而可能拖慢训练（由于列采样器开销）。对特征重要性发生剧烈翻转的动态场景，EMA 的跟踪能力可能不足。
- **偏差风险**：实验数据集主要为表格赛题或合成数据，在其他领域（如推荐系统、NLP 表格化）的泛化性未经验证。

（完）
