---
title: The Inference-Compute Frontier and a Latency-Efficient Architecture for Limit Order Book Prediction
title_zh: 推理-计算前沿与限价订单簿预测的延迟高效架构
authors: C. Evans Hedges
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25986v1"
tags: ["query:ndai"]
score: 9.0
evidence: 分析预测限价订单簿的时序数据
tldr: 在限价订单簿预测中，研究推理计算前沿。使用FI-2010及多种模型，发现预测损失与结构化前向计算量遵循幂律，但延迟空间无此规律，表明延迟不仅是带噪声的计算。基于此提出FastBiNLOB，一种密集轴可分离LOB混合器，以硬件友好操作实现低延迟，在五种子实验中以更低延迟超过y10和y100宏F1指标。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25986v1/fig-002.webp\", \"caption\": \"Figure 1: Excluded MLPLOB operating-point observed versus frontier-implied test cross-entropy. Frontier-implied losses are generated from the full non-MLPLOB structural-work frontier. Shapes distinguish W64, W128, and W256; colors distinguish target horizon.\", \"page\": 4, \"index\": 2, \"width\": 357, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2606.25986v1/fig-003.webp\", \"caption\": \"Figure 2: CF7 example of the full non-MLPLOB structuralwork frontier. Each panel shows one horizon’s non-MLPLOB lower-envelope points, fit power-law curve, and W128 MLPLOB frontier-implied versus actual target test crossentropy.\", \"page\": 4, \"index\": 3, \"width\": 514, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2606.25986v1/fig-004.webp\", \"caption\": \"Figure 3: Structural work andCPU single-observation latency reorder architecture families. The plot uses the matched configuration-level CPU single-observation timing diagnostic, with repeated CF/horizon rows collapsed to unique configurations. W128 MLPLOB has far more structural work than the low/mid pool, but lower CPU single-observation latency than several classical configurations.\", \"page\": 5, \"index\": 4, \"width\": 427, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2606.25986v1/fig-001.webp\", \"caption\": \"Figure 4: FastBiNLOB architecture overview. The model applies BiN-style normalization, embeds the time-major LOB window, runs two residual axis-separable blocks, reduces the temporal axis, and uses separate prediction heads for each horizon.\", \"page\": 6, \"index\": 1, \"width\": 807, \"height\": 342}]"
motivation: 探索限价订单簿预测中推理计算与延迟的缩放规律，并设计低延迟高效架构。
method: 利用FI-2010数据和多种模型拟合计算量-损失前沿，并据此构建硬件友好的FastBiNLOB模型。
result: 非MLPLOB集合的幂律外推R²为0.941；FastBiNLOB在延迟低于SOTA的情况下达到了目标宏F1。
conclusion: 推理计算前沿可用幂律描述，且通过轴可分离混合可实现极低延迟的高精度LOB预测。
---

## 摘要
我们研究了限价订单簿预测中是否会出现类似缩放定律的推理-计算前沿。利用FI-2010和从小型决策树到神经LOB架构的一系列模型，我们发现预测损失与结构前向工作之间的实际经验前沿由幂律很好地概括。特别是，将MLPLOB作为一个架构族排除在外，对低和中等计算量的非MLPLOB前沿拟合的幂律可跨多个数量级外推，并在排除的高计算量MLPLOB目标前沿上达到$R^2=0.941$。在延迟空间中的类似实验给出的结果明显较弱，表明延迟不仅仅是带噪声的计算量。我们利用这一差距来推动FastBiNLOB，这是一种由硬件友好的时间和特征混合操作构建的密集轴可分离LOB混合器。在五次种子实验中，FastBiNLOB以显著低于现有已发表SOTA架构的延迟，超越了已发表的$y_{10}$和$y_{100}$宏F1目标。

## Abstract
We study whether a scaling-law-style inference-compute frontier appears in limit order book prediction. Using FI-2010 and a suite of models ranging from small decision trees to neural LOB architectures, we find that the realized empirical frontier of predictive loss versus structural forward work is well summarized by a power law. In particular, with MLPLOB held out as an architecture family, a power-law fit to the low- and mid-compute non-MLPLOB frontier extrapolates across multiple orders of magnitude and attains $R^2=0.941$ on the excluded high-compute MLPLOB target frontier.   A similar exercise in latency space gives substantially weaker results, showing that latency is not merely noisy compute. We use this gap to motivate FastBiNLOB, a dense axis-separable LOB mixer built from hardware-friendly temporal and feature mixing operations. In a five-seed experiment, FastBiNLOB exceeds the published $y_{10}$ and $y_{100}$ macro-F1 targets at notably lower latency than existing published SOTA architectures.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：限价订单簿（LOB）预测不仅需要准确，还必须满足极低延迟要求，才能在价格变动前捕捉信号。然而，模型复杂度的提升往往伴随着推理延迟的增加，导致“精度高但用不了”的困境。学术界尚不清楚在LOB预测中，模型容量（以计算量衡量）与预测损失之间是否存在稳定的缩放规律，也不清楚延迟是否仅仅是计算量的噪声版本。
- **核心问题**：是否存在一个可归纳的**推理-计算前沿**（inference-compute frontier），即在FI‑2010基准上，不同架构的预测损失与结构化前向工作之间是否服从幂律？进一步，该前沿能否跨架构族外推，从而定位高计算量神经模型的性能？实际测量延迟与计算量之间有何差异？
- **整体含义**：论文意图为LOB预测建立“计算量–损失”的缩放法则，并借此指导低延迟架构设计。通过证明计算量与延迟不可混为一谈，进而提出硬件友好的密集轴可分离混合器FastBiNLOB，在达到甚至超越现有最优（SOTA）精度的同时，显著降低批大小为1的推理延迟。

### 2. 论文提出的方法论
- **结构化前向工作（structural forward work）**
  - 定义一种与硬件无关的可复现容量坐标 $\text{ops}(f)$，精确计数一次观测的前向操作（如决策树的分裂比较次数、叶节点查找、logit相加；CatBoost对称树的深度和查找；随机卷积的时序点积、池化、标准化和线性头；MLPLOB的嵌入、特征混合器、时序混合器和分类头等）。
  - 该计数**不包含**内存访问、缓存、分支预测、向量化、框架调度等实际运行时开销。
- **推理-计算前沿拟合**
  - 给定交叉验证折和预测视界，定义低、中计算量模型集$\mathcal{M}$的**经验下包络**：
    $$L^*(C) = \min\{L(f) : f\in\mathcal{M}, \text{ops}(f) \le C\}$$
    其中$L(f)$为测试交叉熵（以nat计）。
  - 用以下幂律形式拟合并报告：
    $$L(C) = L_\infty + A C^{-\alpha}$$
    拟合前对$C$做中位数缩放，约束$L_\infty, A, \alpha \ge 0$，最小化平方误差。
  - **架构族留出测试**：将所有MLPLOB配置（窗口大小W64/W128/W256）完全排除在拟合之外，仅用决策树、直方图梯度提升、CatBoost、随机卷积逻辑模型等拟合前沿，再用该前沿外推预测被排除的MLPLOB目标损失，计算$R^2$。
- **延迟作为另一坐标**
  - 用CPU上单次观测的测量延迟替换结构工作，重复类似分析，发现前沿弱得多（$R^2=0.468$），且模型族在延迟轴上的排序与计算量轴完全不同。
- **FastBiNLOB架构**
  - **动机**：利用“有用计算”与“低效实现”之间的鸿沟，将计算尽可能放入硬件友好的稠密时序和特征混合操作中，避免注意力和反复的布局变换。
  - **结构**：
      - 输入：完整144维FI‑2010窗口 $\mathbf{x}\in\mathbb{R}^{144\times 128}$。
      - 归一化：简单BiN式归一化，处理量纲混合和非平稳性。
      - 时间主元嵌入：转置为时间主元 $\mathbf{z}_0 = \mathbf{x}^\top \mathbf{W}_\text{in} + \mathbf{E}_\text{time}$，输出形状 $128\times H$（$H$为隐藏宽度）。
      - 残差轴可分离块（重复两次）：
        - 特征通道MLP $F_\theta$：对每个时间步独立应用，混合LOB特征。
        - 时序MLP $G_\phi$：对每个隐藏通道独立应用，全局混合128个事件时间位置。
        - 两次残差加法：$\mathbf{z}' = \mathbf{z} + F_\theta(\mathbf{z})$，$\mathbf{z}_\text{out} = \mathbf{z}' + [G_\phi(\mathbf{z}'^\top)]^\top$。
      - 时间缩减：均值池化（H96 mean）或可学习锥形缩减器（H120 taper）。
      - 多视界预测头：每个预测视界使用独立线性头。
  - **关键设计考量**：无注意力，无Q/K/V投影，无softmax，无块间层归一化；使用GELU激活，dropout 0.05；$H=96$侧重低延迟，$H=120$配时序扩展10追求高性能。

### 3. 实验设计
- **数据集**：FI‑2010限价订单簿数据集。
- **实验通道**：
  - **缩放通道（scaling lane）**：使用 NoAuction Z‑score 归一化的原始 LOB40 特征（CF1–CF9 交叉验证），主指标为**测试交叉熵**。目标是研究推理‑计算前沿，排除 MLPLOB 后进行架构族外推测试。
  - **部署通道（deployment lane）**：使用完整144维FI‑2010特征集，训练天1‑7，测试天8‑10，窗口大小 $W=128$。主指标为**宏F1**（与MLPLOB/TLOB公开比较一致）和**批‑1延迟**。
- **对比方法**：
  - 缩放通道拟合池：决策树、直方图梯度提升、CatBoost对称树、随机卷积逻辑模型。
  - 目标池（留出）：MLPLOB（W64, W128, W256）作为高计算量神经架构代表。
  - 部署通道基准：已发表的MLPLOB和TLOB的宏F1目标，以及对应的延迟。
  - FastBiNLOB自身两种配置：H96 mean（低延迟）和H120 taper（高性能）。
- **延迟审计**：在 MacBook Pro（M3 Max, 16 核CPU, 128 GB RAM）上，使用CPU和MPS后端，PyTorch eager FP32 执行，测量模型单次前向加softmax的热路径，计10000次调用，取P50/P90等百分位。

### 4. 资源与算力
- **训练算力**：论文未明确说明训练所用GPU型号、数量或总训练时长。从模型规模（最大仅为MLPLOB和中等宽度的FastBiNLOB）和数据集大小推断，训练可在普通计算资源上完成，未使用大规模集群。
- **推理延迟测量**：明确在 MacBook Pro（M3 Max, 16 核CPU, 128 GB 内存）上进行，含12 intra‑op线程和16 inter‑op线程，Python 3.12.2, PyTorch 2.12.0，测试热路径的延迟百分位。
- **一般性说明**：论文更关注推理端的延迟优化，而非训练算力消耗。

### 5. 实验数量与充分性
- **缩放通道实验规模**：
  - 初始筛选：从833个CF‑1发现行中选出56个低/中计算量配置（覆盖决策树9个、直方图梯度提升30个、CatBoost 6个、随机卷积11个），固定用于所有折/视界。
  - 留出目标：W64、W128、W256 MLPLOB各45行（9折×5视界），共135个目标行。
  - 主要外推评估：CF2‑CF9上的120个MLPLOB行（排除CF1因数据量小），以及仅保留改善下包络的目标前沿（42行）。
  - 鲁棒性检查：对每个架构族独立施加对数均匀$[\frac12, 2]$乘法扰动，重复500次重新拟合并计算外推$R^2$。
  - 事后在全前沿（包括MLPLOB）上重做拟合，得到$R^2=0.902$。
- **部署通道实验规模**：
  - 五次不同随机种子多任务训练，报告宏F1均值和标准误。
  - 延迟对比涵盖MLPLOB、TLOB（H16）和FastBiNLOB两个配置，使用低值P50作为主对比。
- **充分性与公平性**：
  - 实验设计有清晰的留出策略（架构族排除、交叉验证折排除），避免了信息泄露。
  - 鲁棒性检验验证了结果对计算量定义的敏感性。
  - 部署对比严格遵循已发布协议，使用相同训练/测试划分和指标。
  - 总体实验量大，评估维度全面，具有较强客观性和可复现性。

### 6. 论文的主要结论与发现
- **推理‑计算前沿存在且可外推**：
  - 在原始LOB40特征上，低/中计算量非MLPLOB模型的经验下包络可由幂律很好概括。
  - 排除MLPLOB后，该幂律外推至高计算量MLPLOB目标前沿，获得 $R^2=0.941$（目标前沿），W64+W128 子集甚至达到 $R^2=0.948$。这证明前沿具有跨架构族、跨数量级的规律性。
  - W256被主导（使用更多工作未降低损失），符合前沿应排除无效计算的特征。
- **延迟不是带噪声的计算量**：
  - 用测量延迟替代结构工作，拟合质量大幅下降（$R^2=0.468$），且模型族排序发生显著变化（如CatBoost计算量中位12,276，延迟仅281μs；而直方图梯度提升计算量更低却延迟4.34ms）。这说明延迟取决于计算的组织方式，而非仅计算量。
- **FastBiNLOB 取得延迟‑精度双优**：
  - 在完整144特征部署中，H96 mean以低于MLPLOB约23.7%的延迟超越 $y_{10}$ 宏F1目标；H120 taper以低于TLOB约60.1%的延迟超越 $y_{100}$ 目标，并创下该比较设置下 $y_{10}$ 和 $y_{100}$ 的最高 F1 记录。
  - 该架构通过稠密轴可分离混合，在保持有用计算的同时显著降低了批‑1推理延迟。

### 7. 优点
- **概念创新**：首次在LOB预测中系统建立推理‑计算前沿，并严格区分结构工作和实际延迟，为金融机器学习中的容量‑延迟权衡提供了新视角。
- **方法论严谨**：明确的结构工作计数约定，留出架构族的严格外推测试，以及对计算量定义的鲁棒性分析，增强了结论的可信度。
- **工程实用性强**：FastBiNLOB 设计直接针对批‑1延迟瓶颈，所有操作均为稠密线性层和元素级激活，对现代硬件（CPU/MPS）友好，具有直接部署价值。
- **实验对比公平**：部署实验严格遵循 MLPLOB/TLOB 的公开协议，使用相同的数据划分、特征集和评估指标，结论具有可比性。
- **双重目标实现**：不仅在理论上证明了前沿规律，还据此设计出在精度和延迟上均具竞争力的架构，实现了从分析到构造的闭环。

### 8. 不足与局限
- **范围受限**：
  - 所有结论均基于单一份公开基准 FI‑2010，未在其他LOB数据集或真实市场数据上验证。
  - 原始LOB40缩放通道与完整144部署通道使用不同特征集，虽然服务于同一论点，但不能直接并排比较。
- **计算量定义的专有性**：结构工作计数依赖于手工定义，虽然鲁棒性检验显示在2倍扰动下依然成立，但其作为泛化容量坐标的恰当性尚需更多验证。
- **精度非全面领先**：FastBiNLOB 在 $y_{20}$ 和 $y_{50}$ 上不如 MLPLOB/TLOB，论文仅声称“所选视界”的 SOTA，非全视界最优。
- **延迟测量局限性**：延迟数据基于单一 MacBook Pro 及特定软件栈，结论可能随硬件（如GPU、专用推理芯片）和框架版本变动。FastBiNLOB的延迟优势在批‑1设定下显著，但在批处理或高吞吐场景下可能减弱。
- **架构过于简单**：FastBiNLOB放弃内容自适应的成对交互（如注意力），可能在某些需要精细特征交互的场景中能力不足，论文对此亦有说明。
- **训练细节缺失**：未提供训练超参数、优化器配置和收敛时间，使得完全复现训练过程存在一定障碍。
- **未考虑市场动态变化**：模型在静态数据划分上评估，未考虑市场机制漂移对前沿稳定性和架构泛化能力的影响。

（完）
