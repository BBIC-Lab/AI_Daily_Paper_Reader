---
title: "SYNAPSE: Neuro-Symbolic Visual Thought-to-Text Decoding via Topological Semantic Denoising"
title_zh: SYNAPSE：通过拓扑语义去噪的神经符号视觉思维到文本解码
authors: "Akshaj Murhekar, Abhijit Mishra"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27790v1"
tags: ["query:ndai"]
score: 9.0
evidence: 将EEG神经信号解码为自然语言描述
tldr: 针对EEG视觉想象文本解码中生物噪声导致幻觉和语义不稳的问题，提出轻量级神经符号框架SYNAPSE。该方法在推理时利用常识图结构和潜在示例对EEG语义候选进行符号正则化净化，无需微调大语言模型。实验表明，在多个基准和冻结LLM后端上，SYNAPSE稳定提升语义一致性，对标签消融鲁棒，性能媲美资源密集的微调系统，同时通过编码器内处理原始EEG保护生物隐私。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27790v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the SYNAPSE neuro-symbolic framework. Raw candidate keywords extracted from the baseline frontend via z ·V⊤ undergo topological graph purification to drop spurious singletons and harvest grounded relational facts. Concurrently, the unrefined neural latent vector x queries a historical training matrix E via parallel cosine similarity S = x · E⊤ to fetch nearest-neighbor syntactic blueprints. The purified vocabulary anchors, relational context, and structural templates are fused into a consolidated prompt to guide generation across a suite of frozen large language model (LLM) decoders.\", \"page\": 4, \"index\": 4, \"width\": 941, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2605.27790v1/fig-002.webp\", \"caption\": \"Table 1: Ablation tracking and comparative framework performance over the ImageNet-EEG benchmark. Higher values indicate superior performance across all metrics. Top results are bolded (tied underlined) and second-best are italicized (tied underlined). Setup configurations: A1: Full framework; A2: m = 0; A3: Full w/o Object Labels; A4: Full w/o Exemplars; A5: Full w/o Facts; A6: Minimal baseline (BoW + Obj only).\", \"page\": 6, \"index\": 2, \"width\": 933, \"height\": 798}, {\"url\": \"assets/figures/arxiv/2605.27790v1/fig-001.webp\", \"caption\": \"Table 3: Ablation tracking performance over the THINGS EEG2 cross-exemplar evaluation suite. Higher values indicate superior performance across all metrics. Setup configurations are explicitly mapped as follows: B1: Full framework (BoW + Obj + Facts + Exemp); B2: Full framework w/o Exemplars.\", \"page\": 8, \"index\": 1, \"width\": 950, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2605.27790v1/fig-003.webp\", \"caption\": \"Table 4: Qualitative comparison of generated descriptions from SYNAPSE and the THOUGHT2TEXT baseline. The Pruned BoW column details the resultant pruned token footprint after the topological graph refining phase is completed, captions generated by Qwen2.5-7B.\", \"page\": 12, \"index\": 3, \"width\": 965, \"height\": 961}]"
motivation: 现有EEG到文本解码易受神经噪声影响产生幻觉，亟需提升语义稳定性。
method: 在推理阶段，利用常识图与潜在样例对EEG语义候选进行符号净化，实现不微调LLM的稳定生成。
result: 在多个基准和冻结LLM后端上，SYNAPSE均显著优于无约束提示基线，且性能与微调系统相当。
conclusion: SYNAPSE以轻量神经符号方式有效降低幻觉，保护隐私，为EEG解码提供实用免微调方案。
---

## 摘要
大型语言模型的最新进展加速了开放词汇的脑电图到想象文本解码，即将在视觉感知过程中记录的非侵入式神经活动转化为对所视刺激的连贯自然语言描述。然而，现有系统仍然极易受到生物噪声的影响，受损的神经投射会导致冻结语言模型生成幻觉或语义不稳定的内容。我们提出了SYNAPSE（符号神经对齐用于精确语义提取），这是一个轻量级的神经符号框架，通过在推理时进行符号正则化来稳定神经文本生成。通过利用常识图结构和潜在示例来净化脑电图衍生的语义候选，SYNAPSE在无需端到端微调大语言模型的情况下提高了语义稳定性。在流行的脑电图解码基准和多个冻结大语言模型后端上的实验表明，与无约束提示基线相比，SYNAPSE持续取得提升，在对象标签消融下具有鲁棒性，并且性能可与资源消耗大得多的微调系统相媲美，同时通过将原始脑电图处理完全限定在编码器堆栈内来保护生物特征隐私。

## Abstract
Recent advances in large language models have accelerated open-vocabulary EEG-to-imagined-text decoding, where non-invasive neural activity recorded during visual perception is translated into coherent natural language descriptions of viewed stimuli. However, existing systems remain highly vulnerable to biological noise, where corrupted neural projections induce hallucinated or semantically unstable generation in frozen language models. We introduce SYNAPSE (Symbolic Neural Alignment for Precise Semantic Extraction), a lightweight neuro-symbolic framework that stabilizes neural text generation through inference-time symbolic regularization. By purifying EEG-derived semantic candidates using commonsense graph structure and latent exemplars, SYNAPSE improves semantic stability without end-to-end LLM fine-tuning. Experiments across popular EEG decoding benchmarks and multiple frozen LLM backends demonstrate consistent gains over unconstrained prompting baselines, robustness under object-label ablation, and performance commensurate with substantially more resource-intensive fine-tuned systems, while preserving biometric privacy by localizing raw EEG processing entirely within the encoder stack.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：基于非侵入式脑电图（EEG）的视觉想象文本解码（将大脑信号转化为观看图像的描述）正快速发展，但现有系统**极易受生物噪声干扰**，导致噪声候选词进入冻结的大语言模型（LLM）后引发**语义漂移、注意力分散和幻觉生成**。
- **整体含义**：提出一种**推理时神经符号正则化框架 SYNAPSE**，在不微调 LLM 的前提下，通过常识图结构和潜在样例对 EEG 候选语义进行**拓扑净化与关系锚定**，从而稳定文本生成，保护生物隐私。

### 2. 方法论：核心思想、关键技术细节、公式/算法流程
- **总体思路**：将 EEG 编码器的离散词候选，**先做常识图上的拓扑剪枝，再提取关系事实和检索句法样例**，最后将这些净化后的符号信息打包进提示（prompt），引导冻结 LLM 生成。
- **核心模块**：
  1. **神经前端检索**：利用冻结的 SENSE 前端（ChannelNet 编码器+MLP）得到 512 维隐向量 $z_i$，通过余弦相似度从词汇表 $V$ 检索 top-$k$（$k=15$）候选词集 $W_{\text{raw}}$，公式为：
     $$W_{\text{raw}} = \text{TopK}\left(\sigma\left(\frac{z_i}{\|z_i\|_2} \cdot \left(\frac{V}{\|V\|_2}\right)^\top\right), k=15\right)$$
  2. **知识图谱诱导子图与拓扑剪枝**：将 $W_{\text{raw}}$ 投射到 ConceptNet 常识多图中，构造顶点集 $V_G$，提取权重大于阈值（$w_{\min}=1.0$）的诱导子图 $G$。对节点计算归一化度中心性 $C_D(v) = \deg(v)/(|V_G|-1)$。**保留条件**：
     $$W_{\text{pruned}} = \{v \in V_G \mid C_D(v) > 0 \lor v \in P\}$$
     其中 $P$ 为高优先级锚词集合（前 $m=5$ 个候选）。这一规则既滤除孤立噪声节点，又保护关键神经意图。
  3. **关系事实提取**：从子图边集 $E_G$ 中选取视觉相关关系类型（如 AtLocation、UsedFor 等），映射为自然语言描述，取前 $N_{\text{facts}}=5$ 条构成事实集 $F$。
  4. **跨模态示例检索**：直接用未精炼的隐向量 $x$ 与训练样本嵌入矩阵 $E$ 计算余弦相似度 $S = x \cdot E^\top$，取最近邻（top-$2$）对应的真实标题作为句法模板 $E_{\text{exemplars}}$。
  5. **语言解码**：将 $W_{\text{pruned}}$、$F$、$E_{\text{exemplars}}$ 和视觉对象类别标签融合为结构化提示，送入冻结 LLM 解码生成描述。

### 3. 实验设计
- **数据集/场景**：
  - **CVPR2017（ImageNet-EEG）**：多模态 EEG-文本基准，6 名受试者观看 ImageNet 图像并配有描述标题，遵循 SENSE 前端设置。
  - **THINGS EEG2**：大规模公开 EEG 数据集，1,654 个视觉概念，使用 GPT-5.4-mini 为每个刺激合成文本基准；采用严格的**跨样本评估**：训练使用前 5 个图像样本的平均 EEG，测试用未见过的第 6 个样本。
- **对比方法**：
  - 无约束基线（SENSE 未净化提示）
  - 完全微调系统（THOUGHT2TEXT）
  - **消融配置**：逐步移除对象标签、示例、关系事实，以及仅用净化词袋（BoW）的最小基线，控制优先级参数 $m$ 等。
- **解码器**：多个冻结 LLM 后端，涵盖开源模型（Meta-Llama-3-8B-Instruct、Qwen2.5-7B-Instruct）和商业 API（GPT-4o-mini、Gemini-2.5-flash-lite）。
- **评价指标**：ROUGE（1/2/L）、BLEU（1/4）、METEOR、BERTScore，以及 LLM-as-a-judge 的流畅度和充分度评分。

### 4. 资源与算力
- 论文**未明确提及**训练或推理所使用的 GPU 型号、数量或时长。框架本身是轻量级推理时方法，前端编码器（约 6M 参数）可能不需要大量训练资源，但未给出具体计算量。评估依赖冻结 LLM 的 API 调用或本地部署，固有成本主要取决于解码器规模。

### 5. 实验数量与充分性
- **实验设计丰富且客观**：
  - 覆盖两个具有不同规模与挑战的 EEG 基準（CVPR2017 与 THINGS EEG2），并在 THINGS 上采用跨样本零样本分割。
  - 针对每个解码器，执行了**多种消融配置**（A1~A6、B1~B2），清晰分离各模块贡献。
  - 对比多种开源与商业 LLM，共计数十组测试结果（Table 1、Table 3），包括与 SENSE 基线和 THOUGHT2TEXT 微调系统的横向对比。
  - 还提供了定性案例（Table 4）、拓扑剪枝统计（Table 2）以及完整的提示蓝本附录。
- **公平性**：训练前端的处理策略与基线（SENSE）一致，实验设置公开、可复现。消融对照严谨，排除了对象标签、事实、样本等对性能的干扰，尤其在标签消融下验证了鲁棒性。

### 6. 主要结论与发现
- **推理时符号正则化能有效抑制噪声**：拓扑剪枝去除孤立噪声词，关系事实和句法模板共同防止注意力分散和语义漂移。
- **去耦合范式优于微调范式**：在 ImageNet-EEG 上，SYNAPSE 的完整配置（A1）全面超越无约束提示基线 SENSE，且与资源密集的 THOUGHT2TEXT 微调模型性能相当或更优，尤其在压缩 LLM（如 Qwen）上优势明显。
- **对视觉对象标签的鲁棒性**：移除类别标签（A3）时，符号净化层仍能维持稳定语义，避免了传统检索方法因分类器失效导致的严重退化。
- **在挑战性大规模基准下仍保有效力**：在 THINGS EEG2 的跨样本零样本场景中，SYNAPSE 能将崩溃模型（如 Qwen2.5-7B）的 ROUGE-1 从 9.4 恢复至 21.8，验证了在大词汇量下拓扑净化的必要性。
- **隐私与效率优势**：所有 EEG 处理本地化，无需传输原始信号或调整 LLM 参数。

### 7. 优点（方法或实验亮点）
- **创新的神经符号推理架构**：首次将常识图拓扑剪枝与检索增强生成结合，用于 Brain-to-Text 任务，实现了免微调的噪声免疫。
- **双重保护机制**：度中心性过滤与优先级锚词并集策略，既剔除噪声又防止误删真意图词。
- **模块化与灵活性**：插件式设计，可与任意冻结 LLM 搭配，支持开源和商业模型。
- **实验深度与透明度高**：多数据集、多解码器、细粒度消融、定性分析、完整提示附录，可靠性强。

### 8. 不足与局限
- **仍受图完备性限制**：常识图可能缺失某些稀有或连接薄弱的视觉概念，导致真阳性被错误剪除。
- **无法处理语义一致噪声集群**：如果多个噪声词在图内形成强连接子图，其度中心性>0，会穿过净化层，产生残留幻觉（定性例子中“蘑菇”表现为“披萨”）。
- **示例检索在密集空间下偏移**：在大词汇量场景中，最近邻检索可能因语义重叠引入风格微扰，对高容量模型（如 GPT-4o-mini）有轻微负面效应。
- **生成质量受限于前端检索质量**：解码上限仍取决于 EEG 编码器提取的噪声候选质量，未彻底解决底层信号表征的脆弱性。
- **未提供算力/能耗数据**：缺乏对模型部署与推理成本的定量分析。
- **依赖外部 API 时隐私不完全闭环**：尽管 EEG 处理本地化，但使用云端 LLM 时，净化后的提示仍需传出，端到端隐私需完全本地模型支持。

（完）
