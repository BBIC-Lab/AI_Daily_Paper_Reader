---
title: "Tensor Memory: Fixed-Size Recurrent State for Long-Horizon Transformers"
title_zh: 张量记忆：用于长时程变换器的固定大小循环状态
authors: "Kabir Swain, Sijie Han, Daniel Karl I. Weidele, Mauro Martino, Antonio Torralba"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27686v1"
tags: ["query:ndai"]
score: 7.0
evidence: 用于长程序列处理的循环3D记忆
tldr: Transformer处理长序列时，注意力内存随输入长度增长，且缺乏持久空间状态，影响长程理解和遮挡推理。本文提出Tensor Memory模块，用固定大小的循环3D记忆张量：token通过可微高斯加权软写入预测的连续3D位置，记忆经局部交互和门控循环更新，再连续采样读取融合。模块解耦状态容量与序列长度，保留空间偏置，可即插即用于各类Transformer，在语言、图像、视频基准及诊断套件上验证有效，提升了长时程任务的时空推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27686v1/fig-001.webp\", \"caption\": \"Figure 1. Tensor Memory: a fixed-size 3D voxel state with continuous writes, local recurrent updates, and continuous reads. (1) The memory is a pair of volumes ht, ct ∈ RC×D×H×W (hidden and cell states); each of the D×H×W voxels stores a C-dimensional feature vector and the spatial layout is fixed and constant in input length. (2) Each step t emits a write package (µ (t) write, content (t), σ(t)) that deposits a Gaussian-weighted volume U (t) = content(t) ⊗M (t) around µ (t) write, affecting a local neighbourhood through a smooth mask M (t) (continuous splatting). (3) Concatenating the write volume with the current state [U (t); ht] along the channel axis, a factorized 3D operator (axis-aligned depthwise 3D convolutions plus a pointwise mix) produces ConvLSTM gates (it, ft, ot, gt) that yield the next state (ht+1, ct+1), giving persistence and local voxel-to-voxel interaction. (4) A predicted read coordinate µ (t) read trilinearly samples ht to obtain r(t); a learned projection Wout produces the readout m(t) that is fused back into the chunk through a gated residual.\", \"page\": 2, \"index\": 1, \"width\": 1017, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.27686v1/fig-002.webp\", \"caption\": \"Figure 2. Tensor Memory module, end-to-end per chunk. (1) The chunk t consists of K token embeddings x(t) 1 , . . . , x (t) K . (2) We form two summaries of the chunk: a read query x (t) read = x (t) 1 (first token) and a write summary x (t) write = Wwp x⃗ (t) 1:K (a learned projection over the whole chunk). (3) A shared coordinate head with tied weights produces the read coordinate µ(t) read and the write coordinate µ(t) write. The read coordinate samples the tensor memory via GridSample, returning r(t), which is projected by Wout to the token dimension to give the readout m(t). The write coordinate is paired with a content head (predicting content(t)) and a spread head (predicting σ(t)); together the write package (µ (t) write, content (t), σ(t)) drives the voxel update. (4) The readout m(t) is broadcast to all K tokens of the chunk and fused via a learned gated residual x̂(t) k = x (t) k + σ(γ)m(t), producing the updated chunk.\", \"page\": 4, \"index\": 2, \"width\": 1017, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2605.27686v1/fig-003.webp\", \"caption\": \"Figure 3. Tensor Memory in action, illustrated on three input chunks. (1) The input is partitioned into chunks; here, each chunk is a small group of patches over a single frame, drawn in a distinct colour. (2) For each chunk i, the write head predicts a continuous 3D coordinate µ (i) write ∈ [−1, 1]3 in the voxel memory; sphere radius is proportional to the predicted Gaussian write spread σ(i). (3) Before each write, the model reads at a predicted query coordinate µ (i) read. The read for chunk i queries the memory state produced by chunks 0, . . . , i−1; arrows show the read sequence 1→2→3. (4) Max-intensity projection of the per-voxel memory norm ∥ht∥2 after each chunk: writes accumulate into spatially structured deposits that persist across steps, giving the model an explicit, fixed-size belief state.\", \"page\": 5, \"index\": 3, \"width\": 1017, \"height\": 479}]"
motivation: 现有Transformer的内存随序列线性增长且无持久空间状态，阻碍长程视频理解与遮挡推理。
method: 提出Tensor Memory模块，以固定大小的3D记忆张量支持令牌的连续位置软写入、局部交互更新和连续采样读取。
result: 在语言、图像、视频基准和专用诊断任务上证明了模块的有效性，且可无缝集成到标准Transformer训练中。
conclusion: Tensor Memory通过固定尺寸的循环空间记忆，弥补了Transformer长程时空推理的短板，实现轻量高效的即插即用增强。
---

## 摘要
Transformer通过将空间和时间展平为长标记序列来处理图像和视频。虽然注意力和KV缓存保留了过去的特征，但它们的内存随序列长度增长，并且缺乏显式的、持久的空间状态，使得长时程视频理解和遮挡敏感推理变得困难。我们提出了张量记忆（Tensor Memory），一个轻量级模块，通过添加固定大小的循环3D记忆张量来增强Transformer块：标记通过可微的软写入将内容作为预测的连续3D位置周围的高斯加权体积存入体素网格，记忆通过高效的局部交互算子和门控循环动态进行更新，标记通过带门控残差融合的连续采样读回上下文。由于记忆张量具有恒定大小，张量记忆在保持空间归纳偏置的同时，将状态容量与输入长度解耦。我们在标准的语言、图像和视频基准上以及一个受控的玩具诊断套件上评估了该模块，该套件旨在分离出持久状态有益的情况；它与标准Transformer训练流水线集成，并且可以在不进行其他架构更改的情况下附加到现有块或从中移除。

## Abstract
Transformers process images and videos by flattening space and time into long token sequences. While attention and KV caching preserve past features, their memory grows with sequence length and they lack an explicit, persistent spatial state, making long-horizon video understanding and occlusion-sensitive reasoning difficult. We propose Tensor Memory, a lightweight module that augments Transformer blocks with a fixed-size recurrent 3D memory tensor: tokens write into a voxel grid via a differentiable soft write that deposits content as a Gaussian-weighted volume around a predicted continuous 3D location, the memory is updated with an efficient local interaction operator and gated recurrent dynamics, and tokens read back context via continuous sampling with gated residual fusion. Because the memory tensor has a constant size, Tensor Memory decouples state capacity from input length while preserving a spatial inductive bias. We evaluate the module on standard language, image, and video benchmarks and on a controlled toy diagnostic suite designed to isolate when persistent state is beneficial; it integrates with standard Transformer training pipelines and can be attached to or removed from existing blocks without other architectural changes.