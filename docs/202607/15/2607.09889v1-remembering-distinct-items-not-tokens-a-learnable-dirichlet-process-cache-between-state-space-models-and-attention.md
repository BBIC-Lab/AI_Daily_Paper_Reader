---
title: "Remembering Distinct Items, Not Tokens: A Learnable Dirichlet-Process Cache Between State-Space Models and Attention"
title_zh: 记忆相异项而非词元：状态空间模型与注意力机制之间的可学习狄利克雷过程缓存
authors: "Siddharth Pal, Viktoria Rojkova"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09889v1"
tags: ["query:ndai"]
score: 9.0
evidence: 基于DP-means的序列模型稀疏缓存方法
tldr: 本研究提出一种基于狄利克雷过程混合的稀疏缓存机制，仅当输入新颖时分配记忆槽，使缓存大小跟踪不同项目而非序列长度，从而在递归神经网络中实现接近注意力的回忆能力，同时大幅降低存储。在受控基准和真实数据流上验证了其效能，并表明关键因素是归纳偏差而非模型容量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09889v1/fig-001.webp\", \"caption\": \"Figure 1: Mechanism comparison (K=64 distinct in L=256 tokens). Left: recall — the DP cache and attention solve the task, the fixed-state SSM and recency eviction fall short. Right: stored slots (log scale, the cost) — the DP cache uses one slot per distinct item where attention and the kNN cache store every token.\", \"page\": 6, \"index\": 1, \"width\": 883, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-003.webp\", \"caption\": \"Figure 2: Recall versus cache size against the deployed fixed-budget baselines. The heavy-hitter (H2O-style) cache, which evicts by accumulated attention mass, is far stronger than recency or the sink-plus-window (StreamingLLM-style) cache on this redundant stream — it reaches 0.92 at the distinct-items budget — but the Dirichlet-process allocate-on-novelty frontier dominates all three, reaching perfect recall at that budget.\", \"page\": 7, \"index\": 3, \"width\": 633, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-007.webp\", \"caption\": \"Figure 3: Real recall task (MovieLens genre recall), matched budget. The heavy-hitter (H2O), sink-plus-window (StreamingLLM), recency, and SnapKV caches trail well below the Dirichletprocess cache, which reaches near-perfect recall at the distinct-items budget by covering the rare tail the attention-based policies discard.\", \"page\": 8, \"index\": 7, \"width\": 596, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-009.webp\", \"caption\": \"Figure 4: State-space backbone with the sparse cache. Left: the hybrid matches attention on both the recall head and the long-range integration head, where the SSM alone fails recall. Right: read-cost — the hybrid uses the fewest slots (the distinct-items count).\", \"page\": 9, \"index\": 9, \"width\": 883, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-004.webp\", \"caption\": \"Figure 5: Threshold sensitivity on the real streams. Left: recall is flat at the ceiling across τ ∈ [0.5, 0.8] on all three domains. Right: allocated slots stay pinned at the distinct count over the same range; below the plateau the cache merges distinct entities, above it the cache stores benign duplicates.\", \"page\": 10, \"index\": 4, \"width\": 883, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-002.webp\", \"caption\": \"Figure 6: Online allocate-on-novelty versus offline k-means (clustered attention). The offline method must fix the centroid count M and, even at M = K, recovers the clusters only imperfectly; the online DP cache reaches perfect recall at the distinct-items budget by sweeping its threshold alone, in a single pass and without knowing K.\", \"page\": 11, \"index\": 2, \"width\": 596, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-008.webp\", \"caption\": \"Figure 7: Cost versus stream length N at a fixed number of distinct items. Left: the DP read stays flat (O(K)) while attention’s read grows (O(N)). Right: the slot count stays bounded near K while the DP build cost grows linearly (O(N |slots|)) — the trade is a heavier one-time build for cheap reads and bounded memory.\", \"page\": 12, \"index\": 8, \"width\": 883, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-006.webp\", \"caption\": \"Figure 8: Adaptive concentration on non-stationary demand. Left: the budget (slots per phase) swings with the alternating easy/hard working set, tracking demand while staying bounded, where the fixed-τ cache grows without limit and a fixed budget is flat. Right: at equal average cost the adaptive cache recalls more than a matched fixed budget.\", \"page\": 13, \"index\": 6, \"width\": 883, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2607.09889v1/fig-005.webp\", \"caption\": \"Figure 9: The surprise-adaptive concentration on two real streams. Top (MovieLens): the perwindow demand is nearly stationary, and the adaptive budget and a matched fixed budget tie. Bottom (BGL alerts): the demand is bursty; the adaptive budget tracks the storms (left) and, at equal average slots, out-recalls the matched fixed budget and approaches the worst-caseprovisioned one (right).\", \"page\": 14, \"index\": 5, \"width\": 883, \"height\": 646}]"
motivation: 解决状态空间模型记忆容量有限与注意力机制存储开销过大之间的矛盾，寻求高效记忆中间方案。
method: 利用DP-means聚类规则构建键值缓存，集成到深层递归主干，并设计可学习的新颖性阈值门端到端训练。
result: 该缓存在关联回忆任务中匹配全注意力性能，仅存储不同项目，在回忆-大小权衡上优于固定淘汰缓存，且学习时正确恢复DP-means规则。
conclusion: 新颖性感知的稀疏缓存通过归纳偏差实现高效记忆，在推荐日志等真实数据中得到验证。
---

## 摘要
固定状态序列模型将无界历史压缩为有界状态，其关联记忆上限约为状态维度；注意力机制通过为每个词元保留键值条目突破此上限，但导致二次计算量且缓存随序列增长。我们探索一条中间道路：一种稀疏缓存，仅在输入新颖时分配槽位，使其大小跟踪相异项数量而非词元数量。分配规则为DP-means聚类规则，即狄利克雷过程混合模型的小方差极限，它不作为潜变量推断，而是用作深度循环骨干的键值记忆算子。我们将其发展为两种形式：浓度固定的静态缓存，以及浓度跟随近期新颖率的自适应意外变体。在带有冗余的受控关联记忆基准上，我们证明该缓存仅存储相异项即可匹敌全注意力记忆，在记忆—大小前沿上优于固定预算淘汰缓存，且在状态空间骨干上以所有测试模型中最低的内存，同时回答记忆查询和远程聚合任务。分配是端到端可学习的：一个双参数新颖阈值门仅通过任务损失训练即可精确恢复该规则，而过参数化门则失败，因此有效成分是归纳偏置而非容量。证据来自一系列中等规模的控制机制研究，相异项特性在四个真实数据流（推荐、系统日志、临床事件和保险索赔）上得到确认；真实骨干和真实语料的语言验证在一项伴随研究中进行。

## Abstract
Fixed-state sequence models compress an unbounded past into a bounded state, which caps their associative recall at roughly the state dimension; attention escapes the cap by keeping a key-value entry for every token, at quadratic compute and a cache that grows with the sequence. We study the middle ground: a sparse cache that allocates a slot only when an input is novel, so its size tracks the number of distinct items rather than the number of tokens. The allocation rule is the DP-means clustering rule, the small-variance limit of a Dirichlet-process mixture, used not as latent-variable inference but as the key-value memory operator for a deep recurrent backbone. We develop it in two forms, a static cache with a fixed concentration and a surprise-adaptive variant whose concentration follows the recent novelty rate. On a controlled associative-recall benchmark with redundancy we show that the cache matches full-attention recall while storing only the distinct items, that it dominates a fixed-budget eviction cache on the recall-versus-size frontier, and that on a state-space backbone it answers both a recall query and a long-range aggregate at the lowest memory of any model tested. The allocation is learnable end to end: a two-parameter novelty-threshold gate trained on the task loss alone recovers the rule exactly, whereas an over-parameterized gate fails, so the operative ingredient is the inductive bias rather than capacity. The evidence is a family of controlled mechanism studies at modest scale, with the distinct-items property confirmed on four real streams (recommendation, systems logs, clinical events, and insurance claims); a real-backbone, real-corpus language validation is pursued in a companion study.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：本研究旨在解决序列模型中计算效率与记忆能力之间的根本矛盾。
    *   **状态空间模型** 将历史信息压缩到固定的有界状态中，计算效率高但关联记忆能力有上限。
    *   **注意力机制** 通过为每个词元保留键值对实现了强大的记忆，但代价是缓存大小和计算量随序列长度线性乃至二次增长。
*   **整体含义/研究动机**：论文探索了一种“中间地带”，即一个 **记忆相异项而非词元** 的稀疏缓存。其核心思想是，许多真实序列包含大量冗余，因此模型无需存储每一个词元，而应在输入足够“新颖”时才分配新的记忆槽位。这样，缓存的规模将追踪序列中不同项的数量，而非总长度，从而以更低的成本实现高性能回忆。文章定位为已有思想（DP-means聚类、聚类注意力、增长式缓存）的合成，但创新性地将其用作深度循环网络的键值记忆算子。

### 2. 论文提出的方法论

论文提出了两种基于“新颖性分配”的缓存机制，共享相同的核心操作符，仅在控制新颖性阈值的参数上有所区别。

*   **核心思想**：将狄利克雷过程混合的DP-means聚类规则，转化为深度序列模型的在线键值缓存分配策略。
*   **关键技术细节 (方法一：静态狄利克雷过程缓存)**
    *   **缓存结构**：缓存由一组槽位构成，每个槽位存储一个键、对应的值和一个使用计数器。
    *   **新颖性度量与分配规则**：
        对于每个新输入的键值对 $(k_t, v_t)$，计算其与现有槽位的最大余弦相似度，其新颖度定义为 $nov_t = 1 - \max_{i\in\text{slots}} \text{sim}(k_t, \kappa_i)$。分配决策基于此新颖度与一个阈值 $\tau$ 的比较：
        *   若 $nov_t > \tau$（输入与所有现有槽位都不相似），则创建一个新槽位。
        *   否则，将输入合并到最相似的现有槽位中。
        该规则是DP-means算法的最大后验估计，其中 $\tau$ 扮演浓度参数的角色。
    *   **读取操作**：给定查询 $q$，通过计算查询与所有槽位键的注意力分数来输出预测值：$\hat{v} = \sum_i \text{softmax}_i(\text{sim}(q, \kappa_i)/\theta) \nu_i$。读取成本由槽位数量决定。
    *   **可学习的门控机制**：将固定的阈值规则替换为一个可微分的门控函数 $g_t = \sigma(a (nov_t - b))$，其中 $a$ 和 $b$ 是可学习参数。训练时使用软掩码和预算惩罚项 $(\sum_t g_t - M)^2$ 来使保留的槽位数接近目标预算 $M$。这种极简的参数化旨在证明仅需“归纳偏置”而非模型容量，即可从任务中学会分配规则。

*   **关键技术细节 (方法二：意外自适应缓存)**
    *   **动机**：面对非平稳数据流，固定的阈值/预算无法适应变化的“新颖项”出现速率。
    *   **自适应浓度/预算**：维护一个“温度”变量 $T_t$，它是近期新建槽位信号 $o_t$（当 $nov_t > \tau$ 时为1，否则为0）的指数移动平均，反映当前的“意外”程度或新颖内容爆发率。
    *   **动态预算调整**：有效预算 $M_t = M_0 + \beta T_t$ 随温度升高而增加。当当前槽位数超过 $M_t$ 时，采用最不常用驱逐策略（驱逐使用计数器最小的槽位）来回收容量。这使得缓存在突发性新颖内容期间能临时扩容，在流稳定后能回收资源。

### 3. 实验设计

*   **数据集/场景**
    *   **受控合成基准**：一个定制的关联记忆任务。流包含 $K$ 个不同的键值对，每个重复 $r$ 次，总词元数 $L=rK$。模型处理完整流后，需根据查询键回忆对应的值。通过调节冗余度 $r$，系统性地拉开词元总数与相异项数量的差距。
    *   **真实数据流验证**：
        *   **推荐系统**：MovieLens-100k 电影出现流，任务是回忆电影类别。
        *   **系统日志**：Loghub HDFS 和 BGL 的日志行，任务是日志模板分组与解析。
        *   **临床事件**：MIMIC-IV 处方事件，任务是回忆药物的给药途径。
        *   **保险索赔**：DE-SynPUF 住院诊断代码事件，任务是回忆ICD-9章节。
*   **基准对比方法**
    *   **端点模型**：固定状态模型（对角状态空间模型S4D风格）、全注意力机制。
    *   **固定预算淘汰缓存**：基于最近性驱逐、H2O风格（驱逐累积注意力分数最低的）重击者缓存、StreamingLLM风格的“sink+window”缓存、SnapKV风格缓存。
    *   **其他记忆机制**：存储每个词元的kNN缓存、离线聚类注意力。
*   **对比角度**：在合成基准上比较回忆准确率与缓存槽位数的权衡（前沿）；在真实数据流上比较匹配预算下的回忆性能；与离线聚类方法比较分配效率；比较直接规则与端到端学习门控的性能；测试混合模型（方法一缓存+状态空间模型骨干）的表现。

### 4. 资源与算力

*   **硬件**：文中明确指出，机制研究和真实数据验证主要在 **Apple M1** 和 **Apple M4** 机器上进行。
*   **GPU重运行**：为进行端到端训练对比，实验在**一张NVIDIA T4 GPU（通过Google Colab提供）** 上进行了重运行以作交叉验证。
*   **训练时长**：论文未提供具体的训练时长。

### 5. 实验数量与充分性

*   **实验组数**：论文包含了约九组核心实验，覆盖了合成基准的深入分析、真实数据验证、消融研究和成本分析。
*   **充分性与客观公平性**：
    *   **充分性**：实验设计较为系统。从孤立的机制验证（合成基准），扩展到分布外的真实数据流场景，并进一步考察了端到端可学习性、与骨干模型的组合、在非平稳条件下的适应性，构成了有力的证据链。实验考虑了多种竞争方法，并分析了多种维度的变化。
    *   **客观公平性**：作者主动划定了研究的“范围”，明确指出其证据基于中等规模的机制研究，所对比的基线是“重新实现的机制”而非完整的庞大系统，这体现了高度的客观性。对可学习门控的分析（对比极简参数化和过参数化门）有效地支撑了其关于“归纳偏置”重要性的核心论点。然而，合成数据集中的键是“精心分离”的，这可能使得新颖性信号异常干净，在真实世界具有高度重叠数据的场景中，有效性和阈值选择可能会面临更大挑战，论文将这部分留给了未来的工作。

### 6. 论文的主要结论与发现

*   **核心效率发现**：狄利克雷过程缓存严格按其设计工作，缓存大小收敛到序列中不同项的数量，而非词元总数。在冗余序列上，它可以实现四倍甚至更高（最达143倍）的存储压缩，同时保持与全注意力机制几乎相同的回忆准确率。
*   **性能前沿优势**：在回忆性能与缓存大小的权衡曲线上，本方法的曲线位于所有测试的固定预算淘汰缓存之上，能以更少的槽位实现完美回忆，因为它直接保留了相异项，而非通过注意力统计量来近似。
*   **可学习性与归纳偏置**：一个极简的双参数新颖性门控可以**从任务损失中端到端地完全恢复**DP-means分配规则，而过参数化的门控则失败。这证明有效性的关键在于 **“新颖性”这一归纳偏置**，而非模型的容量。
*   **与骨干模型组合**：将其作为回忆模块与状态空间模型骨干结合，能以**所有模型中的最低内存成本**，同时完成回忆和远程聚合的双任务，显著优于纯状态空间模型（无法回忆）和全注意力机制（内存高）。
*   **自适应机制边界**：意外自适应缓存在**非平稳**数据流上优于同等平均成本的固定预算缓存，其优势恰好在于固定预算在平稳时期浪费了为最坏情况预留的容量。在**平稳**数据流上，两种方法性能持平，明确了该自适应机制的适用边界。

### 7. 优点

*   **定位清晰，贡献明确**：精确地指出了其工作所处的“中间地带”，并坦诚地声明这是一种“合成”，而非全新的孤立原语，贡献定位十分清楚。
*   **方法论简洁优雅**：将经典的贝叶斯非参数聚类规则直接作为记忆分配算子，思想清晰，实现直观。
*   **实验论证系统且严谨**：从合成机制探索到真实场景验证，再到消融实验和性能前沿对比，逻辑链条完整。主动明确研究范围和应用边界，体现了很高的科学严谨性。
*   **洞察深刻**：关于“归纳偏置而非容量”是可学习性关键因素的发现，以及精准界定自适应机制适用边界的分析，为模型设计提供了有价值的见解。
*   **泛化性验证**：在四个不同领域的真实数据流上验证了核心的“相异项”属性，显著增强了结论的可信度。

### 8. 不足与局限

*   **规模与任务范围受限**：主要证据来自中等规模的**受控机制研究**和**合成关联回忆任务**。缺乏在真实大型语言模型骨干（如Mamba）和真实大规模语料库（如WikiText-103）上的端到端语言建模或长上下文理解（如RULER基准）验证，这是论文明确指出的、需要伴随研究进行的下一步。
*   **数据假设较强**：合成基准中的键是“精心分离”的，确保相异项间相似度极低，重复项间相似度极高。在真实世界中，不同实体可能有较高的语义重叠，这会模糊新颖性阈值 $\tau$ 的选择界限，增加合并错误的可能性。
*   **对比基线简化**：由于实验在CPU规模进行，对比的模型是“重新实现的机制”而非部署级的成熟系统（如真正的StreamingLLM、Mamba），因此严格的“系统对决”比较尚未完成。
*   **构建成本权衡**：论文明确指出，缓存构建过程是 $O(N \cdot |\text{slots}|)$ 复杂度，比注意力的 $O(N)$ 追加更昂贵。该方法的优势在于以更昂贵的单次构建代价换取后续多次查询时的廉价读取和恒定内存，这种权衡仅在读操作远多于写操作的场景（如长上下文推理）下有利。
*   **门控机制探索有限**：可学习门控仅测试了最极简的参数化形式，并承认存在一个更大的设计空间（如学习相似度度量、每层特定阈值）未被探索，当前的结论是其探索下限，而非性能上限。

（完）
