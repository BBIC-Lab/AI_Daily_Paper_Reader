---
title: "MoP-JEPA: Hard-Assigned Predictor Mixtures for Stochastic JEPA World Models"
title_zh: MoP-JEPA：用于随机性JEPA世界模型的硬分配预测器混合
authors: "Zhi Song, Ximing Xing, Zhenchao Tang, hanbo Huang, Tianxu Lv, minghao Yang, Zhongzheng Niu, He Bing, Lusheng Wang, Jianhua Yao"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05238v1"
tags: ["query:ndai"]
score: 9.0
evidence: 随机环境中下一潜状态预测
tldr: JEPA世界模型在随机环境中因单一确定性预测器输出条件均值而失效，本文提出MoP-JEPA，采用硬分配的预测器混合，每个预测头对应一种后继模式，可在单次前向传播中枚举转移分布。在OGBench离线数据上，规划成功率从0.02-0.09提升至0.85，并设计了验证协议防止覆盖作弊。该方法在所有任务上优于现有方案，并在真实环境迷宫任务中排名第二。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05238v1/fig-002.webp\", \"caption\": \"Figure 2: Two-step successor beliefs at high-branching cells. Rows: three cells whose two-step successor law has three or four valid modes. Columns: empirical ground truth (green), the dense single predictor (red), and MoP-JEPA (blue). Each panel is the local 2-D latent plane spanned by the true successor embeddings; floor axes give the latent coordinates, and the vertical coordinate is unit-normalized predictive density. Green pins mark true successor latents. Percentages in the left column are empirical two-step branch frequencies; percentages in the right column are router weights πk for active heads. The dense predictor collapses to one peak, while MoP-JEPA re-opens the multimodal fan-out with one predicted peak per branch.\", \"page\": 3, \"index\": 2, \"width\": 483, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2607.05238v1/fig-004.webp\", \"caption\": \"Table 1: The paper’s evidence chain: stochastic targets create conditional-mean collapse; hard-assigned heads specialize to modes; mode enumeration restores a transition graph that can be audited by planning.\", \"page\": 5, \"index\": 4, \"width\": 481, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2607.05238v1/fig-003.webp\", \"caption\": \"Figure 4: Evidence chain before benchmark scores. Left: beyond 2-D mazes, MoP improves the appropriate domain metric (coverage for pixel/SVHN, inverse ADE for ETH/UCY); in DINO-WM, the winning head is hundreds of times closer than the mean head, showing specialization in the official world-model code. Middle: as the true number of modes grows in a controlled conformer branch sweep, dense coverage degrades fast, MDN partially covers, and hard-assigned MoP remains highest. Right: on real two-step maze branchings, dense recovers no successor modes while MoP recovers most 3/4-way successors; this is the empirical specialization predicted by Prop. 3.\", \"page\": 5, \"index\": 3, \"width\": 1010, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2607.05238v1/fig-001.webp\", \"caption\": \"Table 4: Within the JEPA world-model family — same encoder, data, and planner, only the predictor changes — ours wins verified planning on all three mazes (2–5×; 5 seeds.) Left block (realroute): a plan counts only if the model’s blindly-proposed transition graph contains a route of real transitions, so coverage cannot be freeloaded. Dense/gated-MoE/VarJEPA are mode-collapsed (Props. 1–2); MDN covers but routes through non-existent edges (precision 0.14–0.21 vs. ours 0.42–0.56; large-stitch adjudicated at matched K=16/60k steps). Right block: real-environment execution success (official info[’success’], our replanning executor); MDN’s higher medium-stitch number is the executor’s online repair compensating its low-precision graph, which is precisely why model fidelity is judged by realroute. External context: on large-stitch our execution (33) sits second among the seven published OGBench offline-GCRL baselines (Park et al. 2025) (QRL 84; then ours, GCIQL 31, HIQL 13, GCIVL 12, GCBC 7, CRL 0) — notable for a generic world model plus search rather than a policy trained end-to-end for the benchmark.\", \"page\": 7, \"index\": 1, \"width\": 760, \"height\": 181}]"
motivation: JEPA世界模型在随机分支点预测条件均值，导致生成不存在的中间状态，无法支持有效规划。
method: 提出MoP-JEPA，使用硬分配混合预测器，每个头强制对应一种后继模式，实现转移分布的量化，并配套验证协议确保预测可靠性。
result: 在OGBench离线规划中，成功率从单预测器的0.02-0.09大幅提升至0.85，且在所有迷宫上超越软替代方案，真实环境排名第二。
conclusion: 硬分配的混合预测器是解决JEPA随机规划问题的最小可验证方案，能可靠枚举多模态转移，使世界模型可规划。
---

## 摘要
JEPA世界模型通过潜在回归训练单个确定性预测器来预测下一个潜在状态。我们证明，当环境具有随机性时，这在结构上会失败：在分支转换处，回归最优预测器输出后继嵌入的条件均值，即位于真实下一个状态之间的一个点，但它根本不对应任何状态。我们证明了这种崩溃对于确定性和门控混合专家预测器都会发生，并证明了MoP-JEPA的硬分配预测器反而收敛为转换分布的量化器：每个后继模式一个头，可在单次前向传播中枚举，这正是规划器所需的接口。在官方OGBench离线数据上进行无泄漏评估时，基于单预测器模拟的规划表现很差（成功率为0.02–0.09），而基于我们预测模式的规划成功率可达0.85，在所有任务上均优于确定性、门控MoE和变分预测器。由于多预测评估可能引发覆盖率搭便车问题，因此方法中包含一个验证协议：一个与输入无关的码本控制、一个打乱上下文测试、路由器门控读数、转换精度守卫，以及一个验证路由标准，即模型盲式提出其转换图，而真实标签仅用于检查结果。在此标准下，我们的方法在所有三个迷宫中（2–5倍）优于最强的软替代方案，并且该协议识别出该基线原始分数中的剩余差距是由通过不存在的预测转换的路由造成的。同一模型在真实环境中执行，在最困难的迷宫中，在已发布的OGBench基线中位列七者第二。多模态动态决定了JEPA世界模型能否进行规划；具有硬分配的预测器混合是一种最小且可验证的修复。

## Abstract
JEPA world models predict the next latent state with a single deterministic predictor trained by latent regression. We show that this fails structurally when the environment is stochastic: at a branching transition, the regression-optimal predictor outputs the conditional mean of the successor embeddings, a point between the true next states that corresponds to no state at all. We prove this collapse for deterministic and gated mixture-of-experts predictors, and prove that MoP-JEPA's hard-assigned predictors converge instead to a quantizer of the transition distribution: one head per successor mode, enumerable in a single forward pass, which is the interface a planner consumes. On official OGBench offline data with leak-free evaluation, planning over single-predictor rollouts performs poorly ($0.02$--$0.09$ success) while planning over our predicted modes reaches up to $0.85$, ahead of deterministic, gated-MoE, and variational predictors on every task. Because multi-prediction evaluation invites coverage freeloading, a verification protocol is part of the method: an input-agnostic codebook control, a shuffled-context test, router-gated readouts, transition-precision guards, and a verified-route criterion in which the model proposes its transition graph blind and ground truth is used only to check the result. Under this criterion our method outperforms the strongest soft alternative on all three mazes ($2$--$5\times$), and the protocol identifies the remaining gap in that baseline's raw scores as routes through predicted transitions that do not exist. The same model executes in the real environment, placing second of seven against the published OGBench baselines on the hardest maze. Multimodal dynamics decide whether a JEPA world model can plan at all; a mixture of predictors with hard assignment is a minimal and verifiable fix.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：联合嵌入预测架构（JEPA）世界模型通常使用单一确定性预测器（基于潜在回归）来预测下一时刻的潜在状态。本文指出，当环境动态具有**随机性**（即同一上下文允许多个有效后继）时，这种设计在结构上会失效。
- **具体表现**：在分支转换点，回归最优预测器会输出后继嵌入的**条件均值**，该均值往往落在各个真实模式之间，成为一个在现实中根本不存在的“无效状态”。这导致基于该预测器的规划无法分辨不同的未来分支，从而失败。
- **整体含义**：论证了随机性对 JEPA 世界模型预测器接口提出了根本性要求——必须能**枚举多个可能的未来**，而非输出一个平均化的点。论文旨在提供一个最小且可验证的解决方案，并建立一套验证协议以防止多预测评估中常见的“覆盖率搭便车”现象。

### 2. 论文提出的方法论

- **核心思想**：将单一预测器替换为一组**硬分配的混合预测器（Mixture of Predictors, MoP）**。每个预测头通过胜者通吃（Winner-Take-All, WTA）的竞争，专门负责捕获一个不同的后继模式（如左转、右转、直行），从而将预测器输出从单个无效均值转变为一组可枚举的候选后继状态。
- **关键技术细节**：
    - **预测头与路由器**：设有 $K$ 个并行的预测头 $\{g_{\phi_k}\}$，共享上下文输入 $c = [z, a]$。另有一个上下文专属的路由器 $r_\psi$ 估计各头激活的概率 $\pi_k$。
    - **硬分配损失**：对于每个样本，计算所有预测头的输出与目标嵌入的距离 $d_{ik}$，选择距离最小的头 $k_i^* =\arg\min_k d_{ik}$ 作为**胜者**，仅让该头接收梯度来更新参数。损失函数结合了胜者头与目标的余弦距离、路由器对胜者索引的交叉熵损失以及防止头退化的负载均衡正则项：
        $$\mathcal{L}_{\text{MoP}} = \frac{1}{|B|}\sum_{i\in B}\sum_{k} \gamma_{ik} d_{ik} + \lambda_{\text{route}} \frac{1}{|B|}\sum_{i\in B} -\log \pi_{ik_i^*} + \lambda_{\text{bal}} \sum_{k} \bar{\gamma}_k \log(K \bar{\gamma}_k)$$
        其中 $\gamma_{ik}$ 为独热赋值指示符。
    - **收敛性质**：证明该硬分配机制等效于在潜在空间中对转换分布 $p(z'|c)$ 进行 **K-均值量化**，各预测头将收敛至不同模式的聚类中心，而非输出均值。同时，路由器渐进地学会根据上下文预测各模式权重。
    - **验证协议**：为防止模型通过简单地覆盖更多结果（而不具备真正的上下文条件性）来刷高指标，配套了严格的验证措施，包括：与输入无关的码本控制、打乱上下文测试、路由器门控读数、转换精度守卫，以及“验证路由”标准（模型盲猜转换图，仅用真值图检查是否存在一条真实转换组成的路径）。

### 3. 实验设计

- **数据集与场景**：
    - **主要规划基准**：OGBench 离线数据集，包含三个具有随机分支或拼接特性的迷宫环境：`pointmaze-teleport-navigate`（传送分支）、`pointmaze-medium-stitch`、`pointmaze-large-stitch`，以及高维的 `antmaze-teleport`（29维蚂蚁）。
    - **泛化验证场景**：行人轨迹预测（ETH/UCY）、数字图像修复（SVHN）、像素迷宫（DINO-WM 框架下的视觉蚂蚁迷宫），用于验证方法在不同模态和输入类型下的通用性。
- **对比方法（基线）**：在同一编码器、同一离线数据和同一规划器的**严格共协议**下重新实现，仅改变预测器：
    - **Dense**：标准单一确定性预测器（JEPA 默认）。
    - **M3-JEPA**：门控混合专家（MoE），通过加权求和融合为单个输出。
    - **Var-JEPA**：变分单高斯预测器（黄等人 2026 的方案，经过强化实现）。
    - **MDN**：软混合密度网络（在一个 $K \times \lambda$ 网格上搜索最优超参）。
    - **MoP-JEPA**：本文提出的硬分配混合预测器。
    - **码本控制**：作为无上下文依赖的“覆盖搭便车”上限。
- **评估指标**：
    - `planAll`：模型内部图搜索的可达性（规划成功率）。
    - `realroute`：**核心验证指标**，检查模型盲提出的转换图中是否存在一条完全由真实转换构成的路径。
    - `execution`：在真实环境中的闭环执行成功率。
    - `COV-R`：后继模式的覆盖率（原始版和路由器门控版）。
    - `transition precision`：预测转换中真实转换的比例。

### 4. 资源与算力

- 论文中**未明确提及**所使用的具体硬件配置，如 GPU 型号、数量、训练时长等。训练和评估均基于官方 OGBench 离线数据，属于中小规模强化学习世界模型任务，推测所需算力中等，但本文未提供确切数字。

### 5. 实验数量与充分性

- **实验数量充足，覆盖全面**：
    - 在 4 个主要迷宫任务（3 个点迷宫 + 1 个蚂蚁迷宫）上进行了严格的规划验证（每组 5 个种子）。
    - 进行了预测头数量 $K$ 的扫参（图 9），验证了 $K$ 大于等于真实模式数时性能饱和的预测。
    - 包含多个消融实验：硬分配 vs. 独立训练的集成头、软-硬退火训练策略、显式头分离损失的作用。
    - 在泛化部分，对 3 个额外场景（行人、数字、像素世界模型）进行了验证。
    - 通过验证协议中多达 5 种不同的控制测试（码本、打乱、门控、精度、真实路由），系统地剥除了覆盖率搭便车的虚假性能。
- **客观性与公平性**：所有对比均基于相同的编码器和规划器实现，只替换预测头，保证了变量单一；对基线方法（如 Var-JEPA、MDN）进行了公平的网格搜索调参（包括钢化 MDN 到匹配的容量和步数）；使用“验证路由”指标作为最终裁决，避免了多预测方法用不精准但广覆盖的图刷高原始规划分的漏洞。

### 6. 论文的主要结论与发现

- **确定性及软混合预测器在随机分支点必然崩溃**：理论和实验均证实，单一预测器和门控 MoE 均会输出无物理意义的条件均值，导致规划失败（规划成功率 < 0.09）。
- **硬分配是恢复多模态规划能力的关键**：MoP-JEPA 的硬分配头成功地将预测空间量化为多个可枚举的模式，使 `planAll` 成功率达到最高 0.85。
- **验证协议暴露了软方法的失败本质**：MDN 虽在原始覆盖率上很高，但其规划成功建立在不存在的“幻觉”边上（转换精度仅 0.14-0.21），而 MoP-JEPA 是唯一在“验证路由”标准下依然保持高成功率的（精度 0.42-0.56），在三个迷宫上领先基线 2-5 倍。
- **通用性得到验证**：该机制不限于 2D 迷宫，在行人预测、图像修复和高维蚂蚁迷宫中也展示了相似的改进，表明其适用于更广泛的 JEPA 多模态预测任务。

### 7. 优点

- **问题诊断清晰，理论坚实**：明确指出了“条件均值崩溃”这一根本性问题，并通过命题 1-3 给出了严格证明。
- **方法极简而有效**：仅将损失函数改为“胜者通吃”的硬分配，是一种与编码器架构正交的最小化修改，易于集成到现有 JEPA 框架（如 DINO-WM）中。
- **评估方法论严谨**：建立了一套多层次的验证协议，尤其是“验证路由”标准，完整解决了多预测评估中“刷覆盖率而无真规划能力”的漏洞，为领域贡献了重要的审计工具。
- **实验对比公正全面**：在完全统一的协议下重实现并对比了几乎所有相关预测器变体，并对最强基线进行了钢化调参，结论说服力强。

### 8. 不足与局限

- **极限模式覆盖率未满**：在极端四分支场景下，概率低于 10% 的稀有模式可能因头部激活不足而丢失，且提到的几种潜在修复手段（如头部排斥正则项、按模式重采样等）仍待测试。
- **训练不稳定性**：在最难的大拼接迷宫中，WTA 优化呈现“种子双峰性”，即某些随机种子下成功率下降，可能影响训练的稳健性。
- **场景规模相对有限**：目前验证集中在小型迷宫和简单图像任务上，其验证协议和模型的效能能否无缝扩展到**视频级或真实机器人规模**的高维、高噪世界模型，仍是未来工作。
- **资源报告缺失**：未报告训练所需的算力、硬件和耗时，不利于复现成本的评估。
- **外部基线对比不直接**：与已发布的 OGBench 离线 GCRL 基线的比较仅为“外部锚定”，并非同协议比较，仅用于参考。

（完）
