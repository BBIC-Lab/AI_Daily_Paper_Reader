---
title: "RAMP: Recognition parametrisation by Amortised Message Passing"
title_zh: RAMP：基于摊销消息传递的识别参数化
authors: "Lior Fox, Kai Biegun, James Heald, Samo Hromadka, Arielle Rosinski, Maneesh Sahani"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18883v1"
tags: ["query:ndai"]
score: 9.0
evidence: 用于从数据学习潜在表示的摊销消息传递框架
tldr: 无监督学习旨在从高维数据中发现潜在因子，但传统概率模型受限于分布假设或近似方法的可扩展性。本文遵循识别参数化范式，提出RAMP方法，通过学习灵活的非线性摊销消息传递框架隐式定义潜在结构，实现了在复杂高维数据上基于似然的高效潜在分布恢复。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18883v1/fig-001.webp\", \"caption\": \"Figure 3: Inferred posterior means of learnt 2D latents for the pendulum dataset. Left: points coloured by ground-truth angular velocity. Right: points coloured by ground-truth angle. The black line tracks a single trajectory, with representative observations displayed. RAMP learns a latent representation of both dynamical state dimensions, reliably separating angles and angular velocities.\", \"page\": 9, \"index\": 1, \"width\": 722, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2607.18883v1/fig-002.webp\", \"caption\": \"Figure 4: Pose reconstruction. (a) Images from LSP (left) were used to create simplified stick-figure representations, and local patches extracted based on labelled joint positions (coloured circles). By regressing the inferred posterior means at each joint to edge orientations, a sketch of the underlying pose was reconstructed (right). (b) 2D projections of the 6D posterior means inferred at the right knee (left) and neck (right), coloured by the angle (in radians) of the edge connecting the joint to the right ankle (left) and right hip (right), respectively. Each dot represents a different image.\", \"page\": 10, \"index\": 2, \"width\": 772, \"height\": 191}]"
motivation: 现有概率模型在学习潜在依赖关系时，或因分布选择限制可处理性，或因近似方法难以扩展至大规模复杂模型。
method: 提出RAMP，通过识别参数化学习一个灵活、非线性、摊销的消息传递框架来隐式捕捉潜在结构。
result: RAMP能够在表达性强的非线性模型中高效地从复杂高维数据中恢复潜在变量的分布。
conclusion: RAMP为无监督学习提供了一种有效且可扩展的潜在结构发现方法。
---

## 摘要
无监督学习的核心目标是揭示解释观测之间依赖关系的潜在因素。概率模型通常通过引入多个潜在变量来实现这一点，这些变量通过条件关系图相连，其分布参数及其依赖关系从数据中学习。学习要么依赖于允许可处理信念传播的分布选择，要么依赖于在模型规模和复杂性下扩展性差的近似方法。我们基于最近发展的识别参数化建模范式提出了一种替代方法：RAMP，它通过学习灵活、非线性、摊销的消息传递框架来隐式定义潜在结构。我们证明RAMP能够在作用于复杂高维数据的表达性非线性模型中，基于似然有效地恢复潜在变量分布。

## Abstract
A central aim of unsupervised learning is to uncover latent factors that explain dependencies among observations. Probabilistic models typically achieve this by introducing multiple latent variables linked through a graph of conditional relationships, with distributional parameters and their dependence learnt from data. Learning relies either on distributional choices that allow tractable belief propagation, or on approximations that scale poorly with model size and complexity. We build on the recently developed recognition-parametrised modelling paradigm to propose an alternative approach: RAMP, a method that implicitly defines latent structure by learning a flexible, nonlinear, amortised message-passing framework. We show that RAMP enables efficient likelihood-based recovery of latent-variable distributions within expressive nonlinear models acting on complex high-dimensional data.

---

## 论文详细总结（自动生成）

# 论文总结：RAMP: Recognition parametrisation by Amortised Message Passing

## 1. 研究动机与核心问题
- **背景**：无监督学习的核心任务是从高维观测数据中发现能够解释数据依赖关系的潜在因子。传统概率模型通过构建多个潜在变量及其条件关系图（即图形模型）来实现，然后从数据中学习这些变量的分布参数和依赖结构。
- **现有方法的痛点**：
  - 为保证推理的可处理性，往往需要限制分布形式（如共轭、高斯等），牺牲模型的表达能力。
  - 或者依赖近似推理（如变分推理、MCMC），但在模型规模变大、结构变复杂时，这些近似方法的可扩展性急剧下降。
- **核心目标**：在保持模型高表达力（非线性、灵活结构）的同时，实现基于似然的高效潜在分布恢复。由此引出 **RAMP**，旨在通过隐式学习消息传递框架来替代硬编码的结构假设或昂贵的近似推理。

## 2. 方法论
- **总体范式**：RAMP 建立在 **识别参数化建模（recognition-parametrised modelling）** 这一新兴范式之上。该范式不显式指定潜在变量的生成模型结构，而是通过学习一个摊销的推理网络来隐式定义潜在结构。
- **核心思路**：
  - 学习一个**灵活、非线性、摊销的消息传递框架**，该框架能够自动捕捉潜在变量之间的依赖关系。
  - 摊销意味着推理网络能够以“一次性前向传播”的方式直接映射观测数据到潜在变量的后验参数，无需为每个数据点单独运行迭代推断。
- **技术要点推断**（基于摘要和范式描述）：
  - 可能使用图神经网络或消息传递神经网络来参数化潜在变量之间的信息交互，其中消息函数和更新函数由神经网络表示。
  - 整个框架以似然为目标进行端到端优化，既学到了潜在表示的分布，也学到了潜在变量之间条件依赖的传递规则。
  - 公式化表述上，潜在变量 $z$ 的后验近似 $q(z|x)$ 通过一个推理网络 $f_\phi(x)$ 得到，而消息传递步骤则内嵌在该网络中，形成递归或并行计算结构。
- **优势**：避免了手工选择分布族和近似算法的局限，同时保持了对高维复杂数据的可处理性。

## 3. 实验设计
- **使用的数据集与场景**：
  1. **Pendulum（倒立摆）数据集**：一个模拟的动力学系统，用于验证 RAMP 是否能从连续观测中恢复潜在状态（如角度、角速度）。该数据具有低维潜在结构，但观测可以是高维（如图像序列）。
  2. **LSP（Leeds Sports Pose）人体姿态数据集**：真实图像数据，从标注的身体关节点位置提取局部图像块，用于展示 RAMP 在复杂视觉数据上的潜在结构发现能力。
- **评估方式**：
  - 对 Pendulum 数据，可视化学习到的 2D 潜在空间，并与真实物理量（角度、角速度）进行对照，观察是否解耦并可靠分离。
  - 对 LSP 数据，从推断的潜在均值回归重建边缘方向，生成简笔姿态草图；并展示特定关节点的潜在表示如何按相关的肢体角度自然组织。
- **对比方法**：原文未明确提及具体对比的基准方法（如标准 VAE、因子图方法等），但上下文指出传统消息传递或变分推断在大规模非线性模型中难以扩展，RAMP 作为替代方案提出，实验可能侧重于定性展示其能力而非定量指标刷榜。

## 4. 资源与算力
- **文中未明确说明**所使用的 GPU 型号、数量、训练时长等硬件资源信息。从实验规模看，pendulum 模拟数据及 LSP 图像块处理相对轻量，对算力要求可能不高，但缺乏明确记录使得可复现性偏弱。

## 5. 实验数量与充分性
- **实验组数**：至少包含两个不同性质的数据集——一个动力学模拟数据集，一个真实视觉数据集。在每个数据集上展示了可视化分析（潜在空间着色、姿态重建等）。
- **充分性与公平性评价**：
  - **定性充分**：实验有效图解了 RAMP 的核心能力：解耦潜在因子、保留语义结构、支持下游重建。图片证据直观有力。
  - **定量缺失**：未报告任何数值指标（如重建误差、对数似然、互信息、解耦度量），也未进行消融实验（如消息传递步数、网络结构大小的敏感性）。缺少与自编码器、变分自编码器、结构化概率模型等基线方法的定量比较。
  - **客观性**：由于缺少对比基准与统计检验，难以严格评判 RAMP 相较于其他方法的优劣。论文以提出框架为主，实验更多是概念验证。

## 6. 主要结论与发现
- RAMP 能够在高度非线性的概率模型中，以端到端的方式从复杂高维数据中**高效恢复潜在变量的后验分布**。
- 该框架无需预先规定潜在结构或分布形式，而是通过**学习摊销消息传递**自动发现条件依赖，实现了**表达力与可扩展性的统一**。
- 在倒立摆系统和人体姿态数据上，所学习的潜在空间展现出良好的解耦性和语义可解释性。

## 7. 优点与亮点
- **方法创新**：将识别参数化模型与消息传递深度融合，提供了一种“让模型自己学会如何传播信念”的新途径，跳出了传统图模型的设计限制。
- **可扩展性**：摊销推理天然支持大规模数据批处理，避免了逐样本迭代的推理瓶颈。
- **表达力强**：用神经网络组件实现消息传递，能够捕捉任意复杂的非线性和高阶依赖。
- **应用潜力**：为在难以显式建模结构的高维观测（如图像、视频）上进行无监督结构发现提供了灵活工具。

## 8. 不足与局限
- **实验不够全面**：缺少定量评估、标准基准测试和与代表性方法的公平对比，证据等级停留在定性展示。
- **可复现性细节不足**：未报告模型架构超参数、优化设置、计算资源，复现存在困难。
- **理论分析薄弱**：未提供关于摊销消息传递的收敛性、渐近性质或辨识性保证。
- **适用范围未探明**：仅在较小规模、中等复杂度的数据上测试，对于超高维、极度稀疏或大规模图结构数据的扩展性仍未知。
- **对识别参数化范式的依赖**：继承了该范式可能存在的训练不稳定、模式坍塌或后验坍塌等问题，但文中未讨论相应缓解措施。

（完）
