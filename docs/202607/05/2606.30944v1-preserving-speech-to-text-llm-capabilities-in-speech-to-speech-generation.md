---
title: Preserving Speech-to-Text LLM Capabilities in Speech-to-Speech Generation
title_zh: 在语音到语音生成中保留语音到文本大语言模型的能力
authors: "Yuxuan Hu, Heng Lu, Ruchao Fan, Yao Qian, Xiaofei Wang, Jian Xue, Heming Wang, Shuohang Wang, Young Jin Kim, Yelong Shen, Jinyu Li"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30944v1"
tags: ["query:ndai"]
score: 7.0
evidence: 将LLM输出转换为语音令牌序列
tldr: 语音转文本大语言模型（S2T LLM）虽具备强大的语音感知和文本推理能力，但扩展至语音到语音（S2S）生成时，微调骨干会损害原有S2T性能，级联合成则引入瓶颈。本文提出PRIME-Speech框架，冻结S2T骨干，仅训练语音生成模块，通过后解码器与骨干中间隐藏状态同步直接生成音频令牌，并使用打包策略和多令牌预测稳定多轮交互、降低延迟。在多项任务中，该框架在保持S2T能力的同时，输出低错误率口语，首次实现不修改骨干的高质量S2S生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30944v1/fig-002.webp\", \"caption\": \"Fig. 1. Model architecture. A frozen speech–text backbone remains responsible for speech perception and text reasoning. PRIME-Speech attaches a trainable audio post-decoder to intermediate backbone states, so each streaming update runs text and audio branches in parallel from the same hidden-state trajectory. MTP lets the audio branch commit multiple codec tokens per update.\", \"page\": 2, \"index\": 2, \"width\": 806, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2606.30944v1/fig-001.webp\", \"caption\": \"TABLE II PERFORMANCE COMPARISON ACROSS SPEECH TASKS. FOR ENTRIES REPORTED AS “S2T / S2S”, THE LEFT NUMBER IS THE S2T SCORE AND THE RIGHT NUMBER IS THE S2S SCORE. “FLU.” DENOTES THE UTMOS SCORE IN VOCALBENCH. “–” INDICATES UNAVAILABLE RESULTS.\", \"page\": 5, \"index\": 1, \"width\": 1070, \"height\": 246}]"
motivation: 为了在不牺牲S2T LLM原有强大文本推理能力的前提下，实现端到端的语音到语音生成，避免微调带来的性能退化或级联系统的效率瓶颈。
method: 提出PRIME-Speech框架，冻结S2T骨干，训练因果音频后解码器，基于骨干中间隐藏状态、文本和音频历史生成编解码令牌，并采用轮次级KV缓存与位置重置的打包策略、多令牌预测技术。
result: 在语音翻译、口语问答、语音理解和多轮对话任务上，PRIME-Speech保持了与冻结骨干一致的S2T性能，同时生成低词错率的口语响应，且多轮交互稳定高效。
conclusion: PRIME-Speech首次证明了在不修改S2T LLM参数的情况下，能够直接基于隐藏状态生成高质量语音，为保留推理能力的语音到语音系统提供了新范式。
---

## 摘要
强大的语音到文本（S2T）大语言模型已能提供稳健的语音感知和文本推理能力，但增加语音到语音（S2S）输出却面临挑战：微调骨干网络可能降低原有的S2T性能，而附加下游发声器又会重新引入串行的文本到语音瓶颈。我们提出PRIME-Speech，一种冻结骨干网络的S2S转换框架，仅训练语音生成模块。PRIME-Speech将因果音频后解码器与冻结骨干网络的中间隐藏状态同步，从而使得编解码令牌从模型演进的推理轨迹中生成，而非基于已完成的文本块。后解码器使用混合隐藏状态、文本和音频历史进行条件化，训练时的打包策略配合轮次级音频KV缓存和位置重置，可在无需额外多轮S2S训练数据的情况下稳定多轮口语交互。多令牌预测进一步降低了有效编解码预测率，并在不改变推理路径的前提下改善了首音频延迟。在语音翻译、口语问答、语音理解和多轮对话任务中，PRIME-Speech保持了冻结骨干网络的S2T行为，同时生成准确、低词错率的口语响应。

## Abstract
Strong speech-to-text (S2T) LLMs already provide robust speech perception and text reasoning, but adding speech-to-speech (S2S) output is challenging: fine-tuning the backbone can degrade the original S2T performance, while attaching a downstream talker reintroduces a serial text-to-speech bottleneck. We present PRIME-Speech, a frozen-backbone S2S conversion framework that trains only speech-generation modules. PRIME-Speech synchronizes a causal audio post-decoder with intermediate hidden states of the frozen backbone, so codec tokens are generated from the model's evolving reasoning trajectory rather than from completed text chunks. The post-decoder uses mixed hidden-state, text, and audio-history conditioning, and a training-time packing strategy with turn-level audio KV-cache and position reset stabilizes multi-turn spoken interaction without additional multi-turn S2S training data. Multi-token prediction further reduces the effective codec prediction rate and improves first-audio latency without modifying the reasoning path. Across speech translation, spoken QA, speech understanding, and multi-turn dialogue, PRIME-Speech preserves the S2T behavior of the frozen backbone while producing accurate, low-WER spoken responses.