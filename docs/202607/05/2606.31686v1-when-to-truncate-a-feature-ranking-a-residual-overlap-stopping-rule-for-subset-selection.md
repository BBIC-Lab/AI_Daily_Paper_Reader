---
title: "When to Truncate a Feature Ranking: A Residual-Overlap Stopping Rule for Subset Selection"
title_zh: 何时截断特征排序：一种用于子集选择的残差重叠停止规则
authors: Jesus S. Aguilar-Ruiz
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31686v1"
tags: ["query:ndai"]
score: 9.0
evidence: 为特征排序子集选择提出停止规则
tldr: 在监督特征选择中，特征排序后通常缺乏明确的截断标准。本文提出了一种基于残差重叠的停止规则，通过类条件分布的巴氏系数度量边际分离，并以风险校准阈值决定保留特征的最短前缀，实现可解释的全局子集选择。该方法在高维基因组数据上能将特征从数万降至数十个，且预测性能与全特征相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31686v1/fig-001.webp\", \"caption\": \"Table 1: Number of available features, selected prefix lengths and prior-dependent uniform residual-overlap thresholds for ε = 0.001.\", \"page\": 15, \"index\": 1, \"width\": 502, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2606.31686v1/fig-002.webp\", \"caption\": \"Table 2: Predictive performance with ε = 0.001 and uniform residual-overlap calibration.\", \"page\": 15, \"index\": 2, \"width\": 927, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2606.31686v1/fig-003.webp\", \"caption\": \"Table 4: Extract of the variable-wise Bhattacharyya coefficients for the four-class example. Coefficients are assumed to be available for all 100 ranked variables.\", \"page\": 20, \"index\": 3, \"width\": 586, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2606.31686v1/fig-004.webp\", \"caption\": \"Table 5: Residual product overlaps along the first five prefixes.\", \"page\": 20, \"index\": 4, \"width\": 822, \"height\": 229}]"
motivation: 特征排序的第二阶段往往依赖任意截断或交叉验证，缺乏直接的解释和理论依据。
method: 利用每对类的类条件分布间的巴氏系数计算边际重叠，并基于残差乘积重叠与风险校准阈值确定全局子集。
result: 在高维基因组数据上，方法将特征从数万减少到几十个，同时保持统计上相当的预测性能。
conclusion: 所提出的停止规则提供了可解释、高效的特征子集选择方式，特别适用于超高维且无法进行穷举搜索的场景。
---

## 摘要
特征排序在监督特征选择中被广泛使用，因为它们简单、可扩展且易于解释。变量首先根据相关性得分进行排序，然后通过保留排名靠前的变量获得子集。尽管第一阶段已得到广泛研究，第二阶段通常由任意基数、经验阈值或交叉验证决定，缺乏直接解释。这引发了一个基本问题：给定一个特征排序，何时累积的类别分离证据足以停止选择特征？本文开发了一个分布框架，通过明确的校准风险的停止规则，将监督特征排序转化为与类别无关的子集。对于每个变量和每对类别，边缘分离由相应类别条件分布之间的Bhattacharyya系数衡量。所提出的方法通过保留排名的最短前缀来选择所有类别共享的单一全局子集，对于每个相关的类别对比，该前缀的残差乘积重叠降至预设阈值以下。我们为标记乘积边缘问题推导了二元和多类贝叶斯风险界，并从目标全对风险水平获得了残差重叠阈值的依赖于先验和无先验的校准。在高维基因组数据集上的实证比较表明，该规则可以将数万个变量减少到几十个，同时保持与全特征基线在统计上相当的预测性能。由于该停止规则仅需要一维边际重叠估计并扫描预先计算的排名，因此非常适用于无法进行穷举子集搜索且特征排名的可解释截断至关重要的大规模高维场景。

## Abstract
Feature rankings are widely used in supervised feature selection because they are simple, scalable and easy to interpret. Variables are first ranked by a relevance score, and a subset is then obtained by retaining the top-ranked variables. Although the first stage has been extensively studied, the second is often governed by an arbitrary cardinality, an empirical threshold or cross-validation, without a direct interpretation. This raises a basic question: given a feature ranking, when is there enough accumulated class-separation evidence to stop selecting features?   This paper develops a distributional framework for transforming supervised feature rankings into class-independent subsets through an explicit risk-calibrated stopping rule. For each variable and each pair of classes, marginal separation is measured by the Bhattacharyya coefficient between the corresponding class-conditional distributions. The proposed method selects a single global subset shared by all classes by retaining the shortest prefix of a ranking whose residual product overlap falls below a prescribed threshold for every relevant class contrast. We derive binary and multiclass Bayes-risk bounds for the labelled product marginal problem, and obtain prior-dependent and prior-free calibrations of the residual-overlap threshold from a target all-pairs risk level.   An empirical comparison on high-dimensional genomic datasets illustrates that the rule can reduce tens of thousands of variables to a few dozen while maintaining predictive performance statistically comparable to the all-features baseline. As the stopping rule only requires one-dimensional marginal overlap estimates and scans a precomputed ranking, it is well suited to very high-dimensional settings where exhaustive subset search is infeasible and interpretable truncation of feature rankings is essential.

---

## 论文详细总结（自动生成）

好的，以下是对这篇论文的结构化总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：在监督学习的特征选择中，当获得一个按相关性排序的特征列表后，缺乏一个具有明确统计学解释的停止准则来决定保留多少个特征。目前的做法（如固定数量、经验阈值或交叉验证）往往是操作便利导向，而非理论驱动。
*   **研究动机**：高维分类问题中，特征排序方法虽然简单可扩展，但其截断步骤不够透明和可解释。论文旨在回答“从何时起，积累的类别可分性证据足以停止选择特征？”。
*   **整体含义**：提出一种将特征排序转化为具有风险校准解释的、类别无关的特征子集的方法，使截断步骤具有清晰的分布意义。

### 2. 论文提出的方法论

*   **核心思想**：在“乘积边缘模型”的假设下，通过监控序贯加入的特征所累积的类间边缘分布重叠度来决定停止点。当所有相关类别对比的“残差乘积重叠”都低于一个预设阈值时，停止选择。
*   **关键技术细节**：
    *   **边缘分离度量**：对于任意特征 $v$ 和类别对 $(c_i， c_j)$，使用**Bhattacharyya系数** $β_{ij}^v = BC(P_{i,v}, P_{j,v})$ 来衡量其类别条件边缘分布的重叠度。$β$ 越小，分离度越高。与之等价的加法度量是 $w_{ij}^v = -\log β_{ij}^v$。
    *   **乘积边缘模型 (Product Marginal Model)**：对于选定的特征子集 $T$，假设其类条件联合分布 $P_{i,T}^{\otimes}$ 是各边缘分布 $P_{i,v}$ 的乘积。在此模型下，子集 $T$ 对于类别对 $(c_i， c_j)$ 的残差乘积重叠 $B_{ij}(T)$ 可精确分解为 $B_{ij}(T) = \prod_{v \in T} β_{ij}^v$。
    *   **停止规则**：给定一个特征排序 $R$ 和一个统一的残差重叠阈值 $θ \in (0， 1]$，选择排序中最短的前缀 $q^*$ 作为特征子集 $T^*$，使得对于所有类别对 $(c_i, c_j)$，都满足 $B_{ij}(T_{q^*}(R)) \le θ$。等价地，累积的对数分离度需达到 $-\log θ$。
    *   **阈值校准**：阈值 $θ$ 并非任意选择，而是可以从一个目标的、模型下的“全对风险”水平 $ε$ 推导出来。论文给出了依赖于先验和无先验的两种校准公式：$θ_ε = \min(1, ε / \sum_{i<j} \sqrt{π_i π_j})$。
    *   **算法流程**：
        1.  输入一个特征排序 $R$ 及估计的成对Bhattacharyya系数 $β_{ij}^v$。
        2.  初始化累积对数分离度 $A_{ij}=0$。
        3.  按顺序遍历排序中的特征，对每个特征 $v$，更新所有类别对的 $A_{ij} \leftarrow A_{ij} - \log β_{ij}^v$。
        4.  当所有 $A_{ij}$ 都首次 ≥ $-\log θ$ 时停止，当前已遍历的特征集合即为所选子集 $T^*$。
        5.  若遍历完所有特征仍未满足条件，则返回全部特征并标记为“未校准”。

### 3. 实验设计

*   **数据集**：18个来自CuMiDa微阵列数据库的高维基因组数据集，特征数量从22,277到54,675不等。
*   **基准 (Baseline)**：使用**全部特征 (All Features)** 训练的分类器性能作为主要比较基准。
*   **方法比较**：
    *   **外部排序准则**：对比了**互信息 (MI)** 和 **卡方分数 ($χ^2$)** 两种方式生成的特征排序。
    *   **下游分类器**：在选定的特征子集上训练**高斯朴素贝叶斯 (GNB)** 和**逻辑回归 (LR)** 分类器，并比较其性能。
*   **评估方式**：采用分层10折交叉验证。特征排序、边际重叠估计及停止点确定均在训练集内完成。评估指标为准确率和宏观F1分数。

### 4. 资源与算力

*   **论文未明确说明**：文中没有提及进行实验所使用的GPU型号、数量或具体的训练时长等算力信息。考虑到该方法仅需一维边缘重叠估计和对排序的扫描，计算复杂度不高，理论上对算力需求较低。

### 5. 实验数量与充分性

*   **实验数量**：实验覆盖了18个数据集、2种排序准则和2种下游分类器，构成了一个相当全面的比较矩阵。此外，还使用了Friedman检验和经Holm校正的Wilcoxon符号秩检验进行统计显著性分析。
*   **充分性与公平性**：
    *   **充分性**：实验设计充分，旨在验证主要贡献——即在极端降维的同时保持预测性能。统计检验的使用增强了结论的可靠性。
    *   **公平性**：对比公平。所有模型（包括“全特征”基线）都经过相同的交叉验证流程。停止规则所需的参数 $ε$ 是固定的，确保了实验的可重复性。

### 6. 论文的主要结论与发现

*   **极端降维**：该方法能将特征数量从平均42,109个（全特征）显著降低至约42.9个（MI排序）或56.0个（$χ^2$ 排序），维度缩减约三个数量级。
*   **性能保持**：所选出的极小特征子集，在不同分类器上的预测性能（准确率、宏观F1）与使用全特征的基线模型在**统计上无显著差异**。这证明了该停止规则在保留判别信息方面的有效性。
*   **排序准则的鲁棒性**：在该停止规则下，MI和$χ^2$两种排序准则选出的子集在预测性能上也无显著差异。

### 7. 优点

*   **理论驱动与可解释性**：相比固定数目或交叉验证，该方法为截断点提供了明确的、与模型风险相关的统计学解释。
*   **计算高效**：算法仅需一维边缘密度估计和一次排序扫描，非常适用于特征维度极高（如上万维）的场合。
*   **输出类别无关子集**：方法输出一个对所有类别共用的特征子集，兼容标准的多类分类流程，同时提供了用于诊断的成对类别信息。
*   **模块化设计**：该停止规则与生成特征排序的具体方法解耦，可应用于任何已排序的特征列表。

### 8. 不足与局限

*   **模型依赖与独立性假设**：所有理论推导和风险校准都严格依赖于“乘积边缘模型”，即假设选定特征在各类内条件独立。当特征间存在强相关性时，此模型会低估真实的重叠度（对冗余特征重复计数）或忽略依赖结构中的判别信息，导致模型与现实之间存在偏差（calibration gap）。
*   **对输入排序的依赖性**：停止点完全由输入的排序决定。如果排序本身质量不高（如高度冗余的特征都排在前面），可能会导致选出的子集并非最优。
*   **重叠度估计的挑战**：实际中，$β_{ij}^v$ 需要通过样本估计，这在样本量小、类别稀有或连续变量场景下可能不稳定，影响停止决策的可靠性。
*   **实验覆盖的局限**：实验仅局限于基因组学数据，未在其他领域（如文本、图像、金融）的高维数据集上进行验证。
*   **分类器对比有限**：下游分类器只使用了朴素贝叶斯和逻辑回归，未与更复杂的非线性模型（如支持向量机、随机森林、梯度提升树）对比。尽管朴素贝叶斯与该方法的模型假设一致，但评估在其他流行模型上的表现同样重要。

（完）
