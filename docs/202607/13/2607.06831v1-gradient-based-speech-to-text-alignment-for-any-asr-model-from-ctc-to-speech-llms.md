---
title: "Gradient-Based Speech-to-Text Alignment for Any ASR Model: From CTC to Speech LLMs"
title_zh: 基于梯度的语音到文本对齐适用于任何ASR模型：从CTC到语音大语言模型
authors: "Albert Zeyer, Ralf Schlüter, Hermann Ney"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06831v1"
tags: ["query:ndai"]
score: 8.0
evidence: 基于梯度的显著性用于特征重要性
tldr: 本文研究一种通用的基于梯度的语音文本对齐方法，适用于任何可微分ASR模型，包括CTC、Transducer、AED及语音LLM。通过计算教师强制token概率对输入的梯度生成帧级显著图，再用动态规划获取单词边界，无需训练或修改模型。在四个家族16个模型上评估表明，该方法普遍可用，在原生对齐较弱时更优，但计算成本较高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06831v1/fig-002.webp\", \"caption\": \"Fig. 1: Posteriors log ( pt(y=as | xT ′ 1 ) ) s,t ∈ RS×T , gradient scores log softmaxT (G) ∈ RS×T , and log self-attention weights (in RS×T ), all without energy weighting here, each with word boundaries, in comparison to the reference segmentation (silence in white, words in blue, with word boundaries).\", \"page\": 2, \"index\": 2, \"width\": 524, \"height\": 935}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-003.webp\", \"caption\": \"TABLE I: Word-boundary error and accuracy per model, gradient alignment vs. each model’s native or attention aligner, on TIMIT-test and Buckeye, grouped by family. The attention and posterior aligners use the model’s native subword units; the gradient uses characters for AED and speech-LLM and native subwords otherwise (Table IV). Starred Whisper-largev3 gradient row (*) is taken at optimal encoder depth (encoder 3/4, Table X).\", \"page\": 3, \"index\": 3, \"width\": 504, \"height\": 743}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-004.webp\", \"caption\": \"TABLE III: Signed boundary offsets on Buckeye, gradient alignment vs. the model’s alternative aligner. WBE is the mean absolute word-boundary error, start/end off. the signed mean boundary offset (positive = late), and width err. the signed word-width error (0 = correct duration). Parakeet RNN-T is the offline contrast to the streaming transducers.\", \"page\": 4, \"index\": 4, \"width\": 529, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-005.webp\", \"caption\": \"TABLE IV: Character vs. subword targets, one representative model per family, Buckeye. Each model shows its gradient alignment and its model-native alternative. The character target is a valid re-segmentation of the transcript, one token per character with a word-boundary token between words.\", \"page\": 4, \"index\": 5, \"width\": 516, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-006.webp\", \"caption\": \"TABLE II: Hypothesis-mode alignment on Buckeye, where each model aligns its own recognition. Identity-gated F1 at a 50ms collar and Levenshtein-matched WBE against the reference.\", \"page\": 4, \"index\": 6, \"width\": 516, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-007.webp\", \"caption\": \"TABLE VII: Audio-energy token weighting, Buckeye, wordlevel topology. The token scores are weighted by (audio energy)ρ before the DP, and we sweep ρ (ρ=0 disables it) at each blank scheme. This complements Table VI, which sweeps the blank scheme at fixed ρ=0.5.\", \"page\": 5, \"index\": 7, \"width\": 512, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-008.webp\", \"caption\": \"TABLE VIII: Whisper’s cross-attention DTW vs. our aligner (Whisper-base cross-attention or grad. scores, Buckeye). Top row reproduces Whisper’s find_alignment. Columns: alignment-head set (Whisper’s curated heads, our gold-tuned top-k, or single best), token z-norm, median filter, pre-log of the score matrix (log), log-softmax over time (log sm; DP sums these log-scores), mono (✓ = our monotonic DP forbidding vertical step; × = Whisper DTW), silence topology (word = blank only between words; none = no blank), and energy weighting (en).\", \"page\": 5, \"index\": 8, \"width\": 529, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-009.webp\", \"caption\": \"TABLE VI: Blank-scoring scheme across signals, Buckeye, word-level topology, energy weighting fixed at ρ=0.5. The constant blank γ, the energy-aware silence βt = µt − λ z(Et)σt, and the z-score βt = µt + κσt. WBE for the gradient signal and for the native attention signal.\", \"page\": 5, \"index\": 9, \"width\": 529, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2607.06831v1/fig-001.webp\", \"caption\": \"TABLE IX: MMS-FA (wav2vec 2.0) CTC gradient alignment across the internal level the gradient is taken at, Buckeye. The levels run from the convolutional feature encoder through the feature projection to the raw waveform at several pooling rates. The feat-proj linear row equals the MMS-FA gradient number reported in Table I.\", \"page\": 6, \"index\": 1, \"width\": 446, \"height\": 281}]"
motivation: 现有ASR模型的对齐方法依赖编码器帧网格，精度有限，且部分模型（如AED和语音LLM）缺乏原生对齐，需要一种通用、无需训练的对齐方案。
method: 对每个教师强制token，计算对数概率相对于音频输入的梯度，降维得到帧级显著矩阵，再用动态规划一次解码出单词时间边界。
result: 在朗读和自然语音数据集上，该方法为所有测试模型提供了可用对齐，通常稍逊于强原生对齐，但在流式模型等弱原生对齐场景中表现更佳。
conclusion: 基于梯度的对齐是一种通用且易部署的方案，代价是每token一次反向传播，适合对齐需求灵活的场景。
---

## 摘要
语音到文本对齐是指在音频中找到每个词的时间边界。一些模型直接提供这种对齐，而其他模型则不然。连接时序分类（CTC）和转换器模型通过构造具有对齐，而基于注意力的编码器-解码器（AED）和语音大语言模型（LLMs）则不然，它们的词时间通常从注意力权重中读取。所有这些信号都存在于编码器帧网格上，这限制了它们的时间精度。我们研究了一种通用的基于梯度的对齐方法，适用于任何可微的ASR模型。我们计算每个教师强制token的对数概率关于输入的梯度，将其缩减为每帧显著性，并通过一次动态规划将结果矩阵解码为词边界。该方法无需训练、无需修改模型、无需对齐头，适用于包括语音LLMs在内的所有模型家族，并在输入网格上对齐，而不是在较粗的编码器网格上。我们在四个家族的十六个模型上评估了该方法，使用朗读语音（TIMIT）和自发语音（Buckeye），分别与模型自身的原生对齐或基于注意力的对齐进行比较。我们发现，梯度为每个模型都产生了可用的对齐，它通常略逊于强大的原生对齐器，但在原生对齐较弱的情况下表现更好，例如流式模型，其主要缺点是每个token需要一次反向传播的成本。

## Abstract
Speech-to-text alignment means finding the temporal boundaries of each word in the audio. Some models provide such an alignment directly and others do not. Connectionist temporal classification (CTC) and transducer models have an alignment by construction, whereas attention-based encoder-decoders (AED) and speech large language models (LLMs) do not, and their word timings are usually read off the attention weights instead. All of these signals live on the encoder frame grid, which bounds their temporal precision. We study a generic gradient-based alignment that applies to any differentiable ASR model. We take the gradient of each teacher-forced token log probability with respect to the input, reduce it to a per-frame saliency, and decode the resulting matrix into word boundaries with a single dynamic-programming pass. The method needs no training, no model modification and no alignment heads, works across all model families including the speech LLMs, and aligns on the input grid rather than on the coarser encoder grid. We evaluate it on sixteen models from four families, on read (TIMIT) and spontaneous (Buckeye) speech, each against the model's own native or attention-based alignment. We find that the gradient yields a usable alignment for every model, that it is usually somewhat behind a strong native aligner but better where the native alignment is weak, as for the streaming models, and that its main disadvantage is the cost of one backward pass per token.