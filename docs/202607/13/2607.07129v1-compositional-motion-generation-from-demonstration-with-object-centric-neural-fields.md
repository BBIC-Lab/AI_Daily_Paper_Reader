---
title: Compositional Motion Generation from Demonstration with Object-Centric Neural Fields
title_zh: 基于以对象为中心的神经场的演示组合动作生成
authors: "Ahmet Ercan Tekden, Yasemin Bekiroglu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07129v1"
tags: ["query:ndai"]
score: 9.0
evidence: 时序混合专家模型用于组合运动序列生成
tldr: 针对机器人学习需要组合性和数据效率的挑战，本文提出一种从演示中学习的生成式框架，通过对象中心神经场表示场景，结合时间混合专家模型组合运动原语，实现感知与运动的组合式连接。该框架在仿真中仅需少量数据即可完成长时操作，并在真实世界实验中展示了噪声鲁棒性、类别级泛化和在3D场景上直接运行的能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07129v1/fig-001.webp\", \"caption\": \"Fig. 1: System overview. Top: Task sequence with object interactions, followed by the ground-truth and reconstructed images. Middle: Object-specific masks inferred from latent codes, illustrating object-centric scene decomposition. Bottom: Temporal gating weights showing each object’s influence over the trajectory; snapshots above the plot correspond to key events (T0–T3), marked by vertical dotted lines.\", \"page\": 1, \"index\": 1, \"width\": 374, \"height\": 467}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-002.webp\", \"caption\": \"TABLE I: Simulation results (MED/Accuracy).\", \"page\": 6, \"index\": 2, \"width\": 516, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-003.webp\", \"caption\": \"Fig. 9: Illustration of the perception pipeline: input images, languageconditioned mask colorizations produced by the vision–language model, and the resulting image reconstructions.\", \"page\": 7, \"index\": 3, \"width\": 500, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-004.webp\", \"caption\": \"Fig. 10: Example multi-step trials for the second (A-1, A-2, A-3) and third (B-1, B-2, B-3) tasks. Language prompts in the second row allow the user to specify the object to be manipulated.\", \"page\": 7, \"index\": 4, \"width\": 524, \"height\": 260}]"
motivation: 利用组合性原理，通过共享对象级表示连接感知与运动，实现可扩展且数据高效的机器人行为学习。
method: 采用对象中心神经场（结合规范场和潜在变形）渲染场景，并通过时间混合专家模型的门控机制组合对象条件运动原语生成完整轨迹。
result: 仿真中成功完成长时操作任务且所需训练数据显著少于基线，真实实验验证了噪声鲁棒性、类别级泛化和直接3D操作能力。
conclusion: 该组合式生成框架能有效建模机器人行为，在数据效率、泛化性和真实世界适用性方面表现突出。
---

## 摘要
组合性通过将复杂行为组织为更简单元素的组合，使机器人学习具有可扩展性和数据高效性。利用这一原理，我们提出了一种生成式从演示中学习框架，通过共享的对象级表示连接感知和动作，实现机器人行为的组合建模。我们从以对象为中心的神经表示中渲染场景，这些表示将规范神经场与潜在条件变形相结合，以平滑、一致且可解释的方式捕捉位置和几何变化。对于动作生成，时间混合专家模型（MoE）采用门控机制在时间上组合以对象为条件的运动基元，生成完整的轨迹。这种时空组合性保持了运动基元的数据效率，同时将动作建立在视觉结构基础上，实现了在不同场景配置下的系统性泛化。在仿真中，使用所提出的模型成功完成了长周期操作任务，且所需训练数据显著少于其他基于图像的基线方法。真实世界实验进一步展示了该方法对噪声的鲁棒性、通过基于语言的分割模型在类别级别上进行泛化的能力，以及直接操作三维场景表示的能力。

## Abstract
Compositionality, by organizing complex behavior as combinations of simpler elements, enables robot learning that is scalable and data efficient. Leveraging this principle, we propose a generative learning-from-demonstration framework that enables compositional modeling of robotic behavior by connecting perception and motion through shared object-level representations. We render scenes from object-centric neural representations that integrate canonical neural fields with latent-conditioned deformations, capturing positional and geometric variations in a smooth, consistent, and interpretable way. For motion generation, a temporal mixture-of-experts (MoE) employs a gating mechanism to combine object-conditioned movement primitives over time, producing complete trajectories. This spatial-temporal compositionality maintains the data efficiency of movement primitives while grounding motion in visual structure, enabling systematic generalization across diverse scene configurations. In simulation, long-horizon manipulation tasks are successfully completed using the proposed model, which requires significantly less training data than other image-based baselines. Real-world experiments further demonstrate the method's robustness to noise, its ability to generalize at the category level through language-based segmentation models, and its capacity to operate directly on 3D scene representations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- 机器人从演示中学习（LfD）时，面临如何从少量演示数据中学习长周期、多样化行为的难题。
- 日常任务天然具有**组合性**：由多个对象级的交互子任务（到达、抓取、移动、放置）顺序拼接而成。
- 现有方法（如运动基元）往往需要人工定义组合结构或依赖手工特征，基于全局图像的策略又需要大量数据并牺牲数据效率。
- 本文旨在构建一个**数据高效、可组合**的生成式框架，通过**对象中心神经场**建模场景，并通过**时间混合专家模型**对运动进行时序组合，将感知与动作无缝连接，实现从少量演示中泛化到新场景。

### 2. 提出的方法论
#### 2.1 核心思想
将场景和运动都建模为**对象级组合结构**：
- **场景组合**：利用空间混合专家网络（Spatial MoE）对每个对象学习独立的神经场表示（外观、变形、掩膜），并与背景组合。
- **运动组合**：通过时间混合专家网络（Temporal MoE），在不同时间步以不同权重组合多个对象条件的运动基元，生成完整轨迹。

#### 2.2 关键技术细节
- **场景建模 (Sec. IV-A, IV-B)：**
  - 每个对象 $i$ 用一个神经场 $F_i$ 建模外观，一个变形场 $\Delta_i(x|z_i)$ 建模位置变化（受潜在向量 $z_i$ 控制），并通过可学习的对数函数 $l_i$ 产生软掩膜 $M_i$。
  - 像素预测由组合式模型生成：
    $$\hat{I}(x|\{z_i\}) = \sum_{i=1}^N M_i(x-\Delta_i(x|z_i)) F_i(x-\Delta_i(x|z_i)) + M_{bg}F_{bg}(x).$$
  - 训练分阶段进行：先估计背景，再按对象独立预训练（基于粗掩膜的采样与变焦课程学习），最后联合调优。变形场使用 Lipschitz 正则化保证平滑。

- **潜在空间标准化 (Sec. IV-C)：**
  - 对于每个对象，仅用少量“角点”潜在向量 $\mathcal{C}_i$ 覆盖所有演示中观察到的变化。
  - 每个演示的潜在向量 $z_i^{(k)}$ 被重新标记为角点的凸组合，并通过重构误差最小化进行搜索优化（Algorithm 1）。保留前 $L$ 个候选，以增加运动模型训练样本。

- **运动生成 (Sec. IV-D, IV-E)：**
  - 采用时序门控机制，生成每个时间步的条件信号：
    $$c(t) = \sum_{i=1}^N w_i(t) \, c_i(\hat{z}_i), \quad w_i(t) = \frac{\exp(s_i(t))}{\sum_j \exp(s_j(t))}.$$
  - 其中 $c_i(\hat{z}_i)$ 由 FiLM 层生成的调制参数 $\gamma_i^{(\ell)}, \beta_i^{(\ell)}$ 组成，最终通过加权求和得到各层的调制参数。
  - 轨迹被建模为高斯分布：$q(t) \sim \mathcal{N}(\mu(t), \text{diag}(\sigma^2(t)))$，通过最小化负对数似然训练。
  - 训练时采用潜在dropout防止无关变量干扰。

- **推断 (Sec. IV-F)：**
  - 对于新图像，利用训练好的角点集合 $\mathcal{C}_i$ 执行同样的潜在搜索（Algorithm 1），获得各对象潜在向量，再送入运动 MoE 生成轨迹。推理时间单对象约1.4秒（RGB）/0.8秒（3D）。

### 3. 实验设计
#### 3.1 仿真实验 (四个任务)
- 任务场景：
  - **Wall Avoidance**（简单）：控制机器人末端穿过墙壁开口到达目标。
  - **Incline Pick-and-Place**（中等）：将立方体放到不同高度的平台上。
  - **Cup Stacking**（中等）：将小杯子插入大杯子。
  - **Cube Stacking**（困难）：将三个立方体从随机位置堆叠起来。
- 对比方法：
  - CNN-CNMP（条件神经运动基元，使用CNN特征）
  - Diffusion Policy (DP)
  - NFMP（神经场运动基元，仅壁障任务）
  - 消融方法：DINO-FiLM（预训练ViT特征+FiLM）、CNN-FiLM（端到端CNN特征+FiLM）、无时间门控的 “Ours w/o gating”
  - 含中值背景的变体 “Ours w/ MedianBg”
- 评价指标：与演示轨迹的平均欧氏距离 (MED) 和任务成功率。
- 训练规模：低数据与高数据两种设定（低数据10-30条，高数据30-100条），本文方法仅用低数据。

#### 3.2 真实世界实验 (四个任务)
- **桌面拾取放置**：将物体移至碗中，测试对干扰物的鲁棒性。
- **语言引导的拾放（球→容器、杯→目标）**：利用语言分割模型获取掩膜，实现类别级泛化（训练/测试物体不同）。
- **多步抽屉任务**：打开抽屉，从隔间中取出纸箱并放入抽屉，需操作 3D 结构。场景用占有场（基于ESDF）表示，仅4条演示训练，在10个新配置上评估。
- 所有真实任务均使用6自由度示教轨迹，时间对齐依靠夹爪状态。

### 4. 资源与算力
- 论文提及**推断**使用的硬件：Intel Xeon W-2245 CPU，NVIDIA Quadro RTX 6000 GPU。推理速度：RGB 单对象约 1.4 秒，3D 约 0.8 秒。
- 对于**训练**所需的 GPU 型号、数量、训练时长，文中**未明确给出**。
- 从训练数据量（10-100 张图像 + 对应少量轨迹）和网络规模（小型 MLP）推断，整体算力需求不高，应可在单个消费级 GPU 上较快完成。

### 5. 实验数量与充分性
- **仿真实验**：4 个不同难度的任务，至少 2 种训练数据规模，对比了 6 种方法，包含 2 项消融研究（时间门控、背景建模方式）。每个配置均使用 3 次随机种子。图中还给出了完整的数据效率曲线。
- **真实世界实验**：4 个任务，每个任务包含多个测试场景（如拾放 25 个测试例、语言任务 15 例、抽屉任务 10 例），并设有视觉干扰、类别泛化、多物体语言指定等额外测试。
- **整体评价**：实验对比丰富，消融充分，覆盖仿真到真实、2D 到 3D、同物体到类别级泛化，评估客观、公平，实验证明充分。

### 6. 主要结论与发现
- 对象级时空组合建模能大幅提高**数据效率**：在低数据量（10-30 条演示）下，本方法在 MED 和成功率上显著超过对比方法（CNN-CNMP、DP 等），甚至达到或超过它们高数据量下的性能。
- 对象中心的潜在表示具有良好的**解释性**和**泛化能力**，仅需角点组合即可覆盖场景变化。
- 真实实验中展示了**噪声鲁棒性**（干扰物不影响）、**类别级泛化**（借助语言模型获取掩膜）和对**3D 场景的直接操作能力**，证明了方法的实用性。

### 7. 优点
- **组合性设计精巧**：场景和运动分别通过空间、时间 MoE 体现对象级组合，结构清晰，符合直觉。
- **高数据效率**：从原始图像端到端学习运动，却能在极少演示下泛化，克服了传统 MP 和图像策略的缺点。
- **感知-动作深度融合**：通过共享的对象潜在向量，使得场景变化直接反映到运动生成中。
- **3D 扩展简单**：框架天然支持从 2D 图像到 3D 占有场，无需改变核心算法。
- **训练策略实用**：分阶段训练、角点标准化、潜在dropout 等技巧增强了稳定性和泛化。
- 实验扎实，评估全面。

### 8. 不足与局限
- **训练和推理要求场景相对固定**：背景一致、视角固定，且训练时需提供粗糙的物体掩膜（可通过矩形标注），这在动态场景或视角变化大的应用中受限。
- **时间对齐假设较强**：要求演示轨迹在时间上粗略对齐（相同任务阶段对应相同时间索引），若演示变异性太大会影响组合性。文中仅测试了子段内时间扭曲的影响，性能有所下降。
- **外推能力受限**：潜在空间重建依赖于角点集，无法泛化到远超演示范围的参数变化。
- **视觉相似物易混淆**：若干扰物与任务物体外观相似，可能导致错误分解或轨迹偏差（在真实实验失败一例中因棒球硬表面导致抓取失败，但本质可能仍与表示混淆相关）。
- **无法处理动态障碍或在线重规划**：模型仅从一张静态图像推断完整轨迹，不具备闭环反应能力。
- 训练算力细节缺失，难以准确评估资源门槛，但从低数据量推断负担不大。
- 潜在搜索依赖随机采样+梯度优化，推理时间略长（~1.4秒/对象），虽可接受但在高速任务中可能需优化。

（完）
