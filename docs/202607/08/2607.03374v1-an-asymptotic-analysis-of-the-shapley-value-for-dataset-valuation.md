---
title: An Asymptotic Analysis of the Shapley Value for Dataset Valuation
title_zh: 数据集估值中Shapley值的渐近分析
authors: "Mélissa Tamine, Benjamin Heymann, Maxime Vono, Patrick Loiseau"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03374v1"
tags: ["query:ndai"]
score: 9.0
evidence: Shapley值在数据集估值中特征重要性的渐近分析
tldr: 在数据估值问题中，本文利用RKHS均值嵌入将效用建模为经验分布的光滑泛函，对Shapley值进行渐近分析。证明其可简化为一个主导项，代表数据集对总体的边际贡献，并揭示其随数据源数量增长的尺度，为估算方法提供理论基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03374v1/fig-001.webp\", \"caption\": \"Figure 1: Relative error of each estimator averaged over 30 independent repetitions with standard errors. The dashed curve is a reference curve included as a visual guide to assess the relative error decay.\", \"page\": 9, \"index\": 1, \"width\": 822, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2607.03374v1/fig-002.webp\", \"caption\": \"Table 1: Notation for the asymptotic analysis of the Shapley value.\", \"page\": 13, \"index\": 2, \"width\": 764, \"height\": 875}]"
motivation: 探究Shapley值在大规模数据估值场景下的渐近行为，以简化计算并指导实践。
method: 将效用定义为RKHS均值嵌入的泛函，对Shapley值执行渐近展开。
result: 推导出简洁的渐近主导项，刻画一阶贡献并明确估值尺度。
conclusion: 该主导项可作为大规模数据估值的可靠参照，用于校准Shapley值近似方法。
---

## 摘要
我们针对数据集估值场景中的Shapley值提出一种渐近分析，其中效用通过再生核希尔伯特空间（RKHS）均值嵌入被建模为经验分布的光滑泛函。我们证明，尽管Shapley值具有组合定义，但数据源的Shapley值可渐近地由一个简单的主导项捕获。该项可以解释为数据集相对于周围数据总体的一阶贡献。它还揭示了Shapley值随数据源数量增长时的尺度，并为分析现有的Shapley值估计量提供了框架。此外，对于处理大量数据集的从业者而言，该主导项成为一个易于处理的参照基准，可用于评估Shapley值近似方法。

## Abstract
We propose an asymptotic analysis of the Shapley value in a dataset valuation setting in which utilities are modeled as smooth functionals of empirical distributions via reproducing kernel Hilbert space (RKHS) mean embeddings. We prove that, despite its combinatorial definition, the Shapley value of a data source is asymptotically captured by a simple leading term. This term can be interpreted as the first-order contribution of a dataset relative to the surrounding data population. It also identifies the scale of the Shapley value as the number of data sources grows and provides a framework for analyzing existing Shapley value estimators. Moreover, for practitioners working with large numbers of datasets, the leading term becomes a tractable reference against which Shapley value approximations can be benchmarked.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究动机**：在协作机器学习中，多个数据方合作训练模型，如何公平地量化每个数据方贡献的数据集价值是一个关键问题。合作博弈中的 Shapley 值是解决此问题的黄金标准，但计算复杂度极高。
*   **核心问题**：本文旨在研究，当协作的数据源（玩家）数量 $I$ 趋向无穷时，一个特定数据源 $i$ 的 Shapley 值 $\phi_I^i$ 的渐近行为是什么？这个数学上的极限值能否使得 Shapley 值在大型博弈中的分析和计算变得简单？
*   **整体含义**：论文并非设计新的估值算法，而是首次为数据集估值中的 Shapley 值提供渐近理论。它证明，大规模的 Shapley 值可以由一个极为简洁的“主导项”（leading term）很好近似，为理解和评估现有估值近似算法提供了理论基础和基准。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **构建权限博弈序列**：为了进行渐近分析，论文固定一个目标数据方 $i$（其数据集 $D_i$ 不变），然后通过从数据总体中持续采样并添加新的“周围数据方”，来构建一个玩家数量不断增长的博弈序列。
*   **效用函数的平滑建模**：
    *   **分布嵌入**：使用再生核希尔伯特空间（RKHS）的均值嵌入 $\mu(\hat{P}_D)$ 来表示数据集 $D$ 的经验分布。
    *   **平滑效用**：假设效用函数 $v(D)$ 是该嵌入的平滑泛函 $F$，即 $v(D) = F(\mu(\hat{P}_D))$。这意味着数据集的微小变化只会导致效用的平滑变化。
*   **渐近分析与主导项推导**：
    1.  **总体参考点**：定义一个总体参考嵌入 $\mu^* = \mathbb{E}[X_j] / \bar{n}$，它代表了一个典型“周围数据方”的平均特征贡献。
    2.  **一阶信号**：定义目标方 $i$ 的“一阶信号” $c_i = \langle \nabla F(\mu^*), \mu_i - \mu^* \rangle$，该标量度量了 $i$ 的数据分布与总体在效用函数最敏感方向上的差异。
    3.  **一阶泰勒展开**：利用 $F$ 的光滑性，将目标数据方 $i$ 加入一个大型联盟 $S$ 的边际贡献 $\Delta_i(S)$，在 $\mu^*$ 附近进行一阶泰勒展开，并结合联盟嵌入的集中不等式进行近似。
    4.  **主导项**：通过对所有可能的联盟大小 $k$ 进行 Shapley 平均，最终推导出一个简洁的渐近主导项：
        $$ \Theta_I^i = \frac{n_i c_i}{\bar{n}} \cdot \frac{H_{I-1}}{I} $$
    其中 $n_i$ 是目标方数据集大小，$\bar{n}$ 是周围方平均大小，$H_{I-1}$ 是调和数。该项完全分离了数据集大小、数据独特性和Shapley平均的影响。
*   **理论保证**：论文证明了确切 Shapley 值 $\phi_I^i$ 与主导项 $\Theta_I^i$ 之间的 $L_1$ 误差是 $O(1/I)$ 的。由于主导项的量阶为 $\Theta(\log I/I)$，因此相对误差趋向于零。

### 3. 实验设计：数据集、基准、对比方法

*   **实验场景**：一个完全受控的合成实验，其设置与理论假设完全匹配。
*   **数据与效用**：
    *   特征空间为 RKHS $\mathcal{H} = \mathbb{R}^{30}$。
    *   包含 $T=4$ 种潜在玩家类型，每种类型有12个原型数据点，均在半径为0.9的球面上。
    *   固定数据方 $i$ 的属性被特意设定为与其总体嵌入 $\mu^*$ 差距最大的类型，以确保其一阶信号 $c_i \neq 0$。
    *   效用函数定义为 $v(D) = \tanh(\beta \langle \mathbf{w}, \mu(\hat{P}_D) \rangle)$，其中 $\beta=1.5$，$\mathbf{w}$ 方向刻意与固定方 $i$ 的偏离方向对齐。
*   **基准与对比**：论文以推导出的“主导项” $\Theta_I^i$ 作为黄金基准，将三种不同的Shapley值**估计量**与之比较：
    1.  **排列蒙特卡洛 (Permutation Monte Carlo)** $\hat{\phi}_{i, MC}^I$：通过随机排列采样来估计Shapley值。
    2.  **DU-Shapley** $\hat{\phi}_{i, DU}^I$：一种数据集级别的Shapley代理方法。
    3.  **分层蒙特卡洛 (Stratified Monte Carlo)** $\hat{\phi}_{i, Straft}^I$：按联盟大小分层采样的估计方法。
*   **评估指标**：估计量相对于主导项的相对误差 $|\hat{\phi}_I^i / \Theta_I^i - 1|$。
*   **实验过程**：首先采样一个包含 $I_{max}=3500$ 个“周围玩家”的大型群体。然后，通过逐步包含更多玩家（$I=50$ 至 $3500$）来构建规模不同的博弈，并评测估计量向主导项的收敛情况。

### 4. 资源与算力

*   论文明确指出，所有实验**均仅在 CPU 上运行，并在不到一分钟内完成**。
*   未提及使用任何 GPU。

### 5. 实验数量与充分性

*   **实验重复**：整个实验过程独立重复了 $R=30$ 次。
*   **实验规模**：评估了 $16$ 种不同的玩家总数 $I$（从 $50$ 到 $3500$）。
*   **方法对比**：对比了3种主流的Shapley估计方法。
*   **充分性评估**：实验设计非常清晰，目标明确。它并非用于验证方法在真实场景下的优越性，而是用于**实证验证其渐近理论的正确性**。通过30次重复和覆盖广泛 $I$ 值范围，实验结果（图1）有效展示了估计量收敛趋势与理论预测的 $1/\log I$ 参考线的一致性，因此实验充分且客观。结果的可视化也包含了标准差阴影，增加了透明性。

### 6. 论文的主要结论与发现

*   **理论核心**：在平滑效用和独立同分布数据源的假设下，固定数据源的 Shapley 值 $\phi_I^i$ 的渐近性态由一个简单的、可解释的“主导项” $\Theta_I^i$ 完全决定。
*   **价值尺度**：当目标数据源有非零价值时，其 Shapley 值的确切尺度为 $\Theta_P(\log I / I)$。
*   **估计量评价准则**：提出了评估Shapley值估计量的“绝对误差到相对误差原则”：任何绝对误差为 $o(\log I / I)$ 的估计量，其相对误差都将趋近于零。
*   **实验支撑**：合成实验证实，随着玩家数量 $I$ 的增加，几种经典的Shapley估计量均收敛于理论推导的主导项，其误差衰减率与理论预测相符。

### 7. 优点：方法或实验设计上的亮点

*   ****新颖的渐近视角**：首次从渐近角度研究数据集估值中的Shapley值，从一个全新的维度揭示了其行为规律。
*   **理论简洁性**：将指数级复杂的组合计算问题，转化为了一个关于数据集大小、分布差异和简单权重三项乘积的简洁表达式，极大地提升了Shapley值的可解释性。
*   **提供理论基准**：主导项成为一个理论上的“标尺”。在真实的大规模场景中，由于无法计算精确值，可以用该主导项来校准和比较不同Shapley值近似算法的优劣。
*   **理论严谨性**：模型的构建（RKHS嵌入、泰勒展开）与最终的非渐近误差界推导非常清晰、严谨且自成体系。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

*   **模型假设较强**：核心假设（效用是RKHS嵌入的光滑泛函、有界特征映射、数据方独立同分布）在真实机器学习任务（如训练深度神经网络）中可能不成立。这使得理论结果目前还难以直接应用于最前沿的实践。
*   **缺乏真实世界实验**：实验验证完全基于与理论假设一致的合成数据。论文未在真实数据集或真实机器学习效用函数（如测试集准确率）上检验其主导项的近似效果，其实用性有待验证。
*   **固定玩家问题的特殊性**：论文限定分析“一个固定的、预先抽样的数据方”的价值。如何将这种渐近分析推广到对群体中任意数据方的估值，仍是一个开放问题。
*   **基准依赖性**：主导项 $\Theta_I^i$ 的计算需要知道总体参考嵌入 $\mu^*$、平均数据量 $\bar{n}$ 和一阶信号 $c_i$。在实际应用中，这些总体参数本身需要估计，这会引入额外的估计误差，而论文暂未分析这一部分。

（完）
