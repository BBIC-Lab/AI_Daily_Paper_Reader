---
title: Safe Inference-Time Alignment via Lagrangian Reward Augmentation
title_zh: 通过拉格朗日奖励增强实现安全的推理时对齐
authors: "Yaswanth Chittepu, Ativ Joshi, Sohini Chintala, Scott Niekum"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02781v1"
tags: ["query:ndai"]
score: 7.0
evidence: 在解码过程中将模型输出转换为序列的方法
tldr: 推理时对齐通过辅助奖励信号引导冻结语言模型解码，无需微调，但现有方法对安全约束的处理不够原则化。本文提出拉格朗日奖励增强（LARA）框架，通过对偶化将安全约束的KL正则化目标转化为一维凸问题，利用小校准集估计对偶变量，生成增强奖励，即插即用于现有推理时对齐方法。实验表明LARA改善了有用性与无害性间的权衡，Best-of-N重排取得最佳性能，接近微调基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02781v1/fig-001.webp\", \"caption\": \"Figure 1: Expected helpfulness E[r] and expected cost E[c] of the BoN policy as a function of λ, for the Llama base model on the left and Qwen base model on the right. The dashed vertical line indicates the expected cost threshold τ.\", \"page\": 8, \"index\": 1, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2607.02781v1/fig-002.webp\", \"caption\": \"Figure 2: Helpfulness vs Harmfullness for LLama3.2-3b under both model evaluation (left) and GPT evaluation (right). λ∗ for Llama is 0.6 and λmax is chosen to be 2.0.\", \"page\": 9, \"index\": 2, \"width\": 826, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2607.02781v1/fig-003.webp\", \"caption\": \"Figure 3: Helpfulness vs Harmfulness for Qwen2.5-3b under both model evaluation (left) and GPT evaluation (right). We observed that PARGS performed similarly to ARGS for Llama, and hence omit it from the figure. λ∗ for Qwen is 2.0 and λmax is chosen to be 3.0.\", \"page\": 22, \"index\": 3, \"width\": 826, \"height\": 334}]"
motivation: 现有推理时对齐方法只优化单一奖励分数，安全约束要么被忽略，要么依赖手动调节的惩罚项，缺乏原则性的纳入方式。
method: LARA从带约束的KL正则化目标出发，通过对偶化将约束问题简化为非负对偶变量上的一维凸优化，并在校准集上估计该变量，用以生成增强奖励，无缝集成到序列级或token级推理时对齐方法中。
result: 在序列级（如Best-of-N）和token级解码方法上，LARA均改善了有用性-无害性权衡，其中Best-of-N表现最优，接近微调型的直接对齐方法。
conclusion: LARA为安全推理时对齐提供了一种通用且原则性的框架，通过校准的对偶奖励增强，实现了安全约束的有效即插即用。
---

## 摘要
推理时对齐在解码过程中利用辅助奖励信号引导冻结的语言模型，避免了重复更新权重的成本。然而，现有的推理时对齐方法通常优化单个标量分数，因此明确的安全约束要么被忽略，要么通过手动调整的惩罚项来编码。我们提出拉格朗日奖励增强（LARA），一种在安全约束下的通用推理时对齐框架。从带有奖励模型和成本模型的KL正则化约束目标出发，LARA 将约束对偶化，并将优化问题简化为一个关于非负对偶变量的一维凸问题。该对偶变量在一个小规模校准集上估计得出，定义了一个增强奖励，可作为现有推理时对齐方法中即插即用的评分信号。对于序列级采样方法，如 Best-of-N 重排序，校准后的对偶变量对应于期望成本约束问题的解。对于词元级奖励引导解码方法，同样的构造产生一个有原则的对偶校准启发式方法，而非精确的受约束策略保证。我们在序列级和词元级推理时对齐方法上评估了 LARA，发现 LARA 改善了有用性与无害性之间的权衡，其中 Best-of-N 在推理时方法中取得了最佳性能，接近基于微调的直接对齐基线。

## Abstract
Inference-time alignment steers a frozen language model during decoding using auxiliary reward signals, avoiding the cost of repeated weight updates. However, existing inference-time alignment methods typically optimize a single scalar score, so explicit safety constraints must either be ignored or encoded through manually tuned penalties. We propose Lagrangian Reward Augmentation (LARA), a general inference-time alignment framework under safety constraints. Starting from a KL-regularized constrained objective with a reward model and a cost model, LARA dualizes the constraint and reduces the optimization problem to a one-dimensional convex problem over a nonnegative dual variable. Estimated on a small calibration set, this dual variable defines an augmented reward that can be used as a drop-in scoring signal within existing inference-time alignment methods. For sequence-level sampling methods, such as Best-of-N reranking, the calibrated dual variable corresponds to the solution of the expected-cost constrained problem. For token-level reward-guided decoding methods, the same construction yields a principled dual-calibrated heuristic rather than an exact constrained-policy guarantee. We evaluate LARA on both sequence-level and token-level inference-time alignment methods, and find that LARA improves the helpfulness-harmlessness tradeoff, with Best-of-N achieving the best performance among inference-time methods, approaching finetuning-based direct alignment baselines.