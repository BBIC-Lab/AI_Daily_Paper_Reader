---
title: "StateFlow: Dual-State Recurrent Modeling for Long-Horizon Time Series Forecasting"
title_zh: StateFlow：用于长时程时间序列预测的双状态循环建模
authors: "Haroon Gharwi, Yue Dai, Kai Shu"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00197v1"
tags: ["query:ndai"]
score: 9.0
evidence: 用于长周期时间序列预测的双状态循环建模
tldr: 针对长期时间序列预测中的非平稳性和误差累积问题，本文提出StateFlow框架，扩展了VARNN，构建双状态循环模型，同时捕获隐藏状态轨迹（主要动态）和残差记忆轨迹（局部偏差），并通过块解码器与两阶段优化实现直接多步预测。实验表明，StateFlow在标准基准上达到与主流模型相当的性能，同时保持线性循环编码和紧凑设计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00197v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the proposed StateFlow framework with VARNN as the recurrent backbone. VARNN encodes the lookback sequence into hidden-state and residual-memory trajectories, which are then separately summarized by a chunk-based decoder and projected to the future horizon for direct multi-step forecasting.\", \"page\": 2, \"index\": 1, \"width\": 590, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2607.00197v1/fig-002.webp\", \"caption\": \"Table 1: Benchmark results (MSE/MAE) across datasets and horizons.\", \"page\": 7, \"index\": 2, \"width\": 980, \"height\": 695}, {\"url\": \"assets/figures/arxiv/2607.00197v1/fig-005.webp\", \"caption\": \"Table 2: Ablation study of encoder representations under the fixed chunk-decoder setting. Lower values are better. The best result is shown in bold and the second-best result is underlined.\", \"page\": 8, \"index\": 5, \"width\": 586, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2607.00197v1/fig-006.webp\", \"caption\": \"Figure 2: Effect of chunk versus direct decoding on the he representation. The left panel reports test MSE across prediction horizons, while the right panel compares total parameters.\", \"page\": 9, \"index\": 6, \"width\": 814, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2607.00197v1/fig-007.webp\", \"caption\": \"Figure 3: Encoder-only parameter comparison under the matched-representation setting. VARNN uses the smallest encoder.\", \"page\": 9, \"index\": 7, \"width\": 452, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2607.00197v1/fig-004.webp\", \"caption\": \"Table 6: Effect of dual-state fusion strategy under the fixed chunk-decoder setting. Lower MSE/MAE and fewer parameters are better. Extra Params reports the additional parameters used by Joint HE relative to Sep. HE.\", \"page\": 13, \"index\": 4, \"width\": 648, \"height\": 1140}, {\"url\": \"assets/figures/arxiv/2607.00197v1/fig-003.webp\", \"caption\": \"Table 7: Full recurrent encoder and optimization ablation under the matched-representation setting. Results are reported as MSE. Lower values are better. The best result is shown in bold and the second-best result is underlined.\", \"page\": 14, \"index\": 3, \"width\": 650, \"height\": 1040}]"
motivation: 解决长期预测中非平稳性、制度转换和误差累积的挑战，并突破VARNN仅支持单步回归的局限。
method: 提出StateFlow，以VARNN为双状态循环编码器，分别建模隐藏状态和残差记忆轨迹，利用块解码器汇总轨迹并直接预测未来序列，采用先训练编码器单步预测再训练解码器的两阶段优化。
result: 在多个LTSF基准上，StateFlow表现与线性、循环、卷积及Transformer等强基线模型相当，验证了其有效性。
conclusion: StateFlow通过双状态建模和两阶段训练，有效应对长期预测的变异性，提供了紧凑且高性能的解决方案。
---

## 摘要
长时程多变量时间序列预测（LTSF）由于非平稳性、状态切换和误差累积而仍然充满挑战。感知变异性的递归神经网络（VARNN）旨在通过维护由单步预测误差驱动的残差记忆状态来跟踪这种变异性。然而，其原始形式仅限于单步序列回归，并不直接支持多步预测。在这项工作中，我们将VARNN扩展到长时程预测，并引入了StateFlow，这是一个循环预测框架，以VARNN作为双状态循环骨干来从回溯序列中捕获两种互补信号：代表主要时间动态（包括趋势、季节性、水平变化和重复模式）的隐藏状态轨迹，以及代表结构化局部预测偏差的残差记忆轨迹，该偏差由单步基础预测与观测值之间误差的非线性循环变换驱动。一个基于分块的解码器分别总结这些轨迹，并将其映射到未来视野，以实现直接多步预测。我们进一步采用了两阶段优化策略，首先通过单步基础预测目标训练VARNN编码器，以优化回溯序列上的内部表示，然后训练一个特定于视野的解码器以进行直接多步预测。在标准LTSF基准上的实验表明，StateFlow相对于强大的线性、循环、卷积和基于Transformer的基线方法取得了具有竞争力的性能，同时保持了线性循环编码和紧凑的模型设计。

## Abstract
Long-horizon multivariate time series forecasting (LTSF) remains challenging due to non-stationarity, regime shifts, and error accumulation. The Variability-Aware Recursive Neural Network (VARNN) is designed to track such variability by maintaining a residual-memory state driven by one-step prediction errors. However, its original formulation is limited to one-step sequence regression and does not directly support multi-step forecasting. In this work, we extend VARNN to long-horizon forecasting and introduce StateFlow, a recurrent forecasting framework that uses VARNN as a dual-state recurrent backbone to capture two complementary signals from the lookback sequence: a hidden-state trajectory representing primary temporal dynamics, including trend, seasonality, level changes, and recurring patterns, and a residual-memory trajectory representing structured local prediction deviations, driven from a nonlinear recurrent transformation of errors between one-step base predictions and observed values. A chunk-based decoder separately summarizes these trajectories and maps them to the future horizon for direct multi-step forecasting. We further employ a two-stage optimization strategy that first trains the VARNN encoder through a one-step base prediction objective to optimize the internal representations over the lookback sequence, and then trains a horizon-specific decoder for direct multi-step forecasting. Experiments on standard LTSF benchmarks show that StateFlow achieves competitive performance against strong linear, recurrent, convolutional, and Transformer-based baselines while preserving linear recurrent encoding and a compact model design.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：长时程多变量时间序列预测（LTSF）面临非平稳性、状态切换和误差累积等挑战，现有注意力机制（如Transformer）虽有效但存在二次复杂度，而传统循环模型（RNN/LSTM/GRU）仅靠单一隐藏状态难以显式捕获结构化预测偏差。
- **核心问题**：如何将感知变异的递归神经网络（VARNN）从仅支持单步回归扩展至长时程多步预测，并利用额外的残差记忆状态提升预测鲁棒性。
- **整体含义**：提出 StateFlow 框架，以 VARNN 为双状态循环编码器，联合建模序列的主要动态和局部偏差，通过分块解码与两阶段优化，在保持线性复杂度的同时达到与主流 Transformer、线性等强基线相当的性能，为长时程预测提供了一种高效的循环替代方案。

### 2. 方法论
#### 2.1 整体架构
- 包含**实例归一化**、**双状态循环编码器（VARNN）**、**基于分块的预测解码器**。
- 采用通道独立策略，每个变量作为单变量序列处理，共享参数。

#### 2.2 双状态循环编码器
- 对归一化序列，VARNN 维护两个状态：
  - 隐藏状态 $\mathbf{h}_t$：捕捉趋势、季节性、变化等主体动态。
  - 残差记忆状态 $\mathbf{e}_t$：编码单步预测的局部偏差。
- 更新过程：
  - $\mathbf{h}_t = \phi_h\!\big(\mathbf{W}_h[\bar{x}_t; \mathbf{h}_{t-1}; \mathbf{e}_{t-1}] + \mathbf{b}_h\big)$
  - 生成基础一步预测 $\hat{\bar{x}}_{t+1}^{\text{base}} = \mathbf{w}_o^\top \mathbf{h}_t + b_o$
  - 计算残差 $r_t = \bar{x}_{t+1} - \hat{\bar{x}}_{t+1}^{\text{base}}$
  - 残差记忆更新：$\mathbf{e}_t = \phi_e\!\big(\mathbf{W}_e[r_t; \mathbf{e}_{t-1}] + \mathbf{b}_e\big)$
- 输出轨迹：$\mathbf{H} \in \mathbb{R}^{(L-1)\times K}$（隐藏状态轨迹）、$\mathbf{E} \in \mathbb{R}^{(L-1)\times J}$（残差记忆轨迹）。

#### 2.3 分块解码器
- 分别对隐藏和残差轨迹应用滑动窗口分块（窗口大小 $w$，步长 $s$），每个块展平并投影为固定维度的嵌入。
  - $\mathbf{q}_{h,m} = \phi_{c,h}(\mathbf{W}_{c,h} \mathrm{vec}(\mathbf{C}_{h,m}) + \mathbf{b}_{c,h})$
  - $\mathbf{q}_{e,m} = \phi_{c,e}(\mathbf{W}_{c,e} \mathrm{vec}(\mathbf{C}_{e,m}) + \mathbf{b}_{c,e})$
- 拼接所有块嵌入后展平，经线性映射直接输出归一化的预测序列 $\hat{\bar{Y}}_{L+1:L+H}$，再反归一化得到最终预测。

#### 2.4 两阶段优化
- **阶段一**：仅训练编码器的单步基预测损失 $L_{\text{enc}} = \frac{1}{D} \sum_{i=1}^D \left\| \hat{\bar{x}}_{2:L}^{\text{base},(i)} - \bar{x}_{2:L}^{(i)} \right\|_2^2$，学习残差记忆状态。
- **阶段二**：冻结编码器，训练解码器的多步预测损失 $L_{\text{forecast}} = \frac{1}{D} \sum_{i=1}^D \left\| \hat{x}_{L+1:L+H}^{(i)} - x_{L+1:L+H}^{(i)} \right\|_2^2$。
- 优势：提供明确的残差学习信号，预训练编码器可跨不同预测水平复用，仅需训练各自解码器。

#### 2.5 计算复杂度
- 前向传播复杂度为 $\mathcal{O}\big(DL d^2 + D M w (d_h^2 + d_e^2) + D M d H\big)$，其中 $d = d_h + d_e$，$M$ 为块数目。整体与回看长度 $L$ 和预测区间 $H$ 呈线性关系。

### 3. 实验设计
- **数据集**：7 个标准 LTSF 基准：ETTh1, ETTh2, ETTm1, ETTm2, Weather, ECL, Traffic。
- **评估设置**：回看长度固定在 $L=96$，预测区间 $H \in \{96,192,336,720\}$；指标为 MSE 和 MAE。
- **对比基线**：涵盖三类强方法：
  - Transformer类：iTransformer、PatchTST、Crossformer、Autoformer、Stationary
  - 卷积类：TimesNet、SCINet
  - 线性类：DLinear、RLinear（并包含 TiDE）
- **实现细节**：通道独立、PyTorch、随机种子 2026、隐藏维度 32、残差维度 16、块窗口 5、步长 2 等。

### 4. 资源与算力
- 论文**未明确提及**所用 GPU 型号、数量、训练时长及具体能源消耗。仅在实现部分说明使用 PyTorch 框架和固定的随机种子，无其他算力资源信息。

### 5. 实验数量与充分性
- **主实验**：28 个数据集×预测区间的组合，与 10 个基线对比，报告 MSE 和 MAE。
- **消融研究**：
  - 编码器表示比较：双状态 $(h,e)$、仅 $h$、仅 $e$、基础预测与残差 $(base, r)$，横跨 4 个 ETT 数据集×4 个区间。
  - 融合策略：独立分块 vs 联合分块，表格 + 参数对比。
  - 解码器结构：分块解码 vs 直接展平，图示 MSE 和参数量。
  - 循环编码器选择：VARNN vs RNN/GRU/LSTM（匹配表示维度），单/双阶段优化，多数据集多区间，完整表格。
- **总体评价**：实验覆盖全面，对比公平（统一协议、已发布基线结果），消融设计逐步验证了双状态、分块、两阶段训练的必要性与有效性，实验数量充足。

### 6. 主要结论与发现
- StateFlow 在 28 个场景中取得 **15 个 MSE 第一、14 个 MAE 第一**，整体性能与 Transformer、线性模型等强基线相当。
- 双状态表示 $(h,e)$ 优于单独使用隐藏或残差轨迹，残差记忆状态能提供互补的预测信号。
- 分块解码器在**显著降低参数量**（近一半）的同时，保证了与直接展平相近的精度，实现了精度-效率平衡。
- 与其他循环编码器相比，VARNN 用**更少的编码器参数**（如仅 1922 个，而 LSTM 需 9841 个）实现了更低或相近的误差，且在**两阶段优化**下性能提升最为显著。
- 残差记忆循环作为一种**线性复杂度**的时序归纳偏置，是注意力机制的有效替代，尤其适合资源敏感的长时程预测。

### 7. 优点
- **显式偏差建模**：通过残差记忆状态单独捕捉单步预测的系统性误差，丰富了递归表示能力。
- **高效线性缩放**：编码器处理随回看长度线性增长，避免 Transformer 的二次复杂度。
- **灵活训练范式**：两阶段解耦使编码器复用，降低了多区间预测的训练成本，同时稳定优化。
- **参数高效**：VARNN + 分块解码在保持竞争力的同时，整体参数量远小于许多基线。
- **实验严谨**：全面对比三大类模型，多维度消融清晰验证了每个组件的贡献。

### 8. 不足与局限
- **跨变量交互缺失**：采用通道独立策略，未建模变量间依赖，在高维数据集（如 Traffic、ECL）上可能弱于显式利用变量交互的模型（如 iTransformer）。
- **超参数敏感性未充分讨论**：块窗口大小、步长、状态维度等影响未展开研究。
- **计算资源报告缺失**：没有报告训练耗时、显存占用等实际部署成本，影响复现评估。
- **应用领域有限**：仅在公开基准测试，未涉及金融、医疗等需高可靠性的特定领域。
- **可解释性不足**：残差记忆状态虽提供偏差信息，但其内部动态对人类的可解释性较弱。
- **长期稳定性未知**：在更极端的长预测区间（如 1440 步以上）或非平稳突变场景下的鲁棒性需进一步验证。
- **优化器与调参细节缺失**：未说明学习率调整、正则化等具体设置，可能影响公平比较。

（完）
