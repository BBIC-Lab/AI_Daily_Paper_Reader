---
title: Variance-reduced Domain Adaptation using Paired Sampling
title_zh: 使用成对采样的方差缩减领域自适应
authors: Andrea Napoli
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20367v1"
tags: ["query:ndai"]
score: 9.0
evidence: 通过配对采样减少方差的无监督域适应方法，适用于有限标注数据
tldr: 无监督域适应中，相关对齐和最大均值差异等分布匹配损失存在高方差问题，且缺乏有限和结构，无法使用传统方差减少方法。为此，本文提出配对采样域适应（PSDA），一种新颖的随机方差减少技术，通过在域内和域间配对样本形成四元组，最小化梯度方差，并将其转化为线性分配问题求解。实验表明，PSDA能有效降低方差，提升目标域分类精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20367v1/fig-005.webp\", \"caption\": \"Figure 1: Estimator variance vs k for six sampling algorithms.\", \"page\": 5, \"index\": 5, \"width\": 488, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2607.20367v1/fig-001.webp\", \"caption\": \"Table 1: Average test accuracy for Spawrious by data split.\", \"page\": 7, \"index\": 1, \"width\": 968, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2607.20367v1/fig-002.webp\", \"caption\": \"Table 2: Average test accuracy for Office-Home by data split.\", \"page\": 7, \"index\": 2, \"width\": 940, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2607.20367v1/fig-004.webp\", \"caption\": \"Table 3: Average test accuracy for Humpbacks by data split.\", \"page\": 8, \"index\": 4, \"width\": 672, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2607.20367v1/fig-003.webp\", \"caption\": \"Table 4: Average wall-clock training times by dataset (seconds).\", \"page\": 8, \"index\": 3, \"width\": 977, \"height\": 454}]"
motivation: 现有域适应方法的分布匹配损失在小批量优化下方差高，且不兼容经典方差减少技术。
method: 提出PSDA，通过域内和域间配对样本构建四元组，设计最小化期望梯度方差的配对方案，并将其转化为线性分配问题。
result: 模拟显示PSDA方差更低，在三个域偏移数据集上目标域精度优于相关方法。
conclusion: PSDA作为定制方差减少技术，有效解决了域适应中的高方差难题，提升了模型性能。
---

## 摘要
相关对齐和最大均值差异是两种广泛使用的无监督领域自适应分布匹配框架。然而，这些损失的高方差已被证明会削弱其在小批量优化中的有效性。此外，这些损失缺乏有限和结构，因而与经典随机方差缩减（SVR）方法不兼容。本文提出了面向领域自适应的成对采样（PSDA），一种为这类目标量身定制的新型SVR技术。PSDA在域内和跨域对观测进行配对，形成训练时始终一同采样的四元组。配对设计旨在最小化期望梯度方差，并归结为求解一组线性分配问题。仿真结果显示，与相关方法相比方差减小，在三个领域偏移数据集上的实验表明目标域分类精度提升。

## Abstract
Correlation alignment and the maximum mean discrepancy are two widely used distribution-matching frameworks for unsupervised domain adaptation (UDA). However, high variance in these losses has been shown to undermine their effectiveness in minibatch optimisation settings. Furthermore, the losses lack finite-sum structure, which renders them incompatible with classical stochastic variance reduction (SVR) methods. This paper proposes Paired Sampling for Domain Adaptation (PSDA), a novel SVR technique tailored to such objectives. PSDA pairs observations both within and across domains, to form quadruplets that are always sampled together during training. The pairings are designed to minimise expected gradient variance, and reduce to solving a set of linear assignment problems. Our simulations demonstrate reduced variance compared to related methods, and experiments on three domain shift datasets show improved target domain accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：无监督领域自适应（UDA）中常用的分布匹配损失，如相关对齐（CORAL）和最大均值差异（MMD），在小批量随机优化时会出现高方差，导致训练不稳定甚至性能不如不使用域对齐。这些损失缺乏有限和结构，不能直接应用经典的随机方差缩减（SVR）方法。
- **整体含义**：论文提出一种针对这类非可加损失的新型SVR技术——成对采样域自适应（PSDA），通过构建观测之间的依赖关系来显著降低梯度估计的方差，从而提升目标域的分类精度，同时保持计算效率。

### 2. 论文提出的方法论

- **核心思想**：PSDA 通过设计观测间的配对方案，使得当某些样本共同出现在小批量中时，能够最小化域差异统计量（$\hat{D}$）的期望估计误差。该方法同时利用域内和跨域的配对，最终形成总是被一起采样的四元组（每个域两个样本）。
- **关键技术细节**：
  - **两阶段分配**：
    1. **第一阶段（源-目标匹配）**：构建成本矩阵 $C^{st}_{ij} = \mathbb{E}[\|\hat{D} - D\|^2 \mid i \in B_s, j \in B_t]$，并求解线性分配问题，使每个样本被公平分配，形成跨域配对集合 $\mathcal{M}$。
    2. **第二阶段（配对匹配）**：基于 $\mathcal{M}$ 中的源-目标对，计算条件期望误差成本矩阵 $C^{MM}_{rq}$，再次求解线性分配，将两个配对合并为四元组。
  - **成本计算**：针对 MMD 和 CORAL 分别推导了条件核均值与条件协方差矩阵的闭式更新公式，计算高效且可向量化。
  - **采样过程**：训练时直接根据预先计算好的配对采样，每次梯度更新时保留成对或四元组结构，无需修改模型、损失或优化器。
- **与现有SVR的关系**：PSDA 继承并改进了以往方法（如多样化采样、分层采样、顺序优化），通过全局最优的线性分配实现更彻底的方差压缩，且计算复杂度与小批量大小无关。

### 3. 实验设计

- **数据集与场景**：
  - **Spawrious**：包含6种难度的狗品种分类，跨越不同背景环境，共6个域、18,664个样本。
  - **Office-Home**：65类日常物品分类，包含艺术、剪贴画、产品和真实世界四种风格，12个数据划分，15,500个样本。
  - **Humpbacks**：水下声学记录中的座头鲸叫声检测，4个域、8,000个样本。
- **基准对比方法**：
  - **无适配基线**：ERM。
  - **主流UDA方法**：DANN、CDAN、SDAT、ELS、ARM、MCC。
  - **SVR采样器**：k-means++、DPP、Anticlustering、VaRDASS（分层采样）、ORDERED（顺序重排），以及PSDA的两个变体（仅阶段1配对“Paired sampling”和包含两个阶段“Double-paired”）。
- **评估指标**：目标域测试准确率（平均及标准误），以及方差缩减程度的蒙特卡洛模拟。

### 4. 资源与算力

- 论文未明确列出所使用的GPU型号和数量，但提及使用南安普顿大学的IRIDIS高性能计算集群。
- 训练时长方面，给出了各采样器在各数据集上的平均墙钟时间（秒）：例如，PSDA变体在Spawrious上约1218–1329秒，在Humpbacks上约232–286秒，效率优于大多数SVR对比方法。
- 因此，训练算力负担较低，适合普通GPU单卡训练。

### 5. 实验数量与充分性

- **实验规模**：
  - 方差模拟：1组，对比6种采样算法在不同小批量大小 $k$ 下的方差。
  - 准确率实验：在3个不同域偏移数据集上，分别测试了两种UDA损失（CORAL和MMD）与7种采样器的组合，同时对比了6种非SVR的UDA基线。
  - 每个设置重复5次，使用不同随机种子，超参数通过随机搜索独立调优。
- **充分性与公平性**：
  - 对比了最新的SVR技术以及多种经典和现代UDA方法，覆盖图像分类和音频检测任务。
  - 实验中保留一致的训练框架（DomainBed），独立调节超参数以确保公平比较。
  - 汇报了误差棒和多次重复，统计上较为可靠。
  - 但仅测试了三种数据集，且主要在中小规模数据上验证，缺乏进一步消融研究（如配对更新频率的影响）。

### 6. 论文的主要结论与发现

- PSDA 显著降低了MMD和CORAL的梯度方差，尤其在小批量大小较小时优势尤为突出（方差可比次优方法低数个数量级）。
- 在目标域精度上，PSDA（尤其是Double-paired变体）在多数数据集和损失函数下均达到或接近最优，超越了复杂的对抗性UDA方法和现有SVR采样器。
- 方法计算高效，采样一旦完成，训练速度大幅领先于ORDERED等依赖在线优化的方法。
- 总之，PSDA成功地将配对方差缩减引入域自适应，为高噪声的分布匹配损失提供了实用且强大的解决方案。

### 7. 优点

- **创新的方差缩减方法**：首次将基于成对采样的抗差估计思想应用于MMD和CORAL损失的梯度估计，有效解决了UDA中的方差难题。
- **理论推导清晰**：将配对问题转化为线性分配，给出了精确的成本公式，并证明了与总期望的无偏恢复关系。
- **计算效率高**：配对过程可向量化加速，采样不依赖小批量大小，训练时几乎无额外开销。
- **即插即用**：无需修改模型架构、损失函数或优化器，可与现有UDA管线无缝集成，也可与其他SVR方法组合。
- **实验全面且性能领先**：在三个不同领域的数据集上均取得有竞争力的成绩，且方差仿真效果极佳。

### 8. 不足与局限

- **适用损失固定**：当前方法仅针对MMD和CORAL两种分布匹配损失设计，推广至其他UDA损失（如对抗损失）需重新推导成本。
- **周期性重新配对**：特征分布随训练变化，需要周期性重新提取特征并重新计算配对，可能会增加一定的预处理负担（尽管论文声称成本可接受）。
- **实验覆盖范围有限**：仅在三个数据集上验证，未测试大规模数据集；缺乏消融实验分析配对更新频率、配对方式对性能的敏感性。
- **理论假设**：方差缩减依赖于特征空间的平滑性条件，实际作用可能受特征提取器质量影响，未深入分析。
- **极端类别不平衡**：虽然处理了域间数量不平衡的约束，但未专门讨论类别不平衡对配对的影响。

（完）
