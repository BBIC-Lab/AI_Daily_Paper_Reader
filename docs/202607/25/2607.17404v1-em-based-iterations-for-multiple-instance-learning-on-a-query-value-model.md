---
title: EM-based iterations for multiple instance learning on a query-value model
title_zh: 基于查询-值模型的多示例学习的EM迭代算法
authors: Ethan Levien
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17404v1"
tags: ["query:ndai"]
score: 7.0
evidence: 在多实例学习中学习查询和值向量用于包表示
tldr: 本文研究多示例回归中的查询-值模型，将注意力机制解耦为查询方向和值方向，从包级监督同时学习两者。通过推导无噪极限下的EM迭代族（推广EM-DD），并对最大似然估计进行浓度分析，证明值向量单一随机初始化即平均指向正确方向，仅需多项式数量的包即可使EM在常数步内高概率收敛，揭示了协方差浓度与极端值统计的相互作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17404v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison of standard supervised learning and MIL. Each row represents a bag of instances (circles). Instances live in feature space, which here is R2 with coordinates (x1, x2) represented by a hue (edge) and lightness (filling). In standard supervised learning each bag contains a single instance. In MIL, we select an active instance via some selection rule, which takes the lightest circle (middle) or the most orange circle (right). The middle represents an MIL dataset where the feature used for selecting the active instance is also used to label that instance. In contrast, on the right figure the hue is used to select the active instance, but the label comes from the lightness feature. In our model, the directions in feature space used for selection and labeling will be denoted by q and v as a nod to query and value matrices in attention.\", \"page\": 2, \"index\": 1, \"width\": 753, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2607.17404v1/fig-006.webp\", \"caption\": \"Figure 2. Comparison of the EM0 iteration (30) (top row) and ẼM1 (32) (bottom row) as a function of the number of instances per bag n at t = 100 steps from a uniformly random initial assignment. The image plots (right) show which bags have correctly identified active instances. For clarity, we only show a subset of the instances. The n values corresponding to these examples are circled (left). Parameter values: θq∗,v∗ = π/4, m = 5000, d = 15, instances i.i.d. Normal(0, Id).\", \"page\": 9, \"index\": 6, \"width\": 759, \"height\": 764}, {\"url\": \"assets/figures/arxiv/2607.17404v1/fig-003.webp\", \"caption\": \"Figure 3. Trajectories of the match fraction f (t) over iterations. Each panel shows a different variant of the hard EM iteration. Faint colored curves are individual trajectories initialized from random assignments. The bold black curve is the mean over all trajectories. Parameter values: m = 1000, n = 50, d = 10, θq∗,v∗ = 0.\", \"page\": 10, \"index\": 3, \"width\": 857, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2607.17404v1/fig-002.webp\", \"caption\": \"Figure 4. Performance of the EMκ schedules, ẼM1, and the staged method after t = 100 iterations, as a function of the misalignment angle θv∗,q∗ . Columns show the match-fraction error 1−f (left), the value angle θv̂,v∗ (middle), and the query angle θq̂a,q ∗ (right). The top row shows the mean over the ensemble of random initial assignments (shaded bands: ± one standard error); the bottom row shows the minimum over the same ensemble. Parameter values: m = 1000, n = 15, d = 10, 400 replicates per value of θv∗,q∗ .\", \"page\": 10, \"index\": 2, \"width\": 863, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2607.17404v1/fig-004.webp\", \"caption\": \"Figure 5. Value update (Theorem 4.6), conditioned on a given match fraction f . The angle θv̂,v∗ is shown for several values of the alignment parameter u = cos(θv∗,q∗), (left) as simulation vs. theory across f and (right) as a contour over (f, u), with the u values shown on the left indicated by dashed lines. Parameter values: n = 20, d = 3, m = 50,000.\", \"page\": 14, \"index\": 4, \"width\": 951, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2607.17404v1/fig-005.webp\", \"caption\": \"Figure 6. Query update (Theorem 4.8). (left) the angle θq̂a,q∗ as a function ofm for several match fractions f , comparing simulation to the theoretical prediction Eq. (61). (right) a contour plot of the same prediction over (f,m), with the f values shown on the left indicated by dashed lines. Parameter values: n = 20, d = 500.\", \"page\": 14, \"index\": 5, \"width\": 951, \"height\": 372}]"
motivation: 探究现代基于注意力的多示例学习中，如何从包级标签同时学习查询向量和值向量的核心难题。
method: 从softmax模型导出参数化EM迭代族，并在无噪声极限下对随机实例选择进行浓度分析。
result: 值向量的最大似然估计在单次随机初始化时平均方向正确，且仅需多项式数量的包就能使EM算法以高概率在O(1)步收敛。
conclusion: 分析表明经验协方差矩阵的浓度与选择规则的极端统计相互作用，为高效学习提供了理论保证。
---

## 摘要
在多示例回归（MIR）中，数据组织成包（特征空间中实例的集合），目标是学习一个将标签分配给包的映射。一个典型假设是特征空间中存在所谓的概念点，其邻近程度决定了包的标签。受现代基于注意力的MIR架构启发，我们研究了一个softmax模型，该模型将概念点和标记方案解耦。两者分别由特征空间中的查询方向和值方向决定。该问题分离出从包级监督中同时学习查询向量和值向量的基本挑战。基于该模型，我们在无噪声极限下推导出一族参数化迭代方法，其推广了称为EM-DD算法的方法。然后，我们针对从随机选择的实例中获得的查询和值向量的MLE估计量推导了集中结果。我们对值向量的结果表明，随机初始化的值向量平均而言已经指向正确的方向，因此对于EM算法以高概率在O(1)步内收敛，只需要多项式（关于每包实例数和特征维度）数量的包就足够了。该分析的一个关键方面是经验协方差矩阵的集中性与由选择规则产生的极值统计之间的相互作用。

## Abstract
In multiple instance regression (MIR) data are organized into bags (collections of instances in feature space) and the goal is to learn a mapping that assigns labels to bags. A typical assumption is that there is a so-called concept point in feature space, the proximity to which dictates the bag label. Motivated by modern MIR architectures which are based on attention, we study a softmax model that decouples the concept point and the labeling scheme. The two are respectively determined by a query direction and a value direction value in feature space. This problem isolates a basic challenge of learning both the query and value vectors from bag-level supervision. From this model we derive a parametric family of iterations in the noiseless limit, which generalizes a method known as the EM-DD algorithm. We then derive concentration results for the MLE estimators of the query and value vectors obtained from a random selection of instances. Our result for the value vector shows that a single random initialization of the value vector already points in the correct direction on average, so that a polynomial (in the number of instances per bag and the feature dimension) number of bags is enough for the EM algorithm to converge in $O(1)$ steps with high probability. A key aspect of this analysis is the interplay between concentration of empirical covariance matrices and extremal statistics arising from the selection rule.