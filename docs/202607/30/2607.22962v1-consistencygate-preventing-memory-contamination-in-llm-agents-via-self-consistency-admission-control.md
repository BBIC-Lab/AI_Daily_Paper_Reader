---
title: "ConsistencyGate: Preventing Memory Contamination in LLM Agents via Self-Consistency Admission Control"
title_zh: ConsistencyGate：通过自洽准入控制防止LLM智能体中的记忆污染
authors: "Yan Zhang, Shibo Li"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.22962v1"
tags: ["query:ndai"]
score: 8.0
evidence: 在将事实写入内存前多次查询LLM获取软支持分数
tldr: 针对LLM代理在多轮对话中因幻觉事实写入外部记忆而导致后续推理持续错误（记忆污染）的问题，本文提出了ConsistencyGate写入时准入控制机制，通过多次查询LLM获得软支持分数并设定阈值决定是否接受，无需微调且与模型无关。在构建的两个真实对话污染基准和一个合成基准上，该方法相对于写入一切基线有效降低了污染，尤其对隐式陈述事实效果显著，成本相对集中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22962v1/fig-002.webp\", \"caption\": \"Figure 1: Memory contamination and the ConsistencyGate mitigation. (Left) A baseline agent writes every extracted fact to memory, including hallucinated ones (red), which then propagate into downstream answers. (Right) ConsistencyGate samples a verification score K times before each write; facts with low average support are rejected and memory stays clean. (Bottom) Contamination rate and downstream QA accuracy versus interaction turns on MemContam (Qwen2.5-32B-Instruct).\", \"page\": 2, \"index\": 2, \"width\": 904, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2607.22962v1/fig-004.webp\", \"caption\": \"Table 2: Cross-model generalization of ConsistencyGate (K=5). Qwen2.5-32B and Llama3.3-70B use fixed τ=0.7; Gemma-31B and Llama4-Scout report the adaptive schedule (τmin=0.4, τmax=0.8) used in their respective cross-model runs. ConsistencyGate QA F1 on MemContam is the mean over 3 seeds. ρ: contamination rate (%). Prec/Rec: admission precision/recall. Rec of WriteAll is trivially 1.0. Bold: best per model group.\", \"page\": 10, \"index\": 4, \"width\": 902, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2607.22962v1/fig-001.webp\", \"caption\": \"Table 3: Ablation summary on MemContam: key variants per model. Full results (K ∈ {1, 3, 5, 10}, all τ values) in Table 4. Bold: best per model group.\", \"page\": 11, \"index\": 1, \"width\": 909, \"height\": 858}, {\"url\": \"assets/figures/arxiv/2607.22962v1/fig-005.webp\", \"caption\": \"Table 4: Full ablation study on MemContam across all four backbones. K-ablation rows use Adaptive τ (no-tuning fallback); τ -ablation rows use K=5. Recommended configuration: fixed τ=0.7, K=5. Bold: best per section per model.\", \"page\": 18, \"index\": 5, \"width\": 760, \"height\": 1188}, {\"url\": \"assets/figures/arxiv/2607.22962v1/fig-006.webp\", \"caption\": \"Table 5: Contamination rate ρ (%) of soft scoring (K=5, default) vs. LogProb on MemContam (synthetic, structured) and LoCoMo-Contam (real conversations). Bold: lower contamination per dataset per model. LogProb dominates on MemContam; soft scoring dominates on LoCoMo-Contam.\", \"page\": 19, \"index\": 6, \"width\": 672, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2607.22962v1/fig-003.webp\", \"caption\": \"Table 6: Representative ConsistencyGate admission decisions on LoCoMo-Contam. p̂: consistency score (0–1). τ∗: adaptive threshold. ✓: correct decision; ✗: error.\", \"page\": 22, \"index\": 3, \"width\": 868, \"height\": 527}]"
motivation: 现有记忆管理缺乏写入时的正确性校验，导致幻觉事实持续污染记忆并复合错误。
method: ConsistencyGate在写入前多次查询LLM获取候选事实的支持得分，仅当平均分超过阈值时才接受。
result: 在四个LLM骨干和三个新基准上，相对于写入一切基线，ConsistencyGate在所有基准上均减少了记忆污染。
conclusion: ConsistencyGate提供了一种简单有效的写入时准入控制方法，无需微调，可广泛应用于LLM代理以减少记忆污染。
---

## 摘要
在多轮交互中运行的LLM智能体将事实积累在外部记忆库中，并将其作为下游推理的前提重用。因此，某一步写入的幻觉事实会在后续每一步中作为错误前提持续存在，我们将这种失败模式称为记忆污染。现有的记忆管理方法关注检索和容量，但未考虑写入时的正确性；这一准入问题无法通过基于效用或新近度的标准解决，且未经控制的污染会在长轨迹中不断加剧。我们提出了ConsistencyGate，一种写入时准入门控，在提交从上下文c中提取的候选事实m之前，它会K次查询LLM以获得软支持分数，并仅在平均分超过阈值时准入m。该机制与模型无关，无需微调，且在对延迟敏感的部署中可简化为对数概率变体的单次前向传递。为了衡量在自然数据上的效果，我们通过在LoCoMo和MSC的长期对话中植入受控的单一细节错误，构建了两个真实对话基准（LoCoMo-Contam和MSC-Contam），并以一个结构化合成语料库（MemContam）作为补充，该语料库隔离了近乎预言的上限。在四个LLM骨干网络上，与全部写入的基线相比，ConsistencyGate在每个基准上都减少了污染，其成本主要集中在源上下文中仅隐含陈述的事实上。我们发布了所有三个基准以及门控实现。

## Abstract
LLM agents that operate over many turns accumulate facts in an external memory store and reuse them as premises for downstream reasoning. A hallucinated fact written at one step therefore persists as a false premise for every subsequent step, a failure mode we call memory contamination. Existing memory management addresses retrieval and capacity but not write-time correctness; this admission problem cannot be solved by utility- or recency-based criteria, and uncontrolled contamination compounds across long trajectories. We propose ConsistencyGate, a write-time admission gate that, before committing a candidate fact m extracted from context c, queries the LLM K times for a soft support score and admits m only when the average exceeds a threshold. The mechanism is model-agnostic, requires no fine-tuning, and reduces to a single forward pass in a log-probability variant for latency-sensitive deployments. To measure the effect on natural data, we construct two real-conversation benchmarks (LoCoMo-Contam and MSC-Contam) by planting controlled single-detail corruptions in long-term conversations from LoCoMo and MSC, and complement them with a structured synthetic corpus (MemContam) that isolates a near-oracle upper bound. Across four LLM backbones, ConsistencyGate reduces contamination on every benchmark relative to a write-everything baseline, with the cost concentrated on facts that are stated only implicitly in the source context. We release all three benchmarks together with the gate implementation.