---
title: Variable Importance Identification Through Lazy Training for Binary Classification
title_zh: 通过惰性训练进行二分类变量重要性识别
authors: "Anand Singh, Luke Pennella, Eshan Kabir, Xiaoxi Shen"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.22979v1"
tags: ["query:ndai"]
score: 9.0
evidence: 懒训练识别二分类中重要特征
tldr: 本文针对二分类问题，提出一种结合惰性训练与变量重要性框架的高效特征重要性识别算法。该方法假设条件最少，错误率可控，并通过模拟与真实数据验证。为深度学习可解释性在二分类场景的应用提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22979v1/fig-002.webp\", \"caption\": \"Table 1: Empirical rejection rates for LazyVI, Logistic Regression, and DNN-Inference under linear and nonlinear signal settings.\", \"page\": 21, \"index\": 2, \"width\": 970, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2607.22979v1/fig-003.webp\", \"caption\": \"Figure 2: Log-log plot of the sorted NTK eigenvalues. The spectrum decays faster than the red reference line with slope −1, supporting the regularity condition α > 1.\", \"page\": 22, \"index\": 3, \"width\": 786, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2607.22979v1/fig-004.webp\", \"caption\": \"Figure 3: Representative samples of digits ‘8’ and ‘9’ from the test set. The overlay illustrates the 7× 7 pixel grid boundaries used for the fine-grained analysis. Regions are labeled from 1 to 16.\", \"page\": 23, \"index\": 4, \"width\": 686, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2607.22979v1/fig-006.webp\", \"caption\": \"Figure 4: Heatmaps of Variable Importance (VI) across three granularity levels. Columns compare the estimation methods: Lazy Training, Dropout, and Retraining. Warmer colors indicate higher importance.\", \"page\": 24, \"index\": 6, \"width\": 882, \"height\": 729}, {\"url\": \"assets/figures/arxiv/2607.22979v1/fig-007.webp\", \"caption\": \"Figure 5: Heatmaps of Lazy Training p-values across three levels of granularity. The p-values are multiplied by 2, 4, or 16 for the Top vs Bottom, Quadrant, and 16-Region respectively for the Bonferroni correction with values capped at 1, then are transformed using − log10(max{0.0001, p}) to enhance visual contrast and highlight significance. A score at approximately 1.3 and above corresponds to p ≤ .05.\", \"page\": 24, \"index\": 7, \"width\": 979, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2607.22979v1/fig-001.webp\", \"caption\": \"Table 2: Runtime Comparisons For Training Methods\", \"page\": 26, \"index\": 1, \"width\": 882, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2607.22979v1/fig-005.webp\", \"caption\": \"Table 3: The top 10 significant genes detected from the logistic regression and LazyVI for choosing the regularization parameter λ and hyperparameters in the L-BFGS algorithm as described in the simulation studies\", \"page\": 27, \"index\": 5, \"width\": 382, \"height\": 339}]"
motivation: 深度神经网络可解释性研究多聚焦于回归，二分类框架下的变量重要性识别方法有待发展。
method: 采用变量重要性框架，融入惰性训练思想，设计高效特征重要性识别算法。
result: 理论证明错误率可控，模拟和真实数据实验均验证了算法的有效性。
conclusion: 该方法为二分类任务提供了一种理论简洁且可靠的特征重要性识别方案。
---

## 摘要
深度神经网络已广泛应用于许多领域（例如计算机视觉和自然语言处理）；然而，理解其可解释性仍然是一项具有挑战性的任务。近期，大量研究致力于提升深度神经网络的可解释性，其中大部分工作集中于回归框架。在本文中，我们转而关注二分类框架，并采用变量重要性框架结合惰性训练的思想，提出了一种用于识别重要特征的高效算法。从理论角度来看，我们的方法仅依赖于最少的假设，并实现了良好控制的错误率。通过广泛的模拟研究和实际数据应用，验证了所提方法和算法的有效性。

## Abstract
Deep neural networks have been widely used in many applications (e.g., computer vision and natural language processing); however, understanding their explainability remains a challenging task. Recently, substantial research has been devoted to improving the explainability of deep neural networks, with most of this work focusing on the regression framework. In this paper, we instead focus on the binary classification framework and adopt a variable-importance framework combined with the idea of lazy training to propose an efficient algorithm for identifying important features. From a theoretical perspective, our method relies on only a minimal set of assumptions and achieves well-controlled error rates. The validity of the proposed method and algorithm is examined through extensive simulation studies and real-data applications.

---

## 论文详细总结（自动生成）

好的，基于您提供的论文内容，以下是结构化的详细中文总结。

### 1. 论文的核心问题与整体含义

*   **研究问题**：本文致力于解决二分类任务中，如何高效且可靠地识别对预测结果有重要影响的输入特征（即变量重要性），以提升深度神经网络的可解释性。
*   **研究背景与动机**：
    *   深度神经网络虽性能强大，但其“黑箱”特性使得理解和解释其决策过程变得困难。
    *   现有的大多数特征重要性研究方法（如假设检验、模型无关方法）主要关注回归问题，缺乏针对二分类场景的严谨框架。
    *   现有的基于重新训练的方法计算成本高昂，而基于惰性训练（`Lazy Training`）的方法在二分类问题上的理论保证尚不完善（例如，误差率仅为 $O_p(n^{-1/2})$）。

### 2. 论文提出的方法论

*   **核心思想**：
    *   将**变量重要性框架**与**惰性训练**思想相结合。惰性训练的核心是利用神经网络在参数初始化或拟合值附近的一阶泰勒展开（线性近似）来避免完整重新训练网络。
    *   具体地，当评估在去掉某组特征 $X_S$ 后的模型表现时，不重新训练整个深度神经网络，而是在已拟合的全模型参数 $\theta_f$ 处，利用神经正切核（`NTK`）对模型进行线性化，然后仅训练一个岭回归（`Ridge Regression`）逻辑斯蒂模型。

*   **关键技术细节与流程**：
    1.  **全模型拟合**：首先使用所有特征 $X$ 训练一个深度 `ReLU` 神经网络 $h_{\theta_f}$，最小化二元交叉熵损失，得到参数 $\theta_f$。
    2.  **构造缩减特征** $X_{-S}$：将目标特征子集 $S$ 内的变量替换为其样本均值，得到缩减后的输入 $X_{-S}$。
    3.  **惰性更新（Lazy Update）**：为评估特征子集 $S$ 的重要性，需求解如下优化问题以得到参数变化量 $\Delta\theta_S$，而非重新训练网络：
        $$\Delta\theta_S = \arg\min_{w} \frac{1}{n} \sum_{i=1}^{n} \ell\left(h_{\theta_f}(X_{i,-S}) + [\nabla_\theta h_{\theta_f}(X_{i,-S})]^\top w, \; Y_i\right) + \frac{\lambda}{2} \|w\|^2$$
        其中 $\ell$ 是逻辑斯蒂损失，$\lambda$ 是正则化参数。该问题等价于一个以 `NTK` 为特征的核逻辑斯蒂回归，可通过高效的牛顿-拉夫逊（`Newton-Raphson`）算法求解。
    4.  **变量重要性度量**：定义特征组 $S$ 的重要性为 $V(h_{\theta_f}, P_n) - V(\tilde{h}_{\theta_f+\Delta\theta_S}, P_n)$，其中 $V(f, P)$ 是关于分布 $P$ 的负对数似然期望，$\tilde{h}$ 是惰性更新后的线性化模型。重要性估计量 $\hat{\psi}_{n,S}$ 在零假设下被证明是渐近正态的。
    5.  **理论贡献**：
        *   **改进误差率**：将检验统计量的误差率从 $O_p(n^{-1/2})$ 提升至 $o_p(n^{-1/2})$，满足了变量重要性框架的有效性条件。
        *   **减少假设**：理论上仅需极少假设，主要包括神经网络参数数量 $W$ 的增长速率和 `NTK` 矩阵的特征值衰减速率（$\mu_j \le c j^{-\alpha}, \alpha > 1$）。

### 3. 实验设计

*   **模拟实验**：
    *   **数据集**：人工生成数据，包含 10 个独立的标准正态分布特征变量，样本量为 $n=5,000$。二元响应变量 $Y$ 由前 9 个特征通过一个**线性函数**或**非线性函数**生成，第 10 个变量为噪声特征。
    *   **对比方法**：
        *   `LazyVI`（本文方法）。
        *   标准的逻辑斯蒂回归。
        *   `dnn-inference`：一种基于影响函数和渐近近似的深度神经网络推断方法。
    *   **评估指标**：对于真正的重要特征评估**经验功效（Empirical Power）**，对于噪声特征评估**第一类错误率（Type I Error）**。
*   **真实数据实验**：
    *   **数据集1（MNIST）**：二分类任务，区分手写数字‘8’和‘9’（约11,800训练，1983测试）。将 $28 \times 28$ 的图像按不同粒度（半区、象限、$7\times7$ 网格）划分为区域，以区域为特征组评估其重要性。
        *   **对比方法**：`Dropout`（均值填充基线）、`Retraining`（重新训练基线）和 `LazyVI`。
    *   **数据集2（ADNI基因表达数据）**：识别与阿尔茨海默病显著相关的基因。数据集包含 521 个个体和 15,837 个基因表达量。
        *   **对比方法**：标准逻辑斯蒂回归。
*   **`NTK`假设验证**：通过绘制非线性模拟数据下`NTK`特征值的对数-对数图，验证了其衰减速率 $\alpha > 1$ 的假设。

### 4. 资源与算力

*   **文中提及的算力**：论文正文中**未明确说明**所使用的硬件配置，如 GPU 型号、数量等。
*   **训练时长**：
    *   论文在表2中报告了`LazyVI`和`Retraining`等方法的运行时间（以秒为单位）。例如，在 MNIST 数据集的 $7\times7$ 区域实验中，`LazyVI` 的最终拟合时间大约是 `Retrain` 时间的 $1/3$ 到 $1/2$。
    *   与 `dnn-inference` 对比：在模拟实验中，`LazyVI` 在线性和非线性设置下分别耗时约 21,534 秒和 33,766 秒，而 `dnn-inference` 分别耗时 83,324 秒和 166,551 秒。
    *   文末指出，对于超大规模数据集（如 `UK Biobank`），由于需要构建和求逆 $n \times n$ 的 `NTK` 矩阵，当前方法的计算和存储成本会成为瓶颈，需要开发更可扩展的算法。

### 5. 实验数量与充分性

*   **实验数量**：
    1.  **2 组模拟实验**：线性信号和非线性信号场景。
    2.  **2 组真实数据实验**：MNIST 图像分类和 ADNI 基因关联研究。
    3.  **1 组假设验证实验**：`NTK` 特征值衰减行为。
    4.  在 MNIST 实验中，包含了 **3 种**不同的图像分区粒度。
    5.  实验中包含了**消融/对比维度**，将 `LazyVI` 与逻辑斯蒂回归、`dnn-inference`、`Dropout` 和 `Retraining` 等多种方法进行对比。
*   **充分性与客观性**：
    *   **充分性较好**：实验覆盖了模拟和真实数据、线性和非线性关系、有明确答案的模拟检验以及探索性的真实应用等多种场景，验证角度较为全面。
    *   **客观性与公平性**：与多种基准方法进行了比较，特别是与强相关的 `dnn-inference` 和理论上最准确的 `Retraining` 方法进行对比，评估维度包括统计性能（功效/错误率）、计算效率和估计值的准确性。
    *   **潜在不足**：虽然与 `dnn-inference` 进行了对比，但未与近年提出的其他基于神经网络或置换检验的方法进行更广泛的比较。此外，结果报告较为简略，缺乏多次重复实验的误差条（如标准差）。

### 6. 论文的主要结论与发现

*   **理论结论**：
    *   提出了一个适用于二分类问题的惰性变量重要性框架，并严格证明了所提议检验统计量的渐近正态性。
    *   理论分析实现了 $o_p(n^{-1/2})$ 的误差率，优于先前方法。该体系仅依赖少数几个温和的假设。
*   **实验结果**：
    *   **统计表现佳**：在模拟实验中，`LazyVI` 在所有重要变量上都能保持很高的经验功效（多数接近 1），同时能很好地控制第一类错误（接近名义水平 0.05），尤其在线性或非线性信号下均优于 `dnn-inference`。
    *   **计算更高效**：与重新训练（`Retraining`）或 `dnn-inference` 方法相比，`LazyVI` 显著减少了计算时间。
    *   **估计更准确**：与简单的 `Dropout` 方法相比，`LazyVI` 不易高估变量的重要性，其估计值与“金标准”重新训练方法的结果更为接近。
    *   **真实应用有效**：在 MNIST 实验中，它成功识别出区分‘8’和‘9’的关键图像区域（中心底部）。在 ADNI 数据中，它识别出与阿尔茨海默病病理相关的生物学相关基因（如 `MT1H`, `MAPK11`）。

### 7. 优点

*   **理论严谨**：为二分类场景下的变量重要性推断提供了坚实的理论基础，改进了既有理论结果（如误差率），且所需假设较少。
*   **计算高效**：利用惰性训练避免重复训练完整神经网络，将问题转化为核逻辑斯蒂回归，大幅提升了计算效率。
*   **框架通用**：方法基于似然函数构建，可自然扩展至其他广义线性模型的响应变量类型（如计数数据）。
*   **实验设计合理**：通过模拟实验系统地评估了第一类错误和功效，并在真实数据上将 `LazyVI` 与简单均值填充、重新训练等不同复杂度的方法进行对比，清晰展示了其在准确性与效率间的平衡。

### 8. 不足与局限

*   **架构假设限制**：方法目前主要针对全连接网络（Fully Connected Networks），尚未拓展到卷积神经网络（`CNN`）、`LSTM`、`Transformer` 等更复杂的架构。
*   **参数规模限制**：理论要求网络参数数量 $W$ 的增长不能快于样本量 $n$（即 $W = o(\sqrt{n} / \log n)$），这限制了其在目前主流的过参数化大模型上的应用。
*   **计算可扩展性瓶颈**：虽然比重新训练快，但方法的核心是构建并求逆 $n \times n$ 的 `NTK` 矩阵，导致其内存和计算量随样本量 $n$ 二次方或立方增长，难以直接应用于百万级样本的数据集。
*   **实验覆盖度**：
    *   论文仅在 MNIST 数据集上展示了该框架，且未与其他诸如深度 `SHAP`、`Integrated Gradients` 等主流的深度学习解释方法进行对比。
    *   `NTK` 特征值衰减的理论假设仅在模拟数据上进行了验证，未能在真实数据上提供充分的经验证据。
    *   所有实验结果的报告均缺乏标准差或置信区间，难以评估结果的稳定性。

（完）
