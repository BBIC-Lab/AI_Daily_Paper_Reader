---
title: Rethinking Layer-Wise Information Allocation for Vision Foundation Model Adaptation
title_zh: 重新思考视觉基础模型适配中的逐层信息分配
authors: "Yuqi Li, Xi Xiao, Yunbei Zhang, Lin Zhao, Yu Li, Aiden Zhao, Tianyang Wang, Hao Xu, Yingli Tian"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.21973v1"
tags: ["query:ndai"]
score: 9.0
evidence: 通过提示适配利用预训练视觉模型处理下游任务
tldr: "视觉基础模型参数高效适应面临层间信息分配不明的问题，现有提示方法缺乏原则性控制。本文受信息瓶颈启发，提出提示信息瓶颈框架，通过正则化层间压缩与充分性权衡，实现最小且充分的适应。实验在34个数据集上以0.35%可调参数取得领先性能，并增强了鲁棒性和泛化能力，为冻结模型适应提供了信息分配新视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21973v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of our proposed Prompted Information Bottleneck (PIB). A: prompt-based adaptation inserts trainable prompts into a frozen vision backbone without explicitly regulating what information should be preserved or suppressed, which may lead to redundant detail propagation, unstable cross-layer refinement, and early weakening of useful semantic cues. B: PIB introduces an IB-inspired layer-wise objective through complementary compression and sufficiency surrogates, while enforcing a minimal-sufficient path across depth from local evidence retention to semantic discrimination. C: PIB yields smoother redundancy reduction, stronger class separability, and a more ordered cross-layer path.\", \"page\": 4, \"index\": 5, \"width\": 1056, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2607.21973v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of fine-tuning methods with ViT-Base/16 backbone. Bold and underlined indicate best and second-best results. We report mean accuracy across datasets for each benchmark (FGVC: 5 datasets, HTA: 10 datasets, VTAB-1k: 19 datasets), for each VTAB-1k category (Natural, Specialized, and Structured). Please see Appendix for per-task results. Same for Table 2.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2607.21973v1/fig-002.webp\", \"caption\": \"Table 2: Comparison of the fine-tuning methods under different pretraining paradigms. We report VTAB-1k [82] accuracy using ViT-Base [14] as the frozen backbone, pretrained with MAE [20] and MoCo v3 [8] use the same settings.\", \"page\": 6, \"index\": 2, \"width\": 864, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2607.21973v1/fig-003.webp\", \"caption\": \"Figure 3: Average attention logits across transformer depth. We compare the layer-wise attention patterns of VPT and PIB on ViT-Base/16 over 256 VTAB-1k samples. VPT exhibits more fragmented and less stable attention evolution across depth, especially in middle and late layers. In contrast, PIB yields a smoother and more structured attention pattern, indicating a more coherent layer-wise refinement process.\", \"page\": 7, \"index\": 3, \"width\": 1056, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2607.21973v1/fig-004.webp\", \"caption\": \"Figure 5: Qualitative visualization of prompt attention. Comparedwith VPT and ViaPT, ourmethod producesmore concentrated and semantically aligned responses on the foreground object, while reducing scattered activation on background regions. This pattern is consistent across both vehicle and bird categories, suggesting that PIB encourages prompt-conditioned representations to preserve task-relevant evidence and suppress redundant or nuisance-heavy information.\", \"page\": 8, \"index\": 4, \"width\": 1056, \"height\": 313}]"
motivation: 现有提示适应方法缺乏对层间信息保留与丢弃的原则性控制，导致性能敏感且泛化不足。
method: 基于信息瓶颈原理，设计提示信息瓶颈，通过正则化层间压缩-充分性权衡，引导早期层保留任务相关证据、深层丢弃冗余。
result: "在34个数据集上，PIB以平均0.35%参数达到FGVC 92.1%、HTA 93.01%、VTAB-1k 77.33%准确率，并改善鲁棒性。"
conclusion: PIB不仅高效适应冻结模型，还从信息分配视角解释了提示容量缩放行为，减少了捷径依赖。
---

## 摘要
视觉基础模型正越来越多地被重用为下游视觉识别的冻结骨干网络，使得参数高效适配成为一个核心问题。基于提示的适配方法，包括视觉提示微调（VPT），为这些模型的专用化提供了一种轻量级途径，但其逐层行为仍未被充分理解：性能对提示深度、位置和任务分布敏感，且标准域内基准上的增益并不总能转化为稳健的泛化。我们认为这一局限性不仅仅是优化问题，更是一个逐层信息分配问题：现有的基于提示的方法缺乏对提示条件化表示应保留、抑制和跨深度传播的内容的原则性控制。受信息瓶颈原理启发，我们提出了提示信息瓶颈（PIB）框架，该框架正则化了逐层的压缩-充分性权衡，并促进了更连贯的跨层信息路径。其关键思想在于，有效的适配应当最小化但足够充分，在浅层保留与任务相关的局部证据，同时在深层逐步丢弃干扰因素和冗余细节。大量实验表明，PIB 在 34 个数据集上取得了优异性能，在 FGVC 上达到 92.1%，HTA 上达到 93.01%，VTAB-1k 上达到 77.33%，而在主要设置中平均仅调整 0.35% 的参数。超越基准准确率，PIB 有助于解释提示容量扩展的非单调行为，减少捷径依赖，并在分布偏移和细粒度识别场景下提升鲁棒性。这些成果使 PIB 既是一种实用方法，也为适配冻结视觉基础模型提供了一种信息分配的视角。我们的代码可在 https://github.com/itsnotacie/MM-26-PIB 获取。

## Abstract
Vision foundation models are increasingly reused as frozen backbones for downstream visual recognition, making parameter-efficient adaptation a central problem. Prompt-based adaptation, including Visual Prompt Tuning (VPT), provides a lightweight way to specialize these models, but its layer-wise behavior remains poorly understood: performance is sensitive to prompt depth, placement, and task distribution, and gains on standard in-domain benchmarks do not always translate into robust generalization. We argue that this limitation is not solely an optimization issue, but a layer-wise information allocation issue: existing prompt-based methods lack principled control over what prompt-conditioned representations should preserve, suppress, and propagate across depth. Inspired by the Information Bottleneck principle, we introduce Prompted Information Bottlenecks (PIB), a framework that regularizes layer-wise compression-sufficiency trade-offs and promotes a more coherent cross-layer information path. The key idea is that effective adaptation should be minimal yet sufficient, retaining task-relevant local evidence in earlier layers while progressively discarding nuisance factors and redundant details in deeper layers. Extensive experiments show that PIB achieves strong performance across 34 datasets, reaching 92.1% on FGVC, 93.01% on HTA, and 77.33% on VTAB-1k, while tuning only 0.35% parameters on average across the main settings. Beyond benchmark accuracy, PIB helps explain the non-monotonic behavior of prompt capacity scaling, reduces shortcut reliance, and improves robustness under distribution shift and fine-grained recognition settings. These results position PIB as both a practical method and an information-allocation perspective for adapting frozen vision foundation models. Our code is available at https://github.com/itsnotacie/MM-26-PIB

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是对该论文的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究背景**：视觉基础模型（VFMs，特别是ViT）常被用作冻结的特征提取器，以适应下游任务。为降低计算和存储开销，参数高效微调（PEFT）成为主流，其中提示微调（Prompt Tuning）通过插入少量可学习的“提示”令牌来适配模型，代表方法如VPT。
*   **核心问题**：现有提示微调方法本质上仍依赖经验设计，缺乏一个核心问题的原则性理解——**提示条件化后的中间表示，在模型的每一层究竟应该保留、抑制和传递何种信息？** 这导致了一系列难以解释的现象：
    *   **非单调缩放**：简单地增加提示深度或长度并不总能可靠提升性能。
    *   **不稳定的泛化能力**：在标准域内基准上的提升，无法稳定地转化为在细粒度识别、低样本迁移或分布外偏移下的鲁棒性。
*   **整体含义**：论文提出，提示微调的局限性不仅是优化问题，更是一个 **“逐层信息分配”** 的失败。缺乏原则性的控制，导致提示可能过度保留非必要的干扰因素，或过早丢弃与任务相关的局部证据。因此，论文旨在从根本上重新思考提示微调，并为其提供一个信息论的解决框架。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

论文提出了**提示信息瓶颈（Prompted Information Bottleneck, PIB）**框架，其核心思想是将提示微调视为一个跨层的、最小但充分的（minimal-yet-sufficient）信息处理过程。

*   **核心思想**：借鉴信息瓶颈（IB）原理，有效的提示适配应引导模型的中间表示 $H_l$ 形成一个连贯的跨层路径：**浅层保留与任务相关的局部证据（充分性），而深层则逐步压缩和丢弃无关的干扰因素及冗余信息（压缩性）**。

*   **关键技术组件与损失函数**：
    *   **理想化 IB 目标**：对于每一层 $l$，理想目标是 $\min_{\{P_l\}} \sum_{l=1}^{L} \left[ I(H_l; X) - \beta_l I(H_l; Y) \right]$，即最小化表示 $H_l$ 与输入 $X$ 的互信息（压缩），同时最大化其与标签 $Y$ 的互信息（保持充分性）。
    *   **可操作的代理损失（$L_{PIB}$）**：由于直接计算互信息难以实现，PIB 使用以下两个可优化的代理项，构成层级的压缩-充分性权衡：
        *   **压缩代理项（$L_{comp}$）**：通过计算层表示矩阵的归一化相关性矩阵 $C_l$ 的**非对角线能量** $||C_l - I_d||^2_{F, off}$，来抑制特征的冗余和有害信息传播。该项鼓励层表示的不同维度之间去相关，达到压缩效果。
        *   **充分性代理项（$L_{suff}$）**：通过最小化**类内散度与类间散度的比值** $\frac{S^{(l)}_{intra}}{S^{(l)}_{inter} + \epsilon}$，鼓励每层表示在压缩的同时保持对任务的判别能力。
    *   **跨层最小-充分路径（$L_{path}$）**：为强制提示在不同层之间形成连贯的提炼路径（避免信息质量在跨层时出现突变），引入路径正则化项。它惩罚相邻层之间“质量分数” $q_l$ 的上升（因为分数越低越好），以鼓励逐层平滑的改进。
    *   **可选的门控路由（PIB Route）**：引入可学习的门控参数 $r_l \in [0,1]$，允许不同层自适应地调整压缩项和充分性项的相对权重，以提供更精细的层级控制。

*   **最终目标函数**：$L_{total} = L_{task} + L_{PIB} + \eta L_{path}$。所有组件均在冻结的骨干网络上训练，仅更新提示参数、任务头以及可选的路由参数。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

*   **数据集与基准测试（Benchmarks）**：
    *   **FGVC**：包含5个细粒度分类数据集（如 CUB-200, Stanford Cars），评估对细微差别的辨别能力。
    *   **HTA**：包含10个异构的视觉分类数据集（如 DTD纹理, Food-101, SVHN），评估跨不同领域的迁移鲁棒性。
    *   **VTAB-1k**：包含19个任务，分为 **Natural, Specialized, Structured** 三类，每种任务仅提供1000个训练样本，用于评估低资源下的迁移和泛化能力。这是主要的测试平台。

*   **骨干网络**：
    *   **ViT-Base/16** (监督预训练)。
    *   **ViT-Base** (使用 MAE 和 MoCo v3 自监督预训练)。
    *   **Swin-Base** (ImageNet-21k 预训练)，测试在层级式Transformer架构上的兼容性。

*   **对比方法**：论文与大量参数高效微调方法进行了全面比较，包括：
    *   **传统微调**：Full fine-tuning, Linear probing。
    *   **适配器/旁路方法**：Adapter, LoRA, AdaptFormer，Bias-tuning。
    *   **提示微调方法**：VPT-Shallow/Deep, E²VPT, EXPRES, DAM-VP, SA²VP, SPT, LoR-VP，VFPT, ViaPT 等。

### 4. 资源与算力

*   论文中提到所有实验均在 **NVIDIA GPU** 上完成，使用 **PyTorch** 框架和**混合精度训练**以加速。
*   论文**未明确说明**具体使用的 GPU 型号、数量以及详细的训练时长。文中的表述强调了该方法因其仅训练极少参数而带来的“轻度”计算开销，但缺乏定量的算力消耗数据（如 GPU 小时）。

### 5. 实验数量与充分性

*   **实验数量庞大**：实验覆盖了**3个主流基准测试（>30个数据集）**，在**4种不同的预训练骨架**上进行了验证，并与近20种 SOTA 方法进行了对比，构成了非常全面的实验主体。
*   **充分的消融研究**：
    *   **组件消融**：详细剥离了压缩、充分性、路径正则化、路由等每个组件，证明了它们各自及组合的增益。
    *   **设计消融**：独立分析了路径正则化和层级感知加权的作用。
    *   **缩放定律分析**：分析了提示深度和长度在 PIB 与 VPT 上的不同缩放行为，解释了非单调现象。
    *   **鲁棒性分析**：专门评估了模型对捷径线索的依赖，以及在输入损坏、领域偏移和少样本场景下的鲁棒性。
*   **深入的可解释性分析**：论文不仅报告准确率，还通过信息轨迹、冗余度、类可分性、注意力熵、注意力图可视化等手段，对模型内部表征进行了深层诊断，有力地支撑了其核心论点。
*   **公平性**：所有对比方法均遵循相同的冻结骨干网评估协议，且超参数搜索预算有限，确保了公平比较。

### 6. 论文的主要结论与发现

*   **性能优越性**：PIB 在多个基准测试和骨干网络上一致地取得了最优或次优的性能，同时在主流设置下仅微调**0.35%** 的参数，实现了极致的参数效率。
*   **核心失败模式被验证**：论文证明了提示微调的瓶颈确实来自无控制的层间信息分配，PIB 通过正则化该过程，能学习到更连贯的、从局部证据保留到语义抽象的最少-充分信息路径。
*   **提升稳健性与可解释性**：PIB 显著减少了模型对背景、纹理等捷径特征的依赖，在分布外和少样本场景下鲁棒性更强。同时，PIB 的注意力图和诊断曲线表明，其内部表征比 VPT 演变得更稳定、更具结构性。

### 7. 优点：方法或实验设计上有哪些亮点

*   **问题视角新颖**：从“如何设计更好的提示结构”转向“提示应该引导怎样的信息流”，这是一个更高层次的原则性视角。
*   **方法论优雅实用**：巧妙地将信息瓶颈原理转化为可优化的压缩/充分性代理损失，不引入额外的大型可训练模块（如适配器网络），计算开销主要在训练时的统计量计算上，推理时则与原提示微调相同，实现了“训练时正则化，推理时零负担”。
*   **实验论证极其详尽**：实验设计不仅关注最终的准确率数字，更注重通过消融、缩放行为、鲁棒性测试和深入的可解释性分析来系统性地验证其核心论点，论证链条非常完整且有力。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **代理项的近似性**：压缩（相关性去冗余）和充分性（类间可分性）的代理项不完全等效于互信息 $I(H_l;X/Y)$，论文也坦诚指出了这一点，框架本质上是信息论启发的启发式方法。
*   **任务范围局限**：所有评估均局限于**分类任务**。如何将其信息分配思想扩展到目标检测、分割、图像生成或视觉语言检索等更复杂的任务，文中未讨论，其充分性的定义可能需要根本性修改。
*   **对超参数的潜在依赖性**：虽然论文设计了轻量级的配置，但核心的权衡系数（$\lambda_l$, $\gamma_l$, $\eta$）仍然需要设置。特别是在非分类任务或全新领域上，寻找合适的平衡点可能是一个挑战。
*   **未明确算力成本**：论文没有提供具体的算力统计数据（如GPU型号、训练时长），这使得评估其从研发到落地的时间成本变得困难。

（完）
