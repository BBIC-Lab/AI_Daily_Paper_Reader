---
title: A Step Towards Robust Unsupervised Domain Adaptation via Fine-Tuning and Reinforcement Learning
title_zh: 通过微调和强化学习迈向鲁棒的无监督域自适应
authors: "Sushant Dagaji Desale, Rahul Mishra, Ashutosh Kumar Sinha"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03600v1"
tags: ["query:ndai"]
score: 8.0
evidence: 使用CLIP微调和强化学习的无监督域适应
tldr: "无监督域适应的对抗鲁棒性因噪声伪标签和域间分布偏移面临挑战。本文提出SFT+RL框架，先对CLIP模型进行监督对抗微调，再通过强化学习阶段的置信度引导伪标签策略逐步标注目标域样本，结合混合对抗训练提升跨域鲁棒性。在三个数据集上取得平均10.2%的干净准确率和15.8%的对抗鲁棒性提升，优于现有方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03600v1/fig-001.webp\", \"caption\": \"Figure 1: Adversarial domain alignment challenge: Clean source samples (blue), clean target samples (green), and adversarial target samples (red) tend to form indistinguishable clusters in CLIPs embedding space.\", \"page\": 1, \"index\": 1, \"width\": 535, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2607.03600v1/fig-003.webp\", \"caption\": \"Figure 2: Schematic representation of the SFT+RL framework: (a) In this stage, the model is fine-tuned by updating only the CLIP Vision Projection (CVP) layer (partially trainable) and the Linear Classifier (fully trainable), while keeping the Image Encoder (IE), Text Encoder (TE) and CLIP Text Projection (CTP) layer completely frozen. (b) This stage builds on the fine-tuned model to perform adversarial training on the target domain. It utilizes Clean Labels from the source domain and Pseudo Labels (PL) from the target domain. During this stage, both the CVP and Linear Classifier are fully trainable, while the IE, TE and CTP remain frozen. (BP: Backpropagation, SFT: Supervised Fine Tuning, CL: Clean, PGD: Projected Gradient Descent, ADV: Adversarial)\", \"page\": 3, \"index\": 3, \"width\": 829, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.03600v1/fig-004.webp\", \"caption\": \"Table 1: Comparison on PGD 20 attack at ϵ = 2/255 using PACS dataset. Avg accuracy calculated using tables (a) and (b). (a)\", \"page\": 5, \"index\": 4, \"width\": 987, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2607.03600v1/fig-002.webp\", \"caption\": \"Table 2: Comparison on PGD 20 attack at ϵ = 2/255 using OfficeHome. Avg accuracy calculated using tables (a) and (b). (a)\", \"page\": 6, \"index\": 2, \"width\": 985, \"height\": 427}]"
motivation: 解决无监督域适应中伪标签噪声和对抗攻击导致鲁棒性与准确性难以平衡的问题。
method: 提出两阶段框架，第一阶段在源域上用PGD扰动微调CLIP的分类器，第二阶段通过强化学习进行置信度引导的伪标签生成与筛选，并对混合数据做对抗训练。
result: "在OfficeHome、PACS和VisDA数据集上，干净准确率平均提升10.2%，对抗鲁棒性平均提升15.8%。"
conclusion: SFT+RL框架有效兼顾了干净准确率和对抗鲁棒性，通过结合微调与强化学习，显著优于现有方法。
---

## 摘要
无监督域自适应（UDA）中的对抗鲁棒性仍然是一个重大挑战，原因在于噪声伪标签以及干净源域与受到对抗扰动的目标域之间固有的分布偏移。现有方法通常无法在鲁棒性和准确性之间实现最佳权衡，因为由域自适应模型生成的伪标签在对抗攻击下往往会引入分类错误。在这项工作中，我们提出了\textbf{SFT+RL}，一个两阶段的鲁棒UDA框架，它在CLIP预训练视觉编码器的基础上集成了监督微调（SFT）和强化学习（RL）。在SFT阶段，我们利用基于PGD的扰动对标记的源域进行对抗微调线性分类器，同时部分解冻CLIP的投影层。这允许适应对抗噪声，同时保留CLIP丰富的语义先验。我们在RL阶段引入了一种置信度引导的伪标签策略，以逐步为无标签的目标样本打标签。使用衰减的置信度阈值过滤伪标签，以平衡质量与覆盖率，并在由干净源样本与高置信度目标样本组合而成的复合数据集上训练模型。对抗训练应用于混合批次的干净样本和对抗样本，以增强跨域鲁棒性。在三个基准数据集OfficeHome~\cite{tomm-ude}、PACS~\cite{pacs}和VisDA~\cite{visda}上的全面评估证明了我们方法的有效性。值得注意的是，\textbf{SFT+RL}在所有三个数据集上平均提升了\textbf{10.2\%}的干净准确率和\textbf{15.8\%}的对抗鲁棒性，优于现有的最先进方法。

## Abstract
Adversarial robustness in Unsupervised Domain Adaptation (UDA) remains a significant challenge due to noisy pseudo labels and inherent distributional shifts between the clean source and adversarially perturbed target domains. Existing approaches often fail to achieve an optimal trade-off between robustness and accuracy, as pseudo-labels generated by domain-adapted models tend to introduce classification errors under adversarial attacks. In this work, we propose \textbf{SFT+RL}, a two-stage robust UDA framework that integrates Supervised Fine Tuning (SFT) and Reinforcement Learning (RL) on top of CLIP's pre-trained visual encoder. In the SFT stage, we adversarially fine-tune a linear classifier using PGD-based perturbations over the labelled source domain while partially unfreezing CLIP's projection layer. It allows adaptation to adversarial noise while preserving CLIP's rich semantic priors. We introduce a confidence-guided pseudo-labeling strategy in the RL stage to annotate unlabeled target samples progressively. Pseudo labels are filtered using a decaying confidence threshold to balance quality and coverage, and the model is trained on a composite dataset formed by combining clean source samples with high-confidence target samples. Adversarial training is applied to mixed batches of clean and adversarial examples to enhance cross-domain robustness. Comprehensive evaluations on three benchmark datasets OfficeHome~\cite{tomm-ude}, PACS~\cite{pacs}, and VisDA~\cite{visda} demonstrate the effectiveness of our approach. Notably, \textbf{SFT+RL} achieves average improvements of \textbf{10.2\%} in clean accuracy and \textbf{15.8\%} in adversarial robustness across all three datasets, outperforming existing state-of-the-art methods.