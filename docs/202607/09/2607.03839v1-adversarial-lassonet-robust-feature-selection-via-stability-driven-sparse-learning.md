---
title: "Adversarial LassoNet: Robust Feature Selection via Stability-Driven Sparse Learning"
title_zh: 对抗LassoNet：通过稳定性驱动的稀疏学习实现鲁棒特征选择
authors: "Zhen Huang, Peicheng Xu, Junbiao Pang, Yulong Zheng"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03839v1"
tags: ["query:ndai"]
score: 9.0
evidence: 鲁棒特征选择计算重要性权重
tldr: "传统L1正则化稀疏特征选择在噪声下不稳定，影响泛化。对抗训练可提升鲁棒性，但其与分层稀疏选择的结合研究不足。本文提出AdLNet，集成对抗扰动与LassoNet分层稀疏，并推导一阶对抗近似和谱分析。实验表明，AdLNet提升分布外鲁棒性4.4%，特征重复性6.3%，肺癌数据准确率提升5.3%。"
source: arxiv
selection_source: fresh_fetch
motivation: 传统L1稀疏选择对噪声敏感，且对抗训练与分层稀疏结合的问题未被探索。
method: 提出Adversarial LassoNet，将输入空间对抗扰动融合进LassoNet的分层稀疏学习，并给出理论分析。
result: "在ColoredMNIST上将分布外鲁棒性提升4.4%、特征支持可重复性提高6.3%，在肺癌数据上准确率提升5.3%、AUC提升6.0%。"
conclusion: AdLNet通过稳定性驱动的稀疏学习，实现了鲁棒且可复现的特征选择，尤其适用于高维噪声数据。
---

## 摘要
稀疏特征选择对于高维机器学习至关重要，然而传统的ℓ₁正则化方法在观测噪声和虚假相关性下通常很脆弱，导致特征支持集不稳定和泛化能力下降。尽管对抗训练已被广泛用于提升模型鲁棒性，但其与层次化稀疏特征选择的交互仍未被充分探索。在这项工作中，我们提出了对抗LassoNet（AdLNet），一个稳定性驱动的稀疏特征选择框架，它将输入空间的对抗扰动与LassoNet的层次稀疏机制相结合。我们在局部平滑假设下推导了一个易于处理的一阶对抗近似，并提供了一个受神经正切核（NTK）启发的谱分析，以刻画扰动驱动的训练如何减少梯度集中。在高维SERS数据、六个公共基准数据集和ColoredMNIST上的实验表明，AdLNet在保持有竞争力的稀疏选择性能的同时，在ColoredMNIST上以近乎匹配的支持稀疏性，将分布外鲁棒性提高了4.4%，特征支持可重现性提高了6.3%。在高维肺癌筛查数据集上，与普通LassoNet相比，AdLNet的测试准确率提高了5.3%，AUC提高了6.0%。代码和数据集可在https://github.com/719573/Adversarial-LassoNet获取。

## Abstract
Sparse feature selection is critical for high-dimensional machine learning, yet traditional $\ell_1$-regularized methods are often brittle under observational noise and spurious correlations, leading to unstable feature supports and degraded generalization. Although adversarial training has been widely used to improve model robustness, its interaction with hierarchical sparse feature selection remains underexplored. In this work, we propose Adversarial LassoNet (AdLNet), a stability-driven sparse feature selection framework that integrates input-space adversarial perturbations with the hierarchical sparsity mechanism of LassoNet. We derive a tractable first-order adversarial approximation under local smoothness assumptions and provide an NTK-inspired spectral analysis to characterize how perturbation-driven training can reduce gradient concentration. Experiments on high-dimensional SERS data, six public benchmark datasets, and ColoredMNIST show that AdLNet maintains competitive sparse-selection performance while improving out-of-distribution robustness by 4.4\% and feature support reproducibility by 6.3\% under nearly matched support sparsity on ColoredMNIST. On the high-dimensional lung cancer screening dataset, AdLNet achieves a 5.3\% test accuracy gain and a 6.0\% AUC improvement over vanilla LassoNet. Code and dataset are available at https://github.com/719573/Adversarial-LassoNet.

---

## 论文详细总结（自动生成）

- **核心问题与整体含义**  
  - 在高维机器学习中，稀疏特征选择至关重要，但传统的 $\ell_1$ 正则化方法（如 Lasso）在观测噪声和虚假相关性下容易不稳定，导致所选择的特征支持集波动和泛化性能下降。  
  - 对抗训练已被广泛用来提升模型的鲁棒性，然而对抗训练与层次化稀疏特征选择（如 LassoNet 的“残差跳跃连接 + 层级约束”结构）之间的相互作用尚缺乏研究。  
  - 论文旨在提出一个“稳定性驱动”的稀疏特征选择框架，将对输入的局部对抗扰动作为额外的训练信号，引导稀疏选择过程偏好那些在干净样本和扰动下均保持预测能力的特征，从而同时提升特征选择的鲁棒性和可重现性。

- **提出的方法论**  
  - **核心思想**：在 LassoNet 的层次化稀疏优化框架内，引入一个对抗稳定性项（adversarial stability loss），将原始损失替换为混合目标  
    $$L_{\text{mix}} = (1-\alpha)\,L(f_\theta(x), y) + \alpha\,L(f_\theta(x+r^*), y)$$  
    其中 $r^*$ 是局部最坏情况扰动，$\alpha$ 平衡预测精度和局部稳定性。  
  - **关键技术细节**：  
    - 定义对抗敏感度 $S_\rho(x,y;\theta) = \max_{\|r\|_2\le\rho} [L(f_\theta(x+r),y) - L(f_\theta(x),y)]$。  
    - 在一阶平滑假设下，利用一阶泰勒展开和 Cauchy–Schwarz 不等式，得到解析最坏扰动方向  
      $$r^* = \rho \frac{\nabla_x \ell(x)}{\|\nabla_x \ell(x)\|_2 + \epsilon}$$  
      其中 $\ell(x)=L(f_\theta(x), y)$。  
    - 训练时，先对每个 mini-batch 计算 $r^*$（固定），然后计算混合损失并沿 LassoNet 原有的梯度更新和层次近端算子 $\mathrm{HierProx}_{\eta\lambda,M}$ 进行优化。  
  - **理论支撑**：借助经验神经正切核（NTK）的谱分析，给出一种局部条件 $G(E;\Theta) > 0$，在该条件下扰动能提升 NTK 的有效秩（effective rank），即减少梯度方向的集中度，从而提供一种几何解释。

- **实验设计**  
  - **数据集与应用场景**：  
    - 高维医疗光谱数据：SERS 肺癌筛查数据集（87 个样本，1264 维光谱特征）。  
    - 六个公共基准数据集：MNIST、Fashion‑MNIST、ISOLET、COIL‑20、Activity、Mice Protein，覆盖图像、语音、行为识别等任务。  
    - 虚假相关性偏移基准：ColoredMNIST，训练时颜色与标签存在虚假相关（$\{0.1,0.2,0.3\}$），测试时分布偏移。  
  - **对比方法**：  
    - 主要基线：vanilla LassoNet（共享层次稀疏机制，无对抗稳定性）。  
    - 在公共基准数据集上额外对比 FISTA‑Net 和 Deep‑Lasso，代表优化驱动稀疏学习和梯度正则化深度特征选择。  
  - **评估指标**：测试准确率、AUC、敏感度/特异度（SERS）；在 ColoredMNIST 上还评估 ID/OOD 准确率、ID–OOD 差值、Jaccard 指数（特征支持可重现性）；并对 NTK 和 Hessian 进行谱诊断。

- **资源与算力**  
  - 实验在 PyTorch 1.8.0 + CUDA 11.1 上运行，使用一台配备 Intel i7‑7700 CPU、32 GB RAM 和 **单块 NVIDIA GeForce RTX 3080 GPU** 的工作站。  
  - 论文未明确给出训练总时长或单次运行耗时，仅在消融实验表格（Table 5）中记录了不同配置的秒数（例如完整模型约 472 s），可视为相对成本参考。

- **实验数量与充分性**  
  - 实验覆盖多领域数据集（总计 8 个以上），包含高维小样本、标准基准和分布偏移场景，类型丰富。  
  - 对比了 3 种现有方法，并进行了消融实验（完整模型 vs. 仅稳定性损失、仅对抗扰动、梯度范数惩罚）。  
  - 进行了超参数敏感性分析（混合系数 $\alpha$ 的取值扫描）和受控特征预算分析（高稀疏/中稀疏情形下的特征数量对比）。  
  - 提供 NTK 有效秩、主导特征值比、Hessian 曲率等谱诊断，作为理论解释的经验证据。  
  - 各实验均采用五组随机种子，并使用验证集独立选择超参数，避免了数据泄露，实验公平性较好。

- **主要结论与发现**  
  - AdLNet 在不显著增加特征数量的条件下，提升了分布外鲁棒性：ColoredMNIST 上 OOD 准确率提高 4.4%，ID–OOD 差距缩小，特征支持 Jaccard 指数提高 6.3%。  
  - 在高维 SERS 医疗数据上，相较于 vanilla LassoNet，测试准确率提升 5.3%，AUC 提升 6.0%，且在中等稀疏度下优势更明显。  
  - 在六个公共数据集上，AdLNet 在多数任务下达到最优或竞争性准确率，验证了稳定性驱动目标不会损害稀疏选择下的预测性能。  
  - 谱诊断显示 AdLNet 的 NTK 有效秩更高、主导特征值能量占比更低，Hessian 的主特征值和平均曲率更小，表明稳定性训练降低了梯度方向的集中度和损失景观的尖锐度。

- **优点**  
  - 将对抗扰动直接嵌入层次稀疏优化的训练目标中，设计简洁，与原有 LassoNet 优化管线兼容，无需改变层次近端约束本身。  
  - 提供从对抗敏感度的一阶近似到 NTK 谱分析的理论推导，为稳定性提升给出了可解释的几何观点。  
  - 在多个领域和分布偏移场景下进行了充分验证，证实了方法的通用性和鲁棒性提升，且通过受控预算实验排除了“仅因选择更多特征”导致的性能掺杂。  
  - 开源代码和数据，有助于可复现性。

- **不足与局限**  
  - 方法仅在 LassoNet 的层次稀疏框架上进行研究，尚未探索其他形式的稀疏结构（如权重稀疏、注意力稀疏）或更广泛的分布偏移场景。  
  - 对抗扰动方向依赖输入梯度的一阶近似，可能过于局部；在高非线性或强扰动区域，近似误差增大。  
  - 混合系数 $\alpha$ 需要在验证集上调整，且最优值随数据集不同而变化，缺乏自适应机制。  
  - 实验资源限制为单 GPU，未报告大规模数据集或分布式训练下的可扩展性。  
  - 对抗训练会额外增加计算开销，例如在消融中完整 AdLNet 的训练时间约为 vanilla LassoNet 的两倍。  
  - 理论研究基于局部微扰条件，其理论保证与真实训练之间的对应关系较弱，仅提供了解释性证据。

（完）
