---
title: "Beyond Item Order: Temporal Gap Tokenization for Generative Recommendation with Semantic IDs"
title_zh: 超越项目顺序：面向语义ID生成式推荐的时间间隔标记化
authors: "Chengkai Huang, Tianqi Gao, Hongtao Huang, Quan Z. Sheng, Lina Yao"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03918v1"
tags: ["query:ndai"]
score: 9.0
evidence: 时间间隔标记化用于序列推荐
tldr: 基于语义ID的生成式推荐常忽略交互时间间隔，导致无法捕捉兴趣漂移。本文提出ChronoSID，通过时间感知掩码自编码（TA-FAMAE）和离散间隔标记化，将时间信号注入标准管线，使模型能学习时间敏感的转移模式。在亚马逊评论数据集上，ChronoSID优于现有方法，尤其在长间隔场景下提升更明显。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03918v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of ChronoSID. Stage 1 learns item representations with Time-Aware Field-Aware Masked Auto-Encoding (TA-FAMAE), which combines masked feature reconstruction with an auxiliary time-gap prediction objective. Stage 2 adopts Globally Aligned Orthogonal Quantization (GAOQ) to convert the frozen item representations into three-level semantic ID tuples. Stage 3 discretizes historical inter-interaction intervals into fixed log-scale gap tokens and interleaves them with semantic ID tokens as the T5 encoder input for temporally augmented sequence-to-sequence recommendation.\", \"page\": 4, \"index\": 2, \"width\": 1044, \"height\": 604}, {\"url\": \"assets/figures/arxiv/2607.03918v1/fig-001.webp\", \"caption\": \"Table 1: Dataset statistics.\", \"page\": 6, \"index\": 1, \"width\": 541, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2607.03918v1/fig-003.webp\", \"caption\": \"Figure 3: Temporal robustness analysis on MI and VG. Test cases are grouped by the elapsed time between the last historical interaction and the target interaction. We report R@10 and N@10 under different gap groups. The target-side interval is used only for diagnostic grouping and is not provided as model input.\", \"page\": 9, \"index\": 3, \"width\": 1039, \"height\": 217}]"
motivation: 现有语义ID生成推荐方法将用户历史构建为静态标识符序列，完全忽略交互间的时间间隔，缺乏对兴趣连续性和偏好漂移的建模。
method: 提出ChronoSID框架，包含两个互补的时间增强模块：用时间间隔预测辅助任务正则化项表示学习的TA-FAMAE，以及将历史交互间隔离散化为对数尺度间隔标记并与语义ID交织输入序列生成器。
result: 在亚马逊评论基准上，ChronoSID一致优于ReSID等基线，消融实验证实两个时间组件的贡献，诊断分析显示在长间隔场景下增益更显著。
conclusion: 通过简单高效的时间增强，ChronoSID成功让语义ID生成推荐捕获时间敏感的转移模式，有效应对兴趣漂移问题，尤其适合长间隔交互场景。
---

## 摘要
基于语义ID的生成式推荐最近已成为序列推荐的一种可扩展范式，其中每个物品由一组紧凑的离散代码序列表示，而下一个物品的预测被形式化为代码生成。然而，现有方法通常将用户历史构建为静态物品标识符序列，将连续交互之间的时间间隔排除在生成输入之外。这种对时间的忽视存在问题，因为交互间隔提供了关于兴趣连续性和偏好漂移的有用线索。在本文中，我们提出了ChronoSID，一个用于基于语义ID的生成式推荐的轻量级时态增强框架。ChronoSID从两个互补的角度将时态信号注入到标准的三阶段语义ID流程中。首先，我们引入了时间感知的领域感知掩码自编码（TA-FAMAE），通过辅助时间间隔预测目标来规范化物品表示学习。其次，我们将历史交互间隔离散化为固定对数尺度的间隔标记，并将其与语义ID元组交错作为序列到序列生成器的编码器输入。这种设计保留了紧凑的SID生成范式，同时使模型能够捕获时间感知的转移模式。在Amazon评论基准上的实验表明，ChronoSID在ReSID和其他有竞争力的生成式推荐基线上持续改进。消融研究进一步验证了两个时态组件的贡献，诊断分析显示在用户兴趣更可能漂移的长间隔场景下有更明显的提升。

## Abstract
Semantic-ID-based generative recommendation has recently emerged as a scalable paradigm for sequential recommendation, where each item is represented by a compact sequence of discrete codes and next-item prediction is formulated as code generation. Existing methods, however, typically construct user histories as sequences of static item identifiers, leaving the elapsed time between consecutive interactions outside the generative input. This temporal blindness is problematic because inter-interaction gaps provide useful cues about interest continuity and preference drift. In this paper, we propose ChronoSID, a lightweight temporal augmentation framework for semantic-ID-based generative recommendation. ChronoSID injects temporal signals into the standard three-stage semantic-ID pipeline from two complementary perspectives. First, we introduce Time-Aware Field-Aware Masked Auto-Encoding (TA-FAMAE), which regularizes item representation learning with an auxiliary time-gap prediction objective. Second, we discretize historical interaction intervals into fixed log-scale gap tokens and interleave them with semantic ID tuples as the encoder input of the sequence-to sequence generator. This design preserves the compact SID generation paradigm while enabling the model to capture time-aware transition patterns. Experiments on Amazon review benchmarks show that ChronoSID consistently improves over ReSID and other competitive generative recommendation baselines. Ablation studies further verify the contribution of both temporal components, and diagnostic analyses show clearer gains under long-gap scenarios where user interests are more likely to drift.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化总结。

### 1. 论文的核心问题与整体含义

-   **核心问题**：现有的基于语义ID的生成式推荐方法存在“时间盲区”。它们仅将用户交互历史视为物品ID的静态序列，完全忽略了交互之间的**时间间隔**这一关键信息。
-   **研究动机**：
    -   时间间隔蕴含着用户兴趣连续性和偏好漂移的重要线索。例如，短时间内连续交互意味着兴趣的延续，而长时间后的返回交互则可能代表新的意图或偏好变化。
    -   论文通过实证分析（图1）证实，随着交互间隔的增长，用户购买同类目商品的概率（即兴趣连续性）会显著下降，且这一模式在多个领域广泛存在。
-   **整体含义**：旨在不牺牲语义ID生成范式紧凑性和可扩展性的前提下，将交互时间间隔信息显式地注入到推荐模型中，以提升推荐系统对用户动态兴趣的建模能力。

### 2. 论文提出的方法论

论文提出了一个名为 **ChronoSID** 的轻量级时态增强框架，它遵循标准的三阶段语义ID流程，并从两个层面引入时态建模。

-   **核心思想**：
    1.  **物品表示层面**：在物品表示学习中引入时间正则化，使物品向量本身具备“时间可预测性”。
    2.  **序列生成层面**：将历史交互间隔显式地表示为离散符号，并与语义ID序列交织，作为生成模型的输入。

-   **关键技术细节**：
    -   **阶段一：时态感知的领域掩码自编码**
        -   在标准的掩码特征重构任务基础上，增加了一个**辅助时间间隔预测任务**。
        -   对于训练序列的最后一个交互（目标物品），模型在重构其被掩码的特征字段之外，还需通过一个回归头预测它与前一个交互的时间间隔的对数 $\log(1 + \Delta t_L)$。
        -   联合损失函数为：$L_{TA-FAMAE} = L_{feat} + \lambda L_{time}$，其中 $\lambda$ 控制时间正则化的强度。
    -   **阶段二：语义量化**
        -   此阶段保持不变，采用ReSID论文中的“全局对齐正交量化”方法，将冻结的物品表示编码为三级离散语义ID：$\Phi(v) = (c^1_v, c^2_v, c^3_v)$。
    -   **阶段三：时态增强的序列到序列推荐**
        -   **间隔标记化**：将两个历史交互的时间间隔 $\Delta t_l = t_l - t_{l-1}$ 离散化为 $G=5$ 个固定对数尺度的“间隔标记”，如 `<1h`, `[1h,1d)`, `[1d,1w)` 等。
        -   **序列交织**：将离散化的间隔标记 $g_l$ 与物品的语义ID元组 $\Phi(v_l)$ 交织，构建编码器输入：$\mathbf{x}_u = [g_1, \Phi(v_1), g_2, \Phi(v_2), ..., g_L, \Phi(v_L)]$。
        -   **生成范式**：使用轻量级T5模型，编码器输入为交织的时态序列，解码器则仅自回归地生成下一个物品的语义ID元组 $\Phi(v_{L+1})$。

### 3. 实验设计

-   **数据集**：使用了8个Amazon-2023评论数据集的子集，包括乐器、电子游戏、工业与科学、婴儿产品、艺术手工、体育户外、玩具游戏、美妆个护。
-   **评估协议**：采用留一法，每个用户最后一次交互作为测试集，倒数第二次作为验证集，其余为训练集。评估指标为Recall@K和NDCG@K（K=5, 10）。
-   **对比方法**：
    -   **传统序列推荐**：HGN, SASRec, BERT4Rec, S3-Rec。带`*`号的变体表示集成了与ChronoSID相同的物品辅助信息。
    -   **生成式推荐**：TIGER, LETTER, EAGER, UNGER, ETEGRec, **ReSID**。其中ReSID是最直接的基线，因为ChronoSID与其共享相同的量化阶段。

### 4. 资源与算力

-   **硬件**：所有实验均在 **NVIDIA V100 GPU** 上完成。
-   **训练/推理时间**：论文在效率分析实验（表5）中给出了详细的对比。例如，在乐器数据集上，ChronoSID相比ReSID的训练时间从174分钟增加到187分钟（约1.07倍开销），单样本推理延迟从0.69毫秒增至0.86毫秒。总体而言，因引入时态标记带来的额外计算开销被认为是适中的。

### 5. 实验数量与充分性

论文设计了较为全面的实验来验证其主张，覆盖了多个维度，数量与充分性均较好。

-   **主实验（表2）**：在8个数据集上，与12种基线方法进行了性能对比，结果取5次随机种子的平均值。
-   **消融实验（表3）**：在3个数据集上，系统地剥离了`TA-FAMAE`和`间隔标记`两个模块，验证了各自和组合的贡献。
-   **鲁棒性分析（图3）**：在2个数据集上，按测试样本的目标间隔时长分组（<1天, 1天-1周等），评估模型在不同时态条件下的表现。
-   **敏感性分析（表4）**：在两个数据集上，研究了关键超参数（$\lambda$ 和 $G$）的影响。
-   **效率分析（表5）**：在两个数据集上，对比了训练和推理时间开销。
-   **输出层分析（表6）**：在一个数据集上，深入分析了生成语义ID候选的准确性。
-   **流行度分析（图4）**：在两个数据集上，分析了模型对热门和非热门物品的推荐效果。
-   这些实验设计**客观、公平**，尤其是在与ReSID对比时，严格控制了量化阶段相同，仅评估时态建模的影响。

### 6. 论文的主要结论与发现

-   ChronoSID在所有8个数据集和所有评估指标上，均**一致地优于**所有SID-based生成式推荐基线，特别是其直接基准ReSID。
-   **间隔标记注入是性能提升的主要来源**，而`TA-FAMAE`的辅助任务提供了有益的但相对温和的正则化效果，两者结合效果最佳。
-   在**长间隔场景下**（如超过一周或一月），ChronoSID相比ReSID的**提升更为显著**，证明了时态建模在处理兴趣漂移时的有效性。
-   时态增强不仅提升了热门物品的推荐，对**非热门物品的推荐质量也有更大幅度的提升**。
-   ChronoSID在生成更准确的**目标语义ID候选**方面优于ReSID，从生成机制的源头带来了增益。
-   模型对超参数（如 $\lambda$, $G$）有一定鲁棒性，使用适中的、对数尺度的离散化方案即可获得稳定提升。引入的额外算力开销处于可接受范围。

### 7. 优点

-   **问题洞察深刻**：准确识别并解决了“时间盲区”这一先前研究忽略的关键局限。
-   **方法简洁高效**：通过“间隔标记化与序列交织”的优雅设计，在不改变核心生成范式和解码器结构的前提下，以轻量级成本注入了时态信息。
-   **实验严谨全面**：实验设计系统性强，不仅与大量基线对比，还通过消融、鲁棒性、效率和输出层分析等多维度验证，结论扎实可靠。
-   **实用性强**：方法可以无缝集成到现有的三阶段SID流程中，尤其对长间隔用户行为建模有显著提升，应用潜力大。

### 8. 不足与局限

-   **时态离散化策略固定**：使用固定的对数尺度间隔阈值，可能不是所有数据集或场景下的最优解，自适应或可学习的离散化策略（作者也在未来工作中提及）可能是更好的选择。
-   **预测单向性**：模型仅将历史时间间隔作为条件信号，而未联合预测下一个交互的时间间隔，这可能是一个更有挑战但信息更丰富的研究方向。
-   **评估场景局限**：评估仅在Amazon电商评论数据集上进行，且采用留一法，未在会话推荐或线上A/B测试等更动态的场景下验证其有效性。
-   **文本特征依赖**：物品表示学习阶段依赖于物品的文本特征（如标题、类别等），这可能在特征稀疏或无文本特征的冷启动场景下受限。

（完）
