---
title: "TestMate: Test-Time Domain Adaptation Aided by Lightweight Vision Foundation Model"
title_zh: TestMate：由轻量级视觉基础模型辅助的测试时域适应
authors: "Dimitrios Fotiou, Vasileios Mygdalis, Ioannis Pitas"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03810v1"
tags: ["query:ndai"]
score: 9.0
evidence: 利用基础模型进行测试时域适应，直接解决有限标签情况下的适应问题
tldr: 测试时域适应旨在利用无标签流数据实时调整深度模型应对分布漂移，现有方法或需反向传播导致灾难性遗忘和噪声边界，或基于记忆库收敛慢难处理连续漂移。本文提出TestMate，一种免反向传播的实时框架，利用轻量视觉基础模型YOLOv8-seg生成多尺度掩码提案，通过启发式尺寸排序竞争机制融合，从首帧即时适应，避免遗忘并保留细节，可独立或集成提升性能，在三种适应任务上取得最优结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03810v1/fig-001.webp\", \"caption\": \"Figure 1: TestMate is a lightweight, backpropagation-free adaptation module. For a test image, a Segmentation DNN produces an initial output, while a Visual Foundation Model generates highquality unlabeled masks. TestMate processes masks heuristically, from smallest to largest, labels them via majority voting, and fuses them with the DNN output, considering their entropy and region dominance. This parameter-free refinement produces a highfidelity output with sharper boundaries even in small objects. TestMate can function standalone, be applied recursively, or enhance existing TTDA algorithms (dashed arrows).\", \"page\": 1, \"index\": 1, \"width\": 423, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2607.03810v1/fig-003.webp\", \"caption\": \"Table 2: Experimental results for GTA-V [28] → Cityscapes [4] (val) in SFDA and TTDA settings with DeepLabV2 [1] (ResNet-101 [10] backbone). BP denotes if the method utilizes backpropagation to achieve adaptation.\", \"page\": 5, \"index\": 3, \"width\": 1036, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2607.03810v1/fig-004.webp\", \"caption\": \"Figure 3: Qualitative comparison on GTA-V → Cityscapes [4] and FMB [22] → MVSeg [15] Dataset of TTDA methods.\", \"page\": 5, \"index\": 4, \"width\": 810, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2607.03810v1/fig-002.webp\", \"caption\": \"Table 3: Experimental results for FMB [22] → MVSeg [15] Dataset (shuffled test set) in Online TTDA settings with DeepLabV3 [2] (ResNet-101 [10] backbone). BP denotes if the method utilizes backpropagation to achieve adaptation\", \"page\": 7, \"index\": 2, \"width\": 1022, \"height\": 255}]"
motivation: 现有测试时域适应方法因反向传播或记忆库策略存在遗忘、噪声和适应缓慢等问题，难以实时处理连续域漂移。
method: 利用YOLOv8-seg生成多尺度实例掩码提案，通过尺寸优先的启发式竞争方案与主模型融合，实现无需反向传播的即时适应。
result: 在两个基准数据集上，TestMate在TTDA、SFDA和在线TTDA任务中均达到当前最优性能。
conclusion: TestMate提供了一种高效、实时的测试时域适应方案，能有效应对连续域漂移并保持精细分割细节。
---

## 摘要
测试时域适应（TTDA）旨在仅使用实时流式、无标签的测试数据，使深度神经网络适应分布偏移。当前用于语义分割任务的方法存在严重局限性。熵最小化技术需要昂贵的反向传播，有灾难性遗忘的风险，并产生嘈杂的分割边界。基于记忆库的方法虽然无需反向传播，但适应速度慢，需要大量样本才能收敛，并且难以处理连续域偏移。我们提出TestMate，一种新颖的、实时的、无需反向传播的TTDA框架，克服了这些问题。TestMate利用轻量级视觉基础模型的泛化能力来引导适应。我们使用基于零样本实例分割的YOLOv8-seg模型，实时生成多尺度下物体及其部位的无标签掩码提议。这些提议通过一种启发式的大小顺序竞争方案与主模型融合，其中较小的高置信度区域主导并细化周围较大的、不确定性更高的区域中的预测。这种无参数机制能够从第一帧开始立即适应，本质上避免了灾难性遗忘，并有效保留精细的物体细节和边界，即使对于小物体也是如此。TestMate可作为一个独立的、高效的细化模块使用，或无缝集成到现有的TTDA方法中，显著提升其性能。我们在两个基准数据集上展示了最先进的结果，证明了TestMate在三种不同的适应任务中的有效性：TTDA、无源域适应（SFDA）和在线TTDA。代码已公开。

## Abstract
Test-Time Domain Adaptation (TTDA) aims to adapt Deep Neural Networks to distribution shifts using only streaming, unlabeled test data in real time. Current methods for semantic segmentation tasks suffer from critical limitations. Entropy minimization techniques require costly backpropagation, risking catastrophic forgetting and producing noisy segmentation boundaries. Memory-bank methods, while backpropagation-free, exhibit slow adaptation, requiring numerous samples to converge and struggle to handle continuous domain shifts. We introduce TestMate, a novel, real-time, and backpropagation-free TTDA framework that overcomes these issues. TestMate leverages generalization capability of a lightweight Visual Foundation Model to guide the adaptation. We use a zero-shot instance segmentation YOLOv8-seg based model to generate unlabeled mask proposals for objects and their parts at multiple scales in real time. These proposals are fused with the primary model via a heuristic, size-ordered competitive scheme, where small, high-confidence regions dominate and refine predictions in surrounding larger, less certain areas. This paremeter-free mechanism enables immediate adaptation from the first frame, inherently avoids catastrophic forgetting and effectively preserves fine object details and boundaries, even for small objects. TestMate can be used as a standalone, efficient refinement module or seamlessly integrated into existing TTDA methods to significantly boost their performance. We demonstrate state-of-the-art results across two benchmark datasets, proving TestMate's effectiveness in three distinct adaptation tasks: TTDA, Source-Free Domain Adaptation (SFDA), and online-TTDA. Code is available.

---

## 论文详细总结（自动生成）

## 1. 核心问题与研究背景  
- 测试时域适应（TTDA）旨在利用实时、无标签的流式测试数据，使深度模型在线适应分布偏移，对语义分割等安全关键任务尤为重要。  
- 现有方法存在明显短板：  
  - **熵最小化类方法**需要反向传播（BP），存在灾难性遗忘风险，并容易产生噪声分割边界。  
  - **基于记忆库的方法**虽然无需反向传播，但适应速度慢、收敛要求大量样本，难以应对连续域漂移。  
- 为此，论文提出 **TestMate**，通过引入轻量级视觉基础模型的泛化能力，构建一种**无需反向传播、实时、可即时适应**的TTDA框架，克服上述局限。

## 2. 方法论  
- **核心思想**：将轻量级视觉基础模型（YOLOv8‑seg）作为“引导器”，生成高质量无标签掩码提案，通过启发式融合机制修正主分割网络的输出，实现测试时无参数、无梯度更新的适应。  
- **关键技术细节**：  
  - **多尺度掩码生成**：使用基于零样本实例分割的YOLOv8‑seg模型，从测试图像中实时产生物体及部件的多尺度掩码提案。  
  - **大小顺序竞争融合**（size‑ordered competitive scheme）：  
    - 对掩码按面积从小到大排序。  
    - 小掩码区域置信度高、边界清晰，被优先赋予伪标签（通过多数投票确定类别）。  
    - 依次将小掩码的预测覆盖到周围更大、更不确定的区域上，利用**熵**和**区域主导性**进行决策，逐步细化分割边界。  
  - **免反向传播特性**：整个过程不涉及任何梯度计算或参数更新，从第一帧即可开始适应，天然避免灾难性遗忘，并保留小物体细节。  
- **使用方式**：TestMate可作为独立精炼模块，递归应用，或以插件形式无缝集成到现有TTDA方法（如虚线箭头所示），显著提升其性能。

## 3. 实验设计  
- **数据集与场景**：  
  - **GTA‑V → Cityscapes**：合成到真实的域适应，用于SFDA和TTDA设定。  
  - **FMB → MVSeg**：雾天场景域适应，用于在线TTDA设定（打乱测试集顺序）。  
- **基准模型**：主分割网络采用DeepLabV2或DeepLabV3（均为ResNet‑101骨干）。  
- **对比方法**：涵盖现有TTDA、SFDA、在线TTDA的多种代表性技术，表格中明确列出各方法是否使用反向传播（BP）及其性能。  
- **评估指标**：主要使用mIoU，并辅以定性可视化（如图3）。

## 4. 资源与算力  
- 论文未明确披露所用GPU型号、数量、训练时长或推理延迟的具体数值。  
- 仅强调YOLOv8‑seg为轻量级基础模型，配合TestMate可达到实时处理速度，但缺乏与其他方法的详细效率对比数据。

## 5. 实验数量与充分性  
- **实验组数**：在两个基准数据集上，针对三种适应任务（SFDA、TTDA、在线TTDA）进行了系统对比，并展示了定量结果表与定性示例图。  
- **充分性与公平性**：  
  - 使用统一骨干网络和标准评估协议，对比对象选择合理，具有客观可比性。  
  - 包含有无反向传播方法的对照，能清晰反映TestMate的优势。  
  - 实验覆盖了多类域漂移（合成→真实，晴天→雾天），验证了方法的泛化能力，但消融研究（如融合策略中各组件的作用）未在摘要中详述，需查看全文确认。

## 6. 主要结论与发现  
- TestMate在**所有三种适应任务**中均取得**当前最优**的mIoU性能。  
- 能实现**从首帧立即适应**，无需预热样本，避免灾难性遗忘，并有效保持精细分割细节。  
- 作为插件可明显提升现有TTDA方法的效果，即插即用，无需重新训练。

## 7. 优点  
- **高效、免梯度**：无需反向传播，避免遗忘与噪声边界，适合连续域漂移。  
- **实时性强**：依托轻量级基础模型，满足在线部署需求。  
- **高保真输出**：对小物体和边界的处理显著优于同类方法。  
- **灵活集成**：既可作为独立模块，又可用来增强其他TTDA算法，扩展性强。  
- **无参数**：启发式融合方案不引入额外可学习参数，部署简单。

## 8. 不足与局限  
- **对基础模型的依赖**：性能受限于YOLOv8‑seg的泛化能力，若目标域物体外观与基础模型训练集差异过大，掩码提案质量可能下降。  
- **融合启发式的风险**：大小顺序竞争假设小区域更可靠，但在严重遮挡或混淆类别时，多数投票可能产生错误标签，导致误差传播。  
- **实验覆盖有限**：仅测试了语义分割任务和两个数据集，未验证在实例分割、目标检测等其他密集预测任务上的效果。  
- **效率对比不完整**：缺少与基准方法在推理速度、内存占用等方面的详细定量对比。  
- **在线TTDA的混合漂移**：尽管声称能处理连续漂移，但实验中的域转换仅为单一跳跃（合成→真实，晴天→雾天），更复杂的渐进漂移或混合域场景有待进一步检验。

（完）
