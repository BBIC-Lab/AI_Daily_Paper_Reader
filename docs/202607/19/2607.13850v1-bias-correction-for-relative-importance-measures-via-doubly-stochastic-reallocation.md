---
title: Bias Correction for Relative Importance Measures via Doubly Stochastic Reallocation
title_zh: 通过双随机重分配对相对重要性度量的偏差校正
authors: "Tien-En Chang, Argon Chen"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13850v1"
tags: ["query:ndai"]
score: 10.0
evidence: 提出相对重要性度量的偏差校正，计算特征重要性权重
tldr: 相对重要性分析中，广义主导性计算量大，正交化再分配度量（RW和GCD）提供高效替代但存偏差。本文揭示GCD先验偏差源于再分配矩阵行和不等，通过双随机矩阵投影与Sinkhorn算法校正得GCD-SK等方法，消除偏差并提升性能，尤其在第一主成分主导时优于RW，最后给出度量选择指南。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13850v1/fig-002.webp\", \"caption\": \"Figure 1: A p = 10 numerical illustration of the a priori bias in RegPA (GCD) and its correction via MAP and SK algorithms.\", \"page\": 10, \"index\": 2, \"width\": 947, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2607.13850v1/fig-005.webp\", \"caption\": \"Figure 2: Performance of ORMs across predictor structures with mild multicollinearity and mild first principal component (A, B) and strong first principal component (C, D). Median performances of RegPA, RegPA-MAP, RegPASK, CorPA and GDA are highlighted in blue, light blue, light green, red and gray lines, respectively.\", \"page\": 12, \"index\": 5, \"width\": 972, \"height\": 970}, {\"url\": \"assets/figures/arxiv/2607.13850v1/fig-004.webp\", \"caption\": \"Figure 3: Win–loss analysis between RW and GCD-SK under mild multicollinearity (Scenarios 1.1 and 1.2), based on RMSE and Kendall’s τ . Each comparison corresponds to one eigenvalue set: a method wins if a Wilcoxon signed-rank test over the ns = 10 paired correlation matrices sharing those eigenvalues indicates significantly better performance at the 5% level, and the comparison is a tie otherwise. The dashed line marks the threshold λ1/ √ p = 1.5, beyond which the first principal component is considered dominant.\", \"page\": 13, \"index\": 4, \"width\": 966, \"height\": 618}, {\"url\": \"assets/figures/arxiv/2607.13850v1/fig-003.webp\", \"caption\": \"Figure 4: Performance of ORMs across predictor structures with severe multicollinearity and mild first principal component (A, B) and strong first principal component (C, D). Median performances of RegPA, RegPA-MAP, RegPASK, CorPA and GDA are highlighted in blue, light blue, light green, red and gray lines, respectively.\", \"page\": 14, \"index\": 3, \"width\": 972, \"height\": 970}, {\"url\": \"assets/figures/arxiv/2607.13850v1/fig-001.webp\", \"caption\": \"Figure 5: Win–loss analysis between RW and GCD-SK under severe multicollinearity (Scenarios 2.1 and 2.2), based on RMSE and Kendall’s τ . Wins and ties are determined as in Figure 3.\", \"page\": 15, \"index\": 1, \"width\": 966, \"height\": 618}]"
motivation: GCD度量存在先验偏差，RW存在均衡问题，需校正偏差以提升相对重要性度量的准确性。
method: 通过将GCD的再分配矩阵映射为双随机矩阵，利用交替投影和Sinkhorn-Knopp算法校正其结构性行和偏差。
result: GCD-SK成功消除行和偏差，显著改进GCD，并在第一主成分主导时往往优于RW。
conclusion: 研究提出偏差校正方法并验证其有效性，为实际应用中选择合适的相对重要性度量提供了明确指南。
---

## 摘要
相对重要性 (RI) 分析量化每个预测变量对线性模型可解释方差的贡献。广泛使用的基准方法通用优势分析 (GD) 需要评估 $2^p-1$ 个子模型，且随着预测变量数量 $p$ 的增长，计算负担加重。正交化-重分配度量 (ORM)，包括相对权重 (RW) 和 Green--Carroll--DeSarbo 度量 (GCD)，通过将重要性分配给正交化后的预测变量再重分配回原始预测变量，提供了高效的替代方案。然而，每种方法都有其结构性局限：RW 存在水平化问题，会压缩预测变量重要性值之间的差异；而 GCD 则存在先验偏差，在观测到响应变量之前就系统性地偏好某些预测变量。我们证明这种偏差由重分配矩阵的行和决定。在复合对称条件下的闭式分析将 GCD 和 RW 所基于的重分配与 GD 基准相关联，表明均匀的多重共线性本身不会引起先验偏差，并将 RW 的水平化问题形式化为相对于基准的过度收缩。我们通过使用交替投影法 (MAP) 和 Sinkhorn--Knopp (SK) 算法将 GCD 的重分配矩阵映射为双随机矩阵来校正 GCD 的偏差，得到 GCD-MAP 和 GCD-SK。大量模拟表明，GCD-SK 消除了结构性的行和偏差，显著改进了 GCD，并且在第一主成分占主导地位时往往优于 RW。最后，我们给出了选择这些度量的经验指导。

## Abstract
Relative importance (RI) analysis quantifies each predictor's contribution to the explained variance of a linear model. General Dominance (GD), a widely used benchmark, requires evaluating $2^p-1$ sub-models and becomes computationally intensive as the number of predictors $p$ grows. Orthogonalization-Reallocation Measures (ORMs), including Relative Weights (RW) and the Green--Carroll--DeSarbo measure (GCD), provide efficient alternatives by assigning importance to orthogonalized predictors and reallocating it to the original predictors. Each, however, has a structural limitation: RW exhibits a leveling problem that compresses differences among predictor importance values, whereas GCD exhibits an a priori bias that systematically favors certain predictors before a response is observed. We show that this bias is governed by the row-sums of the reallocation matrix. A closed-form analysis under compound symmetry relates the reallocations underlying GCD and RW to a GD-based benchmark, showing that homogeneous multicollinearity alone does not induce an a priori bias and formalizing RW's leveling problem as excess shrinkage relative to the benchmark. We correct GCD's bias by mapping its reallocation matrix to a doubly stochastic matrix using the Method of Alternating Projections (MAP) and the Sinkhorn--Knopp (SK) algorithm, yielding GCD-MAP and GCD-SK. Comprehensive simulations show that GCD-SK removes the structural row-sum bias, substantially improves upon GCD, and often outperforms RW when the first principal component is dominant. We conclude with empirical guidelines for selecting among the measures.

---

## 论文详细总结（自动生成）

好的，以下是该论文的详细中文总结。

### 1. 论文的核心问题与整体含义

- **核心问题**：在线性模型中，用于量化预测变量相对重要性的基准方法——通用优势分析（GD），其计算复杂度随预测变量数量（$p$）呈指数增长（需评估 $2^p-1$ 个子模型）。高效的替代方案“正交化-重分配度量（ORM）”，如相对权重（RW）和 Green–Carroll–DeSarbo 度量（GCD），虽然计算简便，但各自存在结构性缺陷。
    - **RW 的均衡问题**：倾向于压缩预测变量之间的重要性差异，使重要性的评估趋于平均。
    - **GCD 的先验偏差**：在观测到响应变量前，就系统性地偏好某些预测变量。本文旨在揭示并校正 GCD 的这一偏差。
- **整体含义**：该研究不仅从理论上剖析了 GCD 偏差的根源，还提出了有效的校正方法，使得被弃用的 GCD 度量重新成为一个在特定场景下（尤其是当 RW 因均衡问题表现不佳时）更优的、可靠的选择，从而为实践者提供了更完善的工具组合与选择指南。

### 2. 论文提出的方法论

- **核心思想**：揭示并校正 GCD 度量中“先验偏差”的结构性根源，即其重分配矩阵不具备“双随机性”（行和不为1）。通过强制使其重分配矩阵变为双随机矩阵，来消除偏差，生成无偏的新度量。
- **关键技术细节与流程**：
    - **ORM 框架**：方法分为两步：（1）正交化：使用 Johnson 最小转换 $Z = X(X^TX)^{-1/2}$ 将原始预测变量 $X$ 转为正交变量 $Z$。（2）重分配：通过重分配矩阵 $A$ 将正交变量的重要性（由其与 $y$ 的平方相关 $\rho^2_{z_jy}$ 定义）重新分配给原始变量：$D^A = A \cdot \rho^2_{zy}$。
    - **偏差表征**：证明 ORM 的期望归一化重要性与重分配矩阵的行和成正比：$E[D^A_{i}] = \frac{1}{p}\sum_{j=1}^p a_{ij}$。对于 GCD，其行和可表示为预测变量方差膨胀因子（VIF）比值的加权平均，当 VIF 差异大时，高 VIF 的预测变量会被系统性高估，产生先验偏差。
    - **校正方法**： 将一个符合列随机性（列和为1，重要性完全分配）的 GCD 重分配矩阵 $A_{\text{RegPA}}$，映射为一个邻近的、同时满足行和与列和均为1的**双随机矩阵**。文中提出了两种映射算法：
        1.  **交替投影法 (MAP)**：通过在行随机矩阵集和列随机矩阵集之间迭代地进行欧几里得投影，最终收敛到交集（双随机矩阵）上，得到 GCD-MAP 度量。
        2.  **Sinkhorn–Knopp 算法 (SK)**：通过对矩阵交替进行行归一化和列归一化的乘性迭代，等价于在双随机集上进行 Kullback–Leibler (KL) 散度最小化，得到 GCD-SK 度量。

### 3. 实验设计

- **数据集/场景**：采用蒙特卡洛模拟框架，生成大量具有不同预测变量相关性结构的数据。具体通过控制预测变量数量 $p$（3到10）、采样特征值、生成不同的相关矩阵 $\Sigma$、并为每个矩阵采样响应变量系数 $\beta$ 来创建多种场景。场景根据两个关键因素被分为四类：多重共线性的严重程度（由**最大方差膨胀因子** $VIF_{max}/p$ 衡量）和第一主成分的主导程度（由**最大特征值** $\lambda_1/\sqrt{p}$ 衡量）。
- **基准方法**：以计算成本高昂但理论上最全面的**通用优势分析 (GD)** 作为评估所有方法的“黄金标准”。
- **对比方法**：
    - **被校正方法**：原始的 GCD (RegPA) 和 RW (CorPA)。
    - **提出的校正方法**：GCD-MAP (RegPA-MAP) 和 GCD-SK (RegPA-SK)。
    - **其他基线方法**： 基于身份重分配的方法 (IdA) 以及理论上理想但计算不可行的基准 GD-based reallocation (GDA)。

### 4. 资源与算力

- **算力说明**：论文中未明确提及进行蒙特卡洛模拟所使用的具体计算资源，如 GPU 型号、数量或总训练时长。所有实验均为基于 CPU 的统计模拟。

### 5. 实验数量与充分性

- **实验数量**：实验设计详尽且规模庞大。对于 $p \in \{3,4,5,6\}$，生成 1000 组特征值；对于 $p \in \{7,8,9,10\}$，生成 2500 组特征值。每组特征值生成 $n_s=10$ 个不同的相关矩阵，并为每个相关矩阵评估 $n_u=100$ 个随机响应，总计评估了数十万个独立场景。
- **充分性评估**：实验设计非常充分、客观且公平。
    - **多样性**：通过覆盖广泛的 $p$ 值、特征值组合及相关矩阵，系统探索了从无共线性到严重共线性、从无主导成分到强主导成分的各种复杂情况。
    - **公平性**：所有方法都在完全相同的模拟数据上进行评估，并与同一个黄金标准（GD）进行比较。
    - **稳健性**：通过对每个相关矩阵下的100个随机响应进行平均，并使用多种场景分类以及成对的 Win-Loss 统计分析（Wilcoxon符号秩检验），确保了结论的统计稳健性。

### 6. 论文的主要结论与发现

- **源头定位**：GCD 的先验偏差根源于其重分配矩阵的行和不为1，且行和是预测变量 VIF 比值的加权平均，这在理论上完美解释了为何异质性多重共线性会导致偏差，而均匀共线性则不会。
- **校正有效性**：通过强制重分配矩阵具备双随机性，GCD-MAP 和 GCD-SK 方法均成功消除了 GCD 的先验偏差。其中，GCD-SK 的效果更优。
- **性能比较与选择指南**：
    - **弱第一主成分**：在无主导成分时，RW 表现良好且简单。GCD 偏差严重，虽经校正有巨大改善（GCD-SK），但 RW 在此场景下仍有微弱优势。
    - **强第一主成分**：当第一主成分占主导（$\lambda_1/\sqrt{p} \ge 0.9$ 或 $1.5$），RW 因“均衡问题”性能显著下降。此时，GCD 及其校正版本（特别是 GCD-SK）不仅能抵抗偏差，还能利用回归系数的联合信息，性能显著优于 RW。SK 校正大幅扩展了基于回归的重分配方法的优势区域。

### 7. 优点

- **理论贡献深刻**：从重分配矩阵行和的角度，清晰、严谨地形式化了 GCD 先验偏差的成因，并用闭式解阐明了 RW 均衡问题的本质，深化了对 ORM 方法的理论理解。
- **方法论简洁高效**：提出的修正方案（MAP和SK）概念清晰，实现直接，无需增加额外的计算复杂度（仅需对 $p \times p$ 矩阵进行迭代），就成功解决了长期存在的 GCD 偏差问题。
- **实用的指导价值**：基于大规模、结构化的模拟实验，为实践者提供了清晰、具体、分场景的度量选择指南（根据 VIF 和特征值大小选择 RW 或 GCD-SK），具有极高的应用价值。

### 8. 不足与局限

- **实验范围限制**：最大的局限性在于所有模拟和结论均基于预测变量数量 $p \le 10$ 的情况。对于现代高维数据分析中常见的 $p > n$ 或 $p$ 极大的情形，方法的计算可行性和性能表现尚待验证。
- **缺乏真实数据验证**：所有评估均在模拟数据上进行。论文未在真实世界的数据集上验证其方法和选择指南的有效性，这是从理论到应用的一个差距。
- **均衡问题未根本解决**：本文的修正目标是消除先验偏差，并未提供校正 RW 均衡问题的通用方法。在复合对称的闭式解之外，对 RW 的过度收缩特性尚缺乏普遍性的表征与解决方案。
- **GCD-SK 并非全能**：根据结论，在弱第一主成分的多重共线性场景下，推荐的仍然是 RW，这表明 GCD-SK 存在局限性，其性能优势依赖于特定的数据结构。
- **特定假设**：理论推导基于响应在预测空间均匀分布的假设，虽然对评估方法的平均行为是合理的，但强烈的先验信息或特定的响应模式可能会影响方法的相对表现。

（完）
