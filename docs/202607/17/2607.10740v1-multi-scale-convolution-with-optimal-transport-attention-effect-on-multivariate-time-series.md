---
title: Multi-Scale Convolution with Optimal Transport Attention Effect on Multivariate Time Series
title_zh: 多尺度卷积与最优传输注意力对多元时间序列的影响
authors: "HaoChong Fu, Jian Xu"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10740v1"
tags: ["query:ndai"]
score: 9.0
evidence: 多尺度卷积用于多元时间序列
tldr: 针对多元时间序列预测中多粒度结构模式捕捉与噪声抑制的挑战，本文提出MSC-OT模型。该模型采用倒置嵌入，将多尺度卷积增强应用于注意力得分矩阵以捕捉局部变量交互模式，并引入Sinkhorn最优传输正则化确保信息流平衡，通过自适应融合动态集成各组件。在多个基准数据集上的实验表明，模型在短期和长期预测任务中均取得优越性能，消融实验进一步验证了各组件的有效性及其协同贡献。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10740v1/fig-001.webp\", \"caption\": \"Fig. 1. In MSC-OT, we use α to define the base score weighting, β to define the Multi-Scale Convolution weighting, γ to define the Sinkhorn Optimal Transport weighting, and the final result is calculated using an adaptive fusion algorithm.\", \"page\": 1, \"index\": 1, \"width\": 542, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-004.webp\", \"caption\": \"Fig. 2. Inverted embedding is kept for capturing the variate dependency, and the multi-scale convolution is used to compensate for time correlations.\", \"page\": 3, \"index\": 4, \"width\": 542, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-005.webp\", \"caption\": \"Fig. 3. The optimal transport integrated into the attention mechanism (efficiently solved through the Sinkhorn algorithm) suppresses the influence of outliers, ensuring the rationality of the attention distribution.\", \"page\": 4, \"index\": 5, \"width\": 542, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-006.webp\", \"caption\": \"TABLE II MSC-OT WEIGHTING RESULTS ON 4 DATASETS IN DIFFERENT PREDICTION LENGTHS AND AVERAGE RESULTS\", \"page\": 5, \"index\": 6, \"width\": 496, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-002.webp\", \"caption\": \"Fig. 5. The ablation experiment based on ECL data shows that the combination of multi-scale convolution and optimal transport has a better optimization effect.\", \"page\": 6, \"index\": 2, \"width\": 542, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-003.webp\", \"caption\": \"Fig. 6. The ablation experiment based on Traffic data shows that iteration T=2 provides an excellent trade-off between approximation quality and computational efficiency.\", \"page\": 7, \"index\": 3, \"width\": 542, \"height\": 231}]"
motivation: 解决多元时间序列预测中难以同时捕捉多粒度结构模式和抑制噪声的问题。
method: 提出结合多尺度卷积增强与Sinkhorn最优传输正则化的倒置嵌入注意力架构MSC-OT。
result: 在ETT、电力、交通等数据集上取得优异的短期和长期预测性能，消融实验证实各模块有效且协同。
conclusion: MSC-OT通过多尺度卷积与最优传输优化注意力，有效提升多元时间序列预测精度。
---

## 摘要
多元时间序列分析在许多实际应用中发挥着重要作用，但在捕捉多粒度结构模式和适当抑制噪声方面仍然存在一些挑战性问题。本文提出了多尺度卷积与最优传输注意力（MSC-OT）方法。MSC-OT 是一种优化注意力机制的有效架构，它将多尺度卷积与基于倒置嵌入的 Sinkhorn 最优传输方法相结合。倒置嵌入方法将每个变量嵌入为一个标记，使模型能够更好地捕捉跨变量关系。MSC-OT 由两部分组成：（1）多尺度卷积增强，它对基于倒置嵌入的注意力分数矩阵应用多尺度卷积，捕捉由压缩时间表示诱导的变量交互空间中的局部结构模式；（2）Sinkhorn 最优传输正则化，它将注意力计算表述为一个最优传输问题，并采用迭代矩阵缩放来确保跨变量的信息流平衡。自适应融合策略利用 softmax 归一化的可学习权重，动态组合基础注意力、卷积增强和最优传输正则化的分数。在广泛使用的数据集上的实验，包括 ETT、电力、交通、太阳能和汇率数据集，表明 MSC-OT 在短期和长期预测任务中均表现出良好性能。消融实验进一步验证了每个提出组件的有效性，以及它们对提高多元时间序列预测精度的协同贡献。

## Abstract
The analysis of Multivariate Time Series (MTS) plays an important role in a lot of real-world practical applications, but it still remains some challenging problem about capturing multi-granularity structural patterns and suppressing noise appropriately. Multi-Scale Convolution with Optimal Transport Attention (MSC-OT) is proposed in this paper. MSC-OT is a useful architecture to optimize the attention mechanism. It combines multi-scale convolution with Sinkhorn optimal transport method based on inverted embedding. The inverted embedding approach embeds each variable as a token and allows the model to capture cross-variate relationships better. MSC-OT consists of two part: (1) Multi-Scale Convolution Enhancement, that applies multi-scale convolutions to attention score matrices based on inverted embedding, capturing local structural patterns in the variate-interaction space induced by compressed temporal representations; (2) Sinkhorn Optimal Transport Regularization, that formulates attention computation as an optimal transport problem and employs iterative matrix scaling to ensure balanced information flow across variates. Adaptive Fusion Strategy utilizes softmax-normalized learnable weights to dynamically combine base attention, convolution-enhanced, and OT-regularized scores. Experiments on widely-used datasets, including ETT, Electricity, Traffic, Solar-Energy, and Exchange-Rate, show that MSC-OT achieves well performance in both short-term and long-term forecasting tasks. Ablation experiments further validate the effectiveness of each proposed component and their synergistic contributions to improving prediction accuracy for multivariate time series forecasting.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多元时间序列（MTS）预测中，如何在有效建模变量间关系的同时，保留并利用时间维度的多粒度局部结构，并抑制数据中的异常噪声。
- **动机与背景**：
  - Transformer 在 MTS 预测中广泛使用，但传统方式（同时嵌入多变量同一时间步）侧重时间依赖，对跨变量关系建模不足。
  - ITransformer 提出**倒置嵌入**，将每个变量的完整时间序列压缩为一个 token，让注意力直接建模跨变量依赖，但这一压缩过程丢失了细粒度的时间局部模式。
  - 此外，标准 softmax 注意力容易受离群值干扰，导致注意力分布失衡。
  - 针对上述局限，本文提出 **MSC-OT**，旨在补偿倒置嵌入丢失的时间结构，并提升注意力的鲁棒性。

### 2. 论文提出的方法论
- **核心思想**：在倒置嵌入框架下，通过 **多尺度卷积增强** 恢复被压缩的局部时间结构，利用 **Sinkhorn 最优传输正则化** 实现平衡且抗噪的注意力，最后以 **自适应融合** 动态组合三种分数。
- **关键技术细节**：
  - **倒置嵌入基础**：变量 $n$ 的整段时序 $X_{:,n} \in \mathbb{R}^T$ 经线性投影得到 token $h_n = X_{:,n} W_{\text{embed}} + b_{\text{embed}}$，维度 $D$。
  - **多尺度卷积增强**：
    - 对注意力原始分数矩阵 $S = QK^T / \sqrt{d_k}$ （形状 $B \times H \times N \times N$）应用不同核尺寸（$3\times3$ 和 $5\times5$）的 2D 卷积，提取变量交互空间中多粒度的局部结构模式。
    - 多尺度输出取平均：$S_{\text{conv}} = \frac{1}{K}\sum_{i=1}^{K} \text{Conv2d}(S; k_i)$。
  - **Sinkhorn 最优传输正则化**：
    - 将注意力视为熵正则化最优传输问题，求解运输计划 $P^* = \arg\min_{P\in U(r,c)} \langle -S, P \rangle - \lambda H(P)$，其中 $H(P)$ 为熵，$r,c$ 为均匀边标量。
    - 使用 Sinkhorn 迭代算法（矩阵行列交替归一化）高效求解，得到双重随机矩阵 $P$。熵正则化平滑注意力分布，抑制离群值，确保信息流跨变量平衡。
  - **自适应融合**：
    - 最终分数 $S_{\text{final}} = \alpha \cdot S + \beta \cdot S_{\text{conv}} + \gamma \cdot P$，其中 $\alpha,\beta,\gamma$ 为可学习参数，经 softmax 归一化后动态调整。
    - 初始值设为 $[0.7, 0.2, 0.1]$，模型在训练中自适应调整。
  - **数值稳定**：对基础分数进行 tanh 缩放至 $[-8,8]$，对 Sinkhorn 核进行截断与最大元素减法，防止溢出。

### 3. 实验设计
- **数据集**：
  - 共 5 个基准数据集：**ECL**（电力）、**ETT**（4 个子集：ETTh1, ETTh2, ETTm1, ETTm2）、**Exchange**（汇率）、**Traffic**（交通）、**Weather**（天气）。
- **评估设置**：
  - 输入长度固定 96，预测长度 $S \in \{96, 192, 336, 720\}$，覆盖短期与长期预测。
  - 指标：**MSE** 和 **MAE**（越低越好）。
- **对比方法**：
  - 包括 iTransformer、RLinear、PatchTST、Crossformer、TimesNet、SCINet、FEDformer、Autoformer 等近年代表性模型。

### 4. 资源与算力
- 文中**未明确说明**所使用的 GPU 型号、数量、单次训练时长或总体算力消耗。
- 仅提及优化器为 Adam（学习率 $1\times 10^{-4}$），批次大小 32，早停耐心值为 3 个 epoch，实验重复 3 次取平均。代码已在 GitHub 公开。

### 5. 实验数量与充分性
- **实验组合量较大**：
  - 5 个数据集（其中 ETT 含 4 个子集）× 4 个预测长度，共 **20 组主要对比实验**。
  - **消融实验**：验证去除多尺度卷积、去除 Sinkhorn OT、使用固定相等权重的影响，并分析 OT 正则化强度 $\lambda$ 和 Sinkhorn 迭代次数。
  - **权重学习分析**：展示了不同数据集、不同预测长度下自适应权重 $\alpha,\beta,\gamma$ 的变化，体现自适应融合的数据依赖特性。
- **充分性与公平性**：
  - 实验覆盖了多个领域和不同维度特性的数据集，预测长度设置标准，对比方法丰富且具有代表性。
  - 消融实验系统地剥离各组件，验证了协同效应。
  - 公平性方面，基于统一的倒置嵌入框架进行改进，对比 iTransformer 等，实验重复多次，客观上较为公平。

### 6. 论文的主要结论与发现
- MSC-OT 在 **ECL、Traffic、Weather** 三个数据集上取得了最优 MSE，在 **四** 个数据集（除 ETT 平均外）取得了最优 MAE，整体性能显著优于现有主流模型。
- **多尺度卷积增强** 和 **Sinkhorn 最优传输正则化** 不仅各自有效，且具有**协同效应**：卷积捕捉局部结构，最优传输平衡信息并抑制噪声，二者结合比单独使用提升更明显。
- 自适应融合权重在不同数据集上呈现差异化分布（如 ECL 中卷积与 OT 权重占比超 50%），证明动态融合的必要性。
- Sinkhorn 迭代次数 **2** 即可实现精度与效率的良好折衷。

### 7. 优点
- **方法创新**：首次将 Sinkhorn 最优传输作为注意力机制的**补充正则化项**而非完全替代，与标准注意力和卷积增强分数自适应融合，既保留判别力又提升鲁棒性。
- **架构设计精巧**：多尺度卷积直接作用于注意力分数矩阵（变量-变量空间），以极小的计算代价恢复被压缩的局部时序结构。
- **数值鲁棒性**：设计了多层数值稳定策略（tanh 裁剪、核稳定化），使训练可在含有极端值的时间序列上平稳进行。
- **可解释性强**：通过可学习权重直观展示各组件在不同数据上的贡献，消融实验全面，支撑结论。
- **性能优异**：在多个标准基准上超越 iTransformer 及其他强基线，尤其在 Traffic 等高噪高维度任务中优势明显。

### 8. 不足与局限
- **适用范围受限**：方法专门为倒置嵌入设计，应用于传统时序嵌入方式（如按时间步嵌入）时优势减弱，通用性有限。
- **缺少最新基线**：对比方法主要截至 2024 年前后，未纳入可能更近期的时序预测模型（如 TimeMixer、ModernTCN 文中虽有提及但并未作为对比主表？实际上表 I 未包含 TimeMixer、ModernTCN，仅引用在相关工作）。
- **资源透明度不足**：未报告任何 GPU 型号、训练时长与能耗，难以评估实际部署成本。
- **超参数敏感性**：$\lambda$（OT 正则化强度）、初始融合权重及卷积核尺寸等对性能有影响，调参依赖经验。
- **大规模场景未测试**：实验主要集中在常规基准，未在更大规模或工业级数据集（如百万级序列）上验证。
- **解释性可深化**：对卷积捕获的“局部结构模式”以及 OT 如何平衡信息流的分析停留在现象层面，缺乏更深入的理论机制剖析。

（完）
