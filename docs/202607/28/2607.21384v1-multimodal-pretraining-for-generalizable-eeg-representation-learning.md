---
title: Multimodal Pretraining for Generalizable EEG Representation Learning
title_zh: 面向泛化脑电图表示学习的多模态预训练
authors: "Targol Bakhtiarvand, Jugal Kalita, Adham Atyabi"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21384v1"
tags: ["query:ndai"]
score: 9.0
evidence: 多模态脑电图基础模型
tldr: 针对脑电图模型泛化性差的问题，本研究提出多模态预训练基础模型，融合原始信号、时频图和文本信息，采用自监督学习获得癫痫相关表征。在CHB-MIT数据集上微调后，模型性能达当前最优（AUROC 0.878），并展现良好跨数据集适应性与可解释病灶定位能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21384v1/fig-001.webp\", \"caption\": \"Fig. 1. Architecture of the proposed multimodal EEG foundation model. (A) Input: raw EEG windows and CWT scalograms. (B) Dual Encoder: Mamba-based raw encoder, TF Transformer encoder, and retrievalaugmented text branch, each projecting to 256-D. (C) Fusion and pretraining objectives. For downstream seizure detection, only channel adapters and classification head (7.5% of parameters) are fine-tuned.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2607.21384v1/fig-002.webp\", \"caption\": \"TABLE II LOSO RESULTS PER SUBJECT. EVAL BAL.ACC IS COMPUTED ON THE VALIDATION SUBJECT; TEST BAL.ACC ON THE HELD-OUT TEST SUBJECT.\", \"page\": 4, \"index\": 2, \"width\": 425, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2607.21384v1/fig-003.webp\", \"caption\": \"Fig. 5. GradCAM-based interpretability analysis for a seizure window from subject chb22 (predicted probability = 0.717). Top-left: Raw EEG signals (23 channels) with GradCAM saliency overlay (red indicates high importance), highlighting salient transient events. Top-right: Attention pooling weights over time from the raw branch, showing increasing focus toward later segments. Bottom-left: Time-frequency GradCAM map (frequency × time), with high saliency concentrated in mid-to-high frequency bands corresponding to ictal activity. Bottom-right: Attention weights from the time-frequency branch, confirming frequency-localized importance. These results demonstrate that the model focuses on physiologically meaningful EEG patterns.\", \"page\": 5, \"index\": 3, \"width\": 512, \"height\": 318}]"
motivation: 现有脑电图模型通常局限于特定数据集和任务，难以泛化到不同场景，亟需开发可泛化的癫痫检测基础模型。
method: 模型结合Mamba原始信号编码器、ViT时频编码器和文本编码器，通过掩码建模、跨视图对比对齐和时序一致性损失进行自监督预训练。
result: 在CHB-MIT基准上集成模型取得AUROC 0.878的最优性能，并在留一患者测试和跨数据集评估中表现出鲁棒性。
conclusion: 多模态预训练策略显著提升了脑电图表征的泛化能力，实现了鲁棒的癫痫检测和可解释的病灶定位。
---

## 摘要
用于癫痫的脑电图（EEG）模型通常局限于特定数据集和任务。这种局限的方法使得将这些模型应用于不同数据集或不同场景变得具有挑战性。然而，近期在基础模型和自监督学习方面的研究表明，一个适应性强的EEG骨干网络可以支持一系列与EEG相关的任务。在本研究中，我们开发了一个多模态EEG基础模型，该模型结合了基于Mamba架构的原始信号编码器、用于时频数据的Vision Transformer（ViT）风格编码器以及用于文本的轻量级编码器，所有这些都位于一个共享嵌入空间中。预训练过程依赖于几种创新技术，如掩码建模、跨视图对比对齐和时间一致性损失。这些方法旨在无需标注数据的情况下创建丰富的、与癫痫发作相关的表征。为了评估我们预训练模型的有效性和泛化能力，我们在经典的CHB-MIT癫痫发作检测基准和额外的癫痫发作检测数据集上对其进行了微调，并进行了广泛的实验以比较不同的模型变体。在标准的CHB-MIT划分上，我们最佳的单模型达到了0.874的AUROC，而集成模型变体达到了0.878的AUROC，这在该基准上代表了最先进的性能。除了标准的训练-测试划分之外，我们还在留一被试（LOSO）协议下评估了性能，这在之前的EEG癫痫发作建模工作中很少报道，并凸显了患者独立性癫痫发作检测的难度，在19名被试上的平均LOSO平衡准确率为0.558。跨数据集和评估设置，我们的多模态基础模型实现了稳健的癫痫发作检测，并易于适应新的癫痫发作检测场景，同时还支持可解释的癫痫发作定位。

## Abstract
Electroencephalography (EEG) models used for epilepsy are often limited to specific datasets and tasks. This limited approach can make it challenging to apply these models across different datasets or in various situations. However, recent studies in foundation models and self-supervised learning suggest that an adaptable EEG backbone could support a range of EEG related tasks. In this study, we have developed a multimodal EEG foundation model that combines a raw signal encoder based on the Mamba architecture, a Vision Transformer (ViT)-style encoder for time-frequency data, and a lightweight encoder for text, all within a shared embedding space. The pretraining process relies on several innovative techniques, such as masked modeling, cross-view contrastive alignment, and temporal consistency losses. These methods are designed to create rich, seizure-relevant representations without requiring labeled data. To assess the efficacy and generalization of our pretrained model, we fine-tuned it on the canonical CHB-MIT seizure detection benchmark and additional seizure detection datasets, and conducted extensive experiments comparing different model variants. On the standard CHB-MIT split, our best single model achieved an AUROC of 0.874, and an ensemble variant reached 0.878 AUROC, representing state-of-the-art performance on this benchmark. In addition to standard train-test splits, we evaluated performance under a leave-one-subject-out (LOSO) protocol, which is rarely reported in prior EEG seizure modeling work and highlights the difficulty of patient-independent seizure detection, with a mean LOSO balanced accuracy of 0.558 across 19 subjects. Across datasets and evaluation settings, our multimodal foundation model enabled robust seizure detection and straightforward adaptation to new seizure detection scenarios, while also supporting interpretable seizure localization.

---

## 论文详细总结（自动生成）

# 论文总结：面向泛化脑电图表示学习的多模态预训练

## 1. 论文的核心问题与整体含义
- **研究动机**：脑电图（EEG）的癫痫检测模型通常局限于特定数据集和任务，难以在不同患者、不同采集环境或不同数据集间泛化。主要挑战包括信号信噪比低、个体间差异大、标注成本高以及公开数据集之间采样率、通道布局、参考方案不一致。
- **核心问题**：如何构建一个能够灵活迁移到多种癫痫检测场景、同时具备跨患者泛化能力的 EEG 表示模型。
- **整体含义**：提出一种多模态 EEG 基础模型，通过联合学习原始信号、时频图谱和文本信息，在无标注数据上进行大规模预训练，从而获得与癫痫发作相关的通用表示，实现高精度、可解释的癫痫检测，并为更广泛的 EEG 分析任务提供可复用的骨干网络。

## 2. 方法论
- **总体思想**：将 EEG 窗口从两个互补视角建模：原始时域信号与连续小波变换（CWT）生成的时频图谱，并引入文本分支提供额外的语义上下文，三个模态的嵌入被对齐到同一个共享空间。
- **模型架构**：
  - **原始编码器**：接收 $X_{\text{raw}} \in \mathbb{R}^{C \times T}$（$T=3840$），经 1D 卷积 patch embedding（patch_size=64）得到 $N = 60$ 个 token，送入 Mamba 骨干网络，输出 token 级特征 $z_{\text{raw}}$ 和池化后的窗口表示 $e_{\text{raw}} \in \mathbb{R}^{256}$。
  - **时频编码器**：CWT 生成 $X_{\text{tf}} \in \mathbb{R}^{C \times F \times T}$（$F=64$ 频点），通过 ViT 风格的 Conv2D patch embedding 得到时频 token，再由轻量级 Transformer 编码为 $z_{\text{tf}}$ 和 $e_{\text{tf}} \in \mathbb{R}^{256}$。
  - **文本分支**：用 FAISS 索引的 EEG 语料库检索 top-k 段落，经冻结的句子编码器得到 384 维嵌入，再投影到 256 维，与 EEG 表示通过对比损失对齐。
  - **融合**：$e_{\text{fused}} = \text{MLP}([e_{\text{raw}}; e_{\text{tf}}])$，得到最终的窗口级表示。
- **预训练目标**：加权组合四种损失函数：
  $$
  \mathcal{L} = \lambda_m \mathcal{L}_{\text{mask}} + \lambda_f \mathcal{L}_{\text{future}} + \lambda_c \mathcal{L}_{\text{nce}} + \lambda_t \mathcal{L}_{\text{text}}
  $$
  - $\mathcal{L}_{\text{mask}}$：掩码重建损失，学习稳健的时序表征。
  - $\mathcal{L}_{\text{future}}$：时序一致性/未来预测损失，保持时间上的平滑性。
  - $\mathcal{L}_{\text{nce}}$：原始与时频视图间的 InfoNCE 对比损失，推动两个 EEG 视图的对齐。
  - $\mathcal{L}_{\text{text}}$：EEG 与文本之间的对比损失，实现语义对齐。
- **微调策略**：仅更新 7.5% 的参数（通道适配器和分类头），保持预训练骨干冻结，极大降低灾难性遗忘的风险，同时实现高效适应。

## 3. 实验设计
- **数据集**：
  - 预训练：TUH EEG 和 SEED-DV（部分无标签/有情绪标签，提供大规模无监督数据）。
  - 下游评估：CHB-MIT 头皮 EEG 数据集（24 名难治性癫痫儿童），包含发作标注。
- **预处理**：所有信号统一为非重叠 15 秒窗口，重采样至 256 Hz，带通滤波（0.5–70 Hz），每通道 z-score 标准化，并计算 CWT 时频图。
- **评估协议**：
  - **LOSO（留一被试）**：对每位测试被试，使用其余所有被试训练，严格评估患者独立性泛化。
  - **BrainRVQ 固定划分**：使用被试 1–19 训练，20–21 验证，22–23 测试，与已发表基准直接比较。
  - **5 折交叉验证**：在被试 22–23 上做 5 折训练，报告平均和集成结果。
- **对比方法**：包括 EEGNet、ST-Transformer、BENDR、BIOT、LaBraM、CBraMod 以及 BrainRVQ 等主流或最新的 EEG 模型，且均为全参数微调。
- **评价指标**：AUROC、AUC-PR、平衡准确率（BAcc）、TPR、TNR 等。

## 4. 资源与算力
- 论文中**未明确说明**使用的 GPU 型号、数量、显存以及具体的训练时长。
- 仅提及优化器为 AdamW，初始学习率 $3 \times 10^{-4}$，训练 20,000 步，batch size 为 32，并在 10k 步进行学习率衰减。微调阶段采用 cosine 退火、warmup、梯度累积等策略，但未报告实际硬件消耗。

## 5. 实验数量与充分性
- **实验组数**：至少包含三套不同严格程度的评估协议（LOSO、固定划分、5 折 CV），覆盖零样本跨被试、同分布测试和集成验证；另外还进行了可解释性分析（GradCAM、发作起始定位）。
- **充分性与公平性**：
  - 在 LOSO 协议下逐一报告了 19 名被试的结果，提供了罕见的严格患者独立测试基准，避免了数据泄露。
  - 与多个代表性 SOTA 方法在同一固定划分下对比，且输出参数高效微调的结果，对比公平。
  - 然而，少样本校准实验仅覆盖被试 chb01–chb05，被试覆盖度不足；早期检测分析仅示例子 chb22，未进行大规模统计验证。消融实验未单独展开（如模态缺失、损失函数缺省），但在表 III 中间接通过不同基线的单模态或双模态架构体现了模态融合的优势。
  - 总体实验数量对基础模型评估较为充分，但缺乏更多外部数据集的大规模验证和不同预训练数据量的消融，严格性尚有提升空间。

## 6. 论文的主要结论与发现
- **多模态对齐有效**：融合原始信号、时频图和文本对齐的预训练表示，在下游癫痫检测中显著优于单模态或未融合的基础模型。
- **最先进性能**：在 BrainRVQ 固定划分下，仅微调 7.5% 参数即达到平衡准确率 0.781、AUC-PR 0.709，较 BrainRVQ 提升 52.5%；在 5 折 CV 集成后获得 0.878 的 AUROC，刷新 CHB-MIT 基准记录。
- **患者独立检测极其困难**：LOSO 平均 BAcc 仅 0.558，TPR 0.570，个体间差异巨大，强调严格交叉被试评估的必要性。
- **可解释性**：GradCAM 可视化证实模型关注到与临床已知痫样放电（spike-and-wave）匹配的时频模式，并且能在标注发作起始前约 45 秒提前预警。
- **参数高效迁移**：冻结骨干、仅微调极小部分参数即可达到最佳效果，证明预训练表示的通用性。

## 7. 优点
- **多模态联合学习**：将原始波形、时频信息和文本知识融合进统一框架，充分利用生理信号的多视角互补性。
- **严格的评估标准**：首次在 EEG 基础模型上实施 LOSO 全被试交叉验证，提供真实泛化能力下限，推动了评测标准化。
- **参数高效微调**：仅更新不到 8% 的参数，避免灾难性遗忘，同时保持高性能，适合临床部署。
- **可解释性支持**：通过梯度归因和发作起始定位，增强了模型决策的生理学可信度，有助于临床信任。

## 8. 不足与局限
- **LOSO 性能仍然偏低**：平均平衡准确率仅 0.558，无法完全满足临床零适应场景需求，模型对部分患者（如 chb17）完全失效。
- **少样本实验覆盖面窄**：受限于被试数量，不能充分反映患者间多样性，少样本微调策略效果不佳。
- **早期检测验证单一**：仅在一位被试（chb22）上展示 45 秒提前检测，缺乏跨被试统计显著性，鲁棒性存疑。
- **文本分支的信息源限制**：所用文本为通用 EEG 知识库检索，而非患者个体化的临床报告或病史，可能限制了更精准的语义引导。
- **对比基线非全部严格相同尺度**：部分已有模型采用全参数微调，本文采用参数高效方案，AUROC 比较时需注意公平性前提。
- **缺乏外部多中心独立验证**：虽提及跨数据集适应性，但主要量化结果仍集中在 CHB-MIT，对于其他来源（如不同医院、不同年龄）的泛化性未充分验证。

（完）
