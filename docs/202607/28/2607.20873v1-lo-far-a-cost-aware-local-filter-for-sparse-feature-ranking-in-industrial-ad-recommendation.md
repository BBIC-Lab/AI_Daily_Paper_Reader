---
title: "LO-FAR: A Cost-Aware Local Filter for Sparse Feature Ranking in Industrial Ad Recommendation"
title_zh: LO-FAR：面向工业广告推荐中稀疏特征排序的成本感知局部过滤器
authors: "Egemen Erbayat, Luis Duque, Sohini Roychowdhury, Mohammad Amin, Srihari Reddy"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20873v1"
tags: ["query:ndai"]
score: 8.0
evidence: 使用局部估计器根据独立预测信号对稀疏特征进行排序
tldr: 工业广告推荐模型依赖高成本稀疏 ID 特征，需在计算预算内定期重排特征。本文提出 LO-FAR，一种仅用 CPU 的模型无关局部过滤方法，使用独立保留信号排序特征，避免 GPU 重训练。在百万级生产数据上，2 CPU 小时内完成排序，下游 CTR 与 CVR 任务中归一化熵增益与排列重要性等复杂方法相当，证明了轻量局部过滤器在成本敏感场景中的实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20873v1/fig-002.webp\", \"caption\": \"Figure 1: Downstream NE gain (ΔNE) relative to a dense-only reference model trained without sparse features. Each curve corresponds to a ranking method, and the horizontal axis sweeps the retained feature budget 𝐵. LO-FAR remains competitive with shuffle-based importance and BSN across realistic feature budgets while requiring substantially less ranking compute.\", \"page\": 8, \"index\": 2, \"width\": 1056, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2607.20873v1/fig-001.webp\", \"caption\": \"Figure 2: Jaccard similarity over the top-𝑁 retained features for each pair of ranking methods. Similarity is reported as a diagnostic only; end-to-end NE gain (Figure 1) is the primary evaluation criterion. LO-FAR overlaps strongly with the naive baseline and diverges from BSN, as expected from their differing treatments of feature interactions.\", \"page\": 8, \"index\": 1, \"width\": 506, \"height\": 250}]"
motivation: 稀疏特征排序受限于生产环境的计算预算和快速迭代需求，亟需低成本、快速的替代方案。
method: 采用基于独立保留信号的轻量局部估计器在 CPU 上排序特征，无需模型重训练。
result: 在 2 CPU 小时内完成 475 个特征的排序，保留 100–400 特征时，下游任务增益与重排、随机门等方法持平。
conclusion: LO-FAR 证明在严格成本和周转约束下，简单局部过滤器可作为一种可部署的生产选择。
---

## 摘要
工业广告推荐模型高度依赖稀疏、高基数ID列表特征，这些特征编码了用户历史记录和上下文标识符。每个特征都由一个专用的嵌入表支撑，因此这些特征占据了存储、训练和服务成本的主导地位，并且必须随着流量和下游模型的演变而重新评估。因此，稀疏特征排序不仅是一个离线建模问题，也是一个受计算预算和迭代节奏限制的反复出现的系统决策。我们提出了局部化特征排序(LO-FAR)，这是一个仅使用CPU、与模型无关的工作流程，它利用轻量级的局部估计器，根据每个候选特征独立的保留预测信号进行排序，而不是依赖基于排列和随机门的方法所需的GPU约束的重新训练循环。在一个包含超过一百万条已记录交互和475个稀疏ID列表特征的生产数据集上，LO-FAR在大约两小时的CPU时间内完成了排序，并在保留100到400个特征的预算范围内，在下游的点击率和转化率任务上保持了与基于洗牌的重要性、二元随机神经元和基于覆盖率的启发式方法相竞争的归一化熵增益。贡献在于一个可部署的工作流程，表明当成本和周转时间约束成为限制时，简单的局部过滤器可以成为比更重的交互感知替代方案更实用的生产选择。

## Abstract
Industrial ad recommendation models rely heavily on sparse, high-cardinality ID-list features that encode user histories and contextual identifiers. Each is backed by a dedicated embedding table, so these features dominate storage, training, and serving cost and must be revisited as traffic and downstream models evolve. Therefore, sparse feature ranking is not just an offline modeling problem but also a recurrent systems decision limited by compute budgets and iteration cadence. We present Localized Feature Ranking (LO-FAR), a CPU-only, model-agnostic workflow that ranks each candidate feature from its stand-alone held-out predictive signal using lightweight local estimators rather than the GPU-bound retraining loops of permutation- and stochastic-gate-based methods. On a production dataset of more than one million logged interactions and 475 sparse ID-list features, LO-FAR completes ranking in approximately two CPU-hours and preserves downstream Normalized Entropy gains on CTR and CVR tasks that are competitive with shuffle-based importance, Binary Stochastic Neurons, and a coverage-based heuristic across budgets of 100--400 retained features. The contribution is a deployable workflow showing that, when cost and turnaround constraints are binding, a simple local filter can be a practical production choice over heavier interaction-aware alternatives.