---
title: "SiamJEPA: On the Role of Siamese Student Encoders in JEPA"
title_zh: SiamJEPA：论Siamese学生编码器在JEPA中的作用
authors: Makoto Yamada
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.04044v1"
tags: ["query:ndai"]
score: 9.0
evidence: 研究孪生学生编码器用于自监督表示学习
tldr: 联合嵌入预测架构(JEPA)通过预测隐空间嵌入实现自监督学习，现有方法多采用单编码器，而大脑启发的Siamese编码器作用未知。本文探究Siamese学生编码器在JEPA中的角色，提出SiamJEPA，使用带指数移动平均教师的掩码Siamese学生网络。实验表明Siamese编码器作为正则化器，提升表示可分性并加速早期训练，在有限资源下优于单编码器JEPA和掩码自编码器。结论指出Siamese架构是重要的归纳偏置，为JEPA设计提供了简单有效的改进方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04044v1/fig-005.webp\", \"caption\": \"Figure 1: JEPA and SiamJEPA architectures. Sim-1 is a loss function to align the Siamese encoders. The dashed line represents the StopGradient operator. In our implementation, the two masking sets are disjoint. Note that the SiamJEPA architecture is inspired by the brain-inspired representation learning called PhiNet and it can be regarded as a masked prediction variant of PhiNet is the SiamJEPA model.\", \"page\": 4, \"index\": 5, \"width\": 737, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2607.04044v1/fig-001.webp\", \"caption\": \"Table 1: Pretraining and linear probing configurations.\", \"page\": 7, \"index\": 1, \"width\": 577, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2607.04044v1/fig-002.webp\", \"caption\": \"Table 2: ImageNet linear probing performance comparison. The results show that enforcing consistency between the Siamese student encoders substantially improves the quality of the learned representations, leading to higher ImageNet linear probing performance. For all SiamJEPA variants, we use a weight decay of 0.1. Note that MAE is evaluated using the CLS token, whereas SiamJEPA is evaluated using mean pooling. † We conduct the experiments using the official implementation available at https://github.com/facebookresearch/mae. Results for the original I-JEPA are taken from [Assran et al., 2023] (600 training epochs) and are not directly comparable to our 400- epoch setting.\", \"page\": 7, \"index\": 2, \"width\": 510, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2607.04044v1/fig-004.webp\", \"caption\": \"Figure 2: Learning curves for λKL = 0.00001 and λKL = 0.01. With a small regularization coefficient, the KL divergence between the representations produced by the two Siamese student encoders remains large. In contrast, with a larger regularization coefficient, the KL divergence quickly converges to the free-bit threshold (0.1 in our experiments). The linear probing performance follows a similar trend. The training loss starts at a value above 2, drops sharply during the initial stage of training, and then decreases more gradually as learning progresses.\", \"page\": 8, \"index\": 4, \"width\": 734, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2607.04044v1/fig-003.webp\", \"caption\": \"Table 5: Ablation study of SiamJEPA on ImageNet linear probing. Each row modifies one component from the baseline configuration. We set the decoder depth to 1 and the free-bit of KL as 0.1, respectively.\", \"page\": 10, \"index\": 3, \"width\": 714, \"height\": 228}]"
motivation: 探索Siamese学生编码器在JEPA中的未充分利用的潜力，因其更贴合大脑启发的表示学习框架。
method: 提出SiamJEPA，采用带指数移动平均(EMA)教师网络的掩码Siamese学生编码器进行联合嵌入预测。
result: 实验表明Siamese编码器提高表示可分性和早期训练速度，在有限训练预算下线性探测准确率超越单编码器JEPA变体和MAE。
conclusion: Siamese学生编码器不仅是架构选择，而是预测表示学习的重要归纳偏置，为JEPA模型设计提供了新见解。
---

## 摘要
近年来，联合嵌入预测架构（JEPA）作为自监督表示学习的一种有前景的框架，在计算机视觉和机器学习领域引起了广泛关注。与重建像素的掩码自编码器不同，JEPA模型通过预测被掩码区域的潜在嵌入来学习表示。现有的基于JEPA的方法，如I-JEPA和V-JEPA，通常在学生网络中使用单一编码器。相比之下，在学生网络中使用Siamese编码器更自然地与受脑启发的表示学习框架相一致，然而它们在JEPA模型中的作用尚未被充分探索。本文研究了Siamese学生编码器在基于JEPA的表示学习中的效果。为此，我们提出了SiamJEPA，一种配备了指数移动平均（EMA）教师网络的掩码Siamese学生编码器。SiamJEPA也可以被视为受脑启发的表示学习模型PhiNet的JEPA形式化表示。通过在ImageNet上的线性探测实验，我们证明Siamese编码器作为JEPA目标函数的有效正则化器，提高了表示的可分离性，并加速了训练早期阶段的学习。此外，在有限的训练预算下，SiamJEPA始终优于同类的单一编码器JEPA变体，并实现了比需要更长训练时间的掩码自编码器（MAE）更高的线性探测准确率。我们的发现表明，Siamese学生编码器不仅是一种架构选择，而且构成了预测性表示学习的重要归纳偏置。这些结果为基于JEPA的模型设计提供了新的见解，并暗示融入Siamese学生架构为改善自监督表示学习提供了一种简单而有效的方法。

## Abstract
Recently, Joint Embedding Predictive Architectures (JEPAs) have attracted significant attention in the computer vision and machine learning communities as a promising framework for self-supervised representation learning. Unlike masked autoencoders that reconstruct pixels, JEPA models learn representations by predicting latent embeddings of masked regions. Existing JEPA-based methods, such as I-JEPA and V-JEPA, typically employ a single encoder in the student network. In contrast, using Siamese encoders for student network is more naturally aligned with brain-inspired representation learning frameworks, yet their role in JEPA models remains largely unexplored. In this paper, we investigate the effect of Siamese student encoders in JEPA-based representation learning. To this end, we propose SiamJEPA, masked Siamese student encoders equipped with an exponential moving average (EMA) teacher network. SiamJEPA can also be viewed as a JEPA formulation of the brain-inspired representation learning model PhiNet. Through extensive experiments on ImageNet linear probing, we demonstrate that Siamese encoders act as an effective regularizer for the JEPA objective, improving representation separability and accelerating learning during the early stages of training. Furthermore, SiamJEPA consistently outperforms comparable single-encoder JEPA variants under limited training budgets and achieves higher linear probing accuracy than Masked Autoencoders (MAE) which requires longer training. Our findings reveal that Siamese student encoders are not merely an architectural choice but constitute an important inductive bias for predictive representation learning. These results provide new insights into the design of JEPA-based models and suggest that incorporating Siamese student architectures offers a simple yet effective approach for improving self-supervised representation learning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：在联合嵌入预测架构（JEPA）中，Siamese（孪生）学生编码器究竟扮演什么角色？以往JEPA模型（如I‑JEPA、V‑JEPA）几乎都仅使用单一学生编码器，而受脑启发的表示学习方法（如PhiNet）则天然采用Siamese结构，但在JEPA框架下缺乏系统分析和理解。
- **整体含义**：本文旨在填补这一空白，揭示Siamese学生编码器不仅仅是架构选择，而是JEPA预测性表示学习中的一种**有效归纳偏置与正则化工具**，能提升表示质量、加速早期收敛，并在有限训练预算下超越重建类方法（如MAE）。

### 2. 方法论
- **整体架构**：提出 **SiamJEPA**，由共享权重的**两个Siamese学生编码器**（Vision Transformer）和通过指数移动平均（EMA）更新的**教师编码器**组成。输入图像经两路独立、非重叠的掩码处理后，分别送入两个学生编码器，教师编码器接收完整图像。
- **关键技术细节**：
  - 掩码策略：掩码集 $M_1$、$M_2$ 不相交，以防止捷径学习。
  - 预测器网络：
    - 线性预测器 $h$ 用于对齐两个学生编码器的全局表示（CLS token）。
    - 概率预测器 $g$（Transformer）基于后验分布 $q(Z \mid h(H^{(1)}_{\text{cls}}), H^{(2)}_{\text{cls}})$ 和先验分布 $p(Z \mid h(H^{(1)}_{\text{cls}}))$ 来预测被掩码区域的潜在嵌入。
  - 损失函数：
    - **Sim‑1（对齐损失）**：对称的KL散度，使从单视图（先验）能预测另一视图（后验），并在先验支路使用停止梯度 $\text{sg}$。
    - **Sim‑2（预测损失）**：对两个方向的预测输出 $\hat{Y}^{(1)}$、$\hat{Y}^{(2)}$ 与教师编码器输出 $Y$ 在未被两路掩码覆盖的 patchtokens 上计算归一化均方误差（NMSE）。
    - 最终总损失：
      $$L = \frac{1}{2}(\text{MSE}^{(1)} + \text{MSE}^{(2)}) + \frac{\lambda_{\text{KL}}}{2}(\text{KL}^{(1)}_{\text{sg}} + \text{KL}^{(2)}_{\text{sg}}).$$
      $\lambda_{\text{KL}}$ 控制Siamese正则化强度；当 $\lambda_{\text{KL}}=0$ 时可退化为类似单编码器JEPA的变体。
  - 额外机制：KL项使用**自由比特（free‑bit）阈值**以防止分布完全一致而引发表示坍缩。

### 3. 实验设计
- **数据集与评估**：在ImageNet‑1K上预训练，以**线性探测（linear probing）的Top‑1准确率**为主要评价指标。
- **对比方法**：
  - 掩码自编码器（MAE，ViT‑Base，官方实现，400 epochs）。
  - 上下文自编码器（CAE，1600 epochs，仅作参照）。
  - I‑JEPA（原始论文600 epochs，因epoch数不同仅作粗略对照）。
- **基线配置**：ViT‑Base，掩码比0.75，解码器深度1，批大小8192，AdamW优化器，学习率1.5×10⁻⁴，权重衰减0.05/0.1，EMA动量从0.99升至0.9999，线性探测训练90 epochs。
- **消融实验**涵盖：KL正则化系数 $\lambda_{\text{KL}}$、自由比特阈值、掩码策略（随机掩码 vs. 块掩码）、掩码比率、权重衰减、表示提取方式（CLS token、不同层均值池化）、预测器深度等。

### 4. 资源与算力
- 论文明确提到实验使用 **NVIDIA V100、A100 和 H100 GPU**，但**未给出具体GPU数量或总GPU小时数**。
- 预训练轮次多为300 epochs（消融）和400 epochs（最终对比），批大小8192，ViT‑Base参数量，总体算力需求中等但受限于现有报告细节。

### 5. 实验数量与充分性
- **实验组数**：包含系统性的多维消融（至少5组关键超参数研究、2种掩码策略、3种掩码比、2种权重衰减、3种表示目标等），以及最终与多个基线的主表格比较。
- **充分性**：实验设计合理，致力于**公平对比**（基于同一MAE代码库实现SiamJEPA，避免实现差异），但部分观察（如预测器深度影响）受计算资源限制未能彻底验证。整体而言，消融实验较为全面，足以支撑核心结论。
- **客观性**：明确指出与I‑JEPA的对比不直接（训练轮次不同），未追求SOTA，目的明确且聚焦于探究Siamese编码器的作用。

### 6. 主要结论与发现
- Siamese学生编码器作为**有效正则化器**，通过KL一致性约束提升表示的可分离性。
- 适当提高 $\lambda_{\text{KL}}$（如0.01）能显著加速早期收敛并提升最终线性探测性能。
- **块掩码**性能优于随机掩码，但随机掩码经调参后仍具竞争力，降低了实施复杂度。
- 较大权重衰减（0.1）有利于长时间训练，中间层均值池化比CLS token或末层池化提供更具迁移性的表示。
- 在仅400 epochs下，SiamJEPA不仅远超MAE（61.9% → 70.2%），且可在训练效率上媲美更长训练的CAE，确认Siamese架构是一种简单而高效的归纳偏置。

### 7. 优点
- **首次系统研究**Siamese学生编码器在JEPA中的角色，填补了领域空白。
- 方法**简洁通用**，仅增加一个编码器分支与轻量KL损失，易于集成到现有JEPA框架。
- 实验强调**公平性和对照性**，基于统一代码库复现MAE等基线，结论可信度高。
- 训练效率优势明显，为低资源或快速迭代场景提供了实用方案。
- 深入分析揭示了KL正则化、掩码策略和表示提取方式等关键因素的影响。

### 8. 不足与局限
- **模型规模有限**：所有实验仅基于ViT‑Base，未探究在ViT‑Large/Huge上的扩展性。
- **评估任务单一**：仅使用ImageNet线性探测，未涉及语义分割、检测或迁移学习等下游任务，泛化性证据不足。
- **部分对比不直接**：I‑JEPA的训练轮次不一致，且未与更近期的JEPA变体（如LeJEPA、V‑JEPA）公平对齐。
- **部分现象未定论**：如预测器深度的影响因计算资源不足而未能彻底厘清，可能限制实践调参指导。
- **仅限图像领域**：未扩展到视频或多模态数据，而Siamese编码器在视频预测中已被证明有效，未探索其跨模态一致性。

（完）
