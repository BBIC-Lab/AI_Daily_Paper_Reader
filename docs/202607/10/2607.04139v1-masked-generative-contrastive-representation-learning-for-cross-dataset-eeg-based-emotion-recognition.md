---
title: Masked Generative-Contrastive Representation Learning for Cross-Dataset EEG-Based Emotion Recognition
title_zh: 基于掩码生成-对比表示学习的跨数据集脑电情绪识别
authors: "Huqin Weng, Jiayang Huang, Yimin Wen, Jie Du, Chi-Man Vong, Chuangquan Chen"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04139v1"
tags: ["query:ndai"]
score: 9.0
evidence: 对EEG神经信号进行自监督表征学习
tldr: 针对跨数据集脑电情感识别中通道配置不一、噪声干扰和跨被试泛化难题，提出掩码生成-对比表示学习(MGCRL)框架。该框架结合区域图卷积时空编码器、基于JEPA的掩码生成学习以及掩码-原始特征对比学习，同时捕获细粒度噪声鲁棒表示和全局判别特征，在大规模FACED数据集预训练后，于多个SEED系列数据集上微调验证了卓越的跨数据集迁移能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04139v1/fig-004.webp\", \"caption\": \"Fig. 2. The overall structure of MGCRL. (a) MGCRL consists of three stages: pre-training, fine-tuning, and testing. During pre-training, the model jointly optimizes JEPA-based generative and masked dynamic contrastive learning module. The resulting model is fine-tuned on a specific dataset and evaluated on a unseen subject to ensure cross-subject generalization. (b) The Region-aware Spatiotemporal Encoder Module. (c) Masked Dynamic Contrastive Learning Module.\", \"page\": 4, \"index\": 4, \"width\": 1054, \"height\": 1046}, {\"url\": \"assets/figures/arxiv/2607.04139v1/fig-006.webp\", \"caption\": \"Fig. 3. EEG Channel Mapping (9×9 Grid vs. Brain Region Division), where 0 represents missing channels. (a) Dataset A: 62-channels. (b) Dataset B: 30- channels. (c) Dataset A: 17 brain regions (62-channels). (d) Dataset B: 17 brain regions (30-channels).\", \"page\": 5, \"index\": 6, \"width\": 485, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2607.04139v1/fig-003.webp\", \"caption\": \"Fig. 4. Experimental results of MGCRL and EEG2Rep on three datasets for each subject. (a) SEED-IV. (b) SEED-V. (c) SEED-VII.\", \"page\": 8, \"index\": 3, \"width\": 514, \"height\": 818}, {\"url\": \"assets/figures/arxiv/2607.04139v1/fig-005.webp\", \"caption\": \"Fig. 5. The results of fine-tuning different number of subjects on SEED-IV, SEED-V, and SEED-VII.\", \"page\": 9, \"index\": 5, \"width\": 490, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2607.04139v1/fig-001.webp\", \"caption\": \"Fig. 6. The brain region weights are visualized across the three datasets, with the top seven most important regions highlighted in red. Variations in color intensity and transparency indicate the relative weight of each region. (a) SEED-IV. (b)SEED-V. (c) SEED-VII.\", \"page\": 10, \"index\": 1, \"width\": 1056, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2607.04139v1/fig-002.webp\", \"caption\": \"TABLE IV LEAVE-ONE-SUBJECT-OUT EMOTION RECOGNITION ACCURACY ACROSS SESSIONS ON SEED-IV AND SEED-V DATASETS (ACC. AND STD. %).\", \"page\": 10, \"index\": 2, \"width\": 487, \"height\": 282}]"
motivation: 现有自监督学习方法难以有效处理脑电信号的复杂时空依赖、噪声干扰和跨被试差异，限制了跨数据集情感识别的泛化性能。
method: 设计区域感知时空编码器应对通道差异，并融合基于JEPA的生成学习与掩码-原始特征对比学习，分别提取细粒度和全局判别表示。
result: 在FACED预训练后，于SEED、SEED-IV等数据集微调，实验证实MGCRL显著提升了跨数据集情感识别准确率。
conclusion: MGCRL通过生成与对比联合学习，成功提取了通用且鲁棒的脑电情感特征，为跨数据集迁移提供新范式。
---

## 摘要
自监督学习通过改进特征表示和泛化能力，在跨数据集迁移方面展现出强大潜力。然而，其在基于脑电的情绪识别中的应用仍鲜有探索。现有自监督学习方法难以捕捉不同通道配置下脑电信号复杂的时空依赖关系，提取对噪声具有鲁棒性的细粒度表示，以及推导出在受试者间泛化良好的全局特征。为应对这些挑战，我们提出了掩码生成-对比表示学习（MGCRL），这是一个专为基于脑电的情绪识别设计的新型自监督学习框架。MGCRL基于区域感知的时空编码器，融合了生成学习和对比学习，以获取适用于跨数据集泛化的细粒度和全局判别性表示。MGCRL引入了三个关键设计：1）一个时空编码器，结合了基于区域的图卷积，以捕捉局部空间和功能关系，增强区域特定特征学习，并减轻不同数据集间脑电通道配置变化的影响；2）一种基于联合嵌入预测架构（JEPA）的生成学习机制，利用掩码特征捕获噪声鲁棒的细粒度表示，提升模型刻画微妙情绪状态的能力；3）一种对比学习策略，利用掩码和原始特征，在相同刺激下学习时间稳定和跨受试者不变的表示，从而增强情绪判别和跨受试者泛化。在这些设计下，MGCRL展现出学习通用表示的卓越能力。在大型FACED数据集上进行预训练并在多个SEED系列数据集上进行微调的广泛实验证明了MGCRL的有效性。

## Abstract
Self-supervised learning (SSL) shows strong potential for cross-dataset transfer by improving feature representation and generalization. However, its application to EEG-based emotion recognition remains largely unexplored. Existing SSL methods struggle to capture the intricate spatiotemporal dependencies of EEG signals under varying channel configurations, extract fine-grained representations resilient to noise, and derive global features that generalize well across subjects. To address these challenges, we propose Masked Generative-Contrastive Representation Learning (MGCRL), a novel SSL framework specifically designed for EEG-based emotion recognition. Built upon a region-aware spatiotemporal encoder, MGCRL integrates generative and contrastive learning to achieve both fine-grained and global discriminative representations for cross-dataset generalization. MGCRL introduces three key designs: 1) a spatiotemporal encoder that incorporates region-based graph convolution to capture localized spatial and functional relationships, enhancing region-specific feature learning and mitigating the impact of varying EEG channel configurations across datasets; 2) a generative learning mechanism based on the joint embedding predictive architecture (JEPA) that utilizes masked features to capture noise robustness fine-grained representations, improving the model's capability to characterize subtle emotional states; and 3) a contrastive learning strategy that leverages masked and original features to learn temporally stable and cross-subject-invariant representations across the same stimuli, boosting emotion discrimination and cross-subject generalization. Under these designs, MGCRL exhibits remarkable ability to learn universal representation. Extensive experiments involving pretraining on the large FACED dataset and fine-tuning on multiple SEED-series datasets demonstrate the effectiveness of MGCRL.

---

## 论文详细总结（自动生成）

好的，我将根据您提供的论文内容，生成一份结构化的详细中文总结。

### 1. 论文的核心问题与整体含义

本研究聚焦于**基于脑电图（EEG）的跨数据集情感识别**领域。其核心问题在于，现有深度学习模型依赖大量标注数据，且在特定数据集上训练的模型难以泛化到不同设备、不同通道配置和不同受试者群体的新数据集上。

-   **研究动机/背景**：
    -   EEG是构建情感脑机接口的关键技术，但**跨数据集迁移**面临严峻挑战，即数据集之间存在设备差异、标签不兼容和通道配置不匹配等异构性问题。
    -   现有自监督学习方法在应对这些挑战时存在三大局限：
        1.  **时空建模不足**：难以在通道配置变化的情况下，充分捕捉脑区间的交互关系。
        2.  **细粒度表示不佳**：多数生成式方法进行信号级重建，易受噪声干扰，无法有效刻画微妙的情绪状态。
        3.  **跨被试泛化能力弱**：多数方法未能充分建模个体差异和跨被试的情绪状态相似性，限制了泛化性。
    -   **整体含义**：本文旨在提出一个能够从大规模无标签EEG数据中学习**通用、可迁移、且鲁棒**的表示框架，以显著提升跨数据集情感识别的性能。

### 2. 论文提出的方法论

本文提出了一种新颖的自监督学习框架——**掩码生成-对比表示学习**。其核心思想是通过精巧的设计，使生成式和对比式学习范式相互补充，共同从原始脑电信号中提取既包含细粒度噪声鲁棒特征，又具有全局判别力、跨被试不变性的表示。

框架分为**预训练、微调、测试**三阶段，关键技术细节如下：

-   **统一空间模板**：将所有EEG信号投影到一个$9 \times 9$的网格上，缺失电极用零填充，以应对通道不一致问题。

-   **区域感知时空编码器**：
    -   **时域卷积**：首先使用$1 \times 5$的一维卷积核提取动态频率特征。
    -   **脑区内图卷积**：将大脑划分为17个区域。在每个区域内，动态构建图结构进行图卷积。
        -   邻接矩阵 $\mathbf{A}_{k,m}$ 由物理邻接矩阵和样本特定的动态功能邻接矩阵共同决定：$\mathbf{A}_{k,m} = \text{MASK}(\text{ReLU}(\mathbf{A}_{dyna,k,m} \circ (\mathbf{A}_{phys,m} + \mathbf{\Delta}_{k,m})))$。
        -   随后进行归一化：$\tilde{\mathbf{A}}_{k,m} = (\mathbf{D}_{k,m})^{-\frac{1}{2}} (\mathbf{A}_{k,m} + \mathbf{I}_m) (\mathbf{D}_{k,m})^{-\frac{1}{2}}$。
        -   图卷积操作：$\tilde{\mathbf{x}}_{out,k,m} = \text{ReLU}(\tilde{\mathbf{A}}_{k,m} \tilde{\mathbf{x}}_{k,m})$，之后对有效通道进行平均池化聚合，得到每个脑区的紧凑表征。
    -   **全局空间卷积**：对聚合后的所有脑区特征使用尺寸为$(M,1)$的卷积核，整合全局脑区空间信息。
    -   **时序注意力模块**：将特征送入由多头自注意力机制构成的Transformer编码器，捕获全局时序依赖性，得到最终的时域表征$\mathbf{h}_{pret}$。

-   **基于JEPA的生成式学习模块**：
    -   目标是通过**预测抽象特征**而非原始信号来学习语义依赖。
    -   **上下文编码器**：对输入特征$\tilde{\mathbf{x}}_{pret}$进行随机Mask，得到被遮挡的全局表征$\hat{\mathbf{h}}_{pret}$。
    -   **目标编码器**：与上下文编码器结构相同，但参数通过指数移动平均更新：$\bar{\theta} = \eta\bar{\theta} + (1 - \eta)\theta$。输入完整特征，得到目标表征$\bar{\mathbf{h}}_{pret}$。
    -   **预测器**：以$\hat{\mathbf{h}}_{pret}$和可学习的预测矩阵$\mathbf{M}_{pred}$为输入，预测被遮挡部分的特征$\mathbf{o}_{pret}$。
    -   **损失函数**：最小化预测特征与目标特征之间的L2损失：$\mathcal{L}_2 = \frac{1}{B \times t_{pred}} \sum_{i=1}^{B} \sum_{j=1}^{t_{pred}} || \bar{\mathbf{h}}_{pret}^{(i)}(j) - \mathbf{o}_{pret}^{(i)}(j) ||_2^2$。

-   **掩码动态对比学习模块**：
    -   为建模样本间关系，将**原始视图和掩码视图**作为正样本对。
    -   **强正样本对**：同一样本的原始特征$\mathbf{z}_{pret}$和掩码特征$\tilde{\mathbf{z}}_{pret}$。
    -   **弱正样本对（动态加权）**：受到**相同情感刺激**的不同样本也应视为正样本，但其相似性程度是模糊的。为此，引入**刺激标签掩码矩阵**$\mathbf{M}_{stim}$和基于特征相似性动态计算的**关系矩阵**$\mathbf{D}_{weights}$，动态衡量这些样本间的正样本关系。
    -   **损失函数**：最终的对比损失$\mathcal{L}_{contrast}$综合了强正样本相似度$S_{strong}$、动态加权正样本相似度$S_{dyna}$和负样本相似度$S_{nega}$。

-   **总体预训练损失**：$\mathcal{L} = \mathcal{L}_2 + \mathcal{L}_{contrast}$。

-   **微调策略**：移除生成式分支，保留对比学习模块以维持特征判别力，并加入分类头用交叉熵损失进行监督学习。

### 3. 实验设计

-   **数据集与场景**：
    -   **预训练**：在大规模的**FACED**数据集（32通道，123名受试者，9种情绪）上进行自监督预训练。
    -   **微调与测试**：在**SEED-IV**（62通道，15人，4情绪）、**SEED-V**（62通道，16人，5情绪）和**SEED-VII**（62通道，20人，7情绪）数据集上进行。测试采用**跨受试者留一法交叉验证**。为测试泛化性，还进行了**跨会话跨受试者**评估。

-   **基准对比方法**：
    -   BIOT
    -   TS-TCC
    -   EEG2Rep
    -   LaBraM

-   **实验结果**：
    -   在**会话内跨受试者**任务中，MGCRL在三个数据集上均取得最高准确率（SEED-IV: 41.47%, SEED-V: 34.99%, SEED-VII: 34.73%），相较于最优基线（EEG2Rep）分别相对提升4.27%, 3.01%, 和3.70%。
    -   在**跨会话跨受试者**任务中，MGCRL同样表现最佳（SEED-IV: 40.07%, SEED-V: 33.56%），性能下降幅度小，展现了强大的泛化能力。
    -   消融实验证实了$\mathcal{L}_2$损失、脑区内图卷积和对比学习损失三个模块均不可或缺。
    -   与标准对比损失相比，所提出的动态掩码对比损失带来了显著的性能提升。

### 4. 资源与算力

论文未明确说明所使用GPU的型号、数量以及具体的训练时长。

### 5. 实验数量与充分性

-   **实验组数**：实验设计较为全面，主要包括以下几组：
    1.  **主要性能对比**：在3个数据集（SEED-IV, V, VII）上，与会话内的4种先进方法进行对比。
    2.  **跨任务泛化对比**：在2个数据集（SEED-IV, V）上，与会话间的4种先进方法进行对比。
    3.  **消融实验**：在3个数据集上，针对3个关键模块（生成损失、图卷积、对比损失）进行移除实验。
    4.  **损失函数对比**：在3个数据集上，对比标准对比损失与所提出的动态掩码对比损失。
    5.  **微调数据敏感性分析**：在3个数据集上，测试不同数量训练样本下的性能，并对比从零训练的结果。
    6.  **可视化与可解释性分析**：通过可视化脑区权重，分析了模型认为对情绪识别关键的脑区。

-   **充分性与公平性评估**：
    -   **充分性**：实验设计充分，覆盖了性能对比、消融、鲁棒性、泛化性和可解释性等多个维度，有力地支撑了论文的结论。
    -   **客观公平性**：实验设置公平。所有对比方法均依据其原文设置实现，并进一步优化了学习率和批大小，确保其发挥最佳性能。预训练、微调的超参数配置在所有实验中保持一致，体现了客观性。

### 6. 论文的主要结论与发现

1.  **范式融合有效**：通过精巧复用掩码机制，成功整合了生成式和对比式学习范式，两者相互补充，有效克服了各自独立应用时的局限性。
2.  **通用表征学习能力**：所提出的MGCRL框架能从未标注EEG数据中学习到强大的通用表征，显著提升了跨数据集、跨受试者的情感识别性能，并降低了对下游标注数据的依赖。
3.  **关键脑区发现**：模型学习到的重要脑区（如额叶、颞叶、顶叶）分布与现有神经科学研究中关于情绪处理的认知高度吻合，验证了模型的合理性和可解释性。

### 7. 优点

-   **创新的框架设计**：首次针对EEG情感识别，在原始信号上融合了掩码生成式和对比式自监督学习，并实现了结构统一，这是一个重要的方法论贡献。
-   **精巧的模块设计**：
    -   **区域感知时空编码器**：通过脑区内图卷积和动态邻接矩阵，巧妙解决了跨数据集通道不匹配的痛点，同时建模了空间和功能连接。
    -   **掩码动态对比学习**：利用刺激标签和动态相似度加权，有效挖掘了跨受试者间的模糊语义关联，这是对传统对比学习的改进。
-   **强大的泛化能力与鲁棒性**：在多个数据集、多种实验设置（会话内/跨会话）下一致地超越了现有方法，且性能随训练数据增加稳定提升，证明了其鲁棒性。
-   **良好的可解释性**：通过脑区权重可视化，为模型的决策提供了神经科学层面的依据。

### 8. 不足与局限

-   **算力需求未披露**：论文未提及训练所需的计算资源（如GPU型号、数量、训练时长），这使得其他研究者难以评估其复现成本和实际可行性。
-   **数据模态单一**：目前框架仅用于处理EEG信号。对于多模态数据（如结合眼动、表情等）的跨数据集迁移能力尚待探索。
-   **跨数据集实验设置**：预训练数据集仅有FACED，且SEED系列数据集在采集设备和范式上具有一定相似性。模型在差异更大的数据集（如不同电极密度、不同采样率、不同文化背景）之间的迁移能力需要更广泛的验证。
-   **标签异构性处理**：论文虽然可以处理不同标签集的数据集，但在微调时仍利用目标数据集的完整标签信息，并未深入探索在目标数据集标签极度稀缺或部分缺失（如零样本学习）情况下的性能。

（完）
