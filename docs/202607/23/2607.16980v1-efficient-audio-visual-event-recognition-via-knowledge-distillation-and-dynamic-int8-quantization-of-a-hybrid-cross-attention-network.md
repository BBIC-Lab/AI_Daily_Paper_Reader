---
title: Efficient Audio-Visual Event Recognition via Knowledge Distillation and Dynamic INT8 Quantization of a Hybrid Cross-Attention Network
title_zh: 通过知识蒸馏和动态INT8量化的混合交叉注意力网络实现高效视听事件识别
authors: "Parinaz Binandeh Dehaghani, Danilo Pena, A. Pedro Aguiar"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16980v1"
tags: ["query:ndai"]
score: 9.0
evidence: 利用预训练的VideoMAE和AST进行下游视听识别
tldr: "针对视听事件识别中Transformer模型计算量大、难以部署于边缘设备的问题，本文提出结合架构压缩、知识蒸馏和动态INT8量化的高效框架。教师模型融合VideoMAE、AST与混合交叉注意力，学生模型缩减维度与头数并通过蒸馏训练，最后量化压缩。在AVE数据集上，融合模块参数减少59.06%，准确率仅降2.14%，量化后模型仅2.04MB，验证了效率与精度的平衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16980v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the proposed teacher–student compression framework for hybrid cross-attention-based audio-visual event recognition.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-005.webp\", \"caption\": \"Fig. 2. Training and validation accuracy curves of the proposed compressed student model on the AVE dataset.\", \"page\": 9, \"index\": 5, \"width\": 725, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-002.webp\", \"caption\": \"Fig. 3. Training and validation loss curves of the proposed student model during knowledge distillation.\", \"page\": 10, \"index\": 2, \"width\": 725, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-004.webp\", \"caption\": \"Fig. 4. Normalized confusion matrix of the proposed compressed student model on the AVE test set.\", \"page\": 12, \"index\": 4, \"width\": 725, \"height\": 660}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-003.webp\", \"caption\": \"Table 1. Performance comparison of the teacher model, compressed student model, and dynamically quantized INT8 student model on the AVE dataset.\", \"page\": 13, \"index\": 3, \"width\": 858, \"height\": 430}]"
motivation: 高计算复杂度和内存占用阻碍了基于Transformer的视听事件识别模型在资源受限设备上的部署。
method: 通过缩减隐藏维度、注意头数和前馈网络构建轻量学生模型，以知识蒸馏迁移教师模型知识，并应用动态INT8量化进一步压缩。
result: "在AVE数据集上，融合模块参数减少59.06%，准确率下降仅2.14%，量化后模型大小从10.71MB降至2.04MB。"
conclusion: 该框架实现了识别精度与计算效率的良好折衷，适合边缘AI平台部署。
---

## 摘要
视听事件识别（AVER）通过基于Transformer的多模态架构取得了显著的性能提升。然而，这些模型的高计算复杂度、大内存占用和推理成本阻碍了它们在边缘和资源受限设备上的部署。本文提出了一种基于混合交叉注意力的视听事件识别的高效压缩框架，结合了架构模型压缩、知识蒸馏和动态INT8量化。高容量教师模型集成VideoMAE进行视觉表示学习，音频频谱图Transformer（AST）进行音频特征提取，以及混合交叉注意力融合网络进行多模态特征融合。通过减小隐藏特征维度、注意力头数和前馈网络规模，同时保持整体网络架构，构建轻量级学生模型。使用知识蒸馏训练学生模型，有效地从教师模型中迁移判别性知识。最后，应用动态INT8训练后量化进一步减小模型尺寸以实现高效部署。在视听事件（AVE）数据集上的实验结果表明，所提出的框架将多模态融合模块中的可训练参数数量减少了59.06%，与教师模型相比，分类准确率仅下降2.14%。此外，动态INT8量化将模型大小从10.71 MB降至2.04 MB，同时保持有竞争力的识别性能。这些结果表明，所提出的框架在识别准确率和计算效率之间提供了有效的权衡，使其成为在资源受限的边缘AI平台上部署的一种有前景的解决方案。

## Abstract
Audio-visual event recognition (AVER) has achieved significant performance improvements through transformer-based multimodal architectures. However, the high computational complexity, large memory footprint, and inference cost of these models hinder their deployment on edge and resource-constrained devices. This paper presents an efficient compression framework for hybrid cross-attention-based audiovisual event recognition by combining architectural model compression, knowledge distillation, and dynamic INT8 quantization. A high-capacity teacher model integrates VideoMAE for visual representation learning, the Audio Spectrogram Transformer (AST) for audio feature extraction, and a hybrid cross-attention fusion network for multimodal feature integration. A lightweight student model is constructed by reducing the hidden feature dimension, the number of attention heads, and the feedforward network size while preserving the overall network architecture. The student model is trained using knowledge distillation to effectively transfer discriminative knowledge from the teacher. Finally, dynamic INT8 post-training quantization is applied to further reduce the model size for efficient deployment. Experimental results on the Audio-Visual Event (AVE) dataset show that the proposed framework reduces the number of trainable parameters in the multimodal fusion module by 59.06%, with only a 2.14% decrease in classification accuracy compared with the teacher model. Furthermore, dynamic INT8 quantization reduces the model size from 10.71 MB to 2.04 MB while maintaining competitive recognition performance. These results demonstrate that the proposed framework provides an effective trade-off between recognition accuracy and computational efficiency, making it a promising solution for deployment on resource-constrained edge AI platforms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机与背景）
- **核心问题**：基于Transformer的多模态视听事件识别（AVER）模型虽然精度高，但计算复杂度高、内存占用大、推理成本高，难以部署在边缘和资源受限设备（如机器人、穿戴设备、智能交通平台）上。
- **研究背景**：现有模型压缩方法（剪枝、量化、知识蒸馏）多为通用设计，未充分考虑混合交叉注意力融合网络的架构特性，容易破坏多模态交互机制。因此，需要一种**架构感知**的压缩框架，在显著降低算力需求的同时，保留多模态融合的判别能力。

### 2. 论文提出的方法论
- **整体思路**：构建一个“教师−学生”压缩流程，仅压缩可训练的多模态融合与分类网络，冻结预训练的特征提取骨干（VideoMAE、AST），依次应用架构缩减、知识蒸馏和训练后量化。
- **教师模型**：Visual backbone 使用预训练 VideoMAE，Audio backbone 使用预训练 AST，两者提取的特征经线性投影到统一空间后，送入**稳定混合交叉注意力模块**（双向交叉注意力，残差缩放初始化接近零）和模态时序注意力（Modality Temporal Attention），输出多模态特征用于事件分类。
- **学生模型压缩**：保留教师网络的拓扑结构，但缩小融合模块的关键维度：
  - 隐藏嵌入维度：$512 \to 256$
  - 多头注意力头数：$8 \to 4$
  - 前馈网络维度：$1024 \to 512$
- **知识蒸馏**：采用响应式蒸馏，教师权重冻结，学生模型使用组合损失进行优化：
  $$L = (1 - \alpha) L_{\text{CE}} + \alpha T^2 L_{\text{KD}}$$
  其中 $L_{\text{CE}}$ 为标准交叉熵，$L_{\text{KD}}$ 为教师与学生软化输出（温度 $T=4$）之间的 KL 散度，$\alpha=0.8$ 控制蒸馏损失权重。
- **动态 INT8 量化**：对训练后的学生模型的线性层进行训练后动态量化，权重存储为 INT8，激活值在推理时按运行时分布动态量化，无需额外重训练。

### 3. 实验设计
- **数据集**：Audio-Visual Event (AVE) 数据集，包含 4,143 个未剪辑 YouTube 视频，共 28 个事件类别（乐器、动物、车辆、人类活动等），每个视频约 10 秒，按官方划分训练/验证/测试集。
- **预处理**：视频均匀切分为 10 个 1 秒片段，离线使用冻结的 VideoMAE 和 AST 提取视觉与音频特征并缓存，训练与推理仅操作于缓存特征。
- **对比的模型**：
  - 原始教师模型（高容量融合网络）
  - 经知识蒸馏训练的学生模型（压缩融合网络）
  - 进一步动态 INT8 量化的学生模型
- **评估指标**：测试准确率、测试损失、加权精确率/召回率/F1 分数、可训练参数量、模型大小（MB）、检查点大小、压缩比、参数减少率、准确率下降幅度等。

### 4. 资源与算力
- 论文中**未提及**使用的 GPU 型号与数量、训练具体时长或功耗数据。仅说明模型基于 PyTorch 实现，采用 AdamW 优化器（初始学习率 $1\times10^{-4}$，权重衰减 $1\times10^{-4}$），批大小为 4，最多训练 80 个 epoch，并使用了 ReduceLROnPlateau 学习率调度与增强的早停策略。

### 5. 实验数量与充分性
- **实验组数**：主要报告了一组压缩流程的整体对比（教师 vs. 蒸馏学生 vs. 量化学生），提供了训练/验证精度曲线、损失曲线和归一化混淆矩阵，但**未进行消融实验**（例如，单独研究维度缩减、知识蒸馏或量化的独立贡献，也未尝试不同的压缩超参数组合）。
- **实验充分性**：只在一个数据集（AVE）上验证，缺少跨数据集或更大规模基准（如 AudioSet 等）的测试；未与其它压缩方法（如结构化剪枝、低秩近似、其它量化策略）进行横向对比，公平性有限；对量化带来的推理延迟反常上升现象未做深入分析。因此，虽然展示了方法的有效性，但实验的广泛性与说服力有提升空间。

### 6. 论文的主要结论与发现
- 所提出的架构感知压缩框架能够在视听事件识别任务上实现**精度与效率的有效折衷**：
  - 可训练参数减少 **59.06%**（从 6.86M 降至 2.81M），模型大小从 26.16 MB 降至 10.71 MB。
  - 分类准确率仅下降 **2.14%**（84.19% → 82.05%），加权 F1 仍维持在 82.13%。
  - 动态 INT8 量化后，模型大小进一步压缩至 **2.04 MB**，相对教师模型准确率总降幅仅 2.99%（至 81.20%），同时检查点大小从 78.53 MB 降至 4.25 MB。
- 结果表明，通过有策略地缩减融合网络维度并配合知识蒸馏，多模态推理能力能够被有效保留；训练后量化可在不重训练的情况下大幅缩小存储占用，适合内存极度受限的边缘 AI 平台。

### 7. 优点
- **架构感知压缩**：针对混合交叉注意力融合网络专门设计压缩策略，而非采用通用的“一刀切”方法，保持了多模态交互机制。
- **组合式压缩管线**：将架构缩减、知识蒸馏与动态量化串行整合，实现了多层次的效率提升，且量化阶段无需额外训练。
- **显著而均衡的性能**：在仅损失约 2% 绝对精度的代价下，参数和模型存储量均获得数量级的压缩，实用性强。
- **离线特征缓存**：分离固定骨干与可训练融合模块，聚焦于实际部署的推理组件，实验设计贴近实际边缘部署场景。

### 8. 不足与局限
- **缺乏消融与对比实验**：未分离各个压缩步骤的独立效果，未与其它主流压缩方法（如剪枝、低比特量化、其他蒸馏策略）比较，难以评估各模块的增量贡献及方法的相对优势。
- **单数据集验证**：仅在 AVE 这一个规模不大的数据集上测试，结论的泛化性有待在更多样化的大规模视听基准上验证。
- **算力信息缺失**：未报告 GPU 型号、算力消耗和训练时间，使得他人复现与资源评估困难。
- **量化推理延迟问题**：文中提到动态 INT8 量化后 CPU 推理延迟反而略有升高，但未深入分析原因或给出解决方案，可能影响其在追求实时响应的边缘设备上的适用性。
- **压缩维度选择单一**：学生网络的维度缩减（512→256 等）为单一固定配置，未探索更细粒度或更激进的压缩边界，也未尝试结构化剪枝或自动搜索压缩策略。

（完）
