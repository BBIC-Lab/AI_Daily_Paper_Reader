---
title: Word Class Representations Spontaneously Emerge from Successor Representations Trained on Natural Language
title_zh: 词类表征从基于自然语言训练的继任表征中自发涌现
authors: "Mathis Immertreu, Achim Schilling, Thomas Kinfe, Patrick Krauss"
date: 2026-05-23
pdf: "https://arxiv.org/pdf/2605.24585v1"
tags: ["query:ndai"]
score: 9.0
evidence: 通过继任表征学习词表示
tldr: 本研究借鉴强化学习的后继表征，替代传统语言模型的下一个词预测，训练神经网络预测多时间步的未来词分布。在 WikiText-103 上无监督学习后，词表征自发形成按词性聚合的几何结构，且该结构受预测范围调控。该首次系统应用表明句法类别可由预测学习涌现，并连接了强化学习、语言学和认知神经科学。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24585v1/fig-009.webp\", \"caption\": \"Fig. 1 Figure: TD learning targets for successor representations. 1-Step TD (blue), n-Step TD shown for n = 4 (teal), and TD(λ) (gold) differ in how many future token positions contribute discounted features before bootstrapping and how the individual steps are weighted. They range from a single step to an infinite exponentially-weighted horizon.\", \"page\": 5, \"index\": 9, \"width\": 750, \"height\": 586}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-002.webp\", \"caption\": \"Fig. 2 Architecture and Training Flow. Input tokens are embedded and passed through a shared backbone that branches into three discount-specific heads, each producing a successor representation prediction. The Residual Block, the main building unit of both backbone and heads, is illustrated top-left. Training targets are computed via a λ-return formulation that bootstraps from the one-hot encodings of all future tokens and a frozen EMA target network, which maintains a slowly updating copy of the live network weights (lock symbols indicate no gradient flow). The model is optimized by minimizing the KL divergence between predictions and targets, summed across all heads. At inference, the true Successor Representation is recovered by a simple rescaling.\", \"page\": 7, \"index\": 2, \"width\": 731, \"height\": 599}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-001.webp\", \"caption\": \"Fig. 3 Training and Validation Losses. Mean and per-head training (solid black) and validation (dashed red) losses over 10 epochs.\", \"page\": 11, \"index\": 1, \"width\": 753, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-006.webp\", \"caption\": \"Fig. 4 Emergence of Syntactic Structure in SR Geometry. UMAP projections of SR embeddings for the untrained model (left) and after training (epoch 10, right), colored by ground-truth POS tag. Prior to training, embeddings form a structureless cloud with no systematic POS organisation. a) NVA subset (NOUN, VERB, ADJ): after training, embeddings decompose into three spatially separated regions corresponding to the three POS categories. b Extended vocabulary (13 POS categories): after training, most categories converge into a central region with emergent substructure, while a subset project as isolated peripheral strands; numerals show the most distinct separation.\", \"page\": 13, \"index\": 6, \"width\": 752, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-004.webp\", \"caption\": \"Fig. 5 Unsupervised Clustering of Successor Representations and Connectivity Structure (NVA, k=3). To assess the geometric structure of the main syntactic categories in embedding space, SRs’ with γ = 0.2 of the 200 most frequent tokens were sampled from each of three part-ofspeech categories (nouns, verbs, and adjectives), yielding a balanced dataset of 600 tokens. a) UMAP projections of token embeddings, colored by KMeans cluster assignment (left), Bayesian Gaussian Mixture (BGM) cluster assignment (center), and ground-truth Part-of-Speech (POS) tags (right), enabling direct visual comparison between unsupervised cluster solutions and known syntactic categories. b) Cluster purity heatmaps for KMeans clusters, showing the raw POS tag frequency per cluster (left) and the fractional POS tag composition per cluster (right), quantifying the degree to which each cluster captures a coherent syntactic category. c) Directed inter-cluster successor representation (SR) connectivity network. Nodes represent KMeans clusters and are colored according to their majority POS tag; directed edges indicate the magnitude of SR connectivity from source to target cluster, with edge thickness proportional to connection strength. d) SR connectivity matrix corresponding to the network in (c). Rows denote source clusters and columns denote target clusters; each entry reports the fraction of total directed SR connectivity from the source to the target cluster.\", \"page\": 14, \"index\": 4, \"width\": 750, \"height\": 839}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-010.webp\", \"caption\": \"Fig. 6 Unsupervised Clustering of Successor Representations (NVA, k=30). SRs for γ = 0.2 were extracted for a balanced sample of the 200 most frequent tokens per part-of-speech category (nouns, verbs, and adjectives), yielding a dataset of 600 tokens in total. (a) UMAP projections of token embeddings, colored by KMeans cluster assignment (left), BGM cluster assignment (center), and ground-truth POS tags (right). Clusters are aligned across algorithms by maximizing cluster overlap. They receive a base color for their joint majority POS tag, with distinct shades differentiating individual clusters within the same syntactic category to facilitate comparison. (b) Cluster purity heatmaps evaluating the alignment between KMeans clusters and POS categories. The left heatmap shows the raw POS tag frequency across the 30 clusters, and the right heatmap shows the fractional purity, i.e. the proportion of each POS category assigned to each cluster, illustrating the degree of syntactic specialization achieved at higher cluster granularity (K = 30).\", \"page\": 16, \"index\": 10, \"width\": 751, \"height\": 894}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-007.webp\", \"caption\": \"Fig. 7 Clustering quality metrics (ARI and NMI) across SR configurations for the NVA subset. ARI and NMI measure the agreement between a clustering solution and the ground-truth POS labels, correcting for chance and label permutation respectively. (a) ARI (left) and NMI (right) as a function of the SR scale parameter γ, evaluated on the NVA (Noun-Verb-Adjective) subset. Trends are shown for varying cluster granularities (K), comparing KMeans (solid lines) and BGM (dashed lines). (b) ARI and NMI as a function of cluster count K at fixed γ = 0.2, illustrating how cluster resolution affects the recovery of prescribed syntactic structure from the restricted NVA vocabulary.\", \"page\": 17, \"index\": 7, \"width\": 753, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-005.webp\", \"caption\": \"Fig. 8 Clustering of Successor Representations (full, k = 13). SRs for γ = 0.2 were extracted for up to 200 of the most frequent tokens per part-of-speech category across 13 categories, yielding a dataset of 1377 tokens in total. (a) UMAP projections of full vocabulary token embeddings, colored by KMeans cluster assignment (left) and POS tags (right). (b) Cluster purity heatmaps evaluating the alignment between the 13 KMeans clusters and POS categories.\", \"page\": 18, \"index\": 5, \"width\": 750, \"height\": 724}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-008.webp\", \"caption\": \"Fig. 9 Successor representation connectivity within numerical and temporal cluster subnetworks. (a) Directed SR subnetwork restricted to a selection of numerical and temporal clusters. Nodes represent individual KMeans clusters colored by their majority POS tag; directed edges represent inter-cluster transition strengths. (b) SR connectivity matrix corresponding to the subnetwork in (a). The final column (Max External) reports the maximum connectivity from each source cluster to any cluster outside the restricted subset.\", \"page\": 20, \"index\": 8, \"width\": 752, \"height\": 860}, {\"url\": \"assets/figures/arxiv/2605.24585v1/fig-003.webp\", \"caption\": \"Fig. 10 Clustering quality metrics (ARI and NMI) across SR configurations for the extended set of POS tags. (a) ARI (left) and NMI (right) as a function of the SR decay parameter γ, evaluated on the extended POS tag set. Trends are shown for varying cluster granularities (K), comparing KMeans (solid lines) and BGM (dashed lines). (b) ARI and NMI as a function of cluster count K at fixed γ = 0.2, illustrating how cluster resolution affects the recovery of syntactic structure across the extended vocabulary.\", \"page\": 22, \"index\": 3, \"width\": 752, \"height\": 485}]"
motivation: 探索后继表征能否应用于自然语言，使语法结构在无显式监督下自发涌现。
method: 在 WikiText-103 上训练深度残差网络，通过 KL 散度优化预测多时间步未来词分布的后继表征。
result: 学到的词表征按词性清晰分离，短预测范围增强句法结构，长范围整合语义，且词汇内部呈现可解释子类。
conclusion: 后继表征可成功应用于语言，句法类别作为预测学习的结果浮现，建立了强化学习与语言学的桥梁。
---

## 摘要
语言模型通常被训练来预测序列中的下一个词元。在此，我们探索来自强化学习的另一种预测原理：继任表征（SRs），其建模未来状态的期望折扣分布，而非即时下一个状态。我们将此框架迁移至自然语言，并训练神经网络预测跨多个时间范围的未来词分布，从而学习长程转移结构的表征。我们在WikiText-103（1亿零3百万词元；2万词汇量）上训练深度残差神经网络，并使用KL散度优化作为概率分布的继任表征。在没有显式语言监督的情况下，结构化的语言表征自发涌现。训练后，所学习的空间关于词性（POS）类别形成了清晰的几何组织：名词、动词和形容词变得可分离，并可通过无监督聚类恢复。这种组织系统性地依赖于预测范围，短范围产生最强的句法结构，而较长范围则逐渐整合更广泛的上下文和语义信息。在更精细的分辨率下，额外的可解释词汇子结构涌现，揭示了主要词类内部的连贯子类。这些发现表明，句法类别未必需要显式编码，而可能作为预测性序列学习的结果而出现。据我们所知，本工作首次将继任表征系统性地应用于自然语言，并在强化学习、语言学和认知神经科学之间建立了概念桥梁。

## Abstract
Language models are typically trained to predict the next token in a sequence. Here, we explore an alternative predictive principle from reinforcement learning: Successor Representations (SRs), which model the expected discounted distribution of future states rather than the immediate next state. We transfer this framework to natural language and train neural networks to predict future word distributions across multiple temporal horizons, thereby learning representations of long-range transition structure. We train a deep residual neural network on WikiText-103 (103 million tokens; 20,000-word vocabulary) and optimize successor representations as probability distributions using KL divergence. Without explicit linguistic supervision, structured language representations emerge spontaneously. After training, the learned space develops a clear geometric organization with respect to part-of-speech (POS) categories: nouns, verbs, and adjectives become separable and recoverable through unsupervised clustering. This organization depends systematically on predictive horizon, with short horizons producing the strongest syntactic structure and longer horizons increasingly integrating broader contextual and semantic information. At finer resolutions, additional interpretable lexical substructure emerges, revealing coherent subclasses within major word categories. These findings suggest that syntactic categories need not be explicitly encoded but may arise as a consequence of predictive sequence learning. To our knowledge, this work provides the first systematic application of successor representations to natural language and establishes a conceptual bridge between reinforcement learning, linguistics, and cognitive neuroscience.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将以 Markdown 格式，对您提供的论文进行结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义

*   **研究动机**：当前主流语言模型的核心训练目标是预测下一个词元（next-token prediction），这强调局部依赖关系。本研究旨在探索一种源自强化学习的替代性预测框架——继任表征（Successor Representations, SRs），以此探究语言中的结构化组织能否从纯粹的预测性序列学习中自然涌现。
*   **核心问题**：该研究探讨的核心问题是，句法类别（如名词、动词、形容词）是否是语言的固有组件，或者它们是否可能作为对序列结构进行高效预测性抽象的产物而出现。
*   **整体含义**：研究旨在建立强化学习、计算语言学和认知神经科学之间的概念桥梁。其主要贡献在于首次将继任表征系统性地应用于自然语言建模，并论证了语言结构（尤其是词性）可以在没有显式监督的情况下，从学习长程转移结构中自发产生。

### 2. 论文提出的方法论

*   **核心思想**：
    *   **继任表征**：不同于预测下一个词，继任表征模型是在给定当前状态下，预测未来所有状态的**期望折扣分布**，即建模状态间的长程转移结构。公式化为 $M(s, s') = \mathbb{E}_\pi\left[\sum_{t=0}^{\infty}\gamma^t \mathbb{I}(s_{t+1} = s') \mid s_0 = s\right]$，其中 $\gamma$ 是折扣因子，控制预测的时间范围。
    *   **应用于语言**：将自然语言视为一个序列环境（状态空间由词汇定义），词序列构成了在此高维空间中的转移轨迹。本研究训练神经网络预测每个输入词在未来多个时间范围（由不同 $\gamma$ 控制）的词分布。

*   **关键技术细节**：
    *   **学习目标**：使用**时序差分（TD）学习**的泛化形式 TD(λ) 来学习继任表征，以处理语言的非马尔可夫属性。目标计算混合了多个未来步骤的真实观测和来自“目标网络”的预测引导（Bootstrap）。$\lambda$-回报计算为 $\hat{G}_t = \phi(s_{t+1}) + \gamma\left[(1 - \lambda)\hat{M}(s_{t+1}, :) + \lambda \hat{G}_{t+1}\right]$。
    *   **损失函数**：将学习目标重新构建为匹配一个概率分布。模型输出的logits先经过Softmax，然后通过**KL散度（Kullback-Leibler Divergence）**与归一化后的 $\lambda$-回报目标进行匹配。损失函数为 $\mathcal{L}_\gamma = D_{KL}(G_\gamma \parallel \text{softmax}(\psi_\gamma))$。
    *   **模型架构**：采用一个**深度残差网络（ResNet）**，包含一个共享的8层ResNet骨干网络，后接三个独立的8层ResNet预测头。每个预测头对应一个特定的折扣因子 $\gamma \in \{0.2, 0.5, 0.8\}$，专门学习不同时间范围的继任表征。模型使用指数移动平均（EMA）的影子网络来提供稳定的引导目标，避免训练不稳定。

### 3. 实验设计

*   **数据集**：主体实验在 **WikiText-103** 数据集上进行，这是一个包含约1.03亿词元的高质量维基百科文章语料库。词汇表限制为最频繁的20,000个词。
*   **评估基准（Ground-Truth）**：使用 spaCy 的预训练模型对整个语料库进行词性（Part-of-Speech, POS）标注，为词汇表中的每个词元分配其在语料库中出现次数最多的词性标签。这构成了评估聚类结果时的“基准事实”。
*   **分析方法**：
    *   **表征可视化**：在训练前后，使用 **UMAP** 将高维继任表征向量投影到二维空间进行可视化，并用真实词性标签着色，直观观察其几何结构。
    *   **聚类与评估**：采用**共识聚类（Consensus Clustering）**方法，结合了K-Means和贝叶斯高斯混合模型（BGM）两种基础聚类算法，以在不同的聚类数 $K$ 下获得更鲁棒的聚类结果。使用纯度（Purity）、归一化互信息（NMI）和调整兰德指数（ARI）三个指标来量化聚类结果与真实词性标签的一致性。
    *   **转移网络分析**：计算并可视化聚类间的平均继任表征转移权重，构建有向图，以揭示隐含的句法转移模式（如形容词→名词的修饰关系）。
*   **对比方法**：本研究未将继任表征模型与传统语言模型（如LSTM/Transformer）直接进行性能对比。其核心设计是**模型内部分析**，对比不同配置下的结果，主要包括：
    *   **不同预测范围**：对比折扣因子 $\gamma$ 为 0.2, 0.5, 0.8 时对表征几何和聚类质量的影响。
    *   **不同聚类粒度**：对比在不同聚类数 $K$ （如 $K=3, 13, 30, 120$）下，所揭示的语言结构差异。
    *   **不同词汇集**：对比只包含名词、动词、形容词的子集（NVA）和包含13种词性的扩展集。
    *   **不同聚类算法**：对比 K-Means 和 BGM 两种聚类算法结果的一致性。

### 4. 资源与算力

*   **说明**：论文详细列出了训练超参数，如序列长度（80）、隐藏层大小（512）、批大小（160）、学习率（1e-4）及训练周期（10个epochs）等，但**并未明确提及**所使用的硬件配置（如GPU型号、数量）和总训练时长。

### 5. 实验数量与充分性

*   **实验数量**：本研究进行了多维度、系统性的实验。
    *   **训练与验证**：提供了完整的训练和验证损失曲线，证明模型泛化能力。
    *   **质性分析**：提供了多组 UMAP 可视化图，直观展示了训练前后、不同 $\gamma$、不同 $K$ 下的表征几何变化。
    *   **量化评估**：系统地评估了 $K$ 和 $\gamma$ 在不同取值、不同词汇集（NVA vs. 扩展集）和不同聚类算法下的 ARI 和 NMI 得分。
    *   **深入案例分析**：详细分析了 $K=3$, $K=13$, $K=30$, $K=120$ 等聚类粒度下的词元分配、簇纯度和转移模式，揭示了从句法大类到特定构式（如日期表达）的结构。
*   **充分性与客观性**：实验设计**较为充分和客观**。通过多维度（$\gamma$, $K$, 词汇集）、多算法（K-Means, BGM）和多指标（Purity, ARI, NMI）的交叉验证，得出的结论具有鲁棒性。定量分析有效地支撑了可视化观察和定性解读，评估过程是客观的。

### 6. 论文的主要结论与发现

*   **语言结构自发涌现**：在没有任何词性监督的情况下，模型所学的继任表征空间自发形成了清晰的、对应于词性类别的几何组织。名词、动词、形容词可通过无监督聚类有效恢复。
*   **预测范围调控表征几何**：折扣因子 $\gamma$ 系统性地影响着表征的结构。较小的 $\gamma$（短预测范围）更强地保留了局部句法规律（聚类纯度最高）；较大的 $\gamma$（长预测范围）则整合了更广泛的语境和语义信息，导致句法类别间的界限变模糊。
*   **精细的词汇子结构**：在更高的聚类粒度下，模型涌现出超越传统词性标签的可解释的词汇组织，如形容词细分为序数词、标量形容词、颜色词等；动词细分为运动类、认知交流类等；名词细分为时间词、计量单位、媒体类词等。这反映出对构式槽位和功能的捕捉。
*   **构式语法的计算基础**：继任表征空间可以被看作是句法构式从使用事件中涌现的计算基底，其聚类可视为对应于共享分布历史的“损耗记忆痕迹”。研究认为，语法知识可能是一个通用预测性记忆系统应用于序列结构的结果。

### 7. 优点

*   **视角新颖，跨学科桥梁**：首次将强化学习中的继任表征框架系统地引入自然语言处理，成功连接了三个原本相对独立的领域，为语言习得和认知建模提供了新范式。
*   **方法扎实，论证严密**：实验设计非常系统，通过控制变量（$\gamma$, $K$）、多角度评估（NMI, ARI, Purity）和鲁棒性检验（两种聚类算法、共识聚类），使得“结构自发涌现”这一核心观点极具说服力。
*   **可解释性强，洞察深刻**：不仅停留在表明“词性可聚类”的层面，而是深入到簇的内部和簇间转移，揭示了从宏观句法类别到微观构式子类的丰富结构化信息，为构式语法理论提供了计算实例。
*   **生物学合理性探讨**：论文将继任表征的学习与海马体的预测性地图功能、时序差分学习与突触可塑性联系，为语言知识的神经实现提供了有启发性的猜想。

### 8. 不足与局限

*   **语料库单一**：仅在 WikiText-103 这一相对同质化的百科文本上进行训练和验证。其结论是否适用于类型学上差异巨大的语言，或语域更多样化、更接近儿童语言习得环境的语料库，尚待考证。
*   **“类型”表征的局限性**：模型学习的是词的“类型（type）”级别的表征，即一个词形只有一个向量。这无法处理**一词多义（polysemy）**现象（如 `bank` 作为名词“河岸”和作为动词“存款”的分布完全不同），也缺乏对上下文的敏感性。这被认为是本研究的一个根本性局限。
*   **缺乏下游任务验证**：评估完全依赖于对表征空间内部几何属性的分析（聚类）。缺少在有效的下游任务（如词性标注、句法分析、语义相似度判断）上的定量性能评估，使得其与主流上下文相关语言模型的实用性对比不明确。
*   **参数选择缺乏原则**：对于关键超参数 $K$（聚类数）和 $\gamma$（预测范围）的选择，目前依赖定性可解释性或指标峰值，缺少一个理论驱动的模型选择准则来同时优化这些参数。

（完）
