---
title: "AttnGen: Attention-Guided Saliency Learning for Interpretable Genomic Sequence Classification"
title_zh: "AttnGen: 面向可解释基因组序列分类的注意力引导显著性学习"
authors: "Rayhaneh Shabani Nia, Ali Karkehabadi"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14073v1"
tags: ["query:ndai"]
score: 9.0
evidence: 使用注意力机制计算核苷酸级重要性分数
tldr: "AttnGen针对基因组序列分类可解释性难题，提出注意力引导训练框架，通过计算核苷酸重要性并抑制低贡献位置，使模型聚焦关键区域。在基准测试中，以96.73%准确率优于CNN基线，收敛更快，且移除高显著性核苷酸后性能骤降，证明模型依赖少量信息位点，实现了性能与可解释性的同步提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14073v1/fig-001.webp\", \"caption\": \"Fig. 1: Masking patterns for human and worm sequences at different masking levels. Blue: retained positions; red: masked positions.\", \"page\": 5, \"index\": 1, \"width\": 868, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.14073v1/fig-002.webp\", \"caption\": \"Fig. 2: Accuracy under progressive masking of high-gradient positions. Shaded region: ±1 std.\", \"page\": 5, \"index\": 2, \"width\": 592, \"height\": 371}]"
motivation: 深度神经网络在基因组序列分类中预测难以关联生物学模式，需增强可解释性。
method: 提出AttnGen框架，在训练中利用注意力计算重要性分数，并逐步掩蔽低贡献核苷酸位置。
result: "在demo_human_or_worm上达96.73%准确率，超CNN基线的95.83%，扰动实验证实模型聚焦于一小部分关键核苷酸。"
conclusion: 注意力引导掩蔽不仅改善分类性能与训练稳定性，还重塑了模型对序列位点的关注分布，可推广至更复杂场景。
---

## 摘要
深度神经网络在基因组序列分类中取得了强大的性能；然而，将其预测与生物学上有意义的序列模式关联起来仍然具有挑战性。在本工作中，我们提出了AttnGen，一个将可解释性直接嵌入优化过程的注意力引导训练框架。AttnGen利用注意力机制计算核苷酸级别的重要性分数，并在训练过程中逐步抑制低贡献位置。这促使模型将其预测聚焦于一组紧凑的信息区域，同时减少对噪声序列元素的依赖。我们在标准化的demo_human_or_worm基准上评估了AttnGen，这是一个对200个核苷酸序列进行二分类的任务。通过适度的掩码，AttnGen的验证准确率达到96.73%，优于传统CNN基线的95.83%准确率，同时还表现出更快的收敛速度和更高的训练稳定性。为了评估所学的重要性分数是否反映了功能相关的信号，我们通过移除高显著性核苷酸进行了基于扰动的分析。这导致在3000个序列的评估集上准确率从96.9%下降到接近随机水平，表明模型依赖于相对较小的信息位置子集。我们的分析表明，掩码10-20%的位置在预测性能和可解释性之间提供了最有利的权衡。这些结果表明，注意力引导的掩码不仅提高了分类性能，而且重塑了模型在序列位置之间分配重要性的方式。尽管本研究专注于短基因组序列，但所提出的方法可能扩展到更复杂的可解释序列建模场景。

## Abstract
Deep neural networks have achieved strong performance in genomic sequence classification; however, relating their predictions to biologically meaningful sequence patterns remains challenging. In this work, we present AttnGen, an attention-guided training framework that embeds interpretability directly into the optimization process. AttnGen computes nucleotide-level importance scores using an attention mechanism and progressively suppresses low-contribution positions during training. This encourages the model to focus its predictions on a compact set of informative regions while reducing reliance on noisy sequence elements. We evaluate AttnGen on the standardized demo_human_or_worm benchmark, a binary classification task over 200-nucleotide sequences. With moderate masking, AttnGen achieves a validation accuracy of 96.73%, outperforming a conventional CNN baseline with 95.83% accuracy, while also exhibiting faster convergence and improved training stability. To assess whether the learned importance scores reflect functionally relevant signal, we conduct perturbation-based analysis by removing high-saliency nucleotides. This causes accuracy to drop from 96.9% to near chance level on a 3,000-sequence evaluation set, indicating that the model relies on a relatively small subset of informative positions. Our analysis shows that masking 10--20% of positions provides the most favorable trade-off between predictive performance and interpretability. These results suggest that attention-guided masking not only improves classification performance but also reshapes how models distribute importance across sequence positions. Although this study focuses on short genomic sequences, the proposed approach may extend to more complex interpretable sequence modeling settings.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **问题**：深度神经网络在基因组序列分类中虽能取得高准确率，但其决策过程缺乏可解释性，难以将预测结果对应到有生物学意义的序列模式上。
- **背景**：传统的后验解释方法（如显著性图）常不可靠，且解释与训练过程分离；部分工作尝试在训练中引入可解释性约束，但在序列特别是基因组离散符号序列上研究不足。
- **含义**：本工作旨在探索能否在训练优化过程中直接嵌入可解释性，使模型不仅性能高，还能自主聚焦关键核苷酸区域，从而为下游生物学解读提供可信依据。

### 2. 论文提出的方法论
**核心思想**：通过一个轻量级注意力机制在前向传播中估计核苷酸重要性，然后渐进地掩码低重要性位置，并利用KL散度保持原始与掩码输入的预测一致性，从而引导模型聚焦于少量高信息区域。

**关键技术细节**：
- **基础架构**：1D卷积网络，包含嵌入层（128维）、三个卷积块（卷积核大小8，批量归一化，ReLU，最大池化步长2），以及全连接分类头。
- **注意力重要性分数**：
  - 对嵌入张量 $E(x) \in \mathbb{R}^{B\times L\times d}$ 沿特征维度取均值得到位置分数 $s_{b,i}(x) = \frac{1}{d}\sum_{j=1}^d E_{b,i,j}(x)$。
  - 经 softmax 得到归一化权重 $A_{b,i}(x) = \frac{\exp(s_{b,i}(x))}{\sum_{i'=1}^L \exp(s_{b,i'}(x))}$。
- **渐进掩码策略**：
  - 给定掩码比例 $\alpha$，选择权重最小的 $k = \lfloor \alpha L \rfloor$ 个位置，将它们替换为填充符（索引0）生成 $\tilde{x}$。
  - 掩码强度设置为4档：0%（基线）、10–25%（中等）、50%（高）、75%（极高）。
- **损失函数**：$L_{total} = L_{CE}\big(f_\theta(x), y\big) + \lambda D_{KL}\big(f_\theta(x) \| f_\theta(\tilde{x})\big)$，其中 $\lambda=0.1$。
- **训练算法**：每次批次中计算注意力权重、选择并掩码低重要位置、最小化总损失，无需额外反向传播计算梯度显著性。

### 3. 实验设计
- **数据集**：Genomic Benchmarks 中的 `demo_human_or_worm` 标准基准。包含 100,000 条长度 200 的 DNA 序列，二分类区分人类与秀丽隐杆线虫。
- **对比方法**：
  - 传统 CNN（无掩码）作为基线。
  - AttnGen 在不同掩码比例（10%、20%、50%、75%）下的变体。
  - 消融实验：仅随机掩码 + KL（无注意力指导）；仅注意力掩码无 KL；完全基线。
- **评估指标**：分类准确率、收敛速度、训练稳定性；通过梯度重要性排序并逐步掩码来验证注意力分数与预测的关联。

### 4. 资源与算力
- 论文中**未明确提及** GPU 型号、数量或具体的训练时长。仅记录了优化器、批次大小（64）、学习率（0.001）等超参，对硬件资源无说明。

### 5. 实验数量与充分性
- **实验组数**：
  - 主要性能对比表（表I）：5 种配置（基线 + 4 种掩码比例）。
  - 消融实验表（表III）：4 种配置（全方法、随机掩码+KL、注意力掩码无KL、基线）。
  - 梯度扰动分析（表II 与 图2）：对 3000 条序列，逐步移除梯度高重要位置，测量9个掩码数量下的准确率变化。
  - 可视化分析：不同掩码比例下保留/掩码位置的分布（图1）及训练损失曲线稳定性。
- **充分性与公平性**：
  - 所有实验使用相同架构、超参和随机种子，对比公平。
  - 虽只在单一短序列数据集上测试，但消融与扰动实验较为充分，能支撑主要结论。
  - 缺少与其他可解释性引导训练方法（如基于梯度显著性的 SGT）的直接对比，且未在多物种或长序列任务上验证。

### 6. 论文的主要结论与发现
- 注意力引导掩码能提升短序列分类性能：10% 掩码下准确率达 96.73%，比基线 CNN 高出 0.90 pp，且收敛更快。
- 10–20% 的掩码比例在性能与聚焦之间取得最佳平衡；过高比例（75%）则严重损害准确率。
- 消融实验表明，注意力驱动的掩码是关键增益来源，KL 一致性正则化起辅助稳定作用。
- 移除梯度高重要性核苷酸会使准确率急剧下降，证实模型决策高度依赖所学到的紧凑信息位点，增强了可解释性的可信度。

### 7. 优点
- **端到端可解释训练**：将重要性估计融入前向传播，无需重复梯度计算，效率更高。
- **轻量注意力设计**：均值池化加 softmax，计算开销低且训练稳定。
- **渐进掩码与KL约束**：温和地引导模型聚焦，避免硬稀疏带来的信息丢失。
- **充分的扰动验证**：通过梯度重要性分析提供模型依赖性的定量证据，而不仅仅是视觉评估。
- **基准与消融全面**：在标准数据集上进行了多种配置和消融实验，结论可靠。

### 8. 不足与局限
- **数据集单一**：仅在一条短序列（200 nt）二分类任务上评估，未在更复杂任务（如长序列、多类别、回归）或不同物种基因组上验证泛化性。
- **可解释性验证有限**：未识别所聚焦区域是否对应已知生物学基序（如转录因子结合位点），生物学解释深度不足。
- **掩码比例敏感**：需手动选择 $\alpha$ 以平衡性能与聚焦，缺乏自适应机制。
- **对比方法欠缺**：未与原论文 SGT 等梯度显著性引导训练方法直接对比，难以定位相对优势。
- **算力未披露**：缺少计算资源信息，不利于复现和估算开销。

（完）
