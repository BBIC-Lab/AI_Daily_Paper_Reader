---
title: "REALM: Retrospective Encoder Alignment for LFP Modeling"
title_zh: "REALM: 面向LFP建模的回顾式编码器对齐"
authors: "Peicheng Wu, Zhenyu Bu, Runze Ma, Lin Du"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14867v1"
tags: ["query:ndai"]
score: 9.0
evidence: 基于LFP的脑机接口神经解码
tldr: 针对脑机接口中LFP信号解码精度不足且非因果模型不适用于实时系统的问题，提出REALM框架。该框架先预训练双向Mamba-2教师模型，再通过表示对齐和任务监督将其知识蒸馏为因果学生模型，实现实时行为解码。REALM性能优于现有方法，同时参数减少2倍、训练时间减少10倍，展示了LFP-only模型在无线植入式脑机接口中的实用潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14867v1/fig-003.webp\", \"caption\": \"Figure 1. Overview of the REALM framework for causal LFP-based behavior decoding. Left: Experimental configuration. A rhesus macaque performs a 2D motor task: continuous random-target reaching for Makin or center-out reaching for Flint, manipulating a control device to drive a cursor toward visual targets while local field potentials are recorded from primary motor cortex (M1) via a chronically implanted 96-channel Utah array (4mm × 4mm). Right: Causal behavior-decoding pipeline. Raw multi-channel LFP signals are converted into neural tokens by the Neural Tokenizer, encoded into temporally-causal latent representations by the REALM encoder, which was distilled from a non-causal teacher, and mapped to 2D cursor velocity by the Behavior Decoding Head. The full causal student is designed for real-time deployment on edge hardware, illustrated here on the NVIDIA Jetson Orin Nano or Raspberry Pi 5.\", \"page\": 2, \"index\": 3, \"width\": 898, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-006.webp\", \"caption\": \"Figure 2. Tokenization pipeline for multi-session LFP signals. Raw signals are first segmented into 500-timestep windows. A per-channel 1D convolution followed by an efficient channel attention (ECA) module produces channel-attended features of shape (B, 96, dch, T ). These features are then mapped through a shared value embedding and a session-specific spatial embedding, whose sum yields the final token representations.\", \"page\": 4, \"index\": 6, \"width\": 908, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-010.webp\", \"caption\": \"Figure 3. The architecture of the multi-session pretrained BiMamba-2 model. As the self-supervised pretraining objective, the model is asked to reconstruct the LFP raw signals from their latent representations using continuous masked autoencoding (CMAE). This allows the model to learn generalizable representations across datasets recorded during diverse experimental tasks. We use a light decoder/predictor (1 layer) compared with the REALM encoder (8 layers) for the CMAE objective. MSE: mean-squared error.\", \"page\": 5, \"index\": 10, \"width\": 900, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-009.webp\", \"caption\": \"Figure 5. Causal behavior decoding performance of REALM compared with causal baselines and ablations. (a, b) Per-session decoding R2 on the held-out Makin (a, N = 5) and Flint (b, N = 3) sessions, comparing two classical regressors (Linear SSM, Wiener Filter) and three causal sequence models (LSTM, LSTM-5M, REALM). Markers denote mean ± std across three seeds. (c) REALM compared against two control variants: REALM RI (random-initialized backbone, no pretraining or distillation) and REALM PT (pretrained CMAE backbone without distillation). Boxes show inter-quartile range and whiskers the full range across the eight held-out sessions × three seeds; points show individual session means. (d, e) Example decoded cursor velocity traces (vx, top; vy , bottom) from REALM (orange) overlaid on ground truth (grey) for one representative Makin (d) and Flint (e) session. (f) Aggregated R2 for all five causal methods pooled across the eight held-out sessions and three seeds.\", \"page\": 9, \"index\": 9, \"width\": 889, \"height\": 543}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-004.webp\", \"caption\": \"Figure 6. Representational alignment between the bidirectional teacher and the causal student (REALM) is induced by retrospective distillation, but absent in a randomly-initialized control (REALM-RI). (a) Segment-level cosine similarity matrices between final-layer teacher and student embeddings, evaluated on N = 800 held-out segments grouped by session (gray lines): cos(Teacher, REALM) (left) and cos(Teacher, REALM-RI) (right). (b) Per-layer Pearson correlation between teacher and student representations, plotted as a function of student-layer index. (c) Layer-wise centered kernel alignment (CKA) between the 10-layer teacher and the 8-layer student: CKA(Teacher, REALM) (left) and CKA(Teacher, REALM-RI) (right). (d) Participation ratio (effective dimensionality) of layer-wise representations as a function of depth, for the teacher, REALM, and REALM-RI.\", \"page\": 10, \"index\": 4, \"width\": 804, \"height\": 605}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-001.webp\", \"caption\": \"Figure 7. 5-fold stacking provides a consistent improvement over single-fold REALM models. (a, b) Per-session decoding R2 of the 5-fold stacked ensemble for the three causal REALM variants (REALM-S, REALM, REALM-L) on the held-out Makin (a) and Flint (b) sessions. (c) Scatter of stacked R2 vs. base-model R2 across all (architecture × held-out session) combinations (n = 24); the dashed line denotes y = x. (d, e) Example decoded velocity traces (vx, top; vy , bottom) comparing ground truth (grey), the 5-fold ensemble prediction (orange), and a single base model (blue), shown for one Makin (d) and one Flint (e) session. (f) Per-architecture lift ∆R2 = R2 stacked −R2 base aggregated across the eight held-out sessions; bars show mean ± SEM and points show individual sessions.\", \"page\": 11, \"index\": 1, \"width\": 780, \"height\": 472}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-008.webp\", \"caption\": \"Figure 8. Real-time streaming inference benchmarks of REALM across hardware platforms and optimization stacks. (a) Average throughput (fps) for the three causal REALM variants under six platform/optimization configurations on Raspberry Pi 5 and Jetson Orin Nano. (b) Average throughput on Jetson Orin Nano comparing single-fold against 5-fold ensemble inference, for CUDA Graphs and TensorRT FP16 backends. (c, d) Per-step p95 latency (log scale) for the same configurations as (a, b). The dashed line marks the 10 ms / 100 Hz BCI streaming requirement.\", \"page\": 12, \"index\": 8, \"width\": 663, \"height\": 662}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-005.webp\", \"caption\": \"Figure 9. Bidirectional behavior decoding performance of REALM-bi compared with non-causal baselines and ablations. (a, b) Per-session decoding R2 on the held-out Makin (a, N = 5) and Flint (b, N = 3) sessions, comparing two classical regressors with bidirectional input access (Linear SSM (bi), Wiener (bi)) and three non-causal sequence models (BiLSTM, BiLSTM-5M, REALM-bi). Markers denote mean ± std across three seeds. (c) REALM-bi compared against two control variants: REALM-bi RI (random-initialized backbone, no pretraining or distillation) and REALM PT (pretrained CMAE backbone without distillation, shared with the causal pipeline). Boxes show inter-quartile range and whiskers the full range across the eight held-out sessions × three seeds; points show individual session means. (d, e) Example decoded cursor velocity traces (vx, top; vy , bottom) from REALM-bi (orange) overlaid on ground truth (grey) for one representative Makin (d) and Flint (e) session. (f) Aggregated R2 for all five non-causal methods pooled across the eight held-out sessions and three seeds.\", \"page\": 14, \"index\": 5, \"width\": 882, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-002.webp\", \"caption\": \"Figure 10. Scaling behavior of REALM compared to CrossModalDistill (CMD) on the eight held-out sessions. (a) Per-session decoding R2 for three causal REALM students (REALM-S, REALM, REALM-L), two bidirectional teachers (REALM-bi, REALM-Lbi), and two CrossModal baselines (CrossModal Sup; CrossModal Full Sup). Boxes show inter-quartile range and whiskers the full range across the eight held-out sessions × three seeds; points show individual session means. (b) Scaling curves of R2 vs. parameter count for REALM (purple, causal) and REALM-bi (blue, bidirectional). The dashed teal line marks CrossModal Full Sup at its 10.6M parameter point. (c, d) Per-model performance deficit ∆R2 = R2 REALM −R2 CMD averaged across the eight held-out sessions; bars show mean ± SEM across three seeds. (c) vs. CrossModal Sup. (d) vs. CrossModal Full Sup.\", \"page\": 15, \"index\": 2, \"width\": 501, \"height\": 566}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-011.webp\", \"caption\": \"Figure 11. Unsupervised REALM closes most of the gap to supervised decoding, while fully-unsupervised MAE+probe baselines lag substantially. (a, b) Per-session decoding R2 on the held-out Makin (a, N = 5) and Flint (b, N = 3) sessions for four unsupervised variants: Unsup REALM (2.1 M, causal), Unsup REALM-bi (5M, bidirectional), Full Unsup REALM (5M MAE + linear probe), and Full Unsup REALM context (5 M MAE + MLP probe with ±10-step context). Markers denote mean ± std across three seeds. (c) Aggregated R2 across the eight held-out sessions × three seeds. (d, e) Example decoded velocity traces (vx, top; vy , bottom) for one Makin (d) and one Flint (e) session, comparing ground truth (grey), Unsup REALM-bi (orange), and Full Unsup REALM (blue). (f) Per-session performance deficit relative to the matched supervised baseline, ∆R2 = R2 unsup −R2 sup. Bars show the mean across eight sessions; points show individual sessions.\", \"page\": 16, \"index\": 11, \"width\": 868, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2605.14867v1/fig-007.webp\", \"caption\": \"Figure 12. Few-shot data efficiency of REALM compared with ablation baselines on Makin (a, b) and Flint (c, d). (a, c) Decoding R2 as a function of per-session training-data ratio (5%, 10%, 20%, 50%, 80%) for four causal variants: REALM RI (random-initialized backbone, no pretraining or distillation), REALM PT (pretrained CMAE backbone without distillation), REALM (causal student distilled from the bidirectional teacher), and REALMbi (the bidirectional teacher itself). Markers denote mean across three random seeds; shaded bands show ±1 SD. (b, d) Data-efficiency summary: R2 retention at 20% training data, defined as R2(20%)/R2(80%)× 100%. Higher values indicate that the model recovers most of its full-data performance from a quarter of the supervision. Bars show mean and points show individual seeds (N=3). On both datasets, the two distillation-based variants (REALMbi, REALM) retain ≥ 86% of their 80%-data R2 when finetuned on only 20% of session-specific training data, whereas the non-distilled controls drop to 70% (REALM PT) and 58–63% (REALM RI). The gap is largest on Makin, where REALM RI shows substantially higher across-seed variance than the distillation-based variants, indicating that retrospective distillation not only yields higher mean performance under data scarcity but also makes the resulting models more reliable across seeds.\", \"page\": 17, \"index\": 7, \"width\": 528, \"height\": 477}]"
motivation: LFP信号具有低功耗、高稳定性的优势，但其解码模型精度较低且常采用不适于实时部署的非因果架构。
method: REALM采用回顾性蒸馏：先预训练双向Mamba-2教师模型（使用掩码自编码），再通过表示对齐与任务监督的联合目标，将其知识迁移至因果学生模型。
result: REALM在行为解码任务上一致优于SOTA因果及非因果LFP模型，同时实现2倍参数量压缩和10倍训练加速。
conclusion: 回顾性蒸馏有效弥合离线与实时神经解码的鸿沟，使纯LFP模型能够实现竞争性能，为下一代无线脑机接口提供了可扩展方案。
---

## 摘要
由于具有高空间和时间分辨率，锋电位活动一直是行为解码的主要神经信号。然而，随着脑机接口（BCI）向着高通道数和无线操作发展，锋电位信号的高采样频率因其高功耗和带宽需求而成为瓶颈。与锋电位相比，局部场电位（LFP）代表了一种不同时空尺度的脑活动，具有长期稳定性提高、能耗降低和带宽需求降低等关键优势。尽管有这些优点，基于LFP的解码模型通常准确性较低，并且常常依赖于不适合实时部署的非因果架构。为解决这些挑战，我们提出了REALM：一个能够实现因果LFP解码的回顾式蒸馏框架。受语音识别中离线到在线蒸馏策略的启发，REALM将预训练的多会话双向LFP模型中的表征知识迁移到用于实时部署的因果模型中。我们首先使用掩码自编码目标预训练了一个双向Mamba-2教师模型。然后，我们通过表征对齐和任务监督的联合目标，将该教师模型蒸馏为一个紧凑的学生模型。REALM在行为解码方面一致地优于基于LFP的因果和非因果SOTA方法。值得注意的是，我们的REALM在提高解码性能的同时，实现了参数量减少2倍和训练时间减少10倍。这些结果表明，回顾式蒸馏有效地弥合了离线与实时神经解码之间的差距。REALM表明，仅使用LFP的模型可以在不依赖锋电位信号的情况下实现有竞争力的解码性能，为下一代无线植入式BCI提供了一种实用且可扩展的替代方案。

## 速览
**TLDR**：局部场电位(LFP)因低功耗、低带宽优势适合无线脑机接口，但实时解码精度受限。REALM框架受离线到在线蒸馏启发，先预训练双向Mamba-2教师模型，再通过表示对齐与任务监督蒸馏出因果学生模型，实现实时LFP解码。该方法在解码性能、参数效率和训练速度上显著优于现有方法，为下一代无线植入式脑机接口提供了仅靠LFP的实用方案。 \
**Motivation**：LFP信号稳定、低功耗，但现有LFP解码模型精度较低且多依赖非因果架构，无法满足实时需求。 \
**Method**：提出回顾性蒸馏框架REALM，将预训练的双向Mamba-2教师模型的知识迁移到因果学生模型，结合表示对齐与任务监督。 \
**Result**：REALM在行为解码上全面超越因果与非因果SOTA，同时参数减少2倍，训练时间缩短10倍。 \
**Conclusion**：REALM证明仅用LFP可实现有竞争力的实时解码，回顾性蒸馏有效桥接离线与在线神经解码，是实现低功耗无线BCI的可行路径。

---

## Abstract
Spike activity has been the dominant neural signal for behavior decoding due to its high spatial and temporal resolution. However, as brain-computer interfaces (BCIs) move toward high channel counts and wireless operation, the high sampling frequency of spike signals becomes a bottleneck due to high power and bandwidth requirements. Local field potentials (LFPs) represent a different spatial-temporal scale of brain activity compared to spikes, offering key advantages including improved long-term stability, reduced energy consumption, and lower bandwidth requirement. Despite these benefits, LFP-based decoding models typically show reduced accuracy and often rely on non-causal architectures that are unsuitable for real-time deployment. To address these challenges, we propose REALM: a retrospective distillation framework that enables causal LFP decoding. Inspired by offline-to-online distillation strategies in speech recognition, REALM transfers representational knowledge from a pretrained multi-session bidirectional LFP model to a causal version for real-time deployment. We first pretrain a bidirectional Mamba-2 teacher model using a masked autoencoding objective. We then distill this teacher model into a compact student model via a combined objective of representation alignment and task supervision. REALM consistently outperforms both causal and non-causal LFP-based SOTA methods for behavior decoding. Notably, our REALM improves decoding performance while achieving a $2\times$ reduction in parameter count and a $10\times$ reduction in training time. These results demonstrate that retrospective distillation effectively bridges the gap between offline and real-time neural decoding. REALM shows that LFP-only models can achieve competitive decoding performance without reliance on spike signals, offering a practical and scalable alternative for next-generation wireless implantable BCIs.

---

## 论文详细总结（自动生成）

好的，这是根据您提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义
*   **核心问题**：脑机接口（BCI）中，传统的锋电位（spike）信号虽解码精度高，但其高采样率导致功耗和数据带宽需求巨大，不利于无线、全植入式设备的长期使用。局部场电位（LFP）信号具有低功耗、高长期稳定性的优势，但解码精度往往较低，且现有高性能LFP模型多采用非因果架构，无法实时部署。
*   **整体含义**：论文旨在弥合LFP解码在“高精度离线分析”与“低延迟实时应用”之间的鸿沟。通过提出的REALM框架，首次实现了仅依赖LFP信号、可在低功耗边缘设备上实时运行且达到SOTA精度水平的因果解码模型。

### 2. 论文提出的方法论
REALM的核心思想是“回顾式蒸馏”，将离线非因果模型的强大表征能力迁移到在线因果模型中，其流程分为三个阶段：

*   **第一阶段：自监督预训练（双向教师模型）**
    *   **神经分词器（Neural Tokenizer）**: 将原始LFP信号转换为令牌序列。首先通过共享的1D卷积（Conv1D）提取时序特征，然后使用高效通道注意力（ECA）模块自适应加权通道，最后通过共享值嵌入和会话特定空间嵌入之和得到最终令牌。
    *   **连续掩码自编码（CMAE）**: 对输入的令牌序列进行连续时间块的随机掩码（掩码率60%），迫使模型学习时序依赖。
    *   **双向Mamba-2编码器（BiMamba-2）**: 使用双向Mamba-2层作为教师模型的主体架构，该架构能通过状态空间对偶（SSD）核进行高效线性时间递归，适用于长序列建模。预训练目标是在掩码位置重构原始LFP信号，损失函数为掩码位置上的均方误差（MSE）。
        $$L_{CMAE} = \frac{1}{|\mathcal{M}|} \sum_{i \in \mathcal{M}} \|\hat{\mathbf{x}}_i - \mathbf{x}_i\|^2_2$$

*   **第二阶段：回顾式知识蒸馏（从双向教师到因果学生）**
    *   **目标**：将知识从冻结参数的双向教师模型迁移到严格因果的单向Mamba-2学生模型。
    *   **联合蒸馏目标**：结合表征对齐损失和任务监督损失。
        $$L_{distill} = \underbrace{\lambda_{repr} \left(1 - \frac{1}{T} \sum_{t=1}^{T} \frac{\langle z_t^{(s)}, z_t^{(t)} \rangle}{\|z_t^{(s)}\|_2 \|z_t^{(t)}\|_2} \right)}_{\text{表征对齐（余弦相似度最大化）}} + \underbrace{\frac{1}{2T} \sum_{t=1}^{T} \|\mathbf{v}_t - g_\psi(z_t^{(s)})\|^2_2}_{\text{速度预测（任务监督）}}$$
        其中，$z_t^{(s)}$和$z_t^{(t)}$分别是学生和教师的编码器输出表征，$g_\psi$是速度解码头，$\mathbf{v}_t$是真实手部速度。

*   **第三阶段：下游任务微调与评估**
    *   蒸馏得到的因果学生模型可直接用于行为预测任务，或在特定会话上进行微调。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练集**（总计约130小时）：来自3个数据集（Brochier, Churchland, Even-Chen）的6只猴子的多会话LFP数据。
    *   **下游任务评估集**：从Makin数据集（猴子I，5个会话）和Flint数据集（猴子C，3个会话）中预留的未见会话。任务是根据M1脑区的LFP信号解码2D光标速度。
*   **基准与方法对比**：
    *   **经典方法**：Linear SSM、Wiener Filter。
    *   **深度学习方法（因果）**：LSTM、参数匹配的LSTM-5M。
    *   **消融对比（因果）**：随机初始化的同架构学生模型（REALM-RI）、仅预训练未蒸馏的模型（REALM PT）。
    *   **深度学习方法（非因果）**：BiLSTM、参数匹配的BiLSTM-5M。
    *   **SOTA跨模态蒸馏方法**：CrossModalDistill（依赖锋电位信号）。
    *   **性能指标**：使用决定系数$R^2$作为解码成功的主要度量。

### 4. 资源与算力
*   **训练硬件**：所有模型均在**单张NVIDIA A100 GPU**上训练。
*   **训练时长**：总耗时约10小时。其中，教师模型预训练约7.5小时，蒸馏阶段约1.5小时（Makin），每会话微调约20分钟。

### 5. 实验数量与充分性
论文进行了大量且充分的实验来验证其方法，主要包括：
*   **主要因果解码实验**：在2个数据集的8个会话上，对比5种因果方法。
*   **消融实验**：验证了预训练和蒸馏各自的作用。
*   **表示对齐分析**：通过4个角度（片段检索、逐层相关性、CKA相似性、有效维度）深入分析蒸馏是否以及如何成功对齐表征。
*   **集成学习实验**：验证5折模型堆叠的进一步提升效果。
*   **硬件部署评估**：在Raspberry Pi 5和NVIDIA Jetson Orin Nano上，评估不同优化配置（INT8量化、CUDA Graphs、TensorRT FP16）下的吞吐量和延迟。
*   **双向模型扩展**：证明了该方法在非因果（双向）模型上同样有效，并刷新了SOTA。
*   **跨模态对比实验**：证明REALM仅用LFP即可达到或超越依赖于锋电位的CrossModalDistill方法。
*   **无监督学习实验**：评估无监督蒸馏和全无监督流程的表现。
*   **少样本数据效率实验**：测试模型在少量标注数据（5%-80%）下的性能。

这些实验覆盖了性能对比、机制分析、部署验证、方法泛化及效率评估，设计系统且全面，通过多种基线、消融和统计检验确保了客观性和公平性。

### 6. 论文的主要结论与发现
*   **性能卓越**：因果REALM在LFP行为解码上建立了新的SOTA（$R^2=0.711$），显著优于所有因果基线。非因果变体REALM-bi（$R^2=0.775$）同样超越所有非因果基线及依赖锋电位的跨模态蒸馏方法。
*   **蒸馏有效**：性能提升源于结构化的表征迁移，而非模型容量增加。蒸馏让学生模型成功复现了教师模型的层状特征层次结构和表征空间的几何压缩。
*   **高效实用**：REALM成功实现了仅基于LFP的模型在边缘设备上的实时（>100Hz）运行，且具有显著的少样本学习能力（使用20%的会话标签即可恢复超过86%的性能），极具临床转化潜力。

### 7. 优点：方法或实验设计上有哪些亮点
*   **开创性工作**：首次构建了纯LFP的基础模型，并成功实现了从离线到在线、可实时部署的因果解码，填补了领域空白。
*   **方法创新**：“回顾式蒸馏”策略巧妙地将语音识别领域的成功经验迁移到神经解码，通过表征对齐弥合了因果与非因果模型间的鸿沟，为解决该问题提供了优雅的方案。
*   **实现高效部署**：结合Mamba架构的线性复杂度与TensorRT等推理优化技术，真正在低成本、低功耗的边缘设备上实现了高性能实时推理。
*   **实验详尽且说服力强**：不仅提供了全面的性能对比，还对蒸馏机制进行了多维度的可视化分析，并通过硬件部署、跨模态对比和无监督实验，多方位证明了方法的有效性和实用性。

### 8. 不足与局限
*   **实验范围局限**：实验仅在猕猴的2D光标运动控制任务上进行，尚未扩展到人类受试者、其他运动类型（如离散运动、3D运动）或更复杂的任务。
*   **长期稳定性未经实测**：尽管LFP理论上长期稳定，但研究未在数月或数年的电极退化等真实场景下验证模型鲁棒性。
*   **因果与非因果差距不可完全消除**：因果学生模型与双向教师模型之间仍存在约0.07 $R^2$的性能差距，这似乎是移除未来信息造成的不可约损失。
*   **能耗评估缺失**：论文评估了延迟和吞吐量，但未直接评估边缘设备上的每次推理能耗，这对于植入式设备的电源预算至关重要。
*   **数据和模型规模可能限制上限**：双向模型在约5M参数时性能饱和，暗示预训练数据量（130小时）或教师模型容量可能成为进一步扩大模型规模的瓶颈。

（完）
