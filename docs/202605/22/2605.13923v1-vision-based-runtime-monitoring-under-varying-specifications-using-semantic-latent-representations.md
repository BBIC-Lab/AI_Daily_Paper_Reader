---
title: Vision-Based Runtime Monitoring under Varying Specifications using Semantic Latent Representations
title_zh: 基于语义潜在表征的变规范下视觉运行时监控
authors: "Bardh Hoxha, Oliver Schön, Hideki Okamoto, Lars Lindemann, Georgios Fainekos"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13923v1"
tags: ["query:ndai"]
score: 9.0
evidence: 使用语义潜在表示监控过去时间信号时序逻辑
tldr: 该研究针对视觉部分可观测环境，提出可重用的运行时监控器，可认证过去时间信号时序逻辑公式而不需逐公式重训。通过引入语义基础表示（原子鲁棒性向量）和滚动预测监控器，结合保形预测提供有限样本覆盖保证，在基准和真实驾驶数据上验证了有效性与长期范围紧致性提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13923v1/fig-002.webp\", \"caption\": \"Fig. 1. Left: A single trained encoder enc𝜃 maps vision inputs to a latent interface B𝑡 from which formula-specific decoders evaluate any formula 𝜑 ∈ F in a target fragment F. Right: Two choices of interface basis.\", \"page\": 2, \"index\": 2, \"width\": 938, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-005.webp\", \"caption\": \"Fig. 3. Monitor architectures for both benchmarks. Both share a CNN+attention encoder and temporal fusion module. (a) The rolling monitor predicts 𝑚 current-timestep predicates, accumulates them in a streaming buffer, and applies the conformal radius 𝑞𝜑 before temporal composition (interval STL). (b) The semantic-basis monitor predicts 2𝑚 | I | temporal atoms directly; 𝑞𝜑 is applied after composition via the formula’s parse tree. As a concrete instance, the safety specification 𝜑safe = ⊡[0,𝐾 ]𝜇clear has decoder dec𝜑safe (BA 𝑡 ) = 𝜌(⊡[0,𝐾 ]𝜇clear, 𝑥, 𝑡)— a single coordinate of BA 𝑡 . The reach-avoid specification\", \"page\": 5, \"index\": 5, \"width\": 412, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-001.webp\", \"caption\": \"Fig. 4. (a) Crossroad scenario: robot (blue) navigates toward the goal (green) via a CBF controller while three pedestrians converge. Cones show predicate sectors (45◦ half-angle); dashed circle: 𝑑safe=1.0 m. (b–d) 64×64 observations under image nuisances (fog, compression, noise).\", \"page\": 6, \"index\": 1, \"width\": 746, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-003.webp\", \"caption\": \"Fig. 5. Rolling and semantic monitors on a WOMD scenario (88 steps, 8.8 s). (a) Bird’s-eye view of the ego vehicle (blue) and surrounding agents (red); inset: 30 m monitoring viewport. (b–c) Rolling monitor for ⊡[0,4] 𝑝front and ⊡[0,4] 𝑝clear. (d–e) Semantic monitor for the same specifications. Ground truth: black; prediction: dashed; conformal band: blue; white = Safe, gray = Uncertain. Animated version: video.\", \"page\": 7, \"index\": 3, \"width\": 871, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-004.webp\", \"caption\": \"TABLE I Results under Level-2 (random-time) and Level-1 (episodewise) calibration (𝛼=0.10). CSR: certified safe rate. Prec: precision (fraction of safe certifications that are correct). FPR: false-positive rate. GT: ground-truth safe rate (“—” when GT=100%).\", \"page\": 8, \"index\": 4, \"width\": 1064, \"height\": 632}]"
motivation: 现有基于视觉的运行时监控往往需要每个公式重新训练，缺乏可重用性及有限样本下的认证保证。
method: 提出语义基础作为最小预测目标，任意公式通过解析树解码；同时引入仅预测当前谓词的滚动监控器，结合单次保形校准覆盖整个逻辑片段。
result: 在基准上，语义基础监控器在长期界限紧密度提升达4倍，滚动监控器短期更优；在Waymo真实数据上两者均满足覆盖率保证。
conclusion: 可重用的认证监控器能高效从视觉推断时态逻辑安全性质，在长期预测中语义基础方法优势明显。
---

## 摘要
我们研究在部分可观测条件下，基于视觉观测的过去时信号时序逻辑（ptSTL）的认证运行时监控。该监控器需从图像中推断与安全相关的量并提供有限样本保证，同时具有可重用性：一经训练和校准，应能认证目标片段中的任意公式，无需为每个公式重新训练。对于由有限时间原子词典导出的片段，我们证明了语义基，即原子鲁棒性得分向量，是单调、1-Lipschitz 可重用接口类中的最小预测目标：任意公式由一个从语法树推导出的确定性解码器评估，且单次共形校准即可认证整个片段，无需联合界。我们还引入了一种滚动预测监控器，它仅预测当前谓词值并在线重构时间历史；这更易于学习，但在长时间范围内会变得保守。在一个行人路口基准测试中，滚动监控器在短时域上实现了更紧的认证界，而语义基监控器在长时域上最多紧至4倍。我们在真实世界的Waymo驾驶数据上验证了所提出的监控器，两者均经验性地满足共形覆盖保证。

## Abstract
We study certified runtime monitoring of past-time signal temporal logic (ptSTL) from visual observations under partial observability. The monitor must infer safety-relevant quantities from images and provide finite-sample guarantees, while being \emph{reusable}: once trained and calibrated, it should certify any formula in a target fragment without per-formula retraining. For fragments induced by a finite dictionary of temporal atoms, we prove that the \emph{semantic basis}, the vector of atom robustness scores, is the minimum prediction target within the class of monotone, 1-Lipschitz reusable interfaces: any formula is evaluated by a deterministic decoder derived from the parse tree, and a single conformal calibration pass certifies the entire fragment with no union bound. We also introduce a \emph{rolling prediction monitor} that predicts only current predicate values and reconstructs temporal history online; this is easier to learn but grows conservative at long horizons. On a pedestrian-crossroad benchmark, rolling achieves tighter certified bounds at short horizons while the semantic-basis monitor is up to 4-times tighter at long horizons. We validate the presented monitors on real-world Waymo driving data, where both monitors satisfy the conformal coverage guarantee empirically.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：在自主系统中，运行时监控用于评估系统部署期间是否满足安全规范。实际场景中规范会随任务变化，要求监控器可重用，即无需为每个新规范重新训练。
- **核心问题**：在部分可观测（仅视觉图像）条件下，建立一个可重用的、认证的运行时监控器，能够处理一类过去时信号时序逻辑（ptSTL）公式，提供有限样本覆盖保证。
- **挑战**：从图像推断安全相关的物理量（距离、速度等）存在不确定性；传统方法每换一个规范就需要重新训练预测器和校准，缺乏可重用性。
- **整体含义**：论文试图找到一种最小且足够的中间表示（接口），使得单个预训练编码器配合确定性解码器即可覆盖整个公式片段，并用共形预测一次性完成全片段的认证界。

### 2. 论文提出的方法论

- **核心思想**：
  - 构建一个语义接口（semantic interface），使视觉观测映射到可重用的中间表示，任意目标片段的公式通过确定性解码器评估，无需重新学习。
  - 引入**语义基（semantic basis）**：对于由有限原子字典 $\mathcal{A}$ 及其 $\wedge / \vee$ 闭包定义的片段 $\mathcal{F}$，语义基 $\mathbf{B}_t^\mathcal{A} = [\rho(a_1, x, t), \dots, \rho(a_r, x, t)]$ 是支持该片段的最小单调 1-Lipschitz 可解码表示。
  - 提出**滚动预测监控器（rolling monitor）**：仅预测当前时刻的谓词向量，在线积累成历史窗口 $\mathbf{B}_t^P$，再执行时间组合，学习难度低但在长时间域保守。

- **关键技术细节**：
  - **基础定义**：定义原子字典 $\mathcal{A}$ 及其诱导片段 $\mathcal{F}(\mathcal{A})$，以及最大时域 $K_{\max}$。
  - **谓词历史基**：$\mathbf{B}_t^P = [\mu_k(x,t-j)]_{k,j}$，维度 $m(K_{\max}+1)$，片段无关，支持任意有界ptSTL公式。
  - **语义基最小值定理**：证明 $\mathbf{B}_t^\mathcal{A}$ 是所有支持 $\mathcal{F}(\mathcal{A})$ 的单调 1-Lipschitz 表示中的最小统计量（信息序下）。
  - **共形打分与校准**：
    - 定义每个基坐标的单边高估误差 $e_{t,\ell} = \max(0, \hat{B}_{t,\ell} - B_{t,\ell})$，片段级得分 $s_{\mathcal{F}}(t) = \max_\ell e_{t,\ell}/\sigma_\ell$，公式级得分 $s_{\varphi}(t) = \max_{\ell \in \mathrm{supp}(\varphi)} e_{t,\ell}/\sigma_\ell$。
    - 使用 split-conformal 算得分分位数 $\hat{Q}_{1-\alpha}$，得到校正后的基下界 $\underline{B}_{t,\ell} = \hat{B}_{t,\ell} - \tilde{C} \sigma_\ell$。
    - 由单调性，$\mathrm{dec}_\varphi(\underline{\mathbf{B}}) \le \rho(\varphi, x, t)$，且单次校准即可同时保证全片段所有公式（无需联合界）。
  - **两种校准级别**：Level‑2 随机时间保证（保守性低），Level‑1 逐情节所有时间步同时保证（更保守）。
  - **架构差异**：滚动监控器在时间组合前校准（误差随支持尺寸增长）；语义基监控器预测时间聚合后的原子，再解码，校准时已经聚合，故半径几乎不随时域增长。

- **算法流程（文字描述）**：
   1. 用 CNN 编码观测窗口得到潜在向量。
   2. 预测头输出 $\hat{\mathbf{B}}_t$ （语义基或谓词向量）。
   3. 用校准集计算得分分位数 $\tilde{C}$。
   4. 运行时，给定任意 $\varphi\in\mathcal{F}$，通过其解析树构造单调 1‑Lipschitz 解码器，从 $\underline{\mathbf{B}}_t$ 计算认证下界。

### 3. 实验设计

- **数据集/场景**：
  - **仿真交叉路口场景（Crossroad）**：CBF 控制的机器人与三个行人，俯视 64×64 图像，含雾、压缩、噪声等扰动。
  - **Waymo Open Motion Dataset (WOMD v1.3.1)**：真实自动驾驶场景，渲染 64×64 鸟瞰图，使用 50,000 训练、1,066 校准、567 测试情景（~50k 时间步）。
- **评价指标**：
  - 共形半径 $q_\varphi$（越小越紧）。
  - 认证安全率（CSR）、精度（Prec）、假阳性率（FPR）。
  - 真实安全率（GT）作为参考。
- **对比方法**：
  - **滚动监控器**（滚动预测 + 组合前校准）。
  - **语义基监控器**（直接预测原子 + 组合后校准）。
  - **基于 Bonferroni 校正的观察器基线**：预测谓词值，构建对称共形区间，用区间 STL 语义传播，对活跃谓词-时滞支持集进行 Bonferroni 校正。
  - 多种公式：时域缩放公式 $\square_{[0,K]} p_f$（$K=1,4,16$）、复合安全公式、最终可达性公式等。

### 4. 资源与算力

- 文中**未明确提及**所用 GPU 型号、数量、训练时长或具体算力消耗。
- 仅说明使用共享 CNN 编码器（128 维潜在空间）和 4 帧历史窗口，未给出计算开销细节。

### 5. 实验数量与充分性

- **实验组数**：两个主要测试域（Crossroad 和 WOMD），每个域均测试了 Level‑2 和 Level‑1 校准，涉及数十种不同公式（时域缩放、复合安全、可达性等）。
- **评价充分性**：
  - 多个公式覆盖了片段内不同复杂度。
  - 同时报告了认证界紧度、安全率、精度，以及经验覆盖度验证了理论保证。
  - 对比了滚动、语义基和 Bonferroni 基线，覆盖不同保守度层次。
  - 未进行明显的消融研究（如不同字典大小、不同 $K_{\max}$ 对紧度的影响），但现有对比已足够支撑核心结论。
- **客观公平性**：不同监控器使用相同主干网络和共形框架，校准方式保持公平，基线的 Bonferroni 校正是其理论安全保证所必需。

### 6. 论文的主要结论与发现

- 语义基是支持 ptSTL 片段的最小单调 1‑Lipschitz 接口，允许一次性共形校准认证全片段。
- 滚动监控器在短时域上因预测任务简单而给出更紧的界；但随着时域 $K$ 增大，其校准半径线性膨胀。
- 语义基监控器在长时域上优势明显（Crossroad 上 $K=16$ 时半径仅 0.56，滚动为 2.25，差距达 4 倍）。
- 在真实 Waymo 数据上，Level‑2 校准下语义基在所有时域均优于滚动，说明现实场景学习难度差距缩小。
- 两种架构均满足经验覆盖保证（≥ $1-\alpha$），并远优于 Bonferroni 基线。

### 7. 优点

- **理论贡献**：严证明语义基的最小性，将表示能力与认证紧度联系起来。
- **可重用性**：单次训练与校准，任意公式查询即时得到认证下界，实用性强。
- **共形保证的结构化利用**：利用解码器的单调 1‑Lipschitz 性质实现全片段认证，避免多重检验校正带来的保守。
- **架构灵活**：主干网络可替换（如 ViT），仅需重训练预测头。
- **真实场景验证**：在 Waymo 数据上展示了实际可行性。

### 8. 不足与局限

- **算力报告缺失**：未说明训练所需 GPU 资源、时间，复现成本不透明。
- **观测限制**：仅使用单一俯视/鸟瞰图像，实际应用中可能需要多模态融合。
- **动态环境适应性**：假设校准集和测试集交换可交换，但未深入处理分布偏移（仅提及可引入稳健共形方法）。
- **片段选择依赖**：性能依赖于原子字典设计，字典过大会增加预测维度；字典选择方法未讨论。
- **实验多样性**：仅在两个场景测试，未涉及其它传感器噪声类型或更复杂的城市路况。
- **仅处理过去时逻辑**：未来时规范不能直接使用此框架。

（完）
