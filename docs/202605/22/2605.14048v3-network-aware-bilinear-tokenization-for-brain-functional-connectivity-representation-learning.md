---
title: Network-Aware Bilinear Tokenization for Brain Functional Connectivity Representation Learning
title_zh: 面向脑功能连接表征学习的网络感知双线性分词方法
authors: "Leo Milecki, Qingyu Hu, Bahram Jafrasteh, Mert R. Sabuncu, Qingyu Zhao"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14048v3"
tags: ["query:ndai"]
score: 9.0
evidence: 脑功能连接表征学习
tldr: 本文针对静息态脑功能连接(FC)嵌入的tokenization问题，提出NERVE框架，将FC矩阵按大脑网络模块划分成异构patch，并通过结构化双线性分解嵌入，既保留网络身份又将参数从二次降为线性。在三个发育队列上的行为与病理预测任务中，NERVE相比传统MAE和图方法得到更稳定、可迁移的表示，消融实验证实了双线性嵌入和解剖分区的重要性，突出了领域结构先验在自监督学习中的价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14048v3/fig-001.webp\", \"caption\": \"Fig. 1. Overview of NERVE. A. The functional connectivity (FC) matrix is partitioned into patches defined by pairs of functional brain networks. B. Network-aware Bilinear Tokenization. Each functional network is assigned learnable networkspecific weights at initialization, and patch tokens are computed through structured bilinear interactions between network weights during forward. C. MAE Framework. We apply a standard MAE framework to the proposed network-aware tokens, thereby introducing a functionally informed inductive bias over connectivity structure.\", \"page\": 3, \"index\": 1, \"width\": 725, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.14048v3/fig-002.webp\", \"caption\": \"Table 1. Behavioral prediction performance across developmental cohorts. We report Pearson correlation r±∆ with bootstrap 95% CI half-width ∆ from 10-fold cross-validation. n.: negative values, OOD: Out-of-domain.\", \"page\": 6, \"index\": 2, \"width\": 725, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.14048v3/fig-003.webp\", \"caption\": \"Table 3. Parcellation and network structure ablation. We report Pearson r on 10-fold cross-validation. For the Permutation baselines, we report mean ± std r over 100 random permutations of region-to-network assignment.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 240}]"
motivation: 现有FC表示学习中的tokenization方法忽视大脑网络的模块化组织结构，未与功能网络对齐。
method: NERVE将FC矩阵分割为网络内和网络间的连接块patch，并采用双线性分解进行嵌入，降低复杂度。
result: 在ABCD、PNC和CCNP发育队列上，NERVE在行为与病理预测中表现优于基线，且跨队列迁移稳定性高。
conclusion: 融入大脑网络先验的NERVE框架能学习更鲁棒的功能连接表示，验证了领域知识在自监督FC建模中的关键作用。
---

## 摘要
掩码自编码器（MAE）近期在静息态脑功能连接（FC）的自监督表征学习中展现出潜力。然而，一个根本问题仍未解决：如何对FC矩阵进行分词，以契合大规模脑网络固有的模块化组织？现有方法通常采用以脑区为中心或基于图的方案，将FC视为结构同质元素，忽视大规模脑网络组织。我们提出NERVE（基于双线性分词的脑功能连接网络感知表征），这是一种自监督学习框架，通过将FC矩阵划分为网络内与网络间连接块的补丁，重新定义了FC分词。不同于图像MAE中固定大小补丁共享分词器的方式，由网络对定义的FC补丁大小各异，对应不同的功能角色。为解决这一问题，NERVE通过一种新颖的结构化双线性分解来嵌入FC补丁。该公式保留了网络身份，并将参数复杂度从网络数量的二次方缩减为线性缩放。我们在三个大规模发育队列（ABCD、PNC和CCNP）上评估NERVE用于行为和精神病理性预测。与结构无关的MAE变体和基于图的自监督基线相比，所提出的网络感知公式生成了更稳定、更可迁移的表征，尤其在跨队列评估中。消融研究证实，所提出的双线性网络嵌入和解剖学基础的脑区分割对性能至关重要。这些发现强调了将领域特定的结构先验融入功能连接组学自监督学习的重要性。代码见：https://github.com/leomlck/NERVE。

## Abstract
Masked autoencoders (MAEs) have recently shown promise for self-supervised representation learning of resting-state brain functional connectivity (FC). However, a fundamental question remains unresolved: how should FC matrices be tokenized to align with the intrinsic modular organization of large-scale brain networks? Existing approaches typically adopt region-centric or graph-based schemes that treat FC as structurally homogeneous elements and overlook the large-scale network brain organization. We introduce NERVE (Network-Aware Representations of Brain Functional Connectivity via Bilinear Tokenization), a self-supervised learning framework that redefines FC tokenization by partitioning FC matrices into patches of intra- and inter-network connectivity blocks. Unlike image-based MAE, where fixed-size patches share a common tokenizer, FC patches defined by network pairs are heterogeneous in size and correspond to distinct functional roles. To resolve this problem, NERVE embeds FC patches through a novel structured bilinear factorization. This formulation preserves network identity and reduces parameter complexity from quadratic to linear scaling in the number of networks. We evaluate NERVE across three large-scale developmental cohorts (ABCD, PNC, and CCNP) for behavior and psychopathology prediction. Compared to structurally agnostic MAE variants and graph-based self-supervised baselines, the proposed network-aware formulation yields more stable and transferable representations, particularly in cross-cohort evaluation. Ablation studies confirm that the proposed bilinear network embedding and anatomically grounded parcellation are critical for performance. These findings highlight the importance of incorporating domain-specific structural priors into self-supervised learning for functional connectomics. Code is available at: https://github.com/leomlck/NERVE.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，以下是对这篇论文的详细中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景与挑战**：静息态功能磁共振成像（rs-fMRI）衍生的功能连接（FC）矩阵是研究个体大脑组织差异及其与行为、认知关联的核心工具。然而，从高维、低信噪比的FC中提取紧凑且有预测力的表征仍是一大挑战。掩码自编码器（MAE）为此提供了自监督学习框架，但其如何将FC矩阵划分为有意义的“补丁”（tokenization）尚无标准方案，这成为应用MAE于FC的关键瓶颈。
*   **核心问题**：现有方法（如以脑区为中心或基于图的方案）将FC视为结构同质元素，忽视了**大规模脑网络固有的模块化组织**。论文旨在回答：**“如何对FC矩阵进行分词，以使其与大脑功能网络的内在模块化结构对齐？”**。
*   **整体含义**：本文的核心思想是，**领域特定的结构先验（大脑网络组织）是提升FC表征学习性能和泛化能力的关键**。通过设计与大脑网络结构相契合的分词策略，能够学到更稳定、更具可迁移性的脑连接表征。

### 2. 论文提出的方法论

*   **核心思想**：类比图像MAE中空间邻近像素构成图像块，提出将**属于同一功能网络的脑区之间的连接（网络内连接）和不同功能网络之间的连接（网络间连接）作为基本的“补丁”单元**。这样，每个补丁都对应特定的网络内或网络间交互，携带明确的功能角色。
*   **技术框架：NERVE**
    1.  **基于网络的补丁划分（Network-based Patching）**：根据一个预定义的脑图谱（如Schaefer的17网络分区），将R个脑区划分到$N_n$个功能网络，然后将FC矩阵重组成$N_{patch} = N_n(N_n+1)/2$个网络对（$(l, m)$）定义的连接块$x_{l,m}$。这些块大小不一，语义不同。
    2.  **网络感知的双线性分词（Network-Aware Bilinear Tokenization）**：
        *   **动机**：由于每个网络块大小和功能角色各异，简单的共享或独立线性投影存在忽视网络特异性或参数量爆炸（$O(N_n^2)$）的问题。
        *   **机制**：为每个功能网络$l$分配一个可学习的嵌入矩阵$U_l \in \mathbb{R}^{|\mathcal{N}_l| \times d_E}$。对于一个网络块$x_{l,m}$，通过其对应两个网络嵌入矩阵的**Khatri-Rao积**来构建分词器：$W_{l,m} = U_l \odot U_m$。然后，补丁令牌$t_{l,m}$由$t_{l,m} = W_{l,m}^T \text{vec}(x_{l,m})$计算得出。
        *   **优势**：此双线性分解以**网络为中心**进行建模，它将一个连接视为两个网络嵌入在相应维度上的乘积，既**保留了网络身份**，又将参数复杂度从$O(N_n^2 \cdot d_E)$（独立参数）或$O((R/N_n)^2 \cdot d_E)$（共享参数）**降至$O(R \cdot d_E)$（线性于脑区数和网络数）**。
    3.  **MAE框架**：生成的网络令牌序列经过随机掩码后，输入一个标准的Transformer编码器-解码器架构进行重建。解码器同样采用双线性解码层将令牌投影回原始连接块空间。最终损失为掩码补丁的重建均方误差：$\mathcal{L}_{\text{recon}} = \frac{1}{|\mathcal{M}|} \sum_{(l,m) \in \mathcal{M}} \| \hat{x}_{l,m} - x_{l,m} \|_2^2$。

### 3. 实验设计

*   **数据集**：
    *   **训练集**：联合**ABCD**（$N=1791$， 9-10岁）和**PNC**（$N=1416$， 8-23岁）两个大规模发育队列的FC矩阵进行自监督预训练。
    *   **独立测试（域外）**：**CCNP**（$N=178$， 6-17岁）发育队列，作为完全未参与训练的外部验证集。
*   **下游任务基准（Benchmark）**：对预训练模型提取的FC表征，使用**核岭回归（KRR）**预测行为和精神病理性分数。
    *   **ABCD**：预测CBCL量表的**内化、外化和总分**。
    *   **PNC & CCNP**：预测RBC统一量表的**内化、外化和一般精神病理（p因子）分数**。
*   **对比方法**：
    *   **自监督基线**：针对FC或图结构的自监督学习方法，包括**BrainMass**（FC MAE变体）、**GraphMAE**、**BrainGSLs**、**GATE**（图SSL）。
    *   **监督学习基线**：在单个数据集上进行端到端训练的先进方法，包括**BrainNetCNN**、**BrainGNN**、**BrainNetTF**。
    *   **消融变体**：为验证分词策略，对比了NERVE框架下的**共享线性投影**和**补丁特定线性投影**。
*   **评估协议**：所有方法使用统一的10折分层交叉验证评估，报告真实值与预测值之间的Pearson相关系数，并通过1000次bootstrap重采样计算95%置信区间，同时使用配对bootstrap检验评估方法间性能差异的显著性。

### 4. 资源与算力

*   **硬件**：所有模型均在**单块NVIDIA L40 GPU**上训练。
*   **超参数与训练时长**：
    *   **训练轮次**：4,000个epochs。
    *   **批次大小**：1,024。
    *   **优化器**：AdamW，学习率$1\times10^{-2}$，权重衰减$1\times10^{-2}$，采用余弦退火调度与400个epoch的线性预热。
    *   **其他**：使用混合精度训练。
    *   **核心模型配置**：基于Schaefer 17网络图谱（$R=400, N_n=17, N_{patch}=153$），掩码率0.5。编码器为4层、4头、$d_E=256$的Transformer；解码器为1层、2头、$d_D=64$的Transformer。

### 5. 实验数量与充分性

*   **实验多样性**：实验设计较为全面。
    *   包含了**3个独立数据集**上的验证，涵盖了**域内和域外**场景。
    *   对比了**7种相关的自监督和监督学习方法**，形成了全面且公平的比较基准。
*   **消融实验充分**：为关键设计选择进行了严格的消融研究：
    *   **分词策略消融**：对比了双线性、共享线性和补丁特定线性投影。
    *   **结构与图谱消融**：通过**随机打乱脑区网络归属**（100次）来验证网络结构的重要性；测试了**无网络意义的`16x16`方形块划分**；验证了**不同网络粒度（7网络与17网络图谱）**的影响。
*   **实验公平性**：所有自监督模型均在同一数据集（ABCD+PNC）上预训练，并使用完全相同的KRR下游评估协议，确保了对比的公平性。

### 6. 论文的主要结论与发现

*   **网络感知分词有效**：NERVE在不同数据集和绝大多数行为预测指标上，性能一致且显著地优于忽视网络结构的自监督基线（如GraphMAE、BrainMass）以及监督方法。
*   **双线性分词优势**：相比于其他分词策略，提出的双线性网络嵌入在参数量与表达力之间取得了最佳平衡，实现最强预测性能，尤其在域内数据上。
*   **跨队列泛化能力强**：NERVE在完全未见的CCNP外部数据集上表现卓越，尤其是在外化症状预测上取得了$r=0.33$的最高相关，证明了所学表征的稳健性和可迁移性。
*   **解剖先验至关重要**：消融实验证实，当移除真实的网络结构（随机排列脑区）或使用无意义的方形块划分时，模型性能显著下降，突显了将领域先验知识（功能网络组织）嵌入模型设计的重要性。

### 7. 优点

*   **巧妙的领域知识融合**：将神经科学中“大规模功能网络”这一核心概念，巧妙地转化为FC矩阵的分词策略，为在自监督学习中引入结构先验提供了新颖视角。
*   **创新的技术设计**：提出的“网络感知双线性分词”方案，优雅地解决了FC补丁大小不一且语义各异的难题，在保留网络特异性的同时，实现了参数的高效性。
*   **评估严谨全面**：在多个独立大队列上进行域内外评估，与多种前沿方法对比，并通过多组消融实验剥离关键组件的影响，结论具有高度说服力。
*   **可复现性强**：提供了开源代码，并清晰描述了预处理流程、模型配置和评估协议。

### 8. 不足与局限

*   **应用场景局限**：仅在**发育人群的精神病理学和行为预测**任务上进行了验证，方法的普适性（如成人、神经退行性疾病、认知能力预测）有待进一步检验。
*   **依赖于静态FC和预定义图谱**：模型目前基于静态FC和固定的17网络Schaefer图谱。对图谱选择的敏感性以及如何融入动态FC信息，仍是开放的挑战。
*   **预测性能提升幅度**：虽然提升显著且稳定，但预测行为分数的**绝对相关系数仍然不高**（$r<0.35$），这是整个领域的普遍现状，表明从FC预测复杂行为依然困难。
*   **可解释性探索不足**：论文重点在于提升预测性能，对模型所学网络权重的具体含义、令牌之间的注意力模式等可解释性分析尚处于展望阶段。

（完）
