---
title: "Comparative Analysis of Liquid Neural Networks and LSTM for Sequential Pattern Recognition: Robustness, Efficiency, and Clinical Utility"
title_zh: 液态神经网络与LSTM在序列模式识别中的比较分析：鲁棒性、效率及临床实用性
authors: "Ye Kyaw Thu, Thazin Myint Oo, Thepchai Supnithi"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27467v1"
tags: ["query:ndai"]
score: 9.0
evidence: 比较LNN和LSTM在多模态序列模式识别中的表现
tldr: 针对传统循环神经网络难以建模连续时间动态的问题，本研究在四种序列模态（事件相机、草图、笔迹、临床时序）上对比了液体神经网络（CfC）与LSTM的性能。通过时间丢弃压力测试评估鲁棒性，结果显示液体神经网络参数效率更高，对缺失数据更稳健，在原生时序和临床稀疏数据场景中优势显著。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27467v1/fig-001.webp\", \"caption\": \"Fig. 1. Example visualization of an N-MNIST sample (digit “3”). Top: Accumulated event image over the full recording duration, showing all events (left), ON events (middle), and OFF events (right). Bottom: Temporal slices obtained by accumulating events over successive time windows, illustrating the spatio-temporal evolution caused by saccadic motion.\", \"page\": 3, \"index\": 1, \"width\": 500, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.27467v1/fig-003.webp\", \"caption\": \"Fig. 2. Sequential reconstruction of three “bird” sketches from the QuickDraw dataset. Each row shows the progressive accumulation of stroke points from left (early strokes) to right (complete drawing), illustrating the high intraclass variability in stroke order and spatial structure.\", \"page\": 4, \"index\": 3, \"width\": 500, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.27467v1/fig-004.webp\", \"caption\": \"Fig. 3. Example line images from the IAM Handwriting Database, showing variation in writing style, character spacing, and ink density across different writers.\", \"page\": 4, \"index\": 4, \"width\": 500, \"height\": 575}, {\"url\": \"assets/figures/arxiv/2605.27467v1/fig-002.webp\", \"caption\": \"Fig. 4. Example physiological time-series from the PhysioNet Sepsis-3 dataset. Each line represents one clinical variable (e.g., heart rate, temperature) over time. Measurements are irregularly sampled and contain missing values. The vertical dashed line indicates the onset of sepsis according to the Sepsis-3 definition.\", \"page\": 4, \"index\": 2, \"width\": 500, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2605.27467v1/fig-005.webp\", \"caption\": \"Fig. 5. N-MNIST Architecture: CNN backbone with 128 units CfC/LSTM core.\", \"page\": 5, \"index\": 5, \"width\": 266, \"height\": 577}]"
motivation: 传统离散时间循环网络难以捕捉真实世界连续时序动态。
method: 在四种序列数据集上基准测试液体神经网络与LSTM，并进行时间丢弃鲁棒性实验。
result: 液体神经网络展现出更优的参数效率和对缺失数据的鲁棒性，在时序与临床场景中优势突出。
conclusion: 液体神经网络为序列识别提供了一种更高效、鲁棒的方法，尤其适用于数据稀疏的临床环境。
---

## 摘要
传统的循环神经网络（RNN）和长短期记忆（LSTM）单元以离散时间步运行，往往无法捕捉真实物理过程的流动时间动态。液态神经网络（LNN），特别是闭式连续时间（CfC）网络，通过将隐藏状态演化建模为连续微分方程来解决这一问题。在本文中，我们对四种不同的序列模态进行了全面的基准测试：神经形态事件驱动数据（N-MNIST）、基于笔画的绘图（QuickDraw）、视觉手写（IAM）和生理时间序列（PhysioNet Sepsis-3）。此外，我们使用时间丢弃法进行了严格压力测试，以评估模型对缺失数据的鲁棒性。我们的发现表明，LNN在原生时间域和数据稀疏常见的临床环境中，始终提供更优的参数效率和显著更高的鲁棒性。本扩展预印本提供了相关数据集和LNN理论谱系的额外背景，并附有详细附录，记录了我们的完整实现和实验设置。

## Abstract
Traditional Recurrent Neural Networks (RNNs) and Long Short-Term Memory (LSTM) units operate on discrete time steps, often failing to capture the fluid temporal dynamics of real-world physical processes. Liquid Neural Networks (LNNs), specifically Closed-form Continuous-time (CfC) networks, address this by modeling the hidden state evolution as a continuous differential equation. In this paper, we conduct a comprehensive benchmarking study across four distinct sequential modalities: neuromorphic event-based data (N-MNIST), stroke-based drawing (QuickDraw), visual handwriting (IAM), and physiological time-series (PhysioNet Sepsis-3). Furthermore, we perform a rigorous stress test using temporal dropout to evaluate model robustness against missing data. Our findings reveal that LNNs consistently provide superior parameter efficiency and significantly higher robustness in natively temporal domains and clinical environments where data sparsity is prevalent. This extended preprint provides additional background on related datasets and the LNN theoretical lineage, supplemented with a detailed appendix documenting our full implementation and experimental settings.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：传统循环神经网络（RNN/LSTM）以离散时间步建模序列，难以捕捉现实物理系统（如临床监护、神经形态事件）的连续时间动态和不规则采样特性。液态神经网络（LNN），尤其是闭式连续时间（CfC）变体，将隐藏状态演化建模为常微分方程（ODE），为连续时间序列建模提供了新的范式。
- **研究动机**：验证 LNN 在多种时序模态下相比 LSTM 的性能、参数效率与鲁棒性，尤其关注在数据缺失、不规则采样等真实场景中的临床实用性。
- **整体含义**：文章通过多域基准测试与严格时间压力测试，揭示 LNN 在原生时序领域及数据稀疏环境下的优势，为其在关键任务（如脓毒症预警）的部署提供依据。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：直接使用已有的 CfC 模型与标准 LSTM 进行头对头对比，不提出新的模型结构。CfC 的“液态”特性源自输入依赖的时间常数，使其能适应不同的时间分辨率。
- **关键技术细节**：
  - **连续时间状态方程**：CfC 的隐藏状态 $h(t)$ 遵循 ODE：
    $$\frac{dh(t)}{dt} = -[A + f(x(t), \theta)] \odot h(t) + f(x(t), \theta) \odot L$$
    其中 $f(\cdot)$ 调制时间常数，$A$ 为泄漏率，$L$ 为目标状态，$\odot$ 表示逐元素乘积。
  - **闭式求解**：采用闭式连续时间解替代数值 ODE 求解器，使训练效率与 LSTM 相当，同时保留连续时间适应性。
  - **输入依赖的时间常数**：$\tau(x, t)$ 随输入动态变化，使网络在信息密集时加速、稀疏时减速，从而自然处理不规则采样与缺失数据。
- **统一对比框架**：四种任务使用相同核心（CfC 或 LSTM），搭配任务特定骨干（CNN、ResNet-6、线性投影等），训练配置（学习率、优化器等）在各任务内部保持一致。
- **鲁棒性评估方法**：在推理时对输入时间步进行随机掩码（temporal dropout），模拟传感器故障或信号丢失，比较模型在不同掩码率下的性能。

## 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法
- **数据集与场景**：
  - **N-MNIST**：神经形态事件驱动视觉，10 类数字识别，评价指标为准确率（Acc）。
  - **Google QuickDraw**：笔画序列草图识别，10 类子集，评价指标为准确率。
  - **IAM**：离线手写体行级别文本识别，评价指标为字符错误率（CER）。
  - **PhysioNet Sepsis-3**：临床时序早期脓毒症预测，二分类，评价指标为精确率（Precision）、召回率（Recall）、F1 与准确率（Accuracy）。
- **对比方法**：仅比较 LNN（CfC）与 LSTM 两种循环核心，所有网络其他部分（骨干、解码器等）完全相同（IAM 中允许 LSTM 使用更大的双向结构以展示效率差异）。
- **Benchmark**：提供基准数值，如 N-MNIST 上 LSTM 准确率 99.13%；IAM 上 LSTM CER 0.1090；PhysioNet 上 LSTM 假阳性数 151。

## 4. 资源与算力
- 文中仅提及实验在一台配备 NVIDIA GPU 的机器上运行，软件环境为 Ubuntu 22.04、PyTorch 2.x，未给出 GPU 型号、数量或具体训练时长。
- 训练超参数（批次大小、轮次等）在附录中列出，但缺乏算力指标（如浮点运算量或总训练时间）的说明，难以评估实际资源消耗。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平
- **实验组数**：四个主要数据集各运行一组 LNN 和一组 LSTM（PhysioNet 额外包含 256 单元 LNN），共约 9 个主实验；鲁棒性测试覆盖两个数据集（N-MNIST、QuickDraw）下的四种掩码率，每种配置对应一次推理。
- **充分性**：
  - **优点**：跨多模态的系列实验较为全面，压力测试直接衡量对数据缺失的鲁棒性，PhysioNet 中增加了假阳性数量的临床实用性分析。
  - **不足**：所有结果仅基于**单次训练**运行（论文明确声明 “a single training run per configuration”），缺少多次重复的均值与方差，削弱了统计可靠性。
  - **公平性**：同任务内 LNN 与 LSTM 的超参数相同，公平性好；但 IAM 任务中 LSTM 使用了 512 单元双向配置，而 LNN 为 256 单元单向，虽然意在展示参数效率，但对直接性能对比造成了不平衡。
  - **局限**：未进行消融实验（如骨干深度影响），也未与其他连续时间模型（如 Neural ODE）或 Transformer 等先进序列模型对比，结论仅限于 LNN vs LSTM 范围内。

## 6. 论文的主要结论与发现
- **连续时间优势**：LNN 能更好地捕捉连续、不规则采样的时序动态，在神经形态事件和临床不规则数据中表现出更强的泛化能力。
- **鲁棒性突出**：在时间丢弃压力测试中，LNN 对缺失数据极为鲁棒。例如，N-MNIST 缺失 30% 事件时，LNN 保持 91.84% 准确率，而 LSTM 骤降至 77.48%；QuickDraw 缺失 70% 时，LNN 准确率领先约 4 个百分点。
- **临床实用性**：在 PhysioNet 脓毒症预测中，LNN 大幅降低假阳性率（Wider LNN 仅 2 例假阳性），精确率高达 0.94，可有效缓解“警报疲劳”，虽然召回率较低。
- **参数效率**：IAM 任务中，单向 256 单元 LNN 的参数远少于双向 512 单元 LSTM，仍取得可比 CER，表明 LNN 的循环效率更高。

## 7. 优点：方法或实验设计上有哪些亮点
- **多模态横向对比**：涵盖事件驱动、笔画、视觉序列和临床时序四类代表性数据，验证了 LNN 的广泛适应能力。
- **独特的鲁棒性测试**：引入推理时时间丢弃压力测试，直接针对现实中传感器失效与不规则采样的痛点。
- **临床视角深入**：不仅报告传统指标，还重点分析假阳性和警报疲劳，使技术评估贴近临床部署需求。
- **开源与可复现**：提供完整代码、数据处理脚本和训练配置，便于复现和公平对比。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **统计可靠性不足**：所有实验仅进行单次运行，无随机种子重复，无法评估性能波动，结论可能受偶然性影响。
- **对比基线有限**：仅与 LSTM 比较，未纳入 GRU、Neural ODE、Transformer 等其他主流或连续时间模型，无法全面定位 LNN 在领域中的位置。
- **性能并非全面领先**：在 QuickDraw 和 IAM 的常规指标上，LSTM 仍取得略优的准确率或 CER；PhysioNet 上 LNN 的召回率极低（0.08~0.10），可能导致严重漏诊，临床实用性存在局限，仅在“高精度、低召回”特定场景下适用。
- **超参数与公平性**：IAM 中 LSTM 与 LNN 的网络容量不对等，削弱了直接性能对比的公平性；未进行超参数搜索或调优，可能未充分发挥两类模型的潜力。
- **应用限制**：LNN 在召回率上的牺牲意味着不能单独用于需要高灵敏度的筛查系统；且缺乏在真实边缘设备上的部署验证，实际效率增益待进一步检验。

（完）
