---
title: Difficulty-Gated Fusion of Reasoning Views for Temporal Retrieval
title_zh: 时间检索中推理视图的难度门控融合
authors: "Jamie Holdcroft, Abdelrahman Abdallah, Adam Jatowt"
date: 2026-08-09
pdf: "https://arxiv.org/pdf/2608.08940v1"
tags: ["query:ndai"]
score: 8.0
evidence: 查询难度门控融合为推理视图学习逐查询权重
tldr: "针对时间检索中查询扩展后的多个推理视图等权融合浪费准确性的问题，本文提出查询难度门控融合方法。从每个视图的分数分布中提取八维签名（如软最大熵、分数差距、离散度），用约千参数的门控网络预测每查询视图权重进行融合，无需推理时标签、重排序或微调检索器。在Tempo基准上提升全部六个检索器，最强达0.297和0.303 nDCG@10，每查询增益显著，并指出每查询视图选择为下一步方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08940v1/fig-001.webp\", \"caption\": \"Figure 1: Difficulty-gated fusion of reasoning views. A temporal query is expanded into 𝐾 reasoning views; each is retrieved by a same frozen retriever, yielding a ranked list and a score distribution whose shape reveals how reliable that view is for this query. A query-performance-prediction signature 𝜙𝑣 summarizes each view’s score geometry, and a tiny gate 𝑔𝜃 (the only learned component, ∼1k parameters, trained leave-one-task-out) maps these signatures to per-query view weights𝑤𝑣 that drive\", \"page\": 2, \"index\": 1, \"width\": 740, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2608.08940v1/fig-002.webp\", \"caption\": \"Table 1: Reasoning-view fusion gated by query difficulty (QPP) on all 13 Tempo tasks (nDCG@10, ×100, 𝐾=3 views).\", \"page\": 3, \"index\": 2, \"width\": 844, \"height\": 321}]"
motivation: 时间检索依赖共享时间推理而非词汇重叠，查询扩展产生的多个推理视图可靠性不同，等权融合会损失准确性。
method: 从每个推理视图的分数分布中提取八维查询性能预测签名，训练约一千参数的查询难度门控网络生成每查询视图权重进行融合。
result: "在Tempo基准上该方法改善了从BERT到7B解码器的全部六个检索器，弱骨干增益更大，最强达0.297和0.303 nDCG@10，每查询增益显著（p<0.001）。"
conclusion: 查询难度门控融合能有效利用推理视图提升时间检索性能，但距离oracle上限0.364仍有空间，每查询视图选择是具体下一步。
---

## 摘要
推理密集型时间检索要求将查询匹配到相关性取决于共享时间推理而非词汇重叠的文档。将查询扩展为若干改写、使其时间意图显式化并分别检索，可提供这种推理，但以等权重融合所得排序会损失准确性：对任一查询而言，只有部分改写可靠。我们提出查询难度门控的推理视图融合。我们从每个视图读取其分数分布的八维特征，该特征由查询性能预测量构成，如 softmax 熵、分数差距和离散度，并由约一千个参数的门控将这些特征映射为每个查询的视图权重。融合排序在推理时不使用相关性标签、不进行重排序，也不微调检索器；门控采用留一任务法训练。在 Tempo 基准上，该方法改善了我们评估的全部六种检索器，从 BERT 编码器到 7B 解码器检索器，且在较弱骨干上提升最大。最强检索器达到 0.297 和 0.303 nDCG@10，并且相比原始查询的每查询增益在配对 bootstrap 下显著（p<0.001）。每查询 oracle 达到 0.364，而我们实现为 0.297，这表明存在提升空间，并将每查询视图选择确定为具体的下一步。

## Abstract
Reasoning-intensive temporal retrieval requires matching a query to documents whose relevance depends on shared temporal reasoning rather than lexical overlap. Expanding a query into several reformulations that make its temporal intent explicit, and retrieving with each, supplies this reasoning, but fusing the resulting rankings with equal weights wastes accuracy: for any single query, only some reformulations are reliable. We propose query-difficulty-gated fusion of reasoning views. From each view we read an eight-dimensional signature of its score distribution, built from query-performance-prediction quantities such as softmax entropy, score gaps, and dispersion, and a gate of roughly one thousand parameters maps these signatures to per-query view weights. The fused ranking uses no relevance labels at inference, no re-ranking, and no fine-tuning of the retriever; the gate is trained leave-one-task-out. On the \textsc{Tempo} benchmark, the method improves all six retrievers we evaluate, from BERT encoders to 7B decoder retrievers, with the largest gains on the weaker backbones. The strongest retrievers reach $0.297$ and $0.303$ nDCG@10, and the per-query gain over the original query is significant under a paired bootstrap ($p<0.001$). A per-query oracle reaches $0.364$ against our realized $0.297$, exposing headroom that identifies per-query view selection as a concrete next step.