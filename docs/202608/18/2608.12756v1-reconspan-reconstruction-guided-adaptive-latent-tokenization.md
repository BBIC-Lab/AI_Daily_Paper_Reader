---
title: "ReconSpan: Reconstruction-Guided Adaptive Latent Tokenization"
title_zh: ReconSpan：重建引导的自适应潜在标记化
authors: Lixing Li
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.12756v1"
tags: ["query:ndai"]
score: 8.0
evidence: 通过自适应潜在分词学习紧凑表示
tldr: 自适应潜在分词旨在将细粒度输入压缩为与输入相关跨度对应的连续表示。本文提出ReconSpan，利用重建引导将文本划分为可由单个上下文前缀码重构的块，并保留该码作为潜在标记。该方法可在训练中调节平均块长（6.5至12.2），在匹配长度下比随机边界保留更多文本，读者可恢复主题信息但丢失精确细节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.12756v1/fig-005.webp\", \"caption\": \"Table 1: Single-code reconstruction on 500 held-out Wikipedia windows, Transformer encoder (left) versus SONAR (right); both use 1024-dimensional codes. Exact is full-window exact match; RL is ROUGE-L; suffix is the mean length of the longest exactly reconstructed suffix — the contiguous run of most-recent tokens the backward decoder emits without an error.\", \"page\": 5, \"index\": 5, \"width\": 718, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2608.12756v1/fig-001.webp\", \"caption\": \"Figure 1: Chunk-length distributions (raw counts) for four stopping rules on the same 500 Wikipedia documents, each capped at 1000 tokens; vertical lines mark the mean. Looser rules (left to right, top to bottom) shift mass toward longer chunks. Teacher-forced decoding evaluates W = 32 positions per endpoint, so the W=32 cap truncates the few windows the decoder could carry further, producing the small pile-up in the length-32 bin (<1% of chunks).\", \"page\": 6, \"index\": 1, \"width\": 714, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2608.12756v1/fig-004.webp\", \"caption\": \"Table 5: Downstream readout from ReconSpan latent tokens (%). Control substitutes another example’s codes. Text gives raw context to the untouched base model, and roundtrip gives it autoencoder-reconstructed text. Metrics are exact next-word accuracy for LAMBADA, four-way closed-set accuracy for AG News, and answer exact match for HotpotQA. A superscript ∗ marks readout above the shuffled control at 95% confidence, using a normal approximation to the difference of binomial proportions with standard errors computed from the evaluation-set size. Task FT is a supervised direct-readout reference, not a matched-training baseline. Mean len. is the average number of input tokens per latent token; unmarked rows use Failure(1).\", \"page\": 8, \"index\": 4, \"width\": 744, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2608.12756v1/fig-003.webp\", \"caption\": \"Figure 2: Single-code reconstruction ROUGE-L versus encode length, on 500 held-out Wikipedia windows (protocol in Section 4.1). Among the three encoders the Transformer hidden-state variant is strongest at the short lengths that set chunk boundaries; the projected SSM state matches it only at 4× the code width, and the Mamba hidden state is weakest. SONAR is shown for reference; its drop below 16 tokens reflects training on full sentences rather than short fragments.\", \"page\": 13, \"index\": 3, \"width\": 829, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2608.12756v1/fig-002.webp\", \"caption\": \"Figure 3: Per-position backward decode reach for one 63-token passage under Failure(1). Red dashed lines mark the eight interior boundaries of the nine chunks selected by ReconSpan, and the gray dotted line marks the 32-token decoding window. Reach changes sharply across positions, producing unequal chunk lengths and concentrating boundaries near positions where reconstruction fails early. This example motivates studying whether the boundaries align with syntax, discourse, or information density; it does not establish such alignment.\", \"page\": 15, \"index\": 2, \"width\": 826, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2608.12756v1/fig-006.webp\", \"caption\": \"Table 7: Datasets used in the reported experiments. “Not specified” means that the official repository does not publish a named license; it is not an inferred license.\", \"page\": 16, \"index\": 6, \"width\": 802, \"height\": 243}]"
motivation: 现有自适应潜在分词缺乏有效的边界选择依据，需要重建引导来保留更多信息。
method: ReconSpan通过反向解码器从每个块的单前缀码重建文本，并以此重建准则划分块、生成潜在标记。
result: 训练得到的自编码器可产生平均块长6.5至12.2，重建引导边界在匹配长度下比随机边界保留更多文本。
conclusion: 潜在序列能保留主题信息但丢失精确细节，表明重建引导分词在信息压缩与细节保留之间存在权衡。
---

## 摘要
自适应潜在标记化将细粒度输入映射为更短的连续表示序列，这些表示与依赖于输入的片段相关联。我们提出 ReconSpan，它将文本划分为若干块，每个块可由一个反向解码器从单个上下文前缀编码重构，并为每个块保留一个这样的编码作为潜在标记。重建准则在块形成时被应用，使得一个训练好的自编码器可以产生从 6.5 到 12.2 的平均块长度。在匹配的平均长度下，重建引导的边界比随机边界保留更多文本。所得潜在序列的读者能够可靠地恢复主题信息，但难以提取确切细节。

## Abstract
Adaptive latent tokenization maps a fine-grained input to a shorter sequence of continuous representations associated with input-dependent spans. We introduce ReconSpan, which divides text into chunks that a backward decoder can reconstruct from a single contextual prefix code and retains one such code as the latent token for each chunk. The reconstruction criterion is applied when chunks are formed, allowing one trained autoencoder to produce average chunk lengths from 6.5 to 12.2. At matched average length, reconstruction-guided boundaries preserve more text than random boundaries. Readers of the resulting latent sequence recover topic information reliably but struggle to extract exact details.