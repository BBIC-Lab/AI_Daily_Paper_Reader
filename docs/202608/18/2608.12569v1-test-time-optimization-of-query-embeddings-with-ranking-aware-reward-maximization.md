---
title: Test-Time Optimization of Query Embeddings with Ranking Aware Reward Maximization
title_zh: 基于排序感知奖励最大化的查询嵌入测试时优化
authors: "Tianyu Chen, Jiaxing Wu"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.12569v1"
tags: ["query:ndai"]
score: 7.0
evidence: 使用排名奖励和冻结编码器对查询嵌入进行测试时优化，学习紧凑向量表示
tldr: "密集检索器在测试时利用排序奖励可提升结果，但现有方法丢弃信号或需更新权重。本文提出TTT-Embed，在冻结嵌入空间学习轻量向量，仅用标量排序分数优化，无需权重与索引修改。通过范围参数控制奖励重用，在MTEB上提升最高+8.36 nDCG@10，并泛化到新查询/任务，解决灾难性遗忘。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.12569v1/fig-001.webp\", \"caption\": \"Figure 1 | TTT-Embed keeps ranking rewards as a reusable learned vector in the embedding space. A query is encoded once by the frozen encoder, the learned vector 𝒗𝑔 is added to the returned query vector. The vector 𝒗𝑔 is the only updated quantity. An external reward model scores the retrieved candidates; the induced teacher distribution 𝑝T is the target that 𝒗𝑔 is trained to match with the student distribution 𝑝S read off the embeddings. The sharing scope 𝑔 determines how broadly each 𝒗𝑔 is reused, and is the only degree of freedom that changes between our three variants. Nothing inside the embedding system is modified.\", \"page\": 2, \"index\": 1, \"width\": 451, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2608.12569v1/fig-002.webp\", \"caption\": \"Figure 2 | TTT-Embed improves retrieval, and the optimal scope changes with the budget. Panel (a) reports the performance gain over raw retrieval across different normalized budgets, averaged over five embedding models. The background shading indicates the best sharing scope for each sampled budget regime. Panel (b) details these results for each individual embedding model. The dotted line marks full query coverage at depth 10. At this threshold, task-wise TTT-Embed improves raw retrieval by 6.36 nDCG@10 points and outperforms cost-matched direct reranking by 3.15 points. Rewards are from Qwen3-Reranker-4B.\", \"page\": 7, \"index\": 2, \"width\": 977, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2608.12569v1/fig-005.webp\", \"caption\": \"Table 6 | Evaluation-set size for each MTEB retrieval task. “HN” abbreviates the HardNegatives revision. The final row is the pooled count; reported performance nevertheless averages the 15 tasks equally.\", \"page\": 16, \"index\": 5, \"width\": 331, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2608.12569v1/fig-004.webp\", \"caption\": \"Table 8 | Complete matched-budget recovery comparison. Scores are nDCG@10 ×100. Panel (a) evaluates embedding-only retrieval before testtime feedback. Panel (b) freezes SKILLRET-SFT0.6B and gives every method the same 𝑏 = 10 Reranker-4B feedback pairs; gains are relative to its 45.42 MTEB score.\", \"page\": 17, \"index\": 4, \"width\": 477, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2608.12569v1/fig-003.webp\", \"caption\": \"Figure 5 | Transfer of shared feedback for each embedding model. Red and blue curves evaluate task-wise and global-wise states only on held-out queries, using a paired raw-query baseline on the same shrinking set. Purple curves learn a global-wise 𝒗𝑔 from 14 tasks and evaluate it on the held-out fifteenth task. All curves use Reranker-4B feedback and the fixed dose 𝛼(𝑛) = 𝑛/(𝑛 + 1).\", \"page\": 18, \"index\": 3, \"width\": 915, \"height\": 190}]"
motivation: 现有测试时排序奖励在单次查询后即被丢弃，更新权重可重用但受限于参数访问和计算成本。
method: 提出TTT-Embed，将排序奖励蒸馏到冻结模型输出空间中的轻量向量，仅用标量排序分数优化，并通过范围参数控制复用层级。
result: "在五个模型和15个MTEB任务上最高提升+8.36 nDCG@10，并泛化到未见查询（+8.57）和任务（+4.71），同时恢复通用能力（+8.00）。"
conclusion: 排名奖励可作为可重用测试时状态，TTT-Embed为任何嵌入模型包括闭源API提供预算高效适应。
---

## 摘要
稠密检索器通过冻结编码器与预计算索引之间的向量相似度对文档排序。虽然来自重排器或LLM评判器的测试时排序奖励可以改进结果，但现有方法在单次查询后丢弃这一信号。更新检索器权重可使奖励可复用，但这需要参数访问权限，闭源模型不可用，且计算代价高昂。我们提出 TTT-Embed（嵌入的测试时调优），该框架将排序奖励蒸馏为冻结模型输出嵌入空间中的一个轻量级可学习向量。该向量仅根据检索器自身候选文档获得的标量排序分数进行优化，无需访问模型权重、真实标签或修改索引。单个作用域参数控制奖励复用（全局、任务或查询），在固定奖励计算预算下实现可复用性与特异性之间的原则性权衡。我们证明，随着可用奖励预算增大，最优共享作用域会从全局粒度动态转移到任务粒度，最终到查询粒度。在五个嵌入模型和15个MTEB检索任务上评估，TTT-Embed将测试时检索性能最多提升 +8.36 nDCG@10。关键是，学习到的状态能有效泛化到未见查询（最多 +8.57 nDCG@10）和未见任务（最多 +4.71 nDCG@10）。此外，TTT-Embed成功解决了灾难性遗忘：通过使基础权重完全冻结，它恢复了退化的通用能力（最多 +8.00 nDCG@10，甚至超过原始基础模型），同时保持领域内专长。这些结果确立了排序奖励作为一种可复用的测试时状态，可为任何嵌入模型（包括闭源API）实现预算高效的适配。

## Abstract
Dense retrievers rank documents using vector similarity between a frozen encoder and a precomputed index. While test-time ranking rewards from a reranker or LLM judge can improve results, existing methods discard this signal after a single query. Updating the retriever's weights makes rewards reusable, but this requires parameter access, which is unavailable for closed-source models, and is computationally prohibitive. We propose TTT-Embed (Test-Time Tuning of Embeddings), a framework that distills ranking rewards into a lightweight, learned vector within the output embedding space of a frozen model. This vector is optimized purely from scalar ranking scores assigned to the retriever's own candidate documents, requiring no access to model weights, ground-truth labels, or modifications to index. A single scope parameter controls rewards reuse (global, task, or query), enabling a principled trade-off between reusability and specificity under a fixed reward computation budget. We demonstrate that as the available reward budget scales, the optimal sharing scope shifts dynamically from global-wise to task-wise and finally to query-wise. Evaluated across five embedding models and 15 MTEB retrieval tasks, TTT-Embed improves test-time retrieval by up to +8.36 nDCG@10. Crucially, the learned states generalize effectively to unseen queries (up to +8.57 nDCG@10) and unseen tasks (up to +4.71 nDCG@10). Furthermore, TTT-Embed successfully resolves catastrophic forgetting: by leaving base weights entirely frozen, it recovers degraded general capabilities (up to +8.00 nDCG@10, even surpassing the original base model) while preserving in-domain specialization. These results establish ranking rewards as a reusable test-time state, enabling budget-efficient adaptation for any embedding model, including closed-source APIs.