---
title: Dual-Attention and Adversarial Transfer Networks for Sim-to-Real Cross-Orientation Wireless Sensing
title_zh: 双注意力与对抗迁移网络用于仿真到现实的跨方向无线感知
authors: "Linfeng Du, Kehan Wu, Tong Zhang, Rui Wang"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05664v1"
tags: ["query:ndai"]
score: 9.0
evidence: 通过对抗迁移从仿真到真实数据，减少多朝向标注数据的需求
tldr: "针对毫米波人体活动识别中用户朝向变化导致性能下降且多朝向数据采集困难的问题，本文提出物理引导模拟器（S2M-Sense）生成多朝向训练数据，设计双注意力网络提取朝向鲁棒特征，并结合对抗迁移学习弥合仿真与现实差距。实验表明，仅用模拟数据准确率达88.33%，经少量无标签实测数据迁移后提升至95%，优于现有跨域方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05664v1/fig-005.webp\", \"caption\": \"Fig. 1: Architecture of S2M-Sense.\", \"page\": 2, \"index\": 5, \"width\": 514, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2608.05664v1/fig-002.webp\", \"caption\": \"Fig. 2: Proposed Framework of the dual-attention network and adversarial unsupervised transfer learning.\", \"page\": 3, \"index\": 2, \"width\": 982, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2608.05664v1/fig-001.webp\", \"caption\": \"Fig. 3: Measured vs. simulated Doppler spectrograms for four actions (left: measured, right: simulated).\", \"page\": 5, \"index\": 1, \"width\": 1040, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2608.05664v1/fig-003.webp\", \"caption\": \"Fig. 5: Confusion matrices and t-SNE visualizations before and after transfer.\", \"page\": 6, \"index\": 3, \"width\": 1068, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2608.05664v1/fig-004.webp\", \"caption\": \"Fig. 6: Accuracy comparison across various datasets.\", \"page\": 6, \"index\": 4, \"width\": 504, \"height\": 304}]"
motivation: 用户朝向变化导致毫米波感知性能显著下降，而收集有标签多朝向数据成本高昂。
method: 提出物理引导模拟器合成多朝向数据，构建双注意力网络提取朝向鲁棒特征，并引入对抗无监督迁移学习对齐仿真与实测分布。
result: "模拟数据准确率88.33%，经16个无标签实测样本迁移后达95%，模拟真实度SSIM达0.84。"
conclusion: S2M-Sense有效解决了朝向变化和仿真现实差距问题，大幅降低了数据采集需求并提升了识别性能。
---

## 摘要
当用户相对于感知系统的方向发生变化时，毫米波人体活动识别性能会显著下降，而采集带标签的多方向数据既费力又昂贵。为了消除对详尽多方向实测数据的需求，我们开发了一个物理引导的模拟器，从单方向运动合成方向多样的无线训练数据。具体地，为了抑制方向引起的特征变化，我们提出了一个双注意力网络，从双链路多普勒频谱图中提取活动判别性和方向鲁棒性表示。为了弥合仿真到现实的鸿沟，我们引入了一种对抗无监督迁移学习机制，仅使用少量无标签目标域样本对齐特征分布。S2M-Sense平台在再现真实世界信号特征方面展现了高保真度，通过60.48 GHz毫米波实测数据验证，所有4种活动和4个方向下模拟与实测多普勒频谱图之间的平均结构相似性指数（SSIM）为0.84。实验结果表明，仅使用双链路多方向合成数据集，S2M-Sense达到88.33%的识别准确率，在使用少至16个无标签实测样本进行仿真到现实迁移学习后，准确率提升至95%。无论是否使用迁移学习，该方法均优于最先进的跨域感知方法。

## Abstract
Millimeter-wave human activity recognition suffers significant performance degradation when the user's orientation changes relative to the sensing system, yet collecting labeled multi-orientation data is labor-intensive and costly. To eliminate the need for exhaustive multi-orientation measured data, we develop a physics-guided simulator that synthesizes orientation-diverse wireless training data from single-orientation motion. Specifically, to suppress orientation-induced feature variations, we propose a dual-attention network that extracts activity-discriminative and orientation-robust representations from dual-link Doppler spectrograms. To bridge the simulation-to-reality gap, we introduce an adversarial unsupervised transfer learning mechanism that aligns feature distributions using only a small number of unlabeled target-domain samples. The S2M-Sense platform shows high fidelity in reproducing real-world signatures, validated against 60.48 GHz mmWave measured data with an average structural similarity index measure (SSIM) of 0.84 between simulated and measured Doppler spectrograms across all 4 activities and 4 orientations. Experimental results show that S2M-Sense achieves 88.33% recognition accuracy using only the dual-link multi-orientation simulated dataset, which improves to 95% after simulation-to-reality transfer learning with as few as 16 unlabeled measured samples. Both cases with and without transfer learning outperform state-of-the-art cross-domain sensing methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：毫米波无线感知在人体活动识别中前景广阔，但用户朝向的变化会严重畸变无线信号、增大类内差异，导致识别准确率大幅下降。
- **痛点**：采集覆盖所有朝向的有标签实测数据人力成本高、周期长，阻碍了实际部署。
- **整体思路**：提出 S2M-Sense 平台，以 **物理引导的模拟器** 从单朝向深度相机视频生成多朝向无线训练数据，结合 **双注意力网络** 提取朝向鲁棒特征，并用 **对抗无监督迁移学习** 弥合仿真与实测域间差异，最终仅需极少无标签实测样本即可实现高准确率跨朝向识别。

### 2. 方法论
**核心思想**：通过“模拟器+双注意力+对抗域适应”三阶段流水线，化依赖实测数据为依赖少量无标签样本，系统性地解决朝向变化和仿真‑现实鸿沟。
- **物理引导模拟器（数据生成）**
  - 基于深度相机视频，提取 34 个人体关键点，用椭球基元近似人体。
  - 在准静态假设下，信道冲激响应 $h(t,\tau) = u(t,\tau) + v(\tau)$，目标相关部分 $u_k(t,\tau)$ 利用双基地雷达方程及均匀线阵因子建模天线增益，方向性通过阵列因子 $\mathrm{AF}(\theta_k,\phi_k)$ 引入。
  - 通过绕腰部竖直轴的旋转矩阵 $\mathbf{T}(\alpha)$ 对关键点进行变换，生成多朝向运动序列，扩展至全360°方位角。
  - 目标无关成分 $v(\tau)$ 由直射径和静态散射体反射径组成，散射体随机采样模拟真实环境。
  - 用三次样条插值和 One‑Euro 滤波器平滑运动，提升时域连续性与真实感。
- **双注意力网络（特征提取）**
  - 输入：双正交链路的多普勒频谱图，分别经 ResNet‑18 提取特征 $\mathbf{A}_1, \mathbf{A}_2 \in \mathbb{R}^{C\times H\times W}$，级联并批归一化得 $\mathbf{A}\in\mathbb{R}^{2C\times H\times W}$。
  - **位置注意力模块 (PAM)**：通过对 $\mathbf{A}$ 卷积→重排生成查询、键、值矩阵，计算位置注意力图 $\mathbf{S}_p = \text{softmax}((\mathbf{M}_p^2)^\top \mathbf{M}_p^1)$，捕获长程空间依赖，输出 $\mathbf{O}_p$。
  - **通道注意力模块 (CAM)**：将 $\mathbf{A}$ 展平为 $\mathbf{M}_c^1,\mathbf{M}_c^2\in\mathbb{R}^{2C\times N}$，得通道注意力图 $\mathbf{S}_c = \text{softmax}(\mathbf{M}_c^1 (\mathbf{M}_c^2)^\top)$，建模通道间依赖，输出 $\mathbf{O}_c$。
  - 残差融合：$\mathbf{O} = a\cdot\mathbf{O}_p + b\cdot\mathbf{O}_c + \mathbf{A}$，$a,b$ 初始为0的可学习参数，输入全连接分类器，损失为交叉熵。
- **对抗无监督迁移学习（域适应）**
  - 源域（模拟数据）预训练特征提取器 $f_s$ 和分类器。
  - 目标域特征提取器 $f_t$ 初始化同 $f_s$，用少量无标签实测样本。
  - 交替优化：① 固定 $f_t$，训练判别器 $D$ 区分源/目标特征，最小化 $\mathcal{L}_D = -\mathbb{E}_{\mathbf{x}_s}[\log D(f_s(\mathbf{x}_s))] - \mathbb{E}_{\mathbf{x}_t}[\log(1-D(f_t(\mathbf{x}_t)))]$；② 固定 $D$，训练 $f_t$ 混淆判别器，最小化 $\mathcal{L}_f = -\mathbb{E}_{\mathbf{x}_t}[\log D(f_t(\mathbf{x}_t))]$。
  - 最终使目标域特征分布与源域对齐，直接复用源分类器。

### 3. 实验设计
- **数据集与场景**：
  - 实测：60.48 GHz OFDM 被动感知系统，双正交链路，四个活动 (step, fall, sit, kick)，三名志愿者在 45°、135°、225°、315° 四个朝向采集 480 个样本。
  - 模拟：单朝向深度相机视频（10段），通过模拟器生成 3000 个全朝向频谱图，涵盖0°‑360°。
  - 训练/测试主体分离：训练集用志愿者A/B/C，测试集用D/E/F，避免身份泄露。
- **迁移学习设置**：仅使用 16 个无标签实测样本（来自四个方向）进行域适应。
- **对比方法**：三种代表性跨域感知 baseline —— WiDual [9]、WiGRUNT [10]、ImgFi [11]，均在相同数据划分下复现评估。
- **评估指标**：识别准确率、混淆矩阵、t‑SNE 特征可视化，以及频谱图的 SSIM 相似度。

### 4. 资源与算力
论文未明确提及所使用的 GPU 型号、数量或训练时长。仅提及使用 Adam 优化器、学习率 $10^{-4}$、训练 200 个 epoch、批大小 16，但未说明硬件环境。

### 5. 实验数量与充分性
- **实验组数**：① 对比实验：四种数据组合（单朝向、Link1多朝向、Link2多朝向、双链路多朝向）下，本方法与三个 baseline 的准确率对比；② 迁移前后效果对比（混淆矩阵、t‑SNE）；③ 频谱图保真度评估（SSIM 表及可视化）。
- **充分性与公平性**：使用严格的跨主体测试，baseline 均在同一数据集和划分下重新训练，确保公平；多组对照实验和可视化提供了较全面的验证。但受限于四类活动和四个离散测试朝向，场景多样性有限；未进行不同数量无标签样本的敏感性分析（仅用16个）；未报告统计方差（如多次运行的标准差），实验规模总体偏小但逻辑自洽。

### 6. 主要结论与发现
- 模拟器生成的频谱图平均 SSIM 达 0.84，高保真复现实测多普勒特征。
- 仅用模拟数据训练，双链路多朝向模型准确率可达 88.33%；引入 16 个无标签实测样本进行对抗迁移后，准确率提升至 95%。
- 无论是否进行迁移学习，本方法均显著优于 WiDual (78.96%)、WiGRUNT (81.04%)、ImgFi (84.38%)。
- t‑SNE 显示迁移后模拟与实测特征分布明显对齐，域鸿沟得到有效弥合。

### 7. 优点
- **新颖的三合一架构**：将物理模拟、双注意力特征提取与对抗无监督域适应结合，系统性地解决朝向泛化难题。
- **极低实测数据依赖**：仅需少量无标签样本即可适应现实环境，大幅降低部署成本。
- **高保真模拟**：基于椭球基元和阵列天线的信道模型，加入插值和滤波，生成逼真的多普勒频谱图。
- **双链路互补设计**：利用近正交链路提供多视角运动信息，增强朝向鲁棒性。
- **并行注意力与残差融合**：位置和通道注意力并行处理并引入可学习的残差连接，缓解传统串行注意力的退化问题。
- **实验设计严格**：跨主体划分、与 SOTA 方法公平对比、定性+定量验证较为完整。

### 8. 不足与局限
- **活动与场景单一**：仅涉及四类单人肢体活动，未涉及复杂交互、连续动作或多人场景。
- **测试朝向有限**：仅在四个特定角度测试，覆盖度有限，无法完全反映连续朝向平移下的性能。
- **环境假设简化**：模拟器中静态散射体随机生成，与实际室内多径环境仍有差距；未见对不同环境（如不同房间布局）的泛化评估。
- **算力信息缺失**：未交代计算资源与时间开销，难以评估实际训练成本。
- **实验结果统计稳定性**：未给出多次运行的均值和方差，无法判断结果的可靠性波动。
- **超参数敏感性与样本量分析**：未探索不同无标签样本数量对迁移效果的影响，也未对学习率、批大小等关键参数做消融分析。

（完）
