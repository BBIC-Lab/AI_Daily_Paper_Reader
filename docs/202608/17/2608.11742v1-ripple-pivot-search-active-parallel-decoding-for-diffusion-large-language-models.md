---
title: "Ripple-Pivot Search: Active Parallel Decoding for Diffusion Large Language Models"
title_zh: Ripple-Pivot Search：面向扩散大语言模型的主动并行解码
authors: "Yushi Ye, Xu Chen, Haoyun Jiang, Jinsong Lan, Haihong Tang, Bo Han, Ivor Tsang, Yanfeng Wang, Bo Zheng, Jiangchao Yao"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11742v1"
tags: ["query:ndai"]
score: 9.0
evidence: 并行解码算法将模型输出转化为序列
tldr: "扩散大语言模型通过并行解码加速推理，但现有调度仅按位置独立提交，忽略提前承诺带来的涟漪效应。本文提出Ripple-Pivot Search（RPS），主动选择中间熵枢轴并通过前瞻评估确定token，降低剩余位置不确定性，促进并行解码。在多个模型和推理/代码基准上，RPS实现4-10倍墙钟加速，保持质量且精度最高提升5.49%，集成KV缓存后可达18倍。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11742v1/fig-001.webp\", \"caption\": \"Figure 1: Left: Comparison of parallel decoding schedulers. Right: Starting from a partially decoded block, we commit each remaining masked position to its oracle token and measure the resulting entropy reduction ∆Hi at other masked positions (y-axis) against the pre-commit entropy Hbefore of the resolved position (x-axis). Colors indicate the oracle token’s rank under the model distribution, and the shaded band marks the mid-entropy regime.\", \"page\": 2, \"index\": 1, \"width\": 812, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2608.11742v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of RPS. Left: One-step example of RPS decoding. The upper region illustrates pivot selection, while the lower region shows lookahead scoring. Right: Customized attention mask for lookahead forward pass. The packed sequence contains the shared context with candidate branches. Light-blue cells indicate allowed attention and blank cells indicate blocked attention.\", \"page\": 4, \"index\": 5, \"width\": 826, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2608.11742v1/fig-002.webp\", \"caption\": \"Table 1: Results on LLaDA and Dream. Bold indicates the best result.\", \"page\": 7, \"index\": 2, \"width\": 830, \"height\": 694}, {\"url\": \"assets/figures/arxiv/2608.11742v1/fig-003.webp\", \"caption\": \"Table 2: Generation-length robustness on LLaDA and Dream.\", \"page\": 7, \"index\": 3, \"width\": 830, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2608.11742v1/fig-004.webp\", \"caption\": \"Table 5: LLaDA-1.5. Performance and inference speedup comparison across 4 benchmarks.\", \"page\": 14, \"index\": 4, \"width\": 760, \"height\": 560}]"
motivation: 现有并行解码调度忽略早期承诺对后续解码的涟漪效应，限制了加速潜力。
method: 提出Ripple-Pivot Search，通过选择中间熵枢轴位置并前瞻评估其token分配，以最大化下游不确定性降低。
result: "在3个dLLM和4个基准上，RPS实现4-10倍加速，精度较前瞻基线最高提升5.49%，集成KV缓存后达18倍。"
conclusion: RPS有效利用涟漪效应，无需训练即可加速扩散大语言模型并行解码并保持或提升生成质量。
---

## 摘要
扩散大语言模型（dLLMs）已成为自回归语言模型的有力替代方案，并有望通过并行解码实现大幅加速的推理。现有的并行解码调度器通常仅在位置满足逐位置标准后才提交该位置，忽视了早期提交可能对后续解码产生的益处。我们在 dLLM 解码中发现一种涟漪效应：主动提交一个中等熵的枢轴位置，可以显著降低其余被掩码位置的不确定性。这种不确定性的降低使得后续步骤能够并行解码更多 token，从而加速整体解码过程。为利用这一涟漪效应，我们提出 Ripple-Pivot Search (RPS)，一种新颖的免训练解码方法，该方法寻找中等熵位置作为有前景的候选枢轴（在何处解码），并通过前瞻评估确定能带来最大下游收益的 token 赋值（解码什么）。在 3 个 dLLM 和 4 个推理与代码生成基准上，RPS 相比标准解码器实现了 4–10 倍的墙钟时间加速，同时保持生成质量；并且相比先前的前瞻基线，准确率最高提升 5.49%，在大多数设置下还提供了更高的吞吐量。当与 KV 缓存结合时，RPS 相比标准解码器进一步实现了最高 18 倍的墙钟时间加速。

## Abstract
Diffusion Large Language Models (dLLMs) have emerged as a competitive alternative to autoregressive language models, offering the potential for substantially faster inference through parallel decoding. Existing parallel decoding schedulers typically commit positions only after they meet a per-position criterion, overlooking how early commitments may benefit subsequent decoding. We identify a ripple effect in dLLM decoding: proactively committing a mid-entropy pivot position can induce a pronounced reduction in uncertainty across the remaining masked positions. This uncertainty reduction allows subsequent steps to unmask more tokens in parallel, thereby accelerating the overall decoding process. To exploit the ripple effect, we propose Ripple-Pivot Search (RPS), a novel training-free decoding method that seeks mid-entropy positions as promising candidate pivots (where to decode), and determines their token assignment that yields the greatest downstream benefit via lookahead evaluation (what to decode). Across 3 dLLMs and 4 reasoning and code-generation benchmarks, RPS achieves 4-10$\times$ wall-clock speedup over the standard decoder while preserving generation quality, and improves accuracy over the previous lookahead baseline by up to 5.49% while delivering higher throughput in most settings. When integrated with KV caching, RPS further achieves up to 18$\times$ wall-clock speedup over the standard decoder.

---

## 论文详细总结（自动生成）

# Ripple-Pivot Search：面向扩散大语言模型的主动并行解码 论文总结

## 1. 核心问题与整体含义

- **研究背景**：扩散大语言模型（dLLMs）通过并行解码提供比自回归模型更快的推理潜力。每个去噪步骤中，模型同时为所有被掩码位置产生预测，由解码调度器决定哪些位置 unmask 以及分配什么 token。
- **现有调度器局限**：大多数并行解码调度器将“在哪里解码”和“解码什么”两个决策耦合在一起：位置一旦满足某个逐位置准则（如置信度、熵、稳定性等），就提交其贪婪 top-1 预测。这类方法忽视了早期提交对后续解码的主动影响。
- **关键观察：涟漪效应**：论文通过 oracle 分析发现，主动提交一个**中等熵枢轴位置**，可以显著降低其余被掩码位置的不确定性；这种不确定性下降能让后续步骤并行 unmask 更多 token，从而加速整体解码。同时，在 85% 的中熵情形下，正确 token 并不是模型当前 top-1 预测。
- **整体含义**：现有 lookahead 方法主要用前瞻来决定 *where to commit*，但 token 仍固定为 top-1，因此可能错过有益的非贪婪轨迹。RPS 提出联合优化 *where* 和 *what*，利用中熵枢轴和前瞻评估实现加速与质量的更好平衡。

## 2. 方法论

### 2.1 总体流程

RPS 在标准并行解码之上，每个解码步骤加入一个 **枢轴搜索（pivot search）** 模块，包含两个阶段：

1. **枢轴选择（pivot selection）**：在仍被掩码的位置中筛选中熵枢轴。
2. **前瞻评分（lookahead scoring）**：为枢轴构建候选 token 集合，用一次前瞻 forward 评估各候选对下游不确定性的影响，选最优 token 提交。

### 2.2 枢轴选择

- 对每个被掩码位置 $i$，先截断其预测分布为 top-$k_{\max}$ token 集合 $T_i$，并计算保留概率质量：

$$
\mu_i = \sum_{v \in T_i} p_i(v).
$$

- 枢轴通过最大化截断熵、并施加概率质量约束来选择：

$$
i^\star = \arg\max_{i \in M : \mu_i \ge \tau_{\text{pivot}}} \left( - \sum_{v \in T_i} p_i(v) \log p_i(v) \right).
$$

- 含义：约束 $\mu_i \ge \tau_{\text{pivot}}$ 排除过于发散的高熵位置；最大化截断熵避免选择已经几乎确定的位置。最终枢轴自然落在中熵区间，能够有效传播信息。若没有位置满足约束，则跳过本步枢轴搜索，直接进行标准解码。

### 2.3 前瞻评分

- 对选中的枢轴 $i^\star$，构建自适应候选集：

$$
C = \{ v \mid p_{i^\star}(v) \ge r \cdot P^{\max}_{i^\star}, \; v \in T_{i^\star} \} \
