---
title: "Consolidator: Learning Persistent Routed Memory Across Context Boundaries"
title_zh: Consolidator：学习跨上下文边界的持久路由记忆
authors: "Sungwoo Goo, Hwi-yeol Yun, Sangkeun Jung"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11701v1"
tags: ["query:ndai"]
score: 8.0
evidence: 提出跨上下文边界的记忆巩固机制，用于序列预测。
tldr: "研究背景：短期记忆持久化到长期记忆但可能不影响后续访问。提出Consolidator，在Phasor Memory Network中将路由后的短期记忆转换后累积到长期记忆，清除缓存后长期记忆仍可读并输入路由器调节槽选择。在模10映射更新任务上，仅训练0.041%参数，将更新映射召回从44.38%提升到87.02%，且学习巩固优于强制身份累积。贡献：证明巩固的长期记忆既作为可检索内容又作为访问状态。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11701v1/fig-003.webp\", \"caption\": \"Figure 1: Routed latent-memory lifecycle. Tokens write routed STM; at a boundary, Consolidator updates persistent LTM and KV/STM are cleared. Retained LTM supports later reads and conditions subsequent explicit-memory slot selection.\", \"page\": 4, \"index\": 3, \"width\": 973, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2608.11701v1/fig-004.webp\", \"caption\": \"Figure 2: Consolidator-only intervention. Paired evaluations compare learned and forcedidentity consolidation, the first and second same-address writes, and correct, mismatched, and fresh memory.\", \"page\": 9, \"index\": 4, \"width\": 957, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2608.11701v1/fig-002.webp\", \"caption\": \"Figure 3: Direct LTM-conditioned routing. Lines pair corresponding consolidation-training seeds initialized from the same STM-pretraining checkpoint. Direct routing substantially improves updated-mapping LTM recall (a), while immediate STM recall of the second mapping remains exactly matched (b). The off condition retains learned LTM and all read paths; only direct same-level LTM conditioning of the router is removed.\", \"page\": 10, \"index\": 2, \"width\": 664, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2608.11701v1/fig-001.webp\", \"caption\": \"Figure 4: Pre-consolidation and post-reset recall under a dual objective. Adding immediateSTM supervision restores mean pre-consolidation recall across both segments (b) without reducing updated-mapping LTM recall (a). The two metrics are evaluated on separate trajectories using one shared parameter set.\", \"page\": 11, \"index\": 1, \"width\": 664, \"height\": 335}]"
motivation: 单纯将短期记忆复制到长期记忆并不保证保留状态能影响后续记忆访问，需要机制确保持久化状态能调节后续记忆选择。
method: 在Phasor Memory Network中引入Consolidator（共享槽局部算子），在清除KV缓存和STM前将路由的STM转换并累积到LTM，并让LTM同时作为可读内容和路由器的输入条件。
result: "在两段模10映射任务中，仅训练0.041%参数的Consolidator，将更新映射的LTM召回从44.38%提高到87.02%，且学习巩固显著优于强制身份累积。"
conclusion: 巩固的长期记忆既能作为可检索内容，又能作为访问状态塑造后续槽选择，验证了学习性巩固对持久路由记忆的重要性。
---

## 摘要
将短期记忆（STM）复制到较慢的存储中可以保留跨上下文边界的状态，但仅持久化并不能确保保留的状态影响后续的记忆访问。我们在相量记忆网络（PMNet）中使用 Consolidator 检验这一区别；Consolidator 是一个共享的槽局部算子，它在将路由后的 STM 累积到长期记忆（LTM）之前对其进行变换，而无需重放源 token。每次整合后，KV 缓存和 STM 都会被清空。保留的 LTM 仍然可以被读取，并被输入到分层路由器中，从而决定后续输入访问哪些显式记忆槽。我们在一个两段式模 10 映射任务上评估该机制，其中第二段在相同记忆地址更新映射。在第二次整合和重置之后，留出查询必须从 LTM 中恢复更新后的映射。主干网络和记忆接口被冻结，仅训练 12.35K 个 Consolidator 参数（占 29.95M 模型的 0.041%）。在来自同一 STM 预训练检查点的五次成对运行中，直接 LTM 路由将更新映射召回率从 44.38±1.94% 提高到 87.02±1.76%（+42.64±1.10 个百分点），而在两种条件下即时 STM 召回率保持为 89.90%；两者均训练独立的 Consolidator，并保留相同的 LTM 读取路径。学习到的整合在不使用路由时比强制恒等累积高 21.40±1.91 个百分点，在使用路由时高 68.70±1.76 个百分点。因此，在该任务上，整合后的 LTM 既作为可检索内容，也作为影响后续槽选择的访问状态。

## Abstract
Copying short-term memory (STM) into a slower store can preserve state across a context boundary, but persistence alone does not ensure that the retained state influences subsequent memory access. We test this distinction in a Phasor Memory Network (PMNet) using Consolidator, a shared slot-local operator that transforms routed STM before accumulating it into long-term memory (LTM), without replaying the source tokens. After each consolidation, the KV cache and STM are cleared. The retained LTM can still be read and is also fed into the hierarchical router, thereby conditioning which explicit-memory slots subsequent inputs access. We evaluate this mechanism on a two-segment modulo-10 mapping task in which the second segment updates the mapping at the same memory address. Following a second consolidation and reset, a held-out query must recover the updated mapping from LTM. The backbone and memory interface are frozen, leaving only 12.35K Consolidator parameters trainable (0.041\% of a 29.95M model). Across five paired runs from the same STM-pretraining checkpoint, direct LTM routing raises updated-mapping recall from $44.38\pm1.94\%$ to $87.02\pm1.76\%$ ($+42.64\pm1.10$ percentage points), while immediate STM recall remains 89.90\% in both conditions; both train separate Consolidators and retain the same LTM read paths. Learned consolidation outperforms forced identity accumulation by $21.40\pm1.91$ percentage points without routing and $68.70\pm1.76$ with routing. Thus, on this task, consolidated LTM serves as both retrievable content and an access state that shapes subsequent slot selection.