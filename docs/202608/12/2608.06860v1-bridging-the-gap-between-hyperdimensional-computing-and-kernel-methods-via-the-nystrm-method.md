---
title: Bridging the Gap Between Hyperdimensional Computing and Kernel Methods via the Nyström Method
title_zh: 通过Nyström方法弥合超维计算与核方法之间的鸿沟
authors: "Quanling Zhao, Anthony Hitchcock Thomas, Ari Brin, Xiaofan Yu, Tajana Rosing"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.06860v1"
tags: ["query:ndai"]
score: 9.0
evidence: 基于Nyström的原始数据到高维向量映射，用于核表示学习
tldr: "超维计算（HDC）利用高维随机向量实现高效机器学习，但性能受限于数据映射方式。本文提出NysHD方法，通过Nyström方法将用户定义的正半定相似函数转化为HDC编码，从而将核方法中的丰富相似性设计引入HDC，扩展了其适用范围。在图和字符串分类任务上，NysHD相比现有HDC编码方法平均准确率分别提升11%和17%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.06860v1/fig-003.webp\", \"caption\": \"Table 1: Summary of tasks and datasets\", \"page\": 6, \"index\": 3, \"width\": 998, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2608.06860v1/fig-001.webp\", \"caption\": \"Table 2: Experimental results. The best accuracy result for each dataset are highted and second best are underlined.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2608.06860v1/fig-002.webp\", \"caption\": \"Figure 2: Numerical difference of spectral norm between normalized kernel matrices compute directly from kernel function and approximated kernel matrices with our encoding.\", \"page\": 7, \"index\": 2, \"width\": 1054, \"height\": 241}]"
motivation: 现有HDC的映射方法有限，未能充分利用核方法中大量有效的相似函数。
method: 基于Nyström方法提出NysHD，将任意正半定相似函数转化为HDC空间中的编码映射。
result: "NysHD在图和字符串数据集上的分类准确率比现有HDC编码方法平均提高11%和17%。"
conclusion: NysHD成功桥接HDC与核方法，为HDC带来更灵活的映射设计并显著提升性能。
---

## 摘要
超维计算（HDC）是认知科学文献中的一种方法，它使用表示为高维随机向量的数据来解决信息处理任务。该技术具有严格的数学基础，并且易于在节能且高度并行的硬件上实现，例如FPGA和“存内计算”架构。HDC在机器学习中的有效性在很大程度上取决于如何将原始数据映射到高维空间。在这项工作中，我们提出了NysHD，这是一种基于核近似文献中Nyström方法构建此映射的新方法。我们的方法提供了一种简单的方案，可将任何用户定义的正半定相似函数转换为HDC中的等效映射。针对学习问题设计此类函数的文献非常丰富。我们的方法提供了一种将它们引入HDC设置的机制，从而扩展了可以使用HDC处理的问题类型。与现有HDC编码方法的实证评估表明，NysHD在图和字符串数据集上的分类准确率平均分别提高了11%和17%。

## Abstract
Hyperdimensional computing (HDC) is an approach from the cognitive science literature for solving information processing tasks using data represented as high-dimensional random vectors. The technique has a rigorous mathematical backing, and is easy to implement in energy-efficient and highly parallel hardware like FPGAs and "processing-in-memory" architectures. The effectiveness of HDC in machine learning largely depends on how raw data is mapped to high-dimensional space. In this work, we propose NysHD, a new method for constructing this mapping that is based on the Nyström method from the literature on kernel approximation. Our approach provides a simple recipe to turn any user-defined positive-semidefinite similarity function into an equivalent mapping in HDC. There is a vast literature on the design of such functions for learning problems. Our approach provides a mechanism to import them into the HDC setting, expanding the types of problems that can be tackled using HDC. Empirical evaluation against existing HDC encoding methods shows that NysHD can achieve, on average, 11% and 17% better classification accuracy on graph and string datasets respectively.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：超维计算（HDC）的性能高度依赖将原始数据映射到高维空间的编码函数，但现有的 HDC 编码方法通常只能捕捉简单的相似性度量（如 L1/L2 距离、角度距离），难以处理图、序列等复杂结构数据。核方法文献中虽有大量精心设计的相似性函数（核函数），但无法直接用于 HDC 的学习流程。
- **整体含义**：本文试图弥合 HDC 与核方法之间的鸿沟，提出了一种通用的 HDC 编码生成方法，使得任何用户定义的正半定相似性函数（核函数）都能被转换为 HDC 空间中的低精度嵌入，从而将核方法的丰富设计引入 HDC，扩展 HDC 可处理的问题范围，并在一定程度上缩小 HDC 与深度神经网络之间的精度差距。

### 2. 论文提出的方法论
- **核心思路**：结合 Nyström 核近似方法与符号随机投影（sign-thresholded random projection），将任意的正定核函数 $K$ 转化为 HDC 空间中的编码函数 $\phi: \mathcal{X} \to \mathcal{H}$，使得编码后的内积近似保持归一化核值：
  $$
  \mathbb{E}[\phi(\mathbf{x}_i) \cdot \phi(\mathbf{x}_j)] \approx \frac{K(\mathbf{x}_i, \mathbf{x}_j)}{\sqrt{K(\mathbf{x}_i, \mathbf{x}_i) K(\mathbf{x}_j, \mathbf{x}_j)}}.
  $$

- **关键技术细节**：
  - 从训练集中随机采样 $s$ 个地标（landmarks） $\mathbf{z}_1, \dots, \mathbf{z}_s$，构成地标集 $Z$。
  - 计算地标之间的核矩阵 $\mathbf{H}_Z \in \mathbb{R}^{s \times s}$，其中 $(\mathbf{H}_Z)_{ij} = K(\mathbf{z}_i, \mathbf{z}_j)$。
  - 对 $\mathbf{H}_Z$ 做对称特征分解：$\mathbf{H}_Z = \mathbf{Q} \boldsymbol{\Lambda} \mathbf{Q}^\top$。
  - 对任一数据点 $\mathbf{x}$，计算其与所有地标的核值构成向量 $\mathbf{C}(\mathbf{x}) = [K(\mathbf{x}, \mathbf{z}_1), \dots, K(\mathbf{x}, \mathbf{z}_s)]^\top$。
  - Nyström 嵌入为 $\boldsymbol{\phi}_{\text{nys}}(\mathbf{x}) = \boldsymbol{\Lambda}^{-\frac{1}{2}} \mathbf{Q}^\top \mathbf{C}(\mathbf{x})$，此时 $\boldsymbol{\phi}_{\text{nys}}(\mathbf{x}_i) \cdot \boldsymbol{\phi}_{\text{nys}}(\mathbf{x}_j) \approx \hat{G}_{ij}$，其中 $\hat{\mathbf{G}}$ 是近似核矩阵。
  - 为获得适合 HDC 的低精度二值化嵌入，再使用从 $s$ 维单位球面采样的随机矩阵 $\mathbf{P}_{\text{rp}} \in \mathbb{R}^{d \times s}$，通过符号函数生成最终编码：
    $$
    \phi(\mathbf{x}) = \sqrt{\frac{\pi}{2d}} \operatorname{sign}\left( \mathbf{P}_{\text{rp}} \boldsymbol{\phi}_{\text{nys}}(\mathbf{x}) \right).
    $$
    该步骤基于符号阈值随机投影的性质，在期望上保留了归一化核值。

- **算法流程**（用文字概括）：
  1. **生成 Nyström 嵌入矩阵**：采样地标，计算地标核矩阵，特征分解，随机生成投影矩阵，得到 $\mathbf{P}_{\text{nys}} = \mathbf{P}_{\text{rp}} \boldsymbol{\Lambda}^{-\frac{1}{2}} \mathbf{Q}^\top$。
  2. **编码数据点**：对任意 $\mathbf{x}$，计算其与地标的核向量 $\mathbf{C}(\mathbf{x})$，输出 $\sqrt{\frac{\pi}{2d}} \operatorname{sign}(\mathbf{P}_{\text{nys}} \mathbf{C}(\mathbf{x}))$。

- **复杂度**：总复杂度为 $O(\max(s^3, s n m, s n d))$，其中 $n$ 是样本数，$m$ 是核函数计算复杂度，$d$ 是 HDC 维度。典型情况下 $s \ll n$，主要开销在核计算。

### 3. 实验设计
- **任务与数据集**：
  - 图分类：8 个标准图数据集（ENZYMES、NCI1、D&D、BZR、MUTAG、COX2、NCI109、Mutagenicity），来自 TUDataset，包含标签节点或属性节点。
  - 字符串分类：4 个数据集，包括蛋白质序列（Protein）、短信文本（SMS）、DNA 序列（Splice 和 Promoter）。
  - 数据集划分、训练/测试规模等信息在论文表 1 中列出。

- **对比方法**：
  - HDC 基线：图分类使用 GraphHD，字符串分类使用 N-gram HDC 编码。
  - 深度神经网络：图分类对比了 DGCNN、GCN、GIN、GIUNet；字符串对比了微调的大规模模型（ESM-2-8m 用于蛋白质，BERT 用于 SMS）。
  - HDC 学习流程统一采用 OnlineHD 风格（原型生成 + Perceptron 微调）。

- **评测指标**：分类准确率，训练时间（秒）。

### 4. 资源与算力
- 论文中明确说明：
  - 所有实验（除大模型微调外）均在 Intel i5-11400 CPU 上运行。
  - 大模型微调（如 ESM-2-8m、BERT）因 CPU 训练时间过长，使用了 Nvidia RTX 3050 GPU。
- 但未详细给出 GPU 数量、显存占用、单个实验的训练时长以外的功耗等信息。论文只报告了训练时间，未提推理算力或能效分析。

### 5. 实验数量与充分性
- **实验数量**：
  - 共测试 12 个数据集（8 图 + 4 字符串），每个方法在每个数据集上重复 10 次，报告均值与标准差。
  - 给出了归一化核矩阵近似质量的谱范数差异实验（图 2），展示了不同 landmark 占比对近似质量的影响。
  - 提供了训练时间对比，以及对不同 landmark 大小的鲁棒性说明。
- **充分性与公平性**：
  - 对比涵盖了 HDC 内部方法、多种主流图神经网络以及大规模语言模型，覆盖面较广。
  - 所有 HDC 方法使用相同的训练与微调框架，保证内部对比公平。
  - 但深度神经网络的超参数、微调设置等未详细交代，可能引入一定偏差；此外，NysHD 的核函数超参数和 landmark 数量为经验选择，缺乏消融实验探索不同核函数、不同采样策略的影响。

### 6. 论文的主要结论与发现
- NysHD 成功将任意正定核转化为 HDC 编码，内积在期望上保留了归一化核值。
- 在图分类任务上，NysHD 相比 GraphHD 平均准确率提升 11%，在节点属性丰富的 ENZYMES 数据集上提升达 38%。
- 在字符串分类上，相比 N-gram HDC 平均提升 17%，尤其在 Promoter 等数据集上优势明显。
- 与图神经网络相比，NysHD 在 3/8 个图数据集上达到最优，平均准确率超过 DGCNN、GIN、GIUNet，且训练速度显著快于 GCN（平均快 52%）。
- 与预训练语言模型微调相比，NysHD 在生物序列数据集上表现不俗，训练时间大幅缩短（例如 SMS 上 BERT 微调时间是 NysHD 的约 83 倍）。
- 整体上，NysHD 在保持 HDC 学习和推理高效性的前提下，显著拓宽了 HDC 的应用能力。

### 7. 优点
- **方法创新性**：将 Nyström 方法与符号随机投影结合，提供了从核函数到 HDC 编码的通用转换方案，突破了 RFF 仅适用于平移不变核的限制。
- **实用性**：允许直接导入核方法文献中大量成熟的相似性设计，可适配图核、序列核、动态时间规整核等多种复杂结构。
- **理论保证**：给出了期望内积等于归一化核值的定理，为近似质量提供理论依据。
- **实验充分**：跨图、字符串两类任务，与 HDC 基线、多种图神经网络及大模型进行对比，评估维度包括精度与训练效率。
- **易部署**：嵌入生成后可直接与现有 HDC 学习流水线（如 OnlineHD）无缝衔接，且最终编码为低精度二值向量，适合高效硬件。

### 8. 不足与局限
- **样本效率与地标选择**：地标采样目前仅采用无放回均匀采样，未探索更先进的 Nyström 采样策略（如杠杆分数采样、递归采样），可能影响近似质量与效率。
- **核函数依赖与超参数敏感度**：性能高度依赖于所选核函数及其超参数（如 gappy kernel 的 gap 大小、propagation kernel 的迭代次数），但论文未系统探讨核函数选择和超参数的影响。
- **实验规模与推广性**：图、字符串数据集规模相对较小（最大约 4500 训练样本），缺乏更大规模数据或更多领域（如时间序列、图像）的验证。与深度模型的对比中，深度模型的配置和超参数调优过程未详细说明。
- **计算开销**：编码阶段需计算数据点与地标的核值，若地标数较多或核函数复杂，编码开销可能显著高于传统 HDC 随机映射。
- **理论偏差**：内积近似保留的是归一化核值而非原始核值，且期望性质仅在随机投影和地标采样双重随机性下成立，实际方差未做定量分析。
- **未测试硬件实现**：虽然强调 HDC 的硬件友好性，但未在 FPGA 或存内计算平台上展示 NysHD 编码的实际能效或延迟。

（完）
