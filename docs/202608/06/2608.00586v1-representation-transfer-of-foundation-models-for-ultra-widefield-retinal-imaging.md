---
title: Representation Transfer of Foundation Models for Ultra-Widefield Retinal Imaging
title_zh: 超宽场视网膜成像基础模型的表征迁移
authors: "Mingya Alexa Gong, Da Ma, Lovre Antonio Budimir, Ivana Matovinovic, Sven Loncaric, Myeong Jin Ju, Yukun Zhou, Siegfried K. Wagner, Pearse A. Keane, Marinko V. Sarunic"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00586v1"
tags: ["query:ndai"]
score: 9.0
evidence: 评估预训练基础模型表征在下游眼科分类任务上的可迁移性
tldr: 本研究探究了不同预训练策略对超广角眼底图像分类中表征迁移能力的影响。通过在基于补丁的多实例学习框架下比较监督、掩码自编码器、自蒸馏等预训练目标，发现预训练方式显著影响冻结表征的迁移效果，其中自蒸馏和大规模预训练模型表现更优，且注意力分析揭示了预训练策略对证据聚合的差异，部分微调可弥补弱预训练方法的不足。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.00586v1/fig-004.webp\", \"caption\": \"FIGURE 1. Overview of the patch-based multiple instance learning framework used to evaluate foundation model representations. Ultra-widefield retinal images are divided into patches and processed by a feature extractor. Patch-level features are aggregated using an attention-based MIL module to produce a bag-level representation for classification.\", \"page\": 5, \"index\": 4, \"width\": 1008, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2608.00586v1/fig-003.webp\", \"caption\": \"FIGURE 4. Attention heatmaps for one example from each class in the MMRDR dataset. Incorrect classifications are labelled in red with their predicted classes.\", \"page\": 9, \"index\": 3, \"width\": 1008, \"height\": 917}, {\"url\": \"assets/figures/arxiv/2608.00586v1/fig-001.webp\", \"caption\": \"FIGURE 6. Attention heatmaps for selected encoder+MIL models using two examples from the evaluation set: (a) a retinal image with an intraocular tumour and (b) a retinal image without a tumour.\", \"page\": 11, \"index\": 1, \"width\": 1008, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2608.00586v1/fig-002.webp\", \"caption\": \"TABLE II. Full performance metrics for five-class DR classification on MMRDR. Values are reported as mean ± standard deviation across three runs. Best results are shown in bold.\", \"page\": 15, \"index\": 2, \"width\": 765, \"height\": 288}]"
motivation: 探究不同预训练策略如何影响超广角眼底成像中表征的迁移能力，以指导弱监督任务中的模型选择。
method: 在补丁多实例学习框架下，对比不同预训练目标的视觉Transformer编码器在超广角图像疾病分类上的性能，并进行注意力分析和部分微调。
result: 自蒸馏和大规模预训练模型（如DINOv3）优于掩码自编码器，部分微调缩小了弱预训练方法的差距，注意力分析显示了聚合行为的差异。
conclusion: 预训练策略同时影响表征迁移性和补丁证据聚合，进而决定下游分类性能，需根据任务选择合适预训练方法。
---

## 摘要
尽管基础模型已广泛用作医学影像的特征提取器，但不同的预训练策略如何影响所学表征迁移到弱监督眼科成像任务的能力，我们的理解还相对有限。我们在超宽场（UWF）视网膜成像中探讨了这一问题，通过在一个基于图像块的多个实例学习（MIL）框架内评估基础模型表征，用于UWF图像上的疾病分类。我们比较了用有监督、掩码自编码器（MAE）和自蒸馏目标预训练的Vision Transformer编码器，同时保持下游聚合架构不变。在对在ImageNet-1k上预训练的ViT-B编码器进行的控制比较中，预训练目标的选择显著影响了冻结表征的迁移，其中有监督和基于自蒸馏的模型表现优于MAE。一个在更大规模上预训练的当代DINOv3模型取得了最强的整体性能，在五类糖尿病视网膜病变分级上的二次加权kappa系数为0.863，与DINOv1相当。注意力分析进一步揭示了与不同预训练表征相关的独特图像块聚合行为，而部分微调则大幅缩小了MAE的性能差距。这些发现表明，预训练策略影响了表征迁移能力以及MIL中后续的图像块级证据聚合，从而导致下游分类性能的差异。

## Abstract
Despite the widespread adoption of foundation models as feature extractors for medical imaging, relatively little is understood about how different pretraining strategies influence the transferability of learned representations to weakly supervised ophthalmic imaging tasks. We investigate this question in ultra-widefield (UWF) retinal imaging by evaluating foundation model representations within a patch-based multiple instance learning (MIL) framework for disease classification on UWF images. We compare Vision Transformer encoders pretrained with supervised, Masked Autoencoder (MAE), and self-distillation objectives, while keeping the downstream aggregation architecture unchanged. Within a controlled comparison of ViT-B encoders pretrained on ImageNet-1k, the choice of pretraining objective substantially influenced frozen representation transfer, with supervised and self-distillation-based models outperforming MAE. A contemporary DINOv3 model pretrained at a larger scale achieved the strongest overall performance, with a quadratic weighted kappa of 0.863 for five-class diabetic retinopathy grading, comparable with DINOv1. Attention analysis further revealed distinct patch-aggregation behaviours associated with the different pretrained representations, while partial fine-tuning substantially reduced the performance gap for MAE. These findings suggest that pretraining strategy influences both representation transferability and the subsequent aggregation of patch-level evidence within MIL, resulting in differences in downstream classification performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：探究在超宽场（UWF）视网膜成像的弱监督疾病分类任务中，不同的预训练策略（监督、掩码自编码器、自蒸馏等）如何影响基础模型所学表征的迁移能力，以及这种差异是否源于表征本身的可迁移性还是下游多实例学习（MIL）聚合器的证据整合行为。
- **研究背景**：基础模型已广泛用作医学影像的特征提取器，但关于预训练目标对表征迁移至弱监督眼科任务的影响却鲜有系统研究。UWF 图像分辨率极高、病灶分散，天然适合基于图像块的 MIL 框架，因此成为探究该问题的理想场景。
- **整体含义**：该研究表明，预训练策略不仅影响冻结表征的迁移质量，还显著改变了 MIL 中基于注意力的块级证据聚合方式，从而最终决定下游分类性能。这提示在医学影像任务中，需根据任务特点谨慎选择预训练目标，仅靠更大的数据规模或更深的网络并不总能保证最佳表现。

### 2. 论文提出的方法论

- **核心框架**：采用基于图像块的 MIL 框架（patch‑based MIL）评估表征迁移性。将每张 UWF 图像划分为多个图像块，通过冻结的视觉 Transformer（ViT）编码器提取块级特征，再由一个可训练的、基于注意力的 MIL 聚合模块整合所有块级特征，生成整张图像的包级（bag‑level）表征，最后进行分类。
- **关键设计**：保持下游分类架构（MIL 聚合器和分类头）完全一致，仅替换特征提取器（即预训练编码器），从而实现对不同预训练表征的公平、控制变量式比较。
- **预训练目标对比**：
  - **监督预训练**：标准图像分类目标。
  - **掩码自编码器（MAE）**：通过重建被掩码的图像块进行自监督预训练。
  - **自蒸馏（Self‑Distillation）**：如基于 DINO 方法，学生网络与动量教师网络之间进行自蒸馏，无需标签。
- **实验变体**：
  - 在 ImageNet‑1k 上预训练的 ViT‑B 编码器之间进行控制比较。
  - 引入更大规模预训练的当代模型 DINOv3，以考察规模效应。
  - 执行**部分微调**：在冻结表征之上微调 MIL 聚合模块及少量顶层参数，评估能否补偿弱预训练方法的不足。
  - 通过**注意力分析**可视化不同模型在下游分类时的块级注意力热图，揭示证据聚合行为的差异。

### 3. 实验设计

- **数据集与任务**：
  - **MMRDR 数据集**：用于五类糖尿病视网膜病变（DR）分级，评估指标为二次加权 Kappa 系数及多项分类指标（见论文 TABLE II）。
  - **眼内肿瘤检测任务**：根据 FIGURE 6 的描述，还包含一个区分“含眼内肿瘤”与“不含肿瘤”的 UWF 视网膜图像二分类任务，用于进一步验证模型的泛化性。
- **基准与对比方法**：在同一 MIL 框架下，对比以下冻结特征提取器的表现：
  - 监督预训练 ViT‑B
  - MAE 预训练 ViT‑B
  - 自蒸馏预训练模型（如 DINOv1、DINOv3）
  - 不同预训练规模（ImageNet‑1k vs. 大规模数据预训练的 DINOv3）
- **评价维度**：分类性能、注意力热图展示的证据聚合模式、部分微调后的性能提升幅度。

### 4. 资源与算力

- 文中**未明确提及**所用的 GPU 型号、数量、训练时长或具体的计算资源消耗。仅通过模型规模（ViT‑B，以及更大的 DINOv3）和预训练数据量提供了间接的算力需求暗示，但未给出实验层面的设备与耗时数据。

### 5. 实验数量与充分性

- **实验组数量**：大致包含以下几组：
  - 不同预训练目标（监督、MAE、自蒸馏）在 ViT‑B 上的控制比较。
  - 不同预训练规模的对比（ImageNet‑1k 级 vs. 大规模预训练的 DINOv3）。
  - 注意力可视化分析至少覆盖了 MMRDR 多级分类和肿瘤检测两类任务的多组示例。
  - 部分微调实验，用于观察性能差距的缩小。
  - 每个实验设定均报告了多次运行的均值和标准差（如 TABLE II）。
- **充分性与客观性**：实验设计充分，在固定下游结构的前提下，对多种主流预训练范式进行了系统对比，且同时考察了冻结表征性能、聚合行为以及微调补偿效应，论证链条较为完整。对比条件公平，控制了聚合架构与数据划分，并通过多次运行提供了统计稳定性。但也略有局限性：对比的骨干网络主要限于 ViT‑B，未拓展到其他 ViT 变体或卷积网络。

### 6. 论文的主要结论与发现

- 在 ImageNet‑1k 规模下，**预训练目标对冻结表征的迁移性能有显著影响**：监督预训练和自蒸馏模型（DINO）明显优于 MAE。
- 扩大预训练规模可进一步获益：当代 DINOv3 模型取得了最强整体表现，在五类 DR 分级上二次加权 Kappa 达到 **0.863**，与 DINOv1 相当。
- **注意力分析揭示了与预训练策略相关的特异性图像块聚合行为**：不同预训练表征使得 MIL 聚合器关注不同的图像区域，进而影响最终决策。
- **部分微调可以有效缩小弱预训练方法（如 MAE）的性能差距**，说明弱表征虽不宜冻结使用，但经轻微调整后仍可获得竞争性表现。
- 总体而言，**预训练策略同时影响表征迁移性和 MIL 中的块级证据聚合方式**，二者的耦合最终决定了下游分类性能。

### 7. 优点

- **公平且控制良好的比较框架**：采用固定 MIL 架构、仅更换编码器的方式，剥离了聚合模块的影响，使预训练目标与下游性能的因果关系更清晰。
- **多维分析视角**：不仅对比性能指标，还结合注意力可视化和部分微调实验，从现象和机制两个层面解释性能差异，增强了结论的说服力。
- **临床相关性**：聚焦于具有高分辨率和广泛病灶特征的 UWF 图像，贴近真实医学诊断场景，对指导弱监督眼科 AI 模型开发具有实际意义。
- **启发性发现**：明确指出预训练策略对后续证据聚合行为的影响，这为后续设计更适合弱监督任务的预训练范式提供了新思路。

### 8. 不足与局限

- **骨干网络种类单一**：控制比较仅基于 ViT‑B，未验证其他规模或架构（如 ViT‑L、Swin Transformer、ConvNeXt）的结论一致性。
- **任务和数据集覆盖面有限**：仅涉及 DR 分级和眼内肿瘤检测，未在其他类型的眼科任务（如青光眼、年龄相关性黄斑变性）或公共 UWF 数据集上验证，结论的泛化性有待进一步确认。
- **计算资源未透明**：未报告实验消耗的 GPU 资源与时间，不利于后来的研究者评估复现成本和可行性。
- **部分微调配置不详**：虽然提及部分微调缩小差距，但未明确微调的具体层数、学习率等配置，也未系统消融不同微调比例对差异化补偿效果的影响。
- **可解释性深度**：注意力分析尚停留在视觉对比层面，缺乏对块聚合差异的定量化指标或病理关联分析，解释不够深入。

（完）
