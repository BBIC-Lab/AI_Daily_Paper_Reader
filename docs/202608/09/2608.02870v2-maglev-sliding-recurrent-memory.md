---
title: "Maglev: Sliding Recurrent Memory"
title_zh: "Maglev: 滑动循环记忆"
authors: "Bo Liu, Qiang Liu"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02870v2"
tags: ["query:ndai"]
score: 8.0
evidence: 用于下一个词元预测的循环Transformer架构
tldr: Maglev是一种结合滑动窗口注意力与循环内存的Transformer架构，通过预填充器Q生成内存目标，解码器P使用滑动窗口和循环K/V注入进行预测，训练时使用内存一致性损失对齐两者，推理时仅用P。该方法在验证损失和下游预训练基准上优于滑动窗口和潜循环Transformer基线，且参数共享能减少内存占用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02870v2/fig-001.webp\", \"caption\": \"Table 1: Pretraining benchmark results after 43.52B training tokens. FW BPB is evaluated on FineWeb-Edu validation data; LMD denotes LAMBADA. HellaSwag and ARC-Challenge use normalized accuracy. Higher is better except for BPB and perplexity.\", \"page\": 6, \"index\": 1, \"width\": 830, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2608.02870v2/fig-002.webp\", \"caption\": \"Figure 2: Training dynamics for the Maglev consistency objective. The prefiller Q supplies memory targets m′ t, and the decoder P is trained to produce matching memories mt while optimizing nexttoken prediction.\", \"page\": 6, \"index\": 2, \"width\": 826, \"height\": 274}]"
motivation: 解决滑动窗口注意力在捕获长期依赖方面的局限性。
method: 设计双模型架构：预填充器Q利用全注意力产生内存目标，解码器P使用滑动窗口和循环K/V注入，通过内存一致性损失对齐内存。
result: 在验证损失和下游任务上超越多种基线模型。
conclusion: Maglev有效结合了滑动窗口效率和循环记忆能力，参数共享进一步优化了资源消耗。
---

## 摘要
我们引入了Maglev，一种具有固定大小记忆的循环Transformer架构，它推广了滑动窗口注意力，同时在训练期间保持可并行化。Maglev由两个耦合模型组成：预填充器$Q$，其利用完整注意力\footnote{在实践中，我们对$Q$使用交错的全注意力和滑动窗口注意力，因为这样能获得更强的性能。基本要求是$Q$比$P$更具表现力，并且可以访问完整的历史信息。}来产生记忆目标$m'_t$，以及解码器$P$，它仅使用滑动窗口注意力和循环K/V注入来产生用于下一token预测的解码器记忆$m_t$。我们使用记忆一致性损失训练Maglev，该损失将$m_t$与$m'_t$对齐，从而允许推理时仅使用$P$。从经验上看，Maglev相较于滑动窗口和潜在循环Transformer基线，在验证损失和下游预训练基准上有所提升。此外，在$P$和$Q$之间共享参数减少了参数内存，同时保留了大部分收益。

## Abstract
We introduce \ours{}, a recurrent Transformer architecture with fixed-size memory that generalizes sliding-window attention while remaining parallelizable during training. \ours{} consists of two coupled models: a prefiller $Q$, which leverages full attention\footnote{In practice, we use interleaved full and sliding-window attention for $Q$, as this yields stronger performance. The essential requirement is that $Q$ be more expressive than $P$, with access to the full history.} to produce memory targets $m'_t$, and a decoder $P$, which uses only sliding-window attention and recurrent K/V injection to produce decoder memories $m_t$ for next-token prediction. We train \ours{} with a memory consistency loss that aligns $m_t$ with $m'_t$, allowing inference to use $P$ alone. Empirically, \ours{} improves validation loss and downstream pretraining benchmarks over sliding-window and latent recurrent transformer baselines. Moreover, sharing parameters between $P$ and $Q$ reduces parameter memory while preserving most of the gains.