---
title: "Learning from the Future: Privileged Self-Distillation for Sequential Recommendation"
title_zh: 向未来学习：面向序列推荐的优选自蒸馏
authors: "Jiakai Tang, Yang Zhang, See-Kiong Ng, Xu Chen, Wen Chen, Jian Wu, Han Zhu"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27055v1"
tags: ["query:ndai"]
score: 9.0
evidence: 利用特权的未来信息预测用户交互序列中的下一项
tldr: 序列推荐通常使用因果前缀目标训练，忽略未来交互中的用户偏好演化信息。本文提出特权自蒸馏框架（PSD），将未来交互作为仅供训练使用的特权信息，通过共享骨干网络的双视图掩码生成教师和学生分布，将未来交互转化为监督信号，同时保持推理时模型不变。引入可达性门控和动量教师以稳定训练，在多个基准上取得一致提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27055v1/fig-002.webp\", \"caption\": \"Figure 3: Relative improvement of the teacher and student over the SASRec backbone under varying future visibility.\", \"page\": 7, \"index\": 2, \"width\": 473, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2607.27055v1/fig-001.webp\", \"caption\": \"Figure 4: Oracle analysis comparing PSD with its backbone under different future-information settings.\", \"page\": 7, \"index\": 1, \"width\": 498, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2607.27055v1/fig-003.webp\", \"caption\": \"Figure 6: Sensitivity of PSD to the gate percentile 𝛿 and the EMA rate 𝛼 on Video Games and Yelp with the SASRec.\", \"page\": 8, \"index\": 3, \"width\": 468, \"height\": 326}]"
motivation: 现有序列推荐仅利用前缀信息，未来交互能揭示用户意图演化，蕴含更丰富监督，但推理时不可用，需寻找利用方式。
method: 提出PSD，在同一骨干网络上应用前缀和未来感知掩码得到学生和教师视图，通过自蒸馏将未来信息转为训练监督，辅以可达性门控和动量教师。
result: 在公开基准和多种骨干网络上，PSD带来一致且显著的性能提升。
conclusion: PSD有效利用未来交互作为特权信息进行自蒸馏，不改变部署模型与推理成本，显著提升序列推荐效果。
---

## 摘要
序列推荐模型通常在与推理一致的因果（仅前缀）目标下使用独热的下一个物品标签进行训练。尽管这兼容部署，但这种监督方式几乎无法揭示非目标物品之间的相对偏好。然而，记录中的交互序列包含额外的监督来源：目标之后的交互往往揭示了用户意图的演变，使目标更易于解释。我们将这些未来交互视为仅训练时可用的特权信息，可在学习期间使用但推理时不可用。这引出一个自然的问题：未来交互能否提供更丰富的监督，同时保持训练与推理时的预测目标一致？我们提出特权自蒸馏（PSD）框架，将学习时信息与推理时信息分离。PSD 对同一骨干网络应用两种注意力掩码：未来感知视图产生以过去和未来交互为条件的特权教师分布，而仅前缀视图产生用于部署的学生分布。蒸馏特权分布可将未来交互转化为仅训练时的监督，而非推理时的输入。由于两种视图共享骨干网络，教师的优势纯粹来自信息，而非架构，因此无需单独预训练的教师，且教师的监督可随着学生的进化而自适应。PSD 进一步利用优势可达性门控将蒸馏聚焦于观察到的前缀可能支持的教师信号，并采用动量平均教师以稳定目标。该框架端到端地在单阶段优化，部署模型和推理成本保持不变。在公开基准和多种骨干网络上的实验显示了持续一致的改进。

## Abstract
Sequential recommenders are commonly trained with one-hot next-item labels under a causal (prefix-only) objective aligned with inference. While deployment-compatible, this supervision offers little insight into relative preferences among non-target items. Yet logged interaction sequences contain an additional supervisory source: interactions following the target often reveal how user intent evolves, making the target easier to interpret. We treat these future interactions as training-only privileged information, available during learning but not at inference. This raises a natural question: can future interactions provide richer supervision while keeping training aligned with inference-time prediction?   We propose Privileged Self-Distillation (PSD), a framework that separates learning-time information from inference-time information. PSD applies two attention masks to the same backbone: a future-aware view yields a privileged teacher distribution conditioned on past and future interactions, while a prefix-only view yields the student distribution used for deployment. Distilling the privileged distribution converts future interactions into training-only supervision rather than inference-time inputs. Since both views share a backbone, the teacher's advantage is purely informational, not architectural, removing the need for a separately pretrained teacher and letting its supervision adapt as the student evolves. PSD further uses an advantage-reachability gate to focus distillation on teacher signals likely supported by the observed prefix, along with a momentum-averaged teacher for stable targets. The framework is optimized end-to-end in a single stage, leaving the deployed model and inference cost unchanged. Experiments across public benchmarks and diverse backbones show consistent improvements.

---

## 论文详细总结（自动生成）

好的，根据您提供的要求和论文内容，以下是对该论文的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**：
    *   序列推荐通常在与推理一致的目标下，使用“独热标签”（one-hot next-item labels）通过预测下一物品（next-item prediction）进行训练。
    *   这种监督方式信息量低，因为它仅关注被点击的目标物品，对大量非目标物品的相对偏好信息毫不涉及。
    *   然而，训练数据中的未来交互序列（目标物品之后的行为）实际记录了用户意图的演化，为理解当前目标提供了丰富上下文。这构成了一种**训练时可用的特权信息**。
*   **核心问题**：
    *   如何在不牺牲推理时因果性（即部署时模型仅能依赖用户过去的交互）的前提下，有效利用未来交互这一特权信息来提供更丰富的训练监督？
*   **整体含义**：
    *   论文旨在解决训练与推理之间的信息不对称问题，提出一个通用框架，将未来交互从推理时的“输入”转变为一个纯粹的“训练监督信号”，从而提升模型性能而不增加线上部署成本。

### 2. 论文提出的方法论

*   **核心思想**：
    *   提出**特权自蒸馏（PSD）**框架，通过在同一序列推荐骨干网络（backbone）上应用两种不同的注意力掩膜，构造出同一模型的两个视图：一个能看见未来的**特权教师**和一个仅见过去的**因果学生**。通过知识蒸馏，将教师的“软标签”（soft labels）知识传递给学生。
*   **关键技术细节**：
    *   **双掩膜教师与学生**：对序列中的每个目标位置 $t$，**因果掩膜**使学生仅能关注前缀 $S_{<t}$；**特权掩膜**使教师能同时关注前缀 $S_{<t}$ 和未来后缀 $S_{>t}$。两者共享同一个骨干网络的所有参数，教师的优势纯粹来自信息，而非架构。
    *   **优势-可达性门控**：教师对非目标物品的偏好预测（即“暗知识”）并非全部能从因果前缀中推理得出。因此，PSD 在每个训练批次中计算教师与学生预测分布的 KL 散度 $d_t$，并设置一个批次内百分位数阈值 $\gamma_\delta$。仅对满足 $d_t \le \gamma_\delta$ 的“可达”位置进行蒸馏，迫使教师信号不可达的“暗知识”被丢弃，避免干扰。
    *   **动量平均教师**：由于教师和学生是同一模型的参数，教师自身也在不断更新，其预测可能包含瞬时误差，导致自我指涉的监督信号不稳定。PSD 通过维护一个模型参数的**指数移动平均版本 $\bar{\theta}$**，使用 $\bar{\theta}$ 来生成稳定的教师分布 $\bar{p}^T_t$，从而平滑掉训练过程中的瞬时噪声。
*   **算法流程与目标函数**：
    *   **教师损失**：仅在教师视图下，使用传统的下一物品交叉熵损失函数 $L^T_{CE}$，确保其预测以真实目标物品为锚点。
    *   **学生损失**：为学生计算一个带门控的蒸馏损失函数 $L^S_{KD}$。该损失是在被门控选中的位置上，学生分布 $p^S_t$ 与动量平均教师分布 $\bar{p}^T_t$ 之间的 KL 散度。
    *   **最终优化目标**：整体损失函数为 $L_{PSD} = L^T_{CE} + L^S_{KD}$。整个模型端到端单阶段训练，推理时仅使用因果掩膜下的学生模型。

### 3. 实验设计

*   **数据集**：
    *   三个广泛使用的公开基准数据集：Amazon评论数据中的 **Video Games** 和 **CDs & Vinyl** 子集，以及 **Yelp** 商务评论数据集。
*   **评估指标**：
    *   采用全量物品排序评估，使用 **HR@K** 和 **NDCG@K** ($K=10, 20$)。
*   **骨干网络**：
    *   在三个代表性的序列推荐模型上验证PSD的通用性：**SASRec**（单向Transformer）、**BERT4Rec**（双向Transformer）、**UniSRec**（文本增强推荐模型）。
*   **对比方法**：
    *   **基础方法**：各骨干网络的标准训练方式（Base）。
    *   **蒸馏方法**：Ranking Distillation、S4Rec、CSRec（及其变体CS-D、CS-M、CS-T）。
    *   **去噪方法**：TCE、RCE。

### 4. 资源与算力

*   论文中**未明确提及**具体的算力资源，如 GPU 型号、数量或确切的训练时长。这表明其资源消耗可能并非该方法的核心优势或限制。

### 5. 实验数量与充分性

*   **实验数量**：实验设计全面，覆盖面广。
    *   **主实验**：在 3 个数据集 × 3 个骨干网络上，与 8 种基线方法进行了性能对比，产生了主要的对比结果。
    *   **诊断分析**：
        *   **未来可见度影响分析**：通过改变教师可见的未来交互比例，观察教师和学生性能的变化趋势。
        *   **Oracle分析**：通过对比模型在“无未来”、“有未来”测试场景及“Oracle评估”下的表现，量化特权知识在因果视图中的内化程度。
        *   **训练动态与参数敏感性分析**：追踪训练过程中师生胜率，分析门控百分位数 $\delta$ 和动量系数 $\alpha$ 等关键参数对性能的影响。
*   **充分性与公平性**：实验较为充分和公平。
    *   所有对比方法均应用于相同的骨干网络，并且各自的超参数都在验证集上进行了最优调整。
    *   诊断性实验对方法内部机制进行了深入剖析，有效支撑了其方法论动机。
    *   实验从多个维度（数据集、模型架构、对比方法、参数分析）验证了PSD的有效性和鲁棒性。

### 6. 论文的主要结论与发现

*   **一致且显著的性能提升**：**PSD在所有三个数据集和三种骨干网络上，均一致超越所有基线方法**。平均提升达19.8%，尤其在BERT4Rec上提升最为显著（平均35.9%），因为其训练（双向）与推理（单向）的不匹配问题最严重。
*   **特权知识的有效转移**：实验证明，学生模型的性能增益确实源自教师传递的特权知识，且这种增益具有泛化能力，在测试集没有未来交互的样本上同样有效。
*   **门控与动量机制的关键作用**：消融和参数分析表明，优势-可达性门控能有效过滤不可蒸馏的噪声；动量平均教师对稳定训练、防止自我指涉监督带来的负反馈循环至关重要。

### 7. 优点

*   **方法简洁优雅且解耦清晰**：通过仅改变注意力掩膜，巧妙地将信息源（未来交互）与推理路径（因果前缀）解耦，无需引入额外参数或辅助特征。
*   **即插即用的通用性**：作为一个训练策略，PSD可以直接应用于任何基于Transformer的序列推荐骨干网络，且单阶段端到端的训练方式易于实现。
*   **部署无感**：模型训练完成后，学生视图的推理过程与标准模型完全一致，不会增加任何额外的线上推理成本或延迟。
*   **机制设计精巧**：“优势-可达性门控”和“动量平均教师”精准地解决了自蒸馏框架中“暗知识不可达”和“自我指涉监督不稳定”两大核心难题，实验分析也充分验证了其有效性。

### 8. 不足与局限

*   **额外训练开销**：虽然推理无成本，但在训练期间，由于其双掩膜前向传播和教师EMA参数维护，会产生额外的算力和内存开销。
*   **对序列长度的敏感性**：方法依赖于未来的交互序列。对于交互序列非常短的新用户或冷启动场景，未来信息有限，PSD的优势可能会减弱。
*   **超参数依赖性**：PSD引入了门控百分位数 $\delta$ 和EMA率 $\alpha$ 两个关键超参数，虽然实验表明其不敏感，但在新数据集上仍需调优。
*   **应用范围限制**：方法局限于Transformer架构的序列推荐模型，能否有效扩展到基于图神经网络（GNN）或多层感知机（MLP）等其他架构的推荐模型中尚待验证。

（完）
