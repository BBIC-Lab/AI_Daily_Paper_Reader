---
title: Toward a Unified Statistical Theory of Unsupervised Pretraining and Supervised Neural Knowledge Graph Learning
title_zh: 迈向无监督预训练与有监督神经知识图谱学习的统一统计理论
authors: "Jifan Zhang, Miklos Racz, Suqi Liu"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26346v1"
tags: ["query:ndai"]
score: 9.0
evidence: 无监督预训练后接监督学习，利用预训练知识用于下游任务
tldr: 知识图谱学习面临标记数据稀缺和评分函数设计缺乏理论指导的问题。本文提出一个两阶段统一框架：先在异构语料上无监督预训练，再在多种关系类型上监督学习，并建立非渐近风险界，量化了预训练对减少标记样本需求的益处。实验验证了理论并证明方法在大规模基准上有效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26346v1/fig-007.webp\", \"caption\": \"Figure 2: End-to-end risk decomposition (Experiment 3). The n-, m-, and W -sweeps probe the supervised estimation, pretraining, and approximation terms of Theorem 2; the oracle uses the true Zoracle in place of Ẑ.\", \"page\": 18, \"index\": 7, \"width\": 979, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-010.webp\", \"caption\": \"Table 1: Real-data statistics after preprocessing and splitting. PrimeKG uses a hard-enriched test split; hard relations are selected by an initial fixed-embedding model’s per-relation MSE–variance difficulty criterion.\", \"page\": 19, \"index\": 10, \"width\": 968, \"height\": 476}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-002.webp\", \"caption\": \"Figure 3: Random edge-split results on WordNet with relation-balanced training/evaluation and fixed entity embeddings. Results are averaged over 10 seeds and shown as mean ± standard deviation.\", \"page\": 21, \"index\": 2, \"width\": 979, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-001.webp\", \"caption\": \"Figure 4: Hard-relation-focused results on PrimeKG with relation-balanced training/evaluation and fixed entity embeddings. Results are averaged over 10 seeds and shown as mean ± standard deviation.\", \"page\": 21, \"index\": 1, \"width\": 979, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-005.webp\", \"caption\": \"Figure 5: Overall results on PrimeKG with relation-balanced training/evaluation and fixed entity embeddings. Results are averaged over 10 seeds and shown as mean ± standard deviation.\", \"page\": 22, \"index\": 5, \"width\": 963, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-008.webp\", \"caption\": \"Table 2: WordNet statistics after preprocessing and edge splitting with a 60%/20%/20% train/validation/test ratio. The dataset is constructed at the synset level, where nodes correspond to WordNet synsets and edges correspond to semantic or lexical relations derived from WordNet pointers.\", \"page\": 45, \"index\": 8, \"width\": 868, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-006.webp\", \"caption\": \"Table 3: PrimeKG statistics after preprocessing and hard-enriched splitting. Easy relations use a 60%/20%/20% train/validation/test split, while hard relations use the same validation ratio but an enlarged test ratio, yielding a test set enriched with difficult relation types.\", \"page\": 46, \"index\": 6, \"width\": 856, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-003.webp\", \"caption\": \"Table 4: Experimental details.\", \"page\": 47, \"index\": 3, \"width\": 956, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-004.webp\", \"caption\": \"Table 5: WordNet trainable-embedding ablation under the edge split with relation-balanced training and evaluation.\", \"page\": 47, \"index\": 4, \"width\": 856, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-011.webp\", \"caption\": \"Table 6: PrimeKG trainable-embedding ablation for PNKG variants. The “Test set” column indicates whether metrics are computed on all test relations or only on the hard-relation subset.\", \"page\": 48, \"index\": 11, \"width\": 860, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2607.26346v1/fig-009.webp\", \"caption\": \"Table 7: Entity-wise split diagnostic. WordNet values are averaged over 10 seeds; the PrimeKG rows report the entity-split diagnostic values from the same protocol.\", \"page\": 49, \"index\": 9, \"width\": 940, \"height\": 584}]"
motivation: 解决知识图谱中关系特定标记三元组稀缺和评分函数设计随意、缺乏理论支撑的问题。
method: 提出两阶段端到端框架：在异构语料上进行无监督预训练，再在多种关系类型上监督学习，并建立非渐近风险界。
result: 合成实验验证理论组件，真实实验证实方法在大规模知识图谱基准上的有效性。
conclusion: 该统一理论框架展示了无监督预训练能显著降低对标记数据的依赖，提升知识图谱学习的性能和泛化能力。
---

## 摘要
知识图谱学习为表示和推断结构化知识提供了强大框架，具有广泛的实际应用。然而，每个实体缺乏关系特定的标记三元组阻碍了表达模型的训练，而评分函数的特设设计限制了泛化性且缺乏理论基础。我们通过一个有理论依据的端到端训练框架来解决这两个问题，该框架扩展并涵盖了现有方法。我们的框架是一个两阶段过程：先对异构语料进行无监督预训练，然后对多种关系类型进行有监督学习。我们建立了一个非渐近风险界，将预训练表示误差与标记样本复杂度分离开来，形式化地量化了大规模无标签数据对下游知识预测的益处。合成实验验证了每个理论组成部分，真实世界实验则证实了我们的方法在大规模知识图谱基准上的有效性。

## Abstract
Knowledge graph learning provides a powerful framework for representing and inferring structured knowledge, with broad practical applications. However, the scarcity of relation-specific labeled triples per entity hinders the training of expressive models, and the ad hoc design of scoring functions limits generalizability and lacks theoretical grounding. We address both issues with a theoretically grounded, end-to-end training framework that extends and subsumes existing methods. Our framework is a two-stage procedure: unsupervised pretraining over heterogeneous corpora followed by supervised learning with multiple relation types. We establish a nonasymptotic risk bound that disentangles pretraining representation error from labeled-sample complexity, formally quantifying the benefit of large-scale unlabeled data for downstream knowledge prediction. Synthetic experiments validate each theoretical component, and real-world experiments confirm the effectiveness of our approach on large-scale knowledge graph benchmarks.

---

## 论文详细总结（自动生成）

好的，以下是基于给定论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：当前的知识图谱学习面临两大挑战。
    *   **数据稀缺性**：每个实体拥有的、特定关系的标记三元组数量稀少，这限制了对复杂模型的训练。
    *   **理论匮乏**：评分函数的设计多依赖经验（特设），缺乏理论基础，影响了模型的泛化能力。
*   **整体含义**：本文旨在解决上述问题，为知识图谱学习提供一个统一的理论框架。该框架形式化地证明，通过利用大规模、异构的无标签数据进行无监督预训练，可以有效降低下游有监督知识预测任务对标记样本的需求。这意味着可以利用现实中丰富但未被充分挖掘的实体边信息（如文本、元数据）来显著提升模型性能，尤其是在标记数据稀缺的场景下。

### 2. 论文提出的方法论

论文提出了一个名为**预训练神经知识图谱**的两阶段端到端训练框架。

*   **核心思想**：
    1.  第一阶段（无监督预训练）：利用来自多个“视图”的丰富但无标签的实体级信息（如文本描述），通过加权核主成分分析学习低维的实体坐标嵌入。
    2.  第二阶段（有监督学习）：将这些预训练得到的坐标作为输入，使用ReLU网络从有限的标记三元组中学习关系特定的评分函数。

*   **关键技术细节与流程**：
    *   **第一阶段：加权多视图核主成分分析预训练**
        *   **多视图信息建模**：假设共有 $S$ 个视图，对于视图 $s$，观测到的实体向量 $u_{s,j}$ 被建模为“信息加噪声”结构：$u_{s,j} = v_{s,j} + \sigma_{s,j} \frac{\epsilon_{s,j}}{\sqrt{m_s}}$。其中 $v_{s,j}$ 是潜在信号，$\epsilon_{s,j}$ 是高维噪声。
        *   **核矩阵构建**：对于每个视图 $s$，选择一个正定核 $k_s$，计算实体间的相似度矩阵 $K_s^{\text{obs}}(j, j') = \frac{1}{N} k_s(u_{s,j}, u_{s,j'})$。这个框架可以自然容纳预训练语言模型，将其编码器视为映射 $\phi_s$，并诱导出核 $k_s(u, u') = \langle \phi_s(u), \phi_s(u') \rangle$。
        *   **多视图聚合**：给定权重向量 $w = (w_1, \dots, w_S)$，将所有视图的核矩阵加权聚合为 $K^{\text{obs}}(w) = \sum_{s=1}^{S} w_s K_s^{\text{obs}}$。
        *   **提取实体坐标**：计算聚合核矩阵 $K^{\text{obs}}(w)$ 的前 $d$ 个最大特征值对应的特征向量，构成实体坐标矩阵 $\hat{Z} \in \mathbb{R}^{N \times d}$。
    *   **第二阶段：有监督神经网络知识图谱学习**
        *   给定预训练坐标 $\hat{Z}$，为每个关系 $r$ 训练一个带有参数预算 $W$ 和深度 $D_{\text{net}}$ 的有界 ReLU 网络 $f_r$。
        *   对于三元组 $(h, r, t)$，其预测分数为 $f(\hat{Z}; h, r, t) = f_r(\hat{z}_h, \hat{z}_t)$，其中 $\hat{z}_h, \hat{z}_t$ 是头、尾实体的预训练坐标。

*   **核心理论成果**：
    *   **定理 1（子空间恢复界）**：一个非渐近的扰动界。它表明，当多视图核残差相对小于有效信号分离度 $\gamma(w)$ 时，学习到的实体子空间 $\hat{Z}$ 是准确的，且误差上界受控于一个由视图偏差项和方差项组成的可分解表达式。
    $$ \| \sin \Theta(\hat{Z}, Z_{\text{sig}}) \|_F^2 \leq \frac{C}{\gamma(w)^2} \left[ \left( \sum_{s=1}^{S} w_s b_s + \sum_{s \in S_{\text{ip}}} w_s d_s \right)^2 + \frac{1}{\delta} \sum_{s=1}^{S} w_s^2 v_s^2 \right] $$
    *   **定理 2（端到端风险界）**：一个将下游知识图谱预测误差可加性分解的总风险界，清晰地分离了不同来源的误差。
    $$ \| \hat{f}(\hat{Z}; \cdot) - \gamma \|_P^2 \leq \underbrace{C_1 \left( \frac{W}{\log W} \right)^{-\beta/d}}_{\text{神经近似误差}} + \underbrace{C_2 (\sigma_m^2 + B^2) \frac{K D_{\text{net}} W \log W}{n} \log \frac{en}{K D_{\text{net}} W \log W}}_{\text{有监督估计误差}} + \underbrace{C_3 \frac{L_\star^2 \kappa_A^2}{\gamma(w)^2} [\cdots]}_{\text{预训练误差}} + \underbrace{C_4 \delta_{\text{opt}}}_{\text{优化误差}} $$

### 3. 实验设计

实验分为合成模拟和真实数据两部分，以验证理论的不同方面。

*   **合成模拟实验**：
    *   **数据集/场景**：使用已知真实结构的人工生成数据。生成一个共享的低维潜在实体子空间 $Z_\star$，并为多个视图生成带高斯噪声的实体向量。
    *   **对比/验证内容**：
        1.  **实验 1（预训练恢复率）**：固定均匀权重，改变实体维度 $m$，验证子空间恢复误差 $\| \sin \Theta(\hat{Z}, Z_\star) \|_{\text{op}}$ 的衰减速度为理论预测的 $m^{-1/2}$。
        2.  **实验 2（视图权重）**：在视图质量异构的设置下，比较均匀权重、信噪比调整的逆方差权重和一个模拟的预言机权重，以验证理论指导的权重选择优于均匀权重。
        3.  **实验 3（端到端风险分解）**：通过分别改变标记样本量 $n$、预训练样本维度 $m$ 和 ReLU 网络宽度 $W$，观察测试均方误差的变化，分别探测了有监督估计误差、预训练误差和近似误差项。

*   **真实数据实验**：
    *   **数据集**：
        *   **WordNet**：一个词汇语义知识图谱，包含 116,566 个实体，33 种关系，373,330 个正三元组。实体边信息由同义词集定义构成。
        *   **PrimeKG**：一个大型生物医学知识图谱，包含 90,067 个实体，30 种关系，8,100,498 个正三元组。实体边信息由名称、类型和源元数据构成。实验特别设计了一个“困难关系”测试子集以强调异构性。
    *   **对比方法**：
        *   **仅图结构基线**：TransE， DistMult， ComplEx， RotatE。这些方法仅从三元组结构进行学习。
        *   **PNKG 变体**：固定架构，仅改变实体表示。
            *   **随机固定坐标**：作为下界。
            *   **单视图语言模型（LM）坐标**：使用一个预训练 LM 的嵌入作为实体坐标。
            *   **多视图核聚合坐标**：将多个 LM 嵌入作为多视图核，聚合成实体坐标。权重通过验证集选择（如 WordNet 上的均匀权重，PrimeKG 上的 8:1:1 权重）。

### 4. 资源与算力

*   **算力使用**：
    *   **PrimeKG 实验**：主要使用 **NVIDIA GeForce RTX 5080** GPU（16,303 MiB 显存），每个随机种子运行约需 **15 分钟**。
    *   **WordNet 实验**：主要使用 **NVIDIA GeForce RTX 5070** GPU（12,227 MiB 显存），每个随机种子运行约需 **5 分钟**。
    *   **整体规模**：所有真实数据结果均在 **10 个随机种子**上运行并报告均值和标准差。考虑到主流消费级 GPU 和分钟级的训练时间，整体算力消耗不大。

### 5. 实验数量与充分性

*   **实验数量**：论文涵盖了**3 组合成模拟实验**和**2 个真实数据集上的多组对比与消融实验**。
    *   合成实验系统地探测了理论公式中的不同变量（$m$, $n$, $W$）。
    *   真实数据实验在 **WordNet**（边分割、实体分割）和 **PrimeKG**（整体测试、困难关系测试、实体分割）上，对比了 **4 个图结构基线**与**多个 PNKG 变体**（随机、单视图、多视图）。此外，还进行了固定嵌入与可训练嵌入的消融实验。
*   **充分性与公平性**：
    *   **充分性**：实验设计紧密围绕理论，逐一验证了子空间恢复率、权重选择原则和风险分解，合成实验与现实实验相互印证，比较充分。在 PrimeKG 上引入“困难关系”子集，体现了对现实数据复杂性的考量。
    *   **公平性**：所有方法在相同的分割协议、负采样策略和关系平衡的加权目标下进行评估，结果报告了均值和标准差，保证了公平性。对比了多种流行的仅图结构基线，比较维度全面。

### 6. 论文的主要结论与发现

*   **理论结论**：定理 2 的端到端风险界表明，下游知识图谱预测的误差可以被清晰地分解为四项，其中无监督预训练的益处被形式化为一个独立的误差项。当无标签数据足够大时，该项不再成为性能瓶颈，从而在理论上证明了预训练对降低标记样本复杂度的价值。
*   **实证发现**：
    *   **合成实验**：精确验证了预训练子空间恢复率的 $m^{-1/2}$ 衰减速率，以及通过逆方差加权可以逼近最优性能。
    *   **真实数据实验**：
        *   在多数场景下，使用预训练 LM 坐标的 PNKG 模型显著优于随机坐标和仅图结构的基线。
        *   多视图聚合能带来增益，但其效果依赖于数据集：在视图质量相对均匀的 WordNet，均匀权重已是最佳；在视图质量异构的 PrimeKG，通过验证集选择权重更为有效。
        *   **数据依赖的偏差-方差权衡**：在标记数据相对稀疏的 WordNet，固定预训练坐标效果更好；在标记数据丰富的 PrimeKG 整体测试集上，可训练的实体嵌入通过降低关系偏差进一步提升了性能，但在困难关系子集上，固定预训练嵌入仍是最佳初始化方法。

### 7. 优点

*   **理论贡献突出**：首次为“无监督预训练 + 有监督知识图谱学习”这一范式提供了**统一、非渐近的统计理论保证**，清晰地分解和量化了不同来源的误差。
*   **框架通用性强**：提出的 PNKG 框架具有灵活性，能够通过核方法自然地整合任意预训练编码器，并处理异构的实体边信息。
*   **指导实践**：理论分析直接推导出了如**信噪比调整的逆方差视图权重**等具有实践指导意义的启发式规则。
*   **实验验证全面**：实验既包含精确验证理论预测速率的合成实验，也覆盖了词汇和生物医学两个截然不同领域的真实大规模图谱，并对固定/可训练嵌入的权衡进行了细致分析。

### 8. 不足与局限

*   **理论假设限制**：
    *   **固定坐标假设**：理论分析假设预训练坐标在第二阶段是固定的，排除了实际中常见的端到端微调场景的理论分析。
    *   **共享子空间假设**：假设（3.4）要求所有视图的信号共享一个精确的低秩公共子空间，这在视图信息互补而非冗余时可能不成立。
    *   **独立同分布三元组采样**：假设三元组独立同分布，排除了测试实体在预训练时未出现过的归纳式学习场景。
*   **实践局限性**：
    *   **视图权重选择**：理论推导出的最优权重 $w_s^{\text{IV}} \propto g_s / v_s^2$ 依赖于未知的总体参数（如有效信号强度和方差项），在实践中尚无法直接计算，仍需依赖验证集进行启发式选择，缺乏自适应的理论保证。
    *   **未利用图结构**：当前框架将知识图谱的图结构信息仅作为输入特征，没有将其作为一个独立的预训练视图。这在与像 RotatE 这样充分利用图结构的基线对比时，尤其是在标记数据丰富的 PrimeKG 整体测试中，性能未占优势。
    *   **算力限制**：尽管在消费级 GPU 上可运行，但针对数百万实体和数十亿三元组的工业级图谱，其核矩阵构建与特征分解的计算瓶颈仍是实际应用的挑战，论文未就此进行讨论。

（完）
