---
title: "Chaos Is a LADDER: Domain Generalization Beyond Invariance via Reweighting"
title_zh: 混沌为梯：通过重加权实现不变性之外的域泛化
authors: "Yuhang Jiang, Fengchuan Zhang, Sanguo Zhang, Guojun Zhu"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26458v1"
tags: ["query:ndai"]
score: 9.0
evidence: 通过重加权进行域泛化，使模型适应无标注的新目标域
tldr: 领域泛化通常寻求跨域不变表征，但当域本身调节因果映射时，不变性限制性能。本文提出LADDER，一种固定模型流程：学习因果与风格表征，冻结编码器并训练源域特定分类器，仅在推理时利用无标签目标域数据加权集成这些分类器，避免目标标签或模型更新。理论保证源重加权，在模拟、FMoW和iWildCam上验证了总体和分组平均准确率的提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26458v1/fig-005.webp\", \"caption\": \"Figure 1: General structural causal model. The edge e → Y (e) means that the conditional rule ηe(Y | Zc) may vary across domains, not that e is a per-sample causal feature. Style is used by LADDER at the domain-summary level to choose source weights, not as a per-sample classifier input.\", \"page\": 2, \"index\": 5, \"width\": 658, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-001.webp\", \"caption\": \"Table 1: FMoW-WILDS results over five random seeds. Accuracy, worst-region accuracy, average-region accuracy, and ID-to-test accuracy drop are reported in percentage points.\", \"page\": 6, \"index\": 1, \"width\": 686, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-002.webp\", \"caption\": \"Figure 3: FMoW-WILDS accuracy and worst-region accuracy on the ID validation, OOD validation, and OOD test splits. Error bars denote standard deviations over five random seeds.\", \"page\": 6, \"index\": 2, \"width\": 630, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-007.webp\", \"caption\": \"Table 2: iWildCam-WILDS results over five random seeds. Test accuracy, macro F1, and filtered average-group accuracy are reported in percentage points.\", \"page\": 7, \"index\": 7, \"width\": 777, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-006.webp\", \"caption\": \"Figure 6: iWildCam-WILDS test accuracy and filtered averagegroup accuracy. Error bars denote standard deviations over five random seeds.\", \"page\": 7, \"index\": 6, \"width\": 861, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-010.webp\", \"caption\": \"Figure 2: Synthetic simulation KNN source-selection ablation overK ∈ {1, . . . , 8} and “All”. The main setting uses K = 4, while “All” removes the KNN truncation and reweights all available source classifiers.\", \"page\": 14, \"index\": 10, \"width\": 1052, \"height\": 598}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-017.webp\", \"caption\": \"Figure 4: Supplementary style-conditioned pooled ablation in the controlled SCM. Stronger pooled predictors close much of the additive PooledJoint gap, especially in extrapolation, but they also have substantially larger variance across repetitions. LADDER remains competitive in extrapolation and gives the strongest interpolation performance among non-oracle methods.\", \"page\": 16, \"index\": 17, \"width\": 850, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-013.webp\", \"caption\": \"Table 3: FMoW-WILDS results over five random seeds. Accuracy, worst-region accuracy, average-region accuracy, and ID-to-test accuracy drop are reported in percentage points.\", \"page\": 17, \"index\": 13, \"width\": 640, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-014.webp\", \"caption\": \"Figure 5: FMoW-WILDS accuracy and worst-region accuracy on the ID validation, OOD validation, and OOD test splits.\", \"page\": 17, \"index\": 14, \"width\": 714, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-011.webp\", \"caption\": \"Figure 7: FMoW-WILDS KNN source-selection ablation over five random seeds. The main setting uses K = 5, while “All” removes the KNN truncation and reweights all retained source domains. The sweep includes K ∈ {1, 2, 3, 5} and shows that local source selection is stable across nearby neighborhood sizes.\", \"page\": 18, \"index\": 11, \"width\": 676, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-012.webp\", \"caption\": \"Figure 8: Full FMoW-WILDS t-SNE visualization. Panels (a,b) show the causal representation Zc colored by class and domain, respectively; panels (c,d) show the style representation Z (e) s colored by domain and used for source reweighting.\", \"page\": 19, \"index\": 12, \"width\": 616, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-016.webp\", \"caption\": \"Figure 9: FMoW-WILDS source-geometry diagnostic over all 55 source-domain pairs. The horizontal axis is the LADDER source style distance computed from learned source style distributions. The vertical axes report bidirectional source-classifier transfer loss, classconditional Zc prototype distance, two Zc summary distances, and label-distribution distance.\", \"page\": 20, \"index\": 16, \"width\": 847, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-003.webp\", \"caption\": \"Table 4: iWildCam-WILDS results over five random seeds. Test accuracy, macro F1, and filtered average-group accuracy are reported in percentage points.\", \"page\": 21, \"index\": 3, \"width\": 540, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-004.webp\", \"caption\": \"Figure 10: iWildCam-WILDS test accuracy and filtered average-group accuracy.\", \"page\": 21, \"index\": 4, \"width\": 692, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-008.webp\", \"caption\": \"Table 5: iWildCam-WILDS worst-location diagnostics over five random seeds. Raw worstlocation accuracy takes the minimum over all target locations; filtered worst-location accuracy and zero-location counts use locations with at least 50 samples.\", \"page\": 22, \"index\": 8, \"width\": 706, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-009.webp\", \"caption\": \"Figure 12: iWildCam-WILDS KNN source-selection ablation over three random seeds. The main setting fixesK = 6, while “All” removes the KNN truncation and reweights all retained source domains. Across K ∈ {1, 2, 3, 4, 5, 6}, K = 5 gives the highest test accuracy and macro F1, whereas K = 6 gives the strongest filtered average-group accuracy, matching the robustness-oriented protocol used in the main experiments.\", \"page\": 22, \"index\": 9, \"width\": 809, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2607.26458v1/fig-015.webp\", \"caption\": \"Table 7: Main implementation settings used for the reported runs. KNN ablations in the supplement vary only K while keeping the remaining training protocol fixed.\", \"page\": 24, \"index\": 15, \"width\": 922, \"height\": 402}]"
motivation: 传统不变性方法在域影响因果内容到响应的映射时失效，需要利用域风格定位目标域并自适应选择预测规则。
method: LADDER分解表征后冻结编码器，训练源域特定分类器，推理时通过无标签目标域协变量计算权重进行集成预测。
result: 在模拟、FMoW和iWildCam数据集上，LADDER在总体和分组平均准确率上均取得提升，并具备理论保证。
conclusion: LADDER通过推理时重加权克服不变性局限，为域依赖场景提供有效且无目标标签的域泛化方案。
---

## 摘要
域泛化（DG）旨在从多个源域学习并泛化到未见的目标域。大多数 DG 方法追求不变性：它们寻找一个因果表示，其预测规则在不同域之间保持不变。当因果机制稳定时，这一原则有效，但当域本身调节因果内容如何映射到响应时，它就变得局限。在这种情况下，直接将域风格输入预测器可能会产生误导性的捷径，因为风格本身并不会导致响应。然而，多重风格的表面混沌可以成为阶梯：风格能够在源域中定位未见的目标域，并指导应该信任哪些依赖于域的预测规则。我们提出了 Latent Adaptive Domain Disentanglement and Environment Reweighting（LADDER），这是一个固定模型的 DG 流程，学习因果/风格表示，冻结编码器，拟合特定于源的分类器，并仅在推理时使用未标记的目标域协变量集来计算这些固定分类器的权重，无需目标标签或模型状态更新。我们为源重加权建立了理论保证，并在模拟、FMoW 和按位置分组的 iWildCam 协议上验证了 LADDER，在整体准确率和分组平均准确率上均有提升。

## Abstract
Domain generalization (DG) aims to learn from multiple source domains and generalize to unseen target domains. Most DG methods pursue invariance: they seek a causal representation whose prediction rule is invariant across domains. This principle is effective when the causal mechanism is stable, but becomes restrictive when the domain itself modulates how causal content maps to the response. In this case, directly feeding domain style into the predictor can create misleading shortcuts, since style does not by itself cause the response. Yet the apparent chaos of multiple styles can become a ladder: style can locate the unseen target domain among source domains and guide which domain-dependent prediction rules should be trusted. We propose \emph{Latent Adaptive Domain Disentanglement and Environment Reweighting} (LADDER), a fixed-model DG pipeline that learns causal/style representations, freezes the encoders, fits source-specific classifiers, and uses an unlabeled target-domain covariate set only at inference to compute weights over these fixed classifiers, with no target labels or model-state updates. We establish theoretical guarantees for source reweighting and validate LADDER on simulations, FMoW, and a location-grouped iWildCam protocol, with gains in overall and group-averaged accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究动机**：领域泛化（DG）旨在从多个源域学习，并将模型推广到未见过的目标域。大多数DG方法追求“不变性”（Invariance），即学习一个在所有域中都保持相同预测规则的统一模型。
*   **核心问题**：当域本身（例如：不同摄像头的位置、采集年份）会调节“因果内容”到“最终预测”的映射关系时，追求单一不变规则的方法就显得不足。直接将域的风格信息输入单一预测器会引入有害的捷径（shortcuts），但完全忽略风格信息又会失去定位目标域、适应其特定规则的机会。
*   **整体含义**：本文提出，表面上混乱的多域风格信息应被视为“阶梯”（LADDER），用以定位目标域，并有选择地信任那些与目标域风格最接近的源域的预测规则，从而实现“不变性之外”的泛化。

### 2. 论文提出的方法论

*   **核心思想**：提出**潜在自适应域解耦与环境重加权（LADDER）** 框架，它是一个固定模型的推理流程，核心不是训练一个通用模型，而是学习如何根据目标域的风格分布来组合一组特定于源域的模型。
*   **关键技术细节与流程**：
    1.  **表示学习（Representation Learning）**：
        *   使用两个编码器将输入 $x$ 分解为**因果表示** $z_c = f_\theta(x)$（用于类别预测）和**风格表示** $z_s = g_\phi(x)$（用于域定位）。
        *   训练目标 $J$ 结合了分类损失、域分类损失、对抗性域混淆损失和正交损失等，以在操作上解耦这两个表示。
    2.  **模型训练与冻结**：训练完成后，**冻结**两个编码器的参数。为每个源域 $e$ 在固定的因果表示 $z_c$ 上训练一个特定的分类器 $h_{\psi_e}$。
    3.  **风格指纹缓存**：为每个源域计算并缓存其域级别的风格分布（指纹） $\nu_e$，通常为风格表示样本的经验分布。
    4.  **推理时重加权（Inference Reweighting）**：
        *   对于给定的无标签目标域数据集 $D^{(0)}$，仅使用风格编码器生成其目标风格指纹 $\nu`_0$。
        *   计算目标指纹与所有源域指纹之间的距离（如Wasserstein距离的Sinkhorn近似）。
        *   选取距离最近的 $K$ 个源域，根据距离和温度参数 $\tau$ 通过Softmax计算权重 $w_e = \text{Softmax}(-d_e / \tau)$。
        *   最终预测是所选源分类器输出的加权平均：$\hat{\ell}(x) = \sum_{e \in \mathcal{N}_K(0)} w_e h_{\psi_e}(f_\theta(x))$。
    5.  **关键区别**：风格信息仅在推理时用于**非参数地、在域级别上**选择/加权源分类器，而不作为单个样本的分类器输入，避免了训练时的捷径学习。

### 3. 实验设计

*   **数据集与场景**：
    *   **模拟数据（Simulations）**：基于设计的结构因果模型（SCM），用于验证理论，包含内插（Interpolation）和外推（Extrapolation）两种目标域。
    *   **FMoW-WILDS**：卫星图像土地利用分类（62类），域偏移来自年份和地理区域，测试整体准确率和区域鲁棒性。
    *   **iWildCam-WILDS**：野生动物相机陷阱图像分类（182类），域偏移来自不同的摄像头位置，存在严重的长尾分布，测试整体和位置级平均准确率。
*   **Benchmark与对比方法**：
    *   **目标无关（Target-agnostic）Baselines**: ERM（经验风险最小化）, IRM（不变风险最小化）, EQRM, RDM。
    *   **固定模型Baseline**: DSNR（推理时估计域索引的非参数回归模型）。
    *   **源路由控制（Source-routing Controls）**: 在LADDER的固定编码器和源分类器基础上，使用不同的路由策略：
        *   `UniformExperts`: 所有源分类器等权平均。
        *   `NearestExpert`: 仅使用风格距离最近的单个源分类器。
        *   `SampleGate`: 使用冻结的风格-域分类头进行逐样本权重路由。
*   **评价指标**: 测试准确率（Test Accuracy）、最差组准确率（Worst-group Accuracy）、平均组准确率（Avg-group Accuracy）等。

### 4. 资源与算力

*   **文中未明确说明**：论文正文部分未提及所使用的GPU型号、数量或总计算时长。
*   **补充材料信息**：在补充材料（Supplementary Material）的“A.4 Runtime and Storage”一节中，提供了在FMoW和iWildCam数据集上每种子（per seed）的**挂钟时间（wall-clock runtime）**，例如FMoW上LADDER总耗时约85.2分钟，iWildCam上约104.1分钟，但这未明确对应到具体的GPU硬件配置。存储开销方面，LADDER因需存储多个源分类器和风格指纹，会引入几MB到几十MB的额外持久存储。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **3个主要实验场景**：模拟数据、FMoW-WILDS、iWildCam-WILDS。
    *   每个场景均包含多个（5个以上）随机种子的重复实验，并报告了均值和标准差。
    *   **充分的消融研究**：
        *   模拟实验中：对邻居数量 $K$、目标摘要样本数 $n_0$、温度参数 $\tau$ 进行了全面的敏感性分析，并对比了多种使用风格信息的联合训练基线（Pooled-Interaction、Pooled-MLP、StyleHyperNet）。
        *   FMoW实验中：对 $K$ 值进行了消融，并通过t-SNE可视化和源几何诊断（如风格距离与分类器迁移损失的相关性）深入分析了机制。
        *   iWildCam实验中：对比了“split-level”和“location-level”的目标摘要粒度，并对 $K$ 值进行了消融分析。
*   **客观性与公平性**：通过设立`UniformExperts`、`NearestExpert`、`SampleGate`等使用相同骨架和源分类器的控制实验，公平地剥离了“固定源分类器集成”和LADDER特有的“分布级非参数路由”带来的增益，实验设计严谨公平。

### 6. 论文的主要结论与发现

*   **性能提升**：LADDER在模拟和两个真实世界基准测试（特别是iWildCam）上，相较于目标无关方法和其他路由控制方法，在总体准确率和分组鲁棒性（如location-averaged accuracy）上取得了显著提升。
*   **机制验证**：实验表明，域风格信息确实可以用于在推理时进行有效的源域路由。风格距离与源分类器的可迁移性（transfer loss）高度相关，验证了该方法底层假设的合理性。
*   **理论支撑**：理论分析为源重加权的误差提供了保证，将总误差分解为源估计误差、经验分布误差和流形插值误差，并证明了在特定条件下重加权估计器的一致性和优于固定不变预测器的能力。

### 7. 优点

*   **新颖的视角**：明确挑战了DG中绝对化的“不变性”原则，为利用域信息进行自适应预测提供了新范式。
*   **设计简洁清晰**：将表示学习与推理时的路由决策分离，流程（编码->缓存->近邻重加权）逻辑清晰。
*   **隐私友好与适应性强**：推理时不需要目标域的标签，也无需更新模型状态（无梯度反传），适用于目标域数据与训练环境完全分离的场景。
*   **强大的理论基础**：为方法的有效性提供了严格的理论证明，增强了可信度。
*   **全面的消融与分析**：通过精细的控制实验和可视化，深入剖析了方法各组件（$K$, $\tau$，目标摘要粒度）的作用。

### 8. 不足与局限

*   **实验覆盖限制**：实验主要在图像分类任务上进行，其通用性在NLP、语音等其他模态或更复杂的结构化预测任务中尚未得到验证。
*   **路由的拓扑限制**：LADDER的路由本质上是内插（interpolation），即目标域的预测规则是源域规则的凸组合。当目标域在风格空间中远离所有源域（真实的外推）时，其性能可能会大幅下降。
*   **表示解耦的操作性**：论文明确承认其“解耦”是操作性的（operational），即通过损失函数引导其扮演不同角色，而不保证统计上的独立性。这可能导致因果表示和风格表示间存在信息泄漏，尽管实验证明其对最终任务有效。
*   **对目标域样本量的依赖**：推理时路由的可靠性依赖于一个无标签目标协变量集合，其性能与分析表明，当目标域样本极少（如1个）时，路由噪声大，增益有限。

（完）
