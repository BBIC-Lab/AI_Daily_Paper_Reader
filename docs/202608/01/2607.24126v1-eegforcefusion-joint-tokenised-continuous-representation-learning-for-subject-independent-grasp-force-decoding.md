---
title: "EEGForceFusion: Joint Tokenised-Continuous Representation Learning for Subject-Independent Grasp Force Decoding"
title_zh: EEGForceFusion：面向被试独立抓握力解码的联合标记化-连续表示学习
authors: "Sankalp Sunil Turankar, Yogesh Kumar Meena"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24126v1"
tags: ["query:ndai"]
score: 9.0
evidence: 基于EEG的抓握力解码，结合token化-连续表示学习
tldr: 针对脑机接口中连续抓力解码的复杂时间动态、高个体差异和有限泛化性问题，提出EEGForceFusion框架，联合连续与标记化表示，融合卷积循环学习、量化标记化和Transformer时间建模，实现跨被试实时解码。在数据集上离线R²达0.817，实时模拟0.793，延迟满足实时要求，为辅助机器人和神经康复提供有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24126v1/fig-004.webp\", \"caption\": \"Fig. 1: Proposed EEG-to-grasp-force regression framework. Raw EEG signals are first preprocessed and encoded using a CNN-GRU encoder to obtain embeddings (e). These embeddings are tokenised and organised into sequences for transformerbased temporal modelling. In parallel, local temporal variations (∆e) are computed. The final prediction is obtained by fusing embeddings (e), temporal differences (∆e), and transformer-derived features (hTr) through a regression head.\", \"page\": 2, \"index\": 4, \"width\": 827, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2607.24126v1/fig-001.webp\", \"caption\": \"Fig. 2: (Left) Effect of transformer training depth on LOSO performance. Performance peaks at approximately epoch 5. (Right) Fusion performance across transformer configurations (3–7 epochs).\", \"page\": 4, \"index\": 1, \"width\": 810, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2607.24126v1/fig-002.webp\", \"caption\": \"Fig. 3: Representative real-time predictions for six subjects spanning the observed performance range: best (P9, P7), average (P3, P8), and worst (P11, P5). Each subplot shows predicted vs. ground-truth force over an 800-sample segment capturing force onset, sustain, and release. Full per-subject results are reported in Table II.\", \"page\": 4, \"index\": 2, \"width\": 964, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2607.24126v1/fig-003.webp\", \"caption\": \"TABLE III: Comparison with prior methods for EEG-based grasp force decoding. SS = subject-specific; SI = subjectindependent (LOSO). Best SI result in bold.\", \"page\": 5, \"index\": 3, \"width\": 898, \"height\": 265}]"
motivation: 连续抓力解码因复杂时间动态、高个体差异和有限泛化性而面临挑战。
method: 提出联合连续与标记化表示的混合框架，集成卷积循环学习、量化标记化和Transformer时间建模。
result: 在留一被试评估中，离线R²=0.817，实时模拟R²=0.793，延迟满足实时部署需求。
conclusion: 该方法展示了强跨被试泛化能力，验证了混合表示在实时EEG力解码中的实用性。
---

## 摘要
脑机接口建立了神经活动与外部设备之间的连接，能够利用非侵入式脑电图（EEG）恢复运动功能并推进人机交互。然而，由于复杂的时间动态、较高的被试间差异以及现有方法的泛化能力有限，连续抓握力解码仍然是一项挑战。为解决这一问题，我们提出了一种混合EEG解码框架，该框架联合建模连续表示与标记化表示，能够同时捕获精细的神经结构和长程时间依赖关系。所提方法在统一的融合回归架构中集成了卷积-循环表示学习、基于量化的标记化以及基于transformer的时间建模。在WAY-EEG-GAL数据集上按照严格的留一被试交叉验证条件进行的实验评估表明，离线设置下达到R² = 0.817，模拟实时评估中达到R² = 0.793，且延迟满足实时部署需求。这些结果展示了强大的跨被试泛化能力，并突显了混合连续-标记化表示在辅助机器人、神经康复和人机交互中基于EEG的实时力解码方面的实用性。

## Abstract
Brain-machine interfaces provide a link between neural activity and external devices, enabling restoration of motor function and advancing human-machine interaction using non-invasive electroencephalography (EEG). However, continuous grasp force decoding remains challenging due to complex temporal dynamics, high inter-subject variability, and limited generalisation of existing approaches. To address this, we propose a hybrid EEG decoding framework that jointly models continuous and tokenised representations, enabling capture of both fine-grained neural structure and long-range temporal dependencies. The proposed approach integrates convolutional-recurrent representation learning, quantisation-based tokenisation, and transformer-based temporal modelling within a unified fusion-based regression architecture. Experimental evaluation on the WAY-EEG-GAL dataset under strict leave-one-subject-out conditions achieves $R^2$ = 0.817 in offline settings and $R^2$ = 0.793 in simulated real-time evaluation, with latency suitable for real-time deployment. These results demonstrate strong cross-subject generalisation and highlight the practicality of hybrid continuous-tokenised representations for real-time EEG-based force decoding in assistive robotics, neuro-rehabilitation, and human-machine interaction.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与整体含义
*   **核心问题**：基于非侵入式脑电图（EEG）实现**被试独立**的**连续抓握力解码**。现有方法难以处理复杂的时序动态、高个体间变异和有限泛化性。
*   **整体含义**：提出一种**混合表示学习框架**，联合利用**连续的嵌入特征**与**离散的标记（token）序列**，以同时捕获局部精细神经结构和长程时间依赖关系，提升跨被试的力预测准确性和实时性，最终服务于辅助机器人、神经康复和人机交互。

## 2. 方法论
### 核心思想
构建一个统一的融合回归架构，将连续 EEG 嵌入、离散标记化表示和 Transformer 时序建模有机结合，通过冻结的 Transformer 提供可复用的时序特征，实现实时、高泛化的力解码。

### 关键技术细节与流程
1.  **预处理与表示学习**
    *   信号做 1–40 Hz 带通滤波、50 Hz 陷波、共同平均参考，选取 C3, C4, Cz, CP1, CP2, FC2, FC6 等运动相关通道。
    *   将 EEG 分割为固定长度的、重叠的、事件中心的窗口 $X \in \mathbb{R}^{T \times C}$，目标为力 $y$。
    *   使用**CNN‑GRU 编码器**将每个窗口映射为 128 维连续嵌入 $e = f_{\text{enc}}(X)$。
    *   编码器通过多目标损失训练：
        $$L = \alpha L_{\text{force}} + \beta L_{\text{feature}} + \gamma L_{\text{contrast}} + \delta L_{\text{temp}} + \eta L_{\text{var}}$$
        其中各项分别监督力预测、对齐神经生理特征、区分运动状态、保持时序一致性和防止表示坍缩（权重经验设定为 $\alpha=0.3,\beta=0.2,\gamma=0.05,\delta=0.1,\eta=0.1$）。

2.  **离散标记化与时序建模**
    *   利用**有限标量量化（FSQ）** 将连续嵌入 $e$ 投影并离散化：$z_q = \text{Quantize}(f_{\text{proj}}(e))$，获得紧凑的标记。FSQ 无需学习码本，避免码本崩塌，保证被试间的稳定离散化。
    *   将标记序列 $Z$（长度 32）输入**Transformer**，使用位置编码和堆叠自注意力层提取时序特征 $h = f_{tr}(Z) \in \mathbb{R}^{128}$。训练时使用双向注意力，推理时切换为因果掩码以适配流式（模拟实时）场景。

3.  **融合与回归**
    *   构建融合表示：
        $$x = [h_{Tr}, e, \Delta e] \in \mathbb{R}^{384}$$
        其中 $h_{Tr}$ 为 Transformer 输出的全局时序特征，$e$ 为当前嵌入，$\Delta e$ 为连续嵌入间的局部时序差分。
    *   Transformer 作为**冻结的特征提取器**，其输出经注意力池化后与 $e$, $\Delta e$ 拼接，再通过带 GELU 激活的多层感知机（MLP）预测连续力。

4.  **评估协议**
    *   采用**留一被试交叉验证（LOSO）**：用 11 人训练、1 人测试，轮换 12 次取平均。
    *   两种训练设定：①预训练 Transformer + 每折微调融合层；②严格 LOSO（每折均重新训练 Transformer 与融合层）。编码器在所有 12 人上预训练，因此跨被试结果为上界估计。
    *   评价指标：决定系数 $R^2$。
    *   两种模式：**离线**（使用完整过去‑未来窗口）与**模拟实时**（仅用过去上下文，滑动缓冲长度 32，因果掩码）。

## 3. 实验设计
*   **数据集**：WAY-EEG-GAL，包含多通道 EEG 与同步连续抓握力数据，涉及多个被试执行物体交互任务。
*   **对比方法（Benchmark）**：
    *   EEG‑ForceMap 系列：SFLR (线性)，MLR (线形)，PLSR (非线性)，NNR (深度 MLP)。
    *   其他先前工作：Multi‑Modal Decoder (CNN‑LSTM)，Classifier‑Decoder Ensemble (LDA+CNN‑LSTM)，Brain‑Inspired SNN。
    *   重点对比 **NNR**（其在 LOSO 下 $R^2=0.785$ 为最强基线）。
*   **主要实验组别**：
    *   不同 Transformer‑融合配置的性能对比（表 I）。
    *   严格 LOSO 下的参考配置结果（表 II）。
    *   定性结果：6 名代表性被试的预测‑真实力曲线（图 3）。
    *   消融实验：仅嵌入、嵌入+Transformer、标记+Transformer、完整融合（表 IV）。
    *   与先前方法的定量比较（表 III）。

## 4. 资源与算力
*   **硬件**：**1 块 NVIDIA RTX 3090 GPU**， Intel i7 CPU，32 GB RAM。
*   **训练时长**：文中未给出总训练时间，但列出了各模块的训练轮数：
    *   CNN‑GRU 编码器：100 epochs；
    *   FSQ 标记器：50 epochs；
    *   Transformer：最优仅 **5 epochs**（过深则过拟合）；
    *   融合 MLP：100 epochs。
*   **推理延迟**：每样本 **约 1.6–2.3 ms**，满足实时需求。

## 5. 实验数量与充分性
*   **实验数量**：进行了十余组配置的对比（表 I），包含 5 种 Transformer 训练轮数 × 融合训练轮数组合；严格 LOSO 测试（表 II）；消融实验（表 IV）；与 7 种基线方法的比较（表 III）；还分析了 Transformer 训练深度的影响（图 2 左）以及融合训练的收敛特性（图 2 右）。
*   **充分性与公平性**：
    *   实验覆盖离线与模拟实时两种评估模式，贴近实际应用。
    *   采用标准的 LOSO 验证，基线方法直接复现或引用原文结果，具备可比性。
    *   统计检验（Friedman 检验 $p=0.345$，二项符号检验 $p=0.019$）保证了结论的客观性。
    *   消融实验清晰展示了各组件的贡献。
    *   整体实验设计充分，能支撑论文的主要主张。

## 6. 主要结论与发现
*   所提 EEGForceFusion 在离线 LOSO 下取得 **$R^2 = 0.817$**，模拟实时 LOSO 取得 **$R^2 = 0.793$**，分别超过最强基线（NNR，$R^2=0.785$）约 3.2% 与 0.8%。
*   **Transformer 的最佳角色是浅层特征提取器**：训练约 5 epoch 性能达峰，继续训练会造成对被试特定模式的过拟合，导致泛化下降。冻结 Transformer 并仅优化下游融合 MLP 可使框架对训练轮数鲁棒。
*   **标记化‑连续融合**在保持编码器单窗口预测精度的同时，提供了**紧凑、冻结、可复用的时序特征**，支撑实际实时推理，且未显著增加延迟。
*   该方法在绝大多数被试（11/12）上 $R^2 > 0.75$，展示了强大的跨被试泛化潜力。

## 7. 优点
*   **新颖的混合范式**：首次在 EEG 力解码中同时利用连续表示和基于量化的离散标记，平衡了局部细节与长程依赖。
*   **稳健的设计**：使用 FSQ 避免码本崩塌；Transformer 浅层训练 + 冻结策略有效抑制过拟合；融合对训练时长不敏感。
*   **实时友好**：推理延迟低于 3 ms，且因果 Transformer 和冻结的前向计算非常适合流式部署。
*   **全面的实验验证**：LOSO 协议、多基线对比、多配置分析、消融实验、定性可视化及统计检验，实证支撑扎实。

## 8. 不足与局限
*   **数据集单一**：仅在 WAY‑EEG‑GAL 上评估，泛化到其他 EEG 数据集、不同采集范式的能力未经验证。
*   **跨被试性能的上界**：编码器在所有被试上训练，严格意义上的完全无被试依赖的表示学习尚未实现；报告结果为上界估计。
*   **离实用闭环 BCI 尚存差距**：未考虑闭环系统中常见的反馈延迟、信号漂移和用户自适应等问题。
*   **标记空间统计量不足**：仅 12 名被试，对 Transformer 学习有意义的序列先验来说规模太小；更大的 EEG 语料可能进一步释放潜力。
*   **可解释性有限**：标记空间与运动状态结构之间的直接对应关系仍是开放问题。
*   **个别被试性能差**：被试 P5 的 $R^2=0.647$，可能反映了“BCI 盲”现象，模型在低响应被试上的表现仍有待改善。

（完）
