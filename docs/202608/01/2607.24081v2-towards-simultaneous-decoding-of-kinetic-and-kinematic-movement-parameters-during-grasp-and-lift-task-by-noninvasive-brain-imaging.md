---
title: Towards simultaneous decoding of kinetic and kinematic movement parameters during grasp and lift task by noninvasive brain imaging
title_zh: 基于无创脑成像同时解码抓握与提举任务中的动力学与运动学运动参数
authors: "Parth G. Dangi, Yogesh Kumar Meena"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24081v2"
tags: ["query:ndai"]
score: 9.0
evidence: 从EEG信号解码多种运动和动力学参数，用于脑机接口
tldr: 脑机接口中准确解码多个运动参数是关键挑战。本研究提出偏最小二乘回归、多层感知机和注意力回归器三种模型，从脑电图信号同时解码动力学和运动学参数。在抓取举起任务数据集上，注意力模型在多参数解码中取得R²=0.8和29.2毫秒延迟的优异表现，但单参数解码性能下降，而多层感知机更一致但精度较低。这展示了注意力模型在实时多命令脑机接口中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24081v2/fig-003.webp\", \"caption\": \"Fig. 1: The proposed framework describes a method for preprocessing EEG signals and extracting features with the EEGForceMap approach [9]. It employs a regression setup to decode multiple parameters, where N denotes the outputs from individual regressor models and P represents the total number of outputs. The regression techniques used include partial least squares (PLS), multi-layer perceptron (MLP), and transformer-based regressor (TBR).\", \"page\": 2, \"index\": 3, \"width\": 1052, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2607.24081v2/fig-002.webp\", \"caption\": \"TABLE I: List of parameters from WAY-EEG-GAL dataset selected for simultaneous decoding (GAL - Grasp and Lift)\", \"page\": 3, \"index\": 2, \"width\": 948, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2607.24081v2/fig-001.webp\", \"caption\": \"Fig. 2: A sample of scatter plots showing autocorrelation across different movement parameters’ ERDS activity in µ band for between-group (top), and within-group (bottom) parameters (Az2: rotation of index finger along Z-axis; Fx1: force applied by the thumb along the X-axis; Fx2: force applied by the index finger along the X-axis)\", \"page\": 4, \"index\": 1, \"width\": 454, \"height\": 735}]"
motivation: 扩展脑机接口的可用性和控制能力需要同时准确解码多个运动和动力学参数。
method: 使用三种回归模型（偏最小二乘回归、多层感知机、注意力回归器）从脑电图信号解码多运动参数，并在受试者特定和独立条件下评估。
result: 注意力回归器在同时多参数解码中取得最佳性能（R²=0.8，延迟29.2毫秒），但单参数解码效果下降；多层感知机更稳定但精度较低（R²=0.49）。
conclusion: 注意力模型在实时多命令脑机接口系统中有潜力，有助于开发更直观的控制设备。
---

## 摘要
脑机接口可帮助活动受限的个体，如中风幸存者或截肢者。开发脑机接口的关键挑战之一是扩展其可用性和控制能力，这可通过准确解码多个运动学和动力学参数实现。为此，我们提出了三种回归模型：偏最小二乘回归器、多层感知器和基于注意力的回归器，以从脑电信号中解码多个运动参数。我们在WAY EEG GAL数据集上评估了这些模型，重点关注它们在特定受试者和跨受试者条件下的性能，采用两种策略：所有参数共用一个模型，以及为每个参数单独建模的基线。在所有回归器中，基于注意力的回归器取得了最佳性能，R²达到0.8，延迟为29.2毫秒，展示了同步多参数解码的显著提升。然而，其在单参数解码上的性能有所下降。多层感知器在两种解码类型上表现出更一致但较低的准确率（R² = 0.49）。这些发现突显了基于注意力的模型在实时多指令脑机接口系统中的潜力，并有助于开发更直观的控制设备。

## Abstract
Brain-machine interfaces (BMIs) can assist individuals with limited mobility, such as stroke survivors or amputees. One of the key challenges in developing BMIs is expanding their usability and control, which can be achieved by accurately decoding multiple kinematic and kinetic parameters. To address this, we propose three regression models: partial least squares regressor, multilayered perceptron, and attention based regressor, to decode multiple movement parameters from EEG signals. We evaluated these models on the WAY EEG GAL dataset, focusing on their performance under subject specific and subject independent conditions with two strategies: a single model for all parameters and a baseline with separate models for each parameter. Among all regressors, the attention based regressor achieved the best performance, with an $R^2$ of 0.8 and a latency of 29.2 milliseconds, demonstrating significant improvement in simultaneous multi parameter decoding. However, its performance dropped for single parameter decoding. The multi layered perceptron showed more consistent but lower accuracy across both decoding types ($R^2$ = 0.49). These findings highlight the potential of attention based models for real time multi command BMI systems and contribute to the development of more intuitive control devices.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：脑机接口（BMI）面临的一个关键挑战是扩展其控制能力与可用性，这依赖于从非侵入式脑电（EEG）信号中同时且精准地解码多种运动参数（包括动力学与运动学参数）。现有研究大多只能解码单一参数，或使用侵入式方法，限制了 BMI 系统的直观性和实用价值。
- **核心问题**：如何从低信噪比、低空间分辨率的 EEG 信号中，以低延迟同步解码多个性质不同的运动参数（例如力、位置、角度），从而为更灵巧的假肢控制和康复训练提供技术基础。
- **整体含义**：本研究首次系统性地探索了基于非侵入式 EEG 同时解码 24 个动力学与运动学参数的可行性，并提出了性能优异的注意力回归模型，为实时多指令 BMI 系统的发展提供了新方向。

### 2. 论文提出的方法论

- **特征提取与预处理**：沿用了 EEGForceMap 框架，专注于事件相关去同步/同步特征，从覆盖前运动-顶叶网络的 9 个通道中提取 Mu 频段（9-11 Hz）和 Theta 频段（4-7 Hz）的 ERDS 特征，以增强运动相关神经编码的表达。
- **回归模型**：设计了三种解码架构，均以 0.1 秒滑动窗口、0.05 秒步长的伪在线方式运行。
  - **偏最小二乘回归**：传统线性方法，通过自适应超参数调整和主成分数选择优化，最小化均方误差。
  - **多层感知机**：三层全连接网络（512-256-128 节点），使用 Adam 优化器，作为深度学习基线。
  - **基于注意力的回归器**：构建了一个四层编码器的 Transformer 架构，模型维度 32，4 个注意力头，前馈网络维度 256。利用时间步间的自注意力机制捕捉运动参数间的自相关性，序列长度设为 50 个时间步。
- **两种回归策略**：
  - **总回归器**：单个模型同时预测所有 24 个参数。
  - **聚合回归器**：24 个独立模型各自预测一个参数，再合并输出，作为基线对照。

### 3. 实验设计

- **数据集**：WAY-EEG-GAL 数据集，包含 12 名受试者在不同重量和表面条件下的抓握与提举试验。本研究固定表面条件、变化重量，提取 EEG 与 24 个运动参数（6 个手指力分量、9 个手指与手腕的空间位置分量、9 个手指与手腕的角位移分量）。
- **基准对比**：基线为聚合回归器（每参数单独建模），对比总回归器（所有参数同步解码）。同时对比三种回归模型（PLS、MLP、TBR）在不同解码策略下的表现。
- **评估方式**：
  - **受试者特定**：用单个受试者数据训练与测试，按 7:2:1 划分训练、测试、验证集。
  - **受试者独立**：采用留一受试者交叉验证。
- **指标**：决定系数（$R^2$）、均方误差（MSE）和推理延迟（毫秒）。统计检验使用双因素方差分析和 Tukey HSD 事后检验。

### 4. 资源与算力

- 论文**未明确提及**所使用的 GPU 型号、数量或训练时长。仅提到所有模型训练了 100 个 epoch，TBR 的序列长度为 50，为避免过拟合对 MLP 和 TBR 应用了 30% 的输入特征丢弃。算力相关细节缺失，无法评估资源需求。

### 5. 实验数量与充分性

- **实验组数**：共涉及 3 种回归模型 × 2 种策略（总回归与聚合回归）× 2 种受试者条件（特定与独立），至少 12 组对照实验。
- **特征空间分析**：额外进行了 ERDS 特征与参数间相关性的分析，以及组内、组间对比。
- **充分性评价**：实验覆盖了主要的回归范式变体，统计检验与效应量分析增强了结论的可信度。但所用数据集规模较小（仅 12 名受试者），且仅在一个数据集上验证，缺乏外部独立数据集的泛化证明，实验的广泛性和稳健性仍有不足。对比方法也仅限于三种典型回归器，未引入其他主流模型（如卷积神经网络、循环神经网络等）。

### 6. 论文的主要结论与发现

- **特征编码**：运动参数在 ERDS 特征空间中并非独立编码，而是存在中到高度的相互关联（组内相关系数 $r=0.96$，组间 $r=0.82$），这种自相关性会影响回归器的性能。
- **模型性能**：
  - **PLS** 在多参数同步解码中表现很差（$R^2 \approx 0.13$），延迟高，不适合实时应用。
  - **MLP** 精度较 PLS 显著提升，总回归器 $R^2$ 达 0.49，且策略间表现稳定，但不同参数的解码精度差异大。
  - **TBR** 在总回归策略下取得最优成绩（$R^2=0.8$，MSE 约 4.55，延迟约 30 ms），显著优于其他模型；但在聚合策略下性能急剧下降（$R^2 \approx 0.01$），表明其高度依赖参数间的自相关性才能发挥注意力机制的优势。
- **应用启示**：基于注意力的模型因其高精度和低延迟，最有潜力用于实时多指令 BMI，而 MLP 可进一步开发用于需要独立解码高精度单一参数的场景。

### 7. 优点

- **问题新颖性**：首次在非侵入式 EEG 上尝试同步解码涵盖力、空间位置、角度的 24 个运动参数，贴近实际运动控制的复杂需求。
- **模型创新**：将 Transformer 引入回归解码框架，显式利用时间上下文和多参数间的相关性，取得显著性能突破。
- **实验设计严谨**：包含受试者特定和受试者独立两种设置，设置基线聚合策略比较，采用均方误差、决定系数和延迟三重指标，并进行方差分析和效应量评估。
- **可复现性**：提供了代码开源链接，确保方法的透明性。

### 8. 不足与局限

- **数据集单一且规模小**：仅在 WAY-EEG-GAL 数据集上验证，受试者仅 12 人，未在其他公开数据集上测试泛化能力。
- **对比方法有限**：未包含卷积神经网络、循环神经网络或更先进的时序回归模型，缺乏与最新深度学习方法的横向比较。
- **参数种类限制**：参数仅限于抓握提举任务，未考虑伸够阶段的运动参数，未能完整反映日常活动中的运动编码。
- **TBR 的聚合策略失效**：Transformer 在单参数解码时性能极低，说明其高度依赖参数间关联，可能限制其在某些独立性需求较高场景中的应用。
- **算力与实时性验证不充分**：未报告硬件配置和训练时间，未在真实闭环系统中测试，伪在线环境的验证距离实际应用仍有差距。
- **特征分析深度不足**：仅使用相关性和 t 检验，缺乏对神经编码空间更细致的降维可视化或因果分析。

（完）
