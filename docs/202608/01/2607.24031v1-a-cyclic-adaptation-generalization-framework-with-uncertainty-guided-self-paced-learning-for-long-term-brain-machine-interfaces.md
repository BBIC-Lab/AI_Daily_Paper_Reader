---
title: A Cyclic Adaptation-Generalization Framework with Uncertainty-Guided Self-Paced Learning for Long-Term Brain-Machine Interfaces
title_zh: 一种面向长期脑机接口的基于不确定性引导自步学习的循环自适应-泛化框架
authors: "Jiyu Wei, Di Hong, Zhanjie Zhang, Dazhong Rong, Qinming He, Yueming Wang"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24031v1"
tags: ["query:ndai"]
score: 9.0
evidence: 面向长期脑机接口的循环适应-泛化框架，结合不确定性引导的自步学习，直接对应脑机接口迁移学习
tldr: 针对长期脑机接口中神经漂移导致性能下降的问题，现有域适应或域泛化方法难以捕捉细粒度分布偏移。本文提出UnSPC框架，通过不确定性引导的自定进度伪标记挖掘可靠样本，并采用循环适应与泛化策略迭代整合域适应和域泛化，以对齐目标分布并保持鲁棒表征。在多个神经解码数据集上验证了其有效性和鲁棒性，首次实现DA与DG的循环集成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24031v1/fig-005.webp\", \"caption\": \"Fig. 1. UnSPC consists of two key modules: (i) UnSPL mines reliable pseudo-labeled samples; (ii) CycAG cyclically alternates DA and DG.\", \"page\": 1, \"index\": 5, \"width\": 508, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-004.webp\", \"caption\": \"Fig. 2. The overall framework of our UnSPC. A: Robust Pre-training Decoder (RPD) pre-trained on the source domain for initialization. B: Uncertaintyguided Self-paced Pseudo-labeling (UnSPL) selecting and weighing reliable samples on the target domain to form a reliable domain. C: Cyclic AdaptationGeneralization (CycAG), cyclically alternating between adaptation and generalization using reliable pseudo-labeled samples.\", \"page\": 3, \"index\": 4, \"width\": 1040, \"height\": 647}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-002.webp\", \"caption\": \"Fig. 3. Feature distributions and motor trajectories of ideal (supervised), static (uncalibrated), alignment-based method (WDGRL) and our UnSPC on C0–C1. A and B: Feature distributions under different alignments colored by values of speed and direction. C: Motor trajectories colored by trial direction.\", \"page\": 6, \"index\": 2, \"width\": 1035, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-001.webp\", \"caption\": \"TABLE I RECALIBRATION PERFORMANCE COMPARISON (BOLD FOR THE BEST AND UNDERLINE FOR THE SECOND-BEST)\", \"page\": 6, \"index\": 1, \"width\": 1044, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2607.24031v1/fig-003.webp\", \"caption\": \"TABLE II ABLATION RESULTS OF UNSPC\", \"page\": 7, \"index\": 3, \"width\": 508, \"height\": 248}]"
motivation: 解决长期脑机接口中神经漂移带来的性能退化，克服单独使用域适应或域泛化的局限，捕捉细粒度子域分布偏移。
method: 提出UnSPC框架，利用不确定性引导的自定进度伪标记(UnSPL)挖掘高质量伪标签，并通过循环适应与泛化(CycAG)策略迭代整合域适应与域泛化。
result: 在多个神经解码数据集上，UnSPC表现出优异的解码性能和鲁棒性。
conclusion: UnSPC首次循环整合域适应、域泛化与伪标记，为稳定长期脑机接口控制提供了新范式。
---

## 摘要
脑机接口连接大脑与外部设备，在康复、人类能力增强和以人为中心的机器人领域具有巨大潜力。然而，由于神经漂移，侵入式脑机接口在长期部署中面临关键挑战，神经漂移会使解码性能随时间下降，并需要频繁重新校准。现有缓解神经漂移的方法通常仅依赖域自适应（DA）或域泛化（DG），并且往往无法捕捉神经子域间细粒度的分布偏移，导致性能受限。为了克服这些限制，我们提出了不确定性引导的自步循环（UnSPC），这是一个鲁棒框架，通过不确定性引导的自步伪标签（UnSPL）机制协同DA和DG以细化目标域。为了处理跨领域的子域神经漂移，UNSPL被提出用于迭代挖掘可靠的伪标记样本，并使用噪声鲁棒排序策略进行进一步微调。利用这些高质量样本，我们引入了一种新颖的循环自适应与泛化（CycAG）策略，将DA和DG集成在一个迭代循环中，以逐步缓解全局和子域漂移。这种循环过程能够有效对齐不断变化的目标分布，同时保持鲁棒且可迁移的表示，从而缓解长期神经漂移下的性能下降。在多个神经解码数据集上的大量实验展示了UnSPC的有效性和鲁棒性。据我们所知，我们提出的UnSPC是首个将DA和DG与伪标记循环整合的方法，为实现稳定的长期脑机接口控制铺平了道路。

## Abstract
Brain-Machine Interfaces (BMIs), which link the brain to external devices, hold great potential in rehabilitation, human performance augmentation, and human-centered robotics. However, invasive BMIs face a critical challenge for long-term deployment due to neural drift, which degrades decoding performance over time and necessitates frequent recalibration. Existing methods designed to mitigate neural drift typically rely on either domain adaptation (DA) or domain generalization (DG) alone and often fail to capture fine-grained distribution shifts across neural subdomains, resulting in limited performance. To overcome these limitations, we propose Uncertainty-guided Self-paced Cycling (UnSPC), a robust framework that synergizes DA and DG for target domain refining under an Uncertainty-guided Self-paced Pseudo-labeling (UnSPL) mechanism. To handle subdomain neural drift across domains, UNSPL is proposed to iteratively mine reliable pseudo-labeled samples with a noise-robust ranking strategy for further fine-tuning. Leveraging these high-quality samples, we introduce a novel Cycling Adaptation and Generalization (CycAG) strategy, which integrates DA and DG within an iterative cycle to progressively mitigate both global and subdomain drift. This cyclic process enables effective alignment to evolving target distributions while preserving robust and transferable representations, thereby mitigating performance degradation under long-term neural drifts. Extensive experiments on multiple neural decoding datasets demonstrate the effectiveness and robustness of UnSPC. To our knowledge, our proposed UnSPC is the first to cyclically integrate DA and DG with pseudo-labeling, paving the way toward stable long-term BMI controls.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 核心问题与整体含义
- **研究背景**：脑机接口（BMIs）连接大脑与外部设备，在康复与机器人控制等领域有重大潜力。然而，侵入式 BMI（iBMI）在长期使用中面临**神经漂移**挑战（如电极移位、神经可塑性变化），导致解码性能随时间退化，需频繁且昂贵的带标签数据重校准。
- **核心问题**：现有方法通常仅依赖**领域自适应（DA）**或**领域泛化（DG）**中的一种，无法同时兼顾对演化分布的适应力与对未见变化的鲁棒性，尤其难以捕捉对精细解码关键的**子领域漂移**。
- **整体含义**：本文提出**不确定性引导的自步循环（UnSPC）**框架，旨在**无监督**设定下，通过协同 DA 与 DG 并利用高质量伪标签，实现稳健的长期神经解码。

## 2. 方法论
- **核心思想**：利用**不确定性量化**从无标签目标域中挖掘可靠伪标签，并在一个迭代循环中**交替执行 DA 与 DG**，逐步细化模型对目标域的适应与泛化能力。
- **关键技术与流程**：
  - **阶段一：源域鲁棒预训练（RPD）**：在带标签源域（如 day 0）训练一个稳定的骨干网络，采用**混合神经解码**（同时输入干净与随机掩码通道的数据）与**一致性约束**（保持特征空间拓扑结构，并约束师生模型预测一致），以抵抗电极噪声与不稳定。
  - **阶段二：目标域迭代细化**：对无标签目标域数据，执行以下两步循环直至收敛：
    1. **不确定性引导的自步伪标签（UnSPL）**：
       - **可靠样本选择（RSS）**：为每个目标样本赋予一个**混合不确定性得分** $u_i$，它结合了**蒙特卡洛丢弃（MC Dropout）不确定性** $u^{MC}_i$ 和**模型预测不一致性（IC）** $u^{IC}_i$。
       - **自步机制**：阈值 $\tau_w$ 随迭代次数 $w$ 动态放宽，让模型从最可靠的样本开始学习，逐渐纳入更多样本。
       - **可靠样本加权（RSW）**：基于样本在可靠集中的不确定性排名 $r_i$，赋予权重 $z_i = \exp(-\beta \cdot r_i / |R_w|)$，以削弱噪声伪标签的影响。
    2. **循环适应与泛化（CycAG）**：
       - 利用选中并加权的可靠伪标签集 $\mathcal{R}_w$，**交替优化两个互补目标**：
         - **循环 DA（CycDA）**：计算源域与目标域的**最大均值差异（MMD）**损失以对齐全局分布，并用伪标签进行**加权监督学习（WSL）**。
         - **循环 DG（CycDG）**：对目标域施加**师生一致性约束（TSCT）**，并在掩码输入下用伪标签进行**加权掩码神经解码（WMND）**，迫使模型学习对噪声不敏感的鲁棒表示。
  - **教师-学生机制**：整体框架采用指数移动平均（EMA）更新教师模型，用于生成伪标签和最终推理，稳定训练过程。

## 3. 实验设计
- **数据集与场景**：
  - **主要测试**：3个公开的非人灵长类（NHP）侵入式 BMI 数据集（Chewie、Mihili、Jango），覆盖上肢运动任务。每个数据集包含4天记录，day 0 为源域，day 1/2/3 为无标签目标域，时间跨度从几天到数月，模拟长期漂移。
  - **鲁棒性测试**：1个人类受试者在不同日期执行虚拟手臂到达任务的数据集（FALCON）。
- **对比基准（Benchmark）**：
  - **静态解码器**：未校准的源域模型。
  - **方法类型**：单独领域泛化（LFDA）、三种经典无监督领域自适应（DA-DCF、UAN、WDGRL）和一种半监督领域自适应（SeSA）。
  - **评价指标**：皮尔逊相关系数（CC）、决定系数（R²）、均方误差（MSE）。结果均使用5个随机种子重复实验取均值。

## 4. 资源与算力
- 论文中**未明确提及**所使用 GPU 的型号、数量或模型训练时长。仅提及优化器为 Adam，学习率为 $5\times10^{-3}$，且模型为轻量级3层 MLP，可能对算力要求不高。

## 5. 实验数量与充分性
- **主要比较实验**：3个NHP数据集下的9个目标域（如C1-C3, J1-J3, M1-M3）上，UnSPC与6种基线的对比，证明性能优势。
- **消融实验**：在C0→C1任务上，系统地移除了RPD、UnSPL下的RSS和RSW，以及CycAG中的DA步骤、DG步骤和整个循环机制，共7组实验，验证各组件必要性。
- **鲁棒性分析**：
  - 在C1任务上，对4个关键超参数 $\alpha, \beta, \eta, \phi$ 各测试3-4个数值，验证不敏感性。
  - 在其人类数据集上测试3个目标域，验证跨物种泛化能力。
  - 定性展示了特征分布和运动轨迹的可视化结果，同理想解码对比。
- **评估**：实验**设计充分、客观且公平**。涵盖了多个数据集、多维度对比和全面的消融/敏感性分析，结论有坚实的实证支撑。

## 6. 主要结论与发现
- **无监督重校准性能最优**：UnSPC在所有任务的CC和R²指标上均显著超越所有DA和DG类及半监督方法，无需任何目标域标签。
- **技术有效性验证**：融合UnSPL与CycAG是框架成功的核心；仅用伪标签而不做不确定性引导会导致性能剧降，证明其必要性；循环交替DA和DG优于单独使用或简单的组合。
- **鲁棒性强**：UnSPC对超参数不敏感，且能稳定泛化到人类受试者，展现了对真实世界长期BMI应用的潜力。

## 7. 优点
- **方法创新性**：作为首个将DA、DG与回归伪标签循环整合的工作，解决了BMI领域精细子域漂移的挑战，开辟了新的研究范式。
- **技术设计精巧**：UnSPL为连续回归问题设计了有效的不确定性度量和噪声鲁棒的课程学习策略；CycAG巧妙地实现了适应与泛化的互补增益。
- **实验评估扎实**：实验覆盖多个灵长类数据集、跨物种验证和充足的可视化与消融分析，证据链完整且极具说服力。

## 8. 不足与局限
- **算力信息缺失**：未提供任何关于训练耗时或硬件需求的细节，难以评估其实际部署的轻量性和经济成本。
- **目标域数量有限**：主要实验中每个数据集仅测试了3个后续日，且人类数据集规模较小。在更长时间跨度（如跨年）或更极端漂移下的稳定性有待观察。
- **模型假设依赖性**：框架依赖Dropout进行MC采样估计不确定性，这可能对模型具体结构和dropout率设定有一定敏感性，文中未对此细节做分析。
- **应用范围**：当前验证集中在连续运动轨迹解码（回归任务），其在离散分类任务（如目标定向）上的有效性尚未被探讨。

（完）
