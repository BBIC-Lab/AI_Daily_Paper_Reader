---
title: Stochastic Sequential Search in Very-High-Dimensional Feature Selection
title_zh: 超高维特征选择中的随机顺序搜索
authors: "Petr Somol, Jiří Grim"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01502v1"
tags: ["query:ndai"]
score: 7.0
evidence: 随机序列搜索通过依赖感知的逐特征统计进行特征选择，相当于对输入特征的重要性加权
tldr: 在高维特征选择中，传统序列搜索因全维度扫描而计算量剧增，个体排序方法则缺乏特征交互建模。本文提出随机序列搜索家族，通过预算采样的算子替换全扫描，将每步成本固定为常数。具体实现的随机浮动搜索sSFFS，在madelon、gisette和reuters等高维数据集上，以少量评估达到或超越全扫描方法及高级排序方法的准则值和准确率，验证了高效性和有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01502v1/fig-007.webp\", \"caption\": \"Table 1: Evolution of sequential feature selection, one generation per row. Every method up to this work evaluates all available candidates at each step; the proposed budgeted step is the family’s first departure from the full sweep.\", \"page\": 5, \"index\": 7, \"width\": 968, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-004.webp\", \"caption\": \"Figure 1: Anytime best-per-size criterion value on madelon (train-criterion axis: 1-NN-wrapper 3-fold-CV value on the training half — the optimization target): sSFFS (y = 100, ρu = 0.2, warm start 200@10) retains ≥ 98.2% of the full-SFFS value at every size (seed 1, identical data split) and exceeds it at every size above d = 130; the shaded band is the mean±std over 10 seeds (seeds ̸= 1 on independent splits).\", \"page\": 13, \"index\": 4, \"width\": 961, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-002.webp\", \"caption\": \"Figure 2: Where the budget goes under forward-only capping: with forward steps capped at y = 100, the uncapped backward sweeps of the floating correction dominate total cost (57–62% across all ten seeds) once the search explores sizes up to d = 165 — the measured motivation for budgeting the backward direction as well (yb) and for bounded size ranges in the extreme-dimensionality regime.\", \"page\": 14, \"index\": 2, \"width\": 761, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-001.webp\", \"caption\": \"Figure 3: Budget spent uniformly vs. informed (madelon, d = 20, ten paired seeds, identical budget y = 100 per step; train-criterion axis — except where a value is explicitly marked as validated holdout accuracy): softmax sampling matches the full sweep on every seed (means 0.866 vs. 0.863), while uniform sampling never wins against either (vs. softmax: nine losses, one tie within 2 · 10−5) and collapses outright on two seeds (5 and 8: criterion 0.62/0.75, validated accuracy 0.52/0.73) — madelon’s synergistic features are found by the learned statistics, not by luck. At tighter budgets the uniform failure deepens to chance level (Discussion).\", \"page\": 15, \"index\": 1, \"width\": 957, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-008.webp\", \"caption\": \"Figure 4: Budget-response of sSFFS on madelon (train-criterion axis: wrapper-1NN 3-fold CV value on the training half — the optimization target; validated holdout accuracies are quoted in the text). Each point: 10-seed mean (±std in the top row) of the best criterion value attained at size d, versus the compute invested up to that discovery (warm-up included); the per-step budget grows along each curve (y ∈ {10, 25, 50, 100, 200} softmax, {25, 100} controls). Top row: criterion evaluations. Bottom row: cost-model minutes — evaluations weighted by the measured size-dependent evaluation cost ms(d) ≈ 3.68 + 0.487 d, which penalizes evaluations spent at large subset sizes and widens the softmax lead (uniform reaches its best-at-20 not only later but at larger evaluated sizes). Horizontal references: full-SFFS anytime value (identical seed-1 split) and the DAF/BIF rankings at their own budgets (markers). Every sampled arm dominates both rankings on the criterion at both sizes; softmax reaches its quality with the least compute throughout and approaches (at d = 100: matches) the full-sweep asymptote.\", \"page\": 16, \"index\": 8, \"width\": 883, \"height\": 808}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-006.webp\", \"caption\": \"Table 2: Gisette budget ladder (mean over seeds; three per cell, five at y = 100): criterion value J (3-fold CV wrapper on the training half — the search objective) and holdout accuracy of the same subsets, at subset sizes 100 and 300. DAF’s budget is its probe count; the sSFFS totals cover the whole anytime run (all sizes ≤ 320, warm-up included). Reference points: the all-features holdout accuracy is 0.894–0.902 across the three seeds; matched-budget DAF controls on both fairness axes — matched evaluations (138k–233k probes) and matched wall time (1.5–2.4M probes), five seeds each — validate at 0.89–0.95, on the same saturation plateau as the fixed-budget rows above.\", \"page\": 17, \"index\": 6, \"width\": 694, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-003.webp\", \"caption\": \"Figure 5: The fixed-d slices of Figure 4 generalize to the whole per-size frontier (madelon, 10-seed mean anytime best-per-size, train-criterion axis): the softmax family closes monotonically towards the full-SFFS curve as the per-step budget y grows (the y = 200 mean overtakes it near the frontier, d ≥ 162), while at the matched tightest budget the uniform and top-k controls trail softmax y = 25 at 149/165 resp. 164/165 sizes.\", \"page\": 18, \"index\": 3, \"width\": 957, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2608.01502v1/fig-005.webp\", \"caption\": \"Table 3: Reuters (D = 10,105, 33 classes, RR(1,50,40) split): criterion value J (multinomial Bhattacharyya, computed on the training part — the search objective) and MultinomNB holdout accuracy of the same subsets; means over five seeds. Budgets: BIF spends D singleton evaluations; DAF its probe count; the sSFFS totals cover the whole anytime run to the dmax = 500 frontier (warm-up included). DAF probes here use cardinality 500 (a CLI constraint pins probe size to the reported subset size for monotonic criteria); the protocol-conforming card-25 rerun trails or at best matches card-500 at every ladder budget (d=100 holdout .757/.818/.816 vs. .801/.831/.837 at 104/105/106 probes), so the table reports the stronger ranking baseline. “DAF matched” re-ranks with card-25 probes at each seed’s exact y=100 anytime total (0.77–0.90M evaluations, mean 0.81M); the same control at the y=25 totals (2.40–3.03M, mean 2.78M) reproduces the plateau (see text).\", \"page\": 19, \"index\": 5, \"width\": 736, \"height\": 227}]"
motivation: 传统序列搜索在极高维问题中因全扫描而不可行，简单排序方法无法捕捉特征相互作用。
method: 提出预算采样的步进算子，用依赖感知的每特征统计量通过温度控制softmax采样进行候选评估，每步成本固定。
result: sSFFS在多个高维数据集上以显著更少的评估次数，获得接近或超越全扫描方法和排序方法的准则值与准确率。
conclusion: 随机序列搜索为极高维特征选择提供了高效且有效的实用方案。
---

## 摘要
顺序子集搜索——前向选择与浮动回溯及其变体——仍然是特征选择中的质量基准，但该家族中的每一个方法在每一步都要扫描全部剩余候选特征，这使其无法用于超高维问题；在那里，只有单个特征排序仍然实用，而它对特征相互作用的建模很弱或根本没有。我们引入了一对预算采样的步进算子，用每步固定次数的候选评估取代全面扫描。候选者通过温度控制的softmax采样从依赖感知的逐特征统计量中抽取，这些统计量是从搜索执行的每次准则评估中在线学习的，并由均匀探索底限保护；每步成本变得与维度无关。替换这些算子可将任何顺序方法转变为其随机对应版本，从而定义了随机顺序搜索（SSS）家族；我们研究了浮动搜索的随机对应版本，sSFFS。在500维的madelon数据集上，sSFFS在每个子集大小下都保留了至少97%的完整SFFS准则值，而评估次数仅为其四分之一左右，但在相同预算下的均匀采样却在madelon的协同特征上失效了。在5000维的gisette数据集上，远超出完整SFFS的能力范围，sSFFS在匹配预算下超过了DAF和BIF排序的饱和准则水平；保留验证表明，在500个训练样本下，超越顺序前沿的约束瓶颈变成了准则，而不是搜索。在10105维的reuters数据集上，使用可信赖的多项过滤准则，sSFFS在每个子集大小下的搜索目标和保留准确率上都优于BIF和DAF，仅需约两分钟的单核评估工作。论文附有经过验证的独立实现。

## Abstract
Sequential subset search -- forward selection with floating backtracking and its descendants -- remains the quality reference in feature selection, but every member of the family sweeps the full pool of remaining candidate features at each step, which excludes it from very-high-dimensional problems; there, only individual-feature ranking remains practical, and it models feature interplay weakly or not at all. We introduce a budgeted sampled step operator pair that replaces the full sweeps by a fixed number of candidate evaluations per step. Candidates are drawn by temperature-controlled softmax sampling from dependency-aware per-feature statistics learned online from every criterion evaluation the search performs, guarded by a uniform exploration floor; per-step cost becomes independent of dimensionality. Substituting the operators turns any sequential method into its stochastic counterpart, defining the Stochastic Sequential Search (SSS) family; we study the stochastic counterpart of floating search, sSFFS. On 500-dimensional madelon, sSFFS retains at least 97% of the full-SFFS criterion value at every subset size at about a quarter of its evaluations, while uniform sampling at the same budget collapses on madelon's synergistic features. On 5,000-dimensional gisette, far beyond full-SFFS reach, sSFFS exceeds the saturated criterion level of DAF and BIF ranking at matched budgets; holdout validation shows that at 500 training samples the binding constraint beyond the sequential frontier becomes the criterion, not the search. On 10,105-dimensional reuters, under a trustworthy multinomial filter criterion, sSFFS dominates BIF and DAF on the search objective and on holdout accuracy at every subset size, in about two minutes of single-core evaluation work. A verified standalone implementation accompanies the paper.