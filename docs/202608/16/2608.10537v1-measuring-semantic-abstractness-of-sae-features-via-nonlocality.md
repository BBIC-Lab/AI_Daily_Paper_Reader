---
title: Measuring Semantic Abstractness of SAE Features via Nonlocality
title_zh: 通过非局域性测量 SAE 特征的语义抽象度
authors: "Chuqiao Lin, Shivaji Sondhi, Xiao-Liang Qi"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10537v1"
tags: ["query:ndai"]
score: 8.0
evidence: 特征非局部性计算逐位置影响熵，是一种特征重要性权重
tldr: "针对稀疏自编码器特征难以区分表层词汇与高层语义的问题，本文提出特征非局域性（FNL），用每位置影响熵度量特征抽象程度。FNL与现有语义抽象代理指标相关，并能以73-84%准确率区分上下文推理特征与token特征；应用于越狱缓解审计和数学推理引导，验证其作为无标签抽象度见证的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10537v1/fig-003.webp\", \"caption\": \"Figure 2: Decoder-cosine geometry pins H(a) (Gemma-22B layer 12). For each of six anchor features we take its top-10 decoder-cosine neighbours and plot every feature’s H(a). For each individual feature, the computation of FNL samples top-k (k = 32) max-activation firing events across a sample WikiText corpus; the error bar represents the IQR of FNL statistics.\", \"page\": 5, \"index\": 3, \"width\": 500, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2608.10537v1/fig-002.webp\", \"caption\": \"Figure 3: Feature nonlocality traces a concrete-to-abstract continuum (Gemma-2-2B layer 12, T = 128). (a) Seven representative features ranked by mean H(a) (± std across activating events); the three marked (∗, red) are the features shown in panel (b). Single-token detectors (e.g. f608, “Robert”) sit at the low-nonlocality end and utterance-level features (e.g. f13603, epistemic hedging) at the high end. (b) Per-position influence Ja(t ′) over the top-three max-activating contexts of those three features; each token’s background is shaded by |Ja(t′)| and the activating token is boxed. Influence is concentrated on a single token for the low-H(a) feature f608 and grows progressively more diffuse for the mid-H(a) relative-clause connector f10360 (3.2 bits) and the high-H(a) epistemic-uncertainty feature f13603 (5.0 bits).\", \"page\": 6, \"index\": 2, \"width\": 1054, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2608.10537v1/fig-001.webp\", \"caption\": \"Figure 4: Paraphrase robustness Sa versus nonlocality, presentation collapsed to the four token-injection classes (median ± 68% bootstrap CI on both axes, dashed line fitting through the class medians for readability). Per-feature Spearman correlation, ρ = +0.27 (p = 0.011, n = 96 features).\", \"page\": 6, \"index\": 1, \"width\": 502, \"height\": 277}]"
motivation: 现有方法难以可靠判断SAE特征是表面词汇特征还是高层语义特征。
method: 提出特征非局域性（FNL），定义为SAE特征激活的归一化每位置影响的熵。
result: "FNL能区分上下文推理与token特征（73-84%准确率），审计发现越狱缓解主要依赖低FNL位置特征，高FNL特征引导在数学任务上提升4.6点。"
conclusion: FNL是LLM无关、无标签的SAE特征抽象程度度量，可用于评估机制解释和选择下游干预特征。
---

## 摘要
稀疏自编码器（SAEs）通过理解相应的任务相关且因果有效的特征，帮助揭示大语言模型行为（如推理、越狱等）的机制性解释。为了评估此类机制性解释，下游研究必须区分表面词汇特征与真正的高层特征。然而，无论是基于自动可解释性（autointerp）的语义描述还是因果操控效用，都无法完全确定特征的抽象层次。为此，我们引入“特征非局域性”（Feature Nonlocality, FNL），定义为对 SAE 特征激活的归一化逐位置影响的熵。我们报告，FNL 与现有基于 LLM 的特征语义抽象度代理指标相关，并成功区分上下文相关推理特征与词元驱动特征，在随机抽取的上下文特征与词元级特征对中，73%--84% 的情况下正确地将更高 FNL 分配给上下文特征。我们展示了两个下游应用。我们审计了用于越狱缓解的基于 SAE 的特征，并惊讶地发现大多数有效特征是低 FNL 的位置特征，而非真正识别有害意图的特征。我们报告，在 DeepSeek-R1-Distill-Llama-8B 中操控高 FNL 特征使 MATH-500 准确率比未操控模型提高 4.6 个百分点，并且优于操控低 FNL 特征，尽管这些收益因模型而异。我们得出结论：FNL 提供了一种独立于 LLM、无需标签的、用于衡量 SAE 特征抽象层次的相关性指标，可应用于评估机制性解释以及为下游干预选择特征。

## Abstract
Sparse autoencoders (SAEs) have helped uncover mechanistic explanations for LLM behaviours such as reasoning, jailbreaking etc., via understanding the corresponding task-relevant and causally effective features. To evaluate such mechanistic explanations, downstream studies must distinguish surface lexical features from genuinely high-level ones. However, neither an autointerp-based semantic description nor causal steering utility fully resolves the abstraction level of a feature. To this end, we introduce \emph{Feature Nonlocality} (FNL), defined as the entropy of the normalized per-position influence on an SAE feature's activation. We report that FNL correlates with existing LLM-based proxy metrics of feature semantic abstractness, and successfully distinguishes context-dependent reasoning features from token-driven ones, correctly assigning the higher FNL to the contextual feature in $73$--$84\%$ of randomly drawn pairs that consist of one contextual and one token-level feature.   We demonstrate two downstream applications. We audit SAE-based features used for jailbreak mitigation and find surprisingly that most effective features are positional features with low FNL rather than genuinely recognizing harmful intents.   We report that steering high-FNL features in DeepSeek-R1-Distill-Llama-8B improves MATH-500 accuracy by $4.6$ points over the unsteered model and outperforms steering low-FNL features, though the gains are model-specific. We conclude that FNL provides an LLM-independent, label-free, correlational witness of the abstraction level of an SAE feature, with applications in evaluating mechanistic explanations as well as selecting features for downstream interventions.