---
title: "MINT: A Universal Zero-Shot Predictor for Transaction Data"
title_zh: MINT：一种面向交易数据的通用零样本预测器
authors: "Parameswaran Kamalaruban, Viktor Drobnyi, Maeve Madigan, Julia Rozanova, David Sutton, Stuart Burrell"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14198v1"
tags: ["query:ndai"]
score: 9.0
evidence: 分析顺序金融交易数据，利用上下文嵌入实现对新型下游任务的零样本预测
tldr: 针对银行交易数据零样本预测问题，现有方法依赖文本序列化且未能充分利用交易信号。本文提出 MINT 框架，将预训练交易序列编码器与仅解码器 LLM 通过轻量嵌入注入、交易-语言对齐和指令调优连接。MINT 在分布内与分布外预测问答上达到最优性能，同时显著降低输入 token、延迟和内存，证明紧凑交易嵌入优于文本序列化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14198v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of MINT. Transaction histories are en-\", \"page\": 1, \"index\": 5, \"width\": 506, \"height\": 733}, {\"url\": \"assets/figures/arxiv/2608.14198v1/fig-001.webp\", \"caption\": \"Figure 2: Accuracy-efficiency trade-off on predictive QA. The\", \"page\": 2, \"index\": 1, \"width\": 482, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2608.14198v1/fig-003.webp\", \"caption\": \"Table 1: Overall accuracy on extractive and predictive QA tasks, together with inference efficiency metrics (TTFT, decode\", \"page\": 6, \"index\": 3, \"width\": 890, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2608.14198v1/fig-004.webp\", \"caption\": \"Table 2: Ablation study of MINT. We analyze the effects of\", \"page\": 6, \"index\": 4, \"width\": 468, \"height\": 752}, {\"url\": \"assets/figures/arxiv/2608.14198v1/fig-002.webp\", \"caption\": \"Table 3: Impact of LLM-LTM pairing on MINT performance\", \"page\": 6, \"index\": 2, \"width\": 386, \"height\": 377}]"
motivation: 支付基础模型缺乏零样本推理能力，现有 LLM 方法未能充分利用交易数据中的预测信号且文本序列化代价高。
method: 提出 MINT，通过轻量嵌入注入、交易-语言对齐和指令调优连接预训练交易序列编码器与仅解码器 LLM。
result: MINT 在分布内和分布外预测问答上取得最先进性能，并大幅减少输入 token、延迟和内存消耗。
conclusion: 紧凑交易嵌入是比文本序列化更优的交易表示方式，适用于多模态推理和零样本预测任务。
---

## 摘要
银行分析顺序金融交易数据以执行许多任务，包括欺诈预防、信用风险评估和优惠个性化。为了提高这些任务的预测准确性，支付基础模型将交易序列数据编码为丰富的上下文嵌入，然后将其作为特征提供给任务特定模型。然而，这些基础模型并非为在新型下游预测任务中进行灵活的零样本推理而设计，限制了其适应性和实用性。现有基于LLM的零样本预测方法往往未能充分利用交易数据中的预测信号，同时依赖昂贵的文本序列化或扩展性差的任务特定架构。为了解决这些限制，我们提出了面向交易的多模态指令网络（MINT），该框架通过轻量级嵌入注入、交易-语言对齐和指令微调，将预训练的交易序列编码器连接到仅解码器LLM。我们发现，MINT在分布内和分布外问题上均实现了最先进的预测性问答性能，同时与文本序列化基线相比大幅减少了输入token、延迟和内存消耗。通过对表示、对齐策略、训练数据和历史长度的全面分析，我们确认紧凑的交易嵌入在多模态推理和零样本预测任务中是优于文本序列化的交易表示方法。

## Abstract
Banks analyse sequential financial transaction data to perform many tasks, including fraud prevention, credit risk assessment and offer personalization. To improve the predictive accuracy of these tasks, Payments Foundation Models encode transaction sequence data as rich contextual embeddings, which can then be provided to task-specific models as features. However, these Foundation Models are not designed for flexible zero-shot reasoning across novel downstream prediction tasks, limiting their adaptability and utility. Existing LLM-based approaches to zero-shot prediction often fail to fully exploit the predictive signal within transaction data, while relying on costly text serialization or task-specific architectures that scale poorly. To address these limitations, we present the Multimodal Instruction Network for Transactions (MINT), a framework that connects a pretrained transaction sequence encoder to a decoder-only LLM through lightweight embedding injection, transaction-language alignment, and instruction tuning. We find that MINT achieves state-of-the-art predictive question-answering performance in both in-distribution and out-of-distribution questions, while substantially reducing input tokens, latency, and memory consumption compared to text-serialization baselines. Through comprehensive analyses of representations, alignment strategies, training data, and history length, we establish that compact transaction embeddings are a superior approach to transaction representation than text serialization for multimodal reasoning and zero-shot prediction tasks.

---

## 论文详细总结（自动生成）

# MINT: 一种面向交易数据的通用零样本预测器 — 结构化论文总结

## 1. 论文的核心问题与整体含义

- **研究背景**：银行和支付机构需要分析客户的顺序金融交易数据，用于欺诈检测、信用风险评估、优惠个性化等任务。支付基础模型（Payments Foundation Models）已经能把交易序列编码为丰富的上下文嵌入，但这些模型通常不能直接对**新型下游任务进行零样本推理**。
- **核心问题**：现有的大语言模型（LLM）方法在交易理解上通常依赖**文本序列化**，不仅输入 token 成本高、延迟高、内存占用大，还难以充分利用交易数据中的结构化数值和时间信号；而任务特定架构又难以扩展到新的任务类型。因此，论文要解决的核心问题是：**如何高效地将交易序列表示接入 LLM，并实现跨任务的零样本预测性推理**。
- **整体含义**：论文提出并验证了一种“**紧凑交易嵌入 + 指令调优 LLM**”的多模态交易推理范式。与文本序列化相比，这种范式在预测性问答上更强，同时推理资源消耗更低，说明交易嵌入是多模态交易推理中更优的交易表示方式。

## 2. 论文提出的方法论

### 2.1 MINT 总体架构

MINT 由三个部分组成：

- **冻结的交易序列编码器**：基于 Transformer，对每笔交易内部字段建模，并对交易序列建模时间依赖。编码器在大规模交易语料上以自回归**下一事件预测**目标预训练，输出每笔交易的上下文嵌入 $e_i \in \mathbb{R}^{d_e}$。
- **可训练连接器（modality projector）**：将交易嵌入 $e_i$ 映射到 LLM 隐藏空间，得到 $z_i = g_\phi(e_i)$。
- **通过 LoRA 适配的 decoder-only LLM**：LLM 主干冻结，仅更新 LoRA 参数和连接器参数。

### 2.2 交易序列编码器

交易历史表示为：

$$
T = \{(x_i, t_i)\}_{i=1}^{N}
$$

其中 $x_i$ 是第 $i$ 笔交易记录，$t_i$ 是时间戳。数值属性经过对数变换，类别属性使用字段特定的嵌入，字段级 Transformer 建模交易内部关系，序列级 Transformer 建模交易间时序关系。编码器输出：

$$
e_i = E(x_i \mid \{(x_j, t_j)\}_{j=1}^{i})
$$

预训练目标是自回归下一事件预测：

$$
L_{AR} = -\sum_{i=1}^{N} \log p(x_i \mid x_{<i})
$$

其中 $x_i = D(e_{i-1})$，$D$ 是可学习解码器。

### 2.3 连接器

连接器使用带归一化的多层感知器（MLP）：

$$
g_\phi(e) = \mathrm{LN}\left(W_2
